'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var app = require('@firebase/app');
var component = require('@firebase/component');
var util = require('@firebase/util');
var logger$1 = require('@firebase/logger');

const name = "@firebase/database";
const version = "0.14.4";

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** The semver (www.semver.org) version of the SDK. */
let SDK_VERSION = '';
/**
 * SDK_VERSION should be set before any database instance is created
 * @internal
 */
function setSDKVersion(version) {
    SDK_VERSION = version;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Wraps a DOM Storage object and:
 * - automatically encode objects as JSON strings before storing them to allow us to store arbitrary types.
 * - prefixes names with "firebase:" to avoid collisions with app data.
 *
 * We automatically (see storage.js) create two such wrappers, one for sessionStorage,
 * and one for localStorage.
 *
 */
class DOMStorageWrapper {
    /**
     * @param domStorage_ - The underlying storage object (e.g. localStorage or sessionStorage)
     */
    constructor(domStorage_) {
        this.domStorage_ = domStorage_;
        // Use a prefix to avoid collisions with other stuff saved by the app.
        this.prefix_ = 'firebase:';
    }
    /**
     * @param key - The key to save the value under
     * @param value - The value being stored, or null to remove the key.
     */
    set(key, value) {
        if (value == null) {
            this.domStorage_.removeItem(this.prefixedName_(key));
        }
        else {
            this.domStorage_.setItem(this.prefixedName_(key), util.stringify(value));
        }
    }
    /**
     * @returns The value that was stored under this key, or null
     */
    get(key) {
        const storedVal = this.domStorage_.getItem(this.prefixedName_(key));
        if (storedVal == null) {
            return null;
        }
        else {
            return util.jsonEval(storedVal);
        }
    }
    remove(key) {
        this.domStorage_.removeItem(this.prefixedName_(key));
    }
    prefixedName_(name) {
        return this.prefix_ + name;
    }
    toString() {
        return this.domStorage_.toString();
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An in-memory storage implementation that matches the API of DOMStorageWrapper
 * (TODO: create interface for both to implement).
 */
class MemoryStorage {
    constructor() {
        this.cache_ = {};
        this.isInMemoryStorage = true;
    }
    set(key, value) {
        if (value == null) {
            delete this.cache_[key];
        }
        else {
            this.cache_[key] = value;
        }
    }
    get(key) {
        if (util.contains(this.cache_, key)) {
            return this.cache_[key];
        }
        return null;
    }
    remove(key) {
        delete this.cache_[key];
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Helper to create a DOMStorageWrapper or else fall back to MemoryStorage.
 * TODO: Once MemoryStorage and DOMStorageWrapper have a shared interface this method annotation should change
 * to reflect this type
 *
 * @param domStorageName - Name of the underlying storage object
 *   (e.g. 'localStorage' or 'sessionStorage').
 * @returns Turning off type information until a common interface is defined.
 */
const createStoragefor = function (domStorageName) {
    try {
        // NOTE: just accessing "localStorage" or "window['localStorage']" may throw a security exception,
        // so it must be inside the try/catch.
        if (typeof window !== 'undefined' &&
            typeof window[domStorageName] !== 'undefined') {
            // Need to test cache. Just because it's here doesn't mean it works
            const domStorage = window[domStorageName];
            domStorage.setItem('firebase:sentinel', 'cache');
            domStorage.removeItem('firebase:sentinel');
            return new DOMStorageWrapper(domStorage);
        }
    }
    catch (e) { }
    // Failed to create wrapper.  Just return in-memory storage.
    // TODO: log?
    return new MemoryStorage();
};
/** A storage object that lasts across sessions */
const PersistentStorage = createStoragefor('localStorage');
/** A storage object that only lasts one session */
const SessionStorage = createStoragefor('sessionStorage');

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const logClient = new logger$1.Logger('@firebase/database');
/**
 * Returns a locally-unique ID (generated by just incrementing up from 0 each time its called).
 */
const LUIDGenerator = (function () {
    let id = 1;
    return function () {
        return id++;
    };
})();
/**
 * Sha1 hash of the input string
 * @param str - The string to hash
 * @returns {!string} The resulting hash
 */
const sha1 = function (str) {
    const utf8Bytes = util.stringToByteArray(str);
    const sha1 = new util.Sha1();
    sha1.update(utf8Bytes);
    const sha1Bytes = sha1.digest();
    return util.base64.encodeByteArray(sha1Bytes);
};
const buildLogMessage_ = function (...varArgs) {
    let message = '';
    for (let i = 0; i < varArgs.length; i++) {
        const arg = varArgs[i];
        if (Array.isArray(arg) ||
            (arg &&
                typeof arg === 'object' &&
                // eslint-disable-next-line @typescript-eslint/no-explicit-any
                typeof arg.length === 'number')) {
            message += buildLogMessage_.apply(null, arg);
        }
        else if (typeof arg === 'object') {
            message += util.stringify(arg);
        }
        else {
            message += arg;
        }
        message += ' ';
    }
    return message;
};
/**
 * Use this for all debug messages in Firebase.
 */
let logger = null;
/**
 * Flag to check for log availability on first log message
 */
let firstLog_ = true;
/**
 * The implementation of Firebase.enableLogging (defined here to break dependencies)
 * @param logger_ - A flag to turn on logging, or a custom logger
 * @param persistent - Whether or not to persist logging settings across refreshes
 */
const enableLogging$1 = function (logger_, persistent) {
    util.assert(!persistent || logger_ === true || logger_ === false, "Can't turn on custom loggers persistently.");
    if (logger_ === true) {
        logClient.logLevel = logger$1.LogLevel.VERBOSE;
        logger = logClient.log.bind(logClient);
        if (persistent) {
            SessionStorage.set('logging_enabled', true);
        }
    }
    else if (typeof logger_ === 'function') {
        logger = logger_;
    }
    else {
        logger = null;
        SessionStorage.remove('logging_enabled');
    }
};
const log = function (...varArgs) {
    if (firstLog_ === true) {
        firstLog_ = false;
        if (logger === null && SessionStorage.get('logging_enabled') === true) {
            enableLogging$1(true);
        }
    }
    if (logger) {
        const message = buildLogMessage_.apply(null, varArgs);
        logger(message);
    }
};
const logWrapper = function (prefix) {
    return function (...varArgs) {
        log(prefix, ...varArgs);
    };
};
const error = function (...varArgs) {
    const message = 'FIREBASE INTERNAL ERROR: ' + buildLogMessage_(...varArgs);
    logClient.error(message);
};
const fatal = function (...varArgs) {
    const message = `FIREBASE FATAL ERROR: ${buildLogMessage_(...varArgs)}`;
    logClient.error(message);
    throw new Error(message);
};
const warn = function (...varArgs) {
    const message = 'FIREBASE WARNING: ' + buildLogMessage_(...varArgs);
    logClient.warn(message);
};
/**
 * Logs a warning if the containing page uses https. Called when a call to new Firebase
 * does not use https.
 */
const warnIfPageIsSecure = function () {
    // Be very careful accessing browser globals. Who knows what may or may not exist.
    if (typeof window !== 'undefined' &&
        window.location &&
        window.location.protocol &&
        window.location.protocol.indexOf('https:') !== -1) {
        warn('Insecure Firebase access from a secure page. ' +
            'Please use https in calls to new Firebase().');
    }
};
/**
 * Returns true if data is NaN, or +/- Infinity.
 */
const isInvalidJSONNumber = function (data) {
    return (typeof data === 'number' &&
        (data !== data || // NaN
            data === Number.POSITIVE_INFINITY ||
            data === Number.NEGATIVE_INFINITY));
};
const executeWhenDOMReady = function (fn) {
    if (util.isNodeSdk() || document.readyState === 'complete') {
        fn();
    }
    else {
        // Modeled after jQuery. Try DOMContentLoaded and onreadystatechange (which
        // fire before onload), but fall back to onload.
        let called = false;
        const wrappedFn = function () {
            if (!document.body) {
                setTimeout(wrappedFn, Math.floor(10));
                return;
            }
            if (!called) {
                called = true;
                fn();
            }
        };
        if (document.addEventListener) {
            document.addEventListener('DOMContentLoaded', wrappedFn, false);
            // fallback to onload.
            window.addEventListener('load', wrappedFn, false);
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
        }
        else if (document.attachEvent) {
            // IE.
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            document.attachEvent('onreadystatechange', () => {
                if (document.readyState === 'complete') {
                    wrappedFn();
                }
            });
            // fallback to onload.
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            window.attachEvent('onload', wrappedFn);
            // jQuery has an extra hack for IE that we could employ (based on
            // http://javascript.nwbox.com/IEContentLoaded/) But it looks really old.
            // I'm hoping we don't need it.
        }
    }
};
/**
 * Minimum key name. Invalid for actual data, used as a marker to sort before any valid names
 */
const MIN_NAME = '[MIN_NAME]';
/**
 * Maximum key name. Invalid for actual data, used as a marker to sort above any valid names
 */
const MAX_NAME = '[MAX_NAME]';
/**
 * Compares valid Firebase key names, plus min and max name
 */
const nameCompare = function (a, b) {
    if (a === b) {
        return 0;
    }
    else if (a === MIN_NAME || b === MAX_NAME) {
        return -1;
    }
    else if (b === MIN_NAME || a === MAX_NAME) {
        return 1;
    }
    else {
        const aAsInt = tryParseInt(a), bAsInt = tryParseInt(b);
        if (aAsInt !== null) {
            if (bAsInt !== null) {
                return aAsInt - bAsInt === 0 ? a.length - b.length : aAsInt - bAsInt;
            }
            else {
                return -1;
            }
        }
        else if (bAsInt !== null) {
            return 1;
        }
        else {
            return a < b ? -1 : 1;
        }
    }
};
/**
 * @returns {!number} comparison result.
 */
const stringCompare = function (a, b) {
    if (a === b) {
        return 0;
    }
    else if (a < b) {
        return -1;
    }
    else {
        return 1;
    }
};
const requireKey = function (key, obj) {
    if (obj && key in obj) {
        return obj[key];
    }
    else {
        throw new Error('Missing required key (' + key + ') in object: ' + util.stringify(obj));
    }
};
const ObjectToUniqueKey = function (obj) {
    if (typeof obj !== 'object' || obj === null) {
        return util.stringify(obj);
    }
    const keys = [];
    // eslint-disable-next-line guard-for-in
    for (const k in obj) {
        keys.push(k);
    }
    // Export as json, but with the keys sorted.
    keys.sort();
    let key = '{';
    for (let i = 0; i < keys.length; i++) {
        if (i !== 0) {
            key += ',';
        }
        key += util.stringify(keys[i]);
        key += ':';
        key += ObjectToUniqueKey(obj[keys[i]]);
    }
    key += '}';
    return key;
};
/**
 * Splits a string into a number of smaller segments of maximum size
 * @param str - The string
 * @param segsize - The maximum number of chars in the string.
 * @returns The string, split into appropriately-sized chunks
 */
const splitStringBySize = function (str, segsize) {
    const len = str.length;
    if (len <= segsize) {
        return [str];
    }
    const dataSegs = [];
    for (let c = 0; c < len; c += segsize) {
        if (c + segsize > len) {
            dataSegs.push(str.substring(c, len));
        }
        else {
            dataSegs.push(str.substring(c, c + segsize));
        }
    }
    return dataSegs;
};
/**
 * Apply a function to each (key, value) pair in an object or
 * apply a function to each (index, value) pair in an array
 * @param obj - The object or array to iterate over
 * @param fn - The function to apply
 */
function each(obj, fn) {
    for (const key in obj) {
        if (obj.hasOwnProperty(key)) {
            fn(key, obj[key]);
        }
    }
}
/**
 * Borrowed from http://hg.secondlife.com/llsd/src/tip/js/typedarray.js (MIT License)
 * I made one modification at the end and removed the NaN / Infinity
 * handling (since it seemed broken [caused an overflow] and we don't need it).  See MJL comments.
 * @param v - A double
 *
 */
const doubleToIEEE754String = function (v) {
    util.assert(!isInvalidJSONNumber(v), 'Invalid JSON number'); // MJL
    const ebits = 11, fbits = 52;
    const bias = (1 << (ebits - 1)) - 1;
    let s, e, f, ln, i;
    // Compute sign, exponent, fraction
    // Skip NaN / Infinity handling --MJL.
    if (v === 0) {
        e = 0;
        f = 0;
        s = 1 / v === -Infinity ? 1 : 0;
    }
    else {
        s = v < 0;
        v = Math.abs(v);
        if (v >= Math.pow(2, 1 - bias)) {
            // Normalized
            ln = Math.min(Math.floor(Math.log(v) / Math.LN2), bias);
            e = ln + bias;
            f = Math.round(v * Math.pow(2, fbits - ln) - Math.pow(2, fbits));
        }
        else {
            // Denormalized
            e = 0;
            f = Math.round(v / Math.pow(2, 1 - bias - fbits));
        }
    }
    // Pack sign, exponent, fraction
    const bits = [];
    for (i = fbits; i; i -= 1) {
        bits.push(f % 2 ? 1 : 0);
        f = Math.floor(f / 2);
    }
    for (i = ebits; i; i -= 1) {
        bits.push(e % 2 ? 1 : 0);
        e = Math.floor(e / 2);
    }
    bits.push(s ? 1 : 0);
    bits.reverse();
    const str = bits.join('');
    // Return the data as a hex string. --MJL
    let hexByteString = '';
    for (i = 0; i < 64; i += 8) {
        let hexByte = parseInt(str.substr(i, 8), 2).toString(16);
        if (hexByte.length === 1) {
            hexByte = '0' + hexByte;
        }
        hexByteString = hexByteString + hexByte;
    }
    return hexByteString.toLowerCase();
};
/**
 * Used to detect if we're in a Chrome content script (which executes in an
 * isolated environment where long-polling doesn't work).
 */
const isChromeExtensionContentScript = function () {
    return !!(typeof window === 'object' &&
        window['chrome'] &&
        window['chrome']['extension'] &&
        !/^chrome/.test(window.location.href));
};
/**
 * Used to detect if we're in a Windows 8 Store app.
 */
const isWindowsStoreApp = function () {
    // Check for the presence of a couple WinRT globals
    return typeof Windows === 'object' && typeof Windows.UI === 'object';
};
/**
 * Converts a server error code to a Javascript Error
 */
function errorForServerCode(code, query) {
    let reason = 'Unknown Error';
    if (code === 'too_big') {
        reason =
            'The data requested exceeds the maximum size ' +
                'that can be accessed with a single request.';
    }
    else if (code === 'permission_denied') {
        reason = "Client doesn't have permission to access the desired data.";
    }
    else if (code === 'unavailable') {
        reason = 'The service is unavailable';
    }
    const error = new Error(code + ' at ' + query._path.toString() + ': ' + reason);
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    error.code = code.toUpperCase();
    return error;
}
/**
 * Used to test for integer-looking strings
 */
const INTEGER_REGEXP_ = new RegExp('^-?(0*)\\d{1,10}$');
/**
 * For use in keys, the minimum possible 32-bit integer.
 */
const INTEGER_32_MIN = -2147483648;
/**
 * For use in kyes, the maximum possible 32-bit integer.
 */
const INTEGER_32_MAX = 2147483647;
/**
 * If the string contains a 32-bit integer, return it.  Else return null.
 */
const tryParseInt = function (str) {
    if (INTEGER_REGEXP_.test(str)) {
        const intVal = Number(str);
        if (intVal >= INTEGER_32_MIN && intVal <= INTEGER_32_MAX) {
            return intVal;
        }
    }
    return null;
};
/**
 * Helper to run some code but catch any exceptions and re-throw them later.
 * Useful for preventing user callbacks from breaking internal code.
 *
 * Re-throwing the exception from a setTimeout is a little evil, but it's very
 * convenient (we don't have to try to figure out when is a safe point to
 * re-throw it), and the behavior seems reasonable:
 *
 * * If you aren't pausing on exceptions, you get an error in the console with
 *   the correct stack trace.
 * * If you're pausing on all exceptions, the debugger will pause on your
 *   exception and then again when we rethrow it.
 * * If you're only pausing on uncaught exceptions, the debugger will only pause
 *   on us re-throwing it.
 *
 * @param fn - The code to guard.
 */
const exceptionGuard = function (fn) {
    try {
        fn();
    }
    catch (e) {
        // Re-throw exception when it's safe.
        setTimeout(() => {
            // It used to be that "throw e" would result in a good console error with
            // relevant context, but as of Chrome 39, you just get the firebase.js
            // file/line number where we re-throw it, which is useless. So we log
            // e.stack explicitly.
            const stack = e.stack || '';
            warn('Exception was thrown by user callback.', stack);
            throw e;
        }, Math.floor(0));
    }
};
/**
 * @returns {boolean} true if we think we're currently being crawled.
 */
const beingCrawled = function () {
    const userAgent = (typeof window === 'object' &&
        window['navigator'] &&
        window['navigator']['userAgent']) ||
        '';
    // For now we whitelist the most popular crawlers.  We should refine this to be the set of crawlers we
    // believe to support JavaScript/AJAX rendering.
    // NOTE: Google Webmaster Tools doesn't really belong, but their "This is how a visitor to your website
    // would have seen the page" is flaky if we don't treat it as a crawler.
    return (userAgent.search(/googlebot|google webmaster tools|bingbot|yahoo! slurp|baiduspider|yandexbot|duckduckbot/i) >= 0);
};
/**
 * Same as setTimeout() except on Node.JS it will /not/ prevent the process from exiting.
 *
 * It is removed with clearTimeout() as normal.
 *
 * @param fn - Function to run.
 * @param time - Milliseconds to wait before running.
 * @returns The setTimeout() return value.
 */
const setTimeoutNonBlocking = function (fn, time) {
    const timeout = setTimeout(fn, time);
    // Note: at the time of this comment, unrefTimer is under the unstable set of APIs. Run with --unstable to enable the API.
    if (typeof timeout === 'number' &&
        // @ts-ignore Is only defined in Deno environments.
        typeof Deno !== 'undefined' &&
        // @ts-ignore Deno and unrefTimer are only defined in Deno environments.
        Deno['unrefTimer']) {
        // @ts-ignore Deno and unrefTimer are only defined in Deno environments.
        Deno.unrefTimer(timeout);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
    }
    else if (typeof timeout === 'object' && timeout['unref']) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        timeout['unref']();
    }
    return timeout;
};

/**
 * @license
 * Copyright 2021 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Abstraction around AppCheck's token fetching capabilities.
 */
class AppCheckTokenProvider {
    constructor(appName_, appCheckProvider) {
        this.appName_ = appName_;
        this.appCheckProvider = appCheckProvider;
        this.appCheck = appCheckProvider === null || appCheckProvider === void 0 ? void 0 : appCheckProvider.getImmediate({ optional: true });
        if (!this.appCheck) {
            appCheckProvider === null || appCheckProvider === void 0 ? void 0 : appCheckProvider.get().then(appCheck => (this.appCheck = appCheck));
        }
    }
    getToken(forceRefresh) {
        if (!this.appCheck) {
            return new Promise((resolve, reject) => {
                // Support delayed initialization of FirebaseAppCheck. This allows our
                // customers to initialize the RTDB SDK before initializing Firebase
                // AppCheck and ensures that all requests are authenticated if a token
                // becomes available before the timoeout below expires.
                setTimeout(() => {
                    if (this.appCheck) {
                        this.getToken(forceRefresh).then(resolve, reject);
                    }
                    else {
                        resolve(null);
                    }
                }, 0);
            });
        }
        return this.appCheck.getToken(forceRefresh);
    }
    addTokenChangeListener(listener) {
        var _a;
        (_a = this.appCheckProvider) === null || _a === void 0 ? void 0 : _a.get().then(appCheck => appCheck.addTokenListener(listener));
    }
    notifyForInvalidToken() {
        warn(`Provided AppCheck credentials for the app named "${this.appName_}" ` +
            'are invalid. This usually indicates your app was not initialized correctly.');
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Abstraction around FirebaseApp's token fetching capabilities.
 */
class FirebaseAuthTokenProvider {
    constructor(appName_, firebaseOptions_, authProvider_) {
        this.appName_ = appName_;
        this.firebaseOptions_ = firebaseOptions_;
        this.authProvider_ = authProvider_;
        this.auth_ = null;
        this.auth_ = authProvider_.getImmediate({ optional: true });
        if (!this.auth_) {
            authProvider_.onInit(auth => (this.auth_ = auth));
        }
    }
    getToken(forceRefresh) {
        if (!this.auth_) {
            return new Promise((resolve, reject) => {
                // Support delayed initialization of FirebaseAuth. This allows our
                // customers to initialize the RTDB SDK before initializing Firebase
                // Auth and ensures that all requests are authenticated if a token
                // becomes available before the timoeout below expires.
                setTimeout(() => {
                    if (this.auth_) {
                        this.getToken(forceRefresh).then(resolve, reject);
                    }
                    else {
                        resolve(null);
                    }
                }, 0);
            });
        }
        return this.auth_.getToken(forceRefresh).catch(error => {
            // TODO: Need to figure out all the cases this is raised and whether
            // this makes sense.
            if (error && error.code === 'auth/token-not-initialized') {
                log('Got auth/token-not-initialized error.  Treating as null token.');
                return null;
            }
            else {
                return Promise.reject(error);
            }
        });
    }
    addTokenChangeListener(listener) {
        // TODO: We might want to wrap the listener and call it with no args to
        // avoid a leaky abstraction, but that makes removing the listener harder.
        if (this.auth_) {
            this.auth_.addAuthTokenListener(listener);
        }
        else {
            this.authProvider_
                .get()
                .then(auth => auth.addAuthTokenListener(listener));
        }
    }
    removeTokenChangeListener(listener) {
        this.authProvider_
            .get()
            .then(auth => auth.removeAuthTokenListener(listener));
    }
    notifyForInvalidToken() {
        let errorMessage = 'Provided authentication credentials for the app named "' +
            this.appName_ +
            '" are invalid. This usually indicates your app was not ' +
            'initialized correctly. ';
        if ('credential' in this.firebaseOptions_) {
            errorMessage +=
                'Make sure the "credential" property provided to initializeApp() ' +
                    'is authorized to access the specified "databaseURL" and is from the correct ' +
                    'project.';
        }
        else if ('serviceAccount' in this.firebaseOptions_) {
            errorMessage +=
                'Make sure the "serviceAccount" property provided to initializeApp() ' +
                    'is authorized to access the specified "databaseURL" and is from the correct ' +
                    'project.';
        }
        else {
            errorMessage +=
                'Make sure the "apiKey" and "databaseURL" properties provided to ' +
                    'initializeApp() match the values provided for your app at ' +
                    'https://console.firebase.google.com/.';
        }
        warn(errorMessage);
    }
}
/* AuthTokenProvider that supplies a constant token. Used by Admin SDK or mockUserToken with emulators. */
class EmulatorTokenProvider {
    constructor(accessToken) {
        this.accessToken = accessToken;
    }
    getToken(forceRefresh) {
        return Promise.resolve({
            accessToken: this.accessToken
        });
    }
    addTokenChangeListener(listener) {
        // Invoke the listener immediately to match the behavior in Firebase Auth
        // (see packages/auth/src/auth.js#L1807)
        listener(this.accessToken);
    }
    removeTokenChangeListener(listener) { }
    notifyForInvalidToken() { }
}
/** A string that is treated as an admin access token by the RTDB emulator. Used by Admin SDK. */
EmulatorTokenProvider.OWNER = 'owner';

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const PROTOCOL_VERSION = '5';
const VERSION_PARAM = 'v';
const TRANSPORT_SESSION_PARAM = 's';
const REFERER_PARAM = 'r';
const FORGE_REF = 'f';
// Matches console.firebase.google.com, firebase-console-*.corp.google.com and
// firebase.corp.google.com
const FORGE_DOMAIN_RE = /(console\.firebase|firebase-console-\w+\.corp|firebase\.corp)\.google\.com/;
const LAST_SESSION_PARAM = 'ls';
const APPLICATION_ID_PARAM = 'p';
const APP_CHECK_TOKEN_PARAM = 'ac';
const WEBSOCKET = 'websocket';
const LONG_POLLING = 'long_polling';

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A class that holds metadata about a Repo object
 */
class RepoInfo {
    /**
     * @param host - Hostname portion of the url for the repo
     * @param secure - Whether or not this repo is accessed over ssl
     * @param namespace - The namespace represented by the repo
     * @param webSocketOnly - Whether to prefer websockets over all other transports (used by Nest).
     * @param nodeAdmin - Whether this instance uses Admin SDK credentials
     * @param persistenceKey - Override the default session persistence storage key
     */
    constructor(host, secure, namespace, webSocketOnly, nodeAdmin = false, persistenceKey = '', includeNamespaceInQueryParams = false, isUsingEmulator = false) {
        this.secure = secure;
        this.namespace = namespace;
        this.webSocketOnly = webSocketOnly;
        this.nodeAdmin = nodeAdmin;
        this.persistenceKey = persistenceKey;
        this.includeNamespaceInQueryParams = includeNamespaceInQueryParams;
        this.isUsingEmulator = isUsingEmulator;
        this._host = host.toLowerCase();
        this._domain = this._host.substr(this._host.indexOf('.') + 1);
        this.internalHost =
            PersistentStorage.get('host:' + host) || this._host;
    }
    isCacheableHost() {
        return this.internalHost.substr(0, 2) === 's-';
    }
    isCustomHost() {
        return (this._domain !== 'firebaseio.com' &&
            this._domain !== 'firebaseio-demo.com');
    }
    get host() {
        return this._host;
    }
    set host(newHost) {
        if (newHost !== this.internalHost) {
            this.internalHost = newHost;
            if (this.isCacheableHost()) {
                PersistentStorage.set('host:' + this._host, this.internalHost);
            }
        }
    }
    toString() {
        let str = this.toURLString();
        if (this.persistenceKey) {
            str += '<' + this.persistenceKey + '>';
        }
        return str;
    }
    toURLString() {
        const protocol = this.secure ? 'https://' : 'http://';
        const query = this.includeNamespaceInQueryParams
            ? `?ns=${this.namespace}`
            : '';
        return `${protocol}${this.host}/${query}`;
    }
}
function repoInfoNeedsQueryParam(repoInfo) {
    return (repoInfo.host !== repoInfo.internalHost ||
        repoInfo.isCustomHost() ||
        repoInfo.includeNamespaceInQueryParams);
}
/**
 * Returns the websocket URL for this repo
 * @param repoInfo - RepoInfo object
 * @param type - of connection
 * @param params - list
 * @returns The URL for this repo
 */
function repoInfoConnectionURL(repoInfo, type, params) {
    util.assert(typeof type === 'string', 'typeof type must == string');
    util.assert(typeof params === 'object', 'typeof params must == object');
    let connURL;
    if (type === WEBSOCKET) {
        connURL =
            (repoInfo.secure ? 'wss://' : 'ws://') + repoInfo.internalHost + '/.ws?';
    }
    else if (type === LONG_POLLING) {
        connURL =
            (repoInfo.secure ? 'https://' : 'http://') +
                repoInfo.internalHost +
                '/.lp?';
    }
    else {
        throw new Error('Unknown connection type: ' + type);
    }
    if (repoInfoNeedsQueryParam(repoInfo)) {
        params['ns'] = repoInfo.namespace;
    }
    const pairs = [];
    each(params, (key, value) => {
        pairs.push(key + '=' + value);
    });
    return connURL + pairs.join('&');
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Tracks a collection of stats.
 */
class StatsCollection {
    constructor() {
        this.counters_ = {};
    }
    incrementCounter(name, amount = 1) {
        if (!util.contains(this.counters_, name)) {
            this.counters_[name] = 0;
        }
        this.counters_[name] += amount;
    }
    get() {
        return util.deepCopy(this.counters_);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const collections = {};
const reporters = {};
function statsManagerGetCollection(repoInfo) {
    const hashString = repoInfo.toString();
    if (!collections[hashString]) {
        collections[hashString] = new StatsCollection();
    }
    return collections[hashString];
}
function statsManagerGetOrCreateReporter(repoInfo, creatorFunction) {
    const hashString = repoInfo.toString();
    if (!reporters[hashString]) {
        reporters[hashString] = creatorFunction();
    }
    return reporters[hashString];
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * This class ensures the packets from the server arrive in order
 * This class takes data from the server and ensures it gets passed into the callbacks in order.
 */
class PacketReceiver {
    /**
     * @param onMessage_
     */
    constructor(onMessage_) {
        this.onMessage_ = onMessage_;
        this.pendingResponses = [];
        this.currentResponseNum = 0;
        this.closeAfterResponse = -1;
        this.onClose = null;
    }
    closeAfter(responseNum, callback) {
        this.closeAfterResponse = responseNum;
        this.onClose = callback;
        if (this.closeAfterResponse < this.currentResponseNum) {
            this.onClose();
            this.onClose = null;
        }
    }
    /**
     * Each message from the server comes with a response number, and an array of data. The responseNumber
     * allows us to ensure that we process them in the right order, since we can't be guaranteed that all
     * browsers will respond in the same order as the requests we sent
     */
    handleResponse(requestNum, data) {
        this.pendingResponses[requestNum] = data;
        while (this.pendingResponses[this.currentResponseNum]) {
            const toProcess = this.pendingResponses[this.currentResponseNum];
            delete this.pendingResponses[this.currentResponseNum];
            for (let i = 0; i < toProcess.length; ++i) {
                if (toProcess[i]) {
                    exceptionGuard(() => {
                        this.onMessage_(toProcess[i]);
                    });
                }
            }
            if (this.currentResponseNum === this.closeAfterResponse) {
                if (this.onClose) {
                    this.onClose();
                    this.onClose = null;
                }
                break;
            }
            this.currentResponseNum++;
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// URL query parameters associated with longpolling
const FIREBASE_LONGPOLL_START_PARAM = 'start';
const FIREBASE_LONGPOLL_CLOSE_COMMAND = 'close';
const FIREBASE_LONGPOLL_COMMAND_CB_NAME = 'pLPCommand';
const FIREBASE_LONGPOLL_DATA_CB_NAME = 'pRTLPCB';
const FIREBASE_LONGPOLL_ID_PARAM = 'id';
const FIREBASE_LONGPOLL_PW_PARAM = 'pw';
const FIREBASE_LONGPOLL_SERIAL_PARAM = 'ser';
const FIREBASE_LONGPOLL_CALLBACK_ID_PARAM = 'cb';
const FIREBASE_LONGPOLL_SEGMENT_NUM_PARAM = 'seg';
const FIREBASE_LONGPOLL_SEGMENTS_IN_PACKET = 'ts';
const FIREBASE_LONGPOLL_DATA_PARAM = 'd';
const FIREBASE_LONGPOLL_DISCONN_FRAME_REQUEST_PARAM = 'dframe';
//Data size constants.
//TODO: Perf: the maximum length actually differs from browser to browser.
// We should check what browser we're on and set accordingly.
const MAX_URL_DATA_SIZE = 1870;
const SEG_HEADER_SIZE = 30; //ie: &seg=8299234&ts=982389123&d=
const MAX_PAYLOAD_SIZE = MAX_URL_DATA_SIZE - SEG_HEADER_SIZE;
/**
 * Keepalive period
 * send a fresh request at minimum every 25 seconds. Opera has a maximum request
 * length of 30 seconds that we can't exceed.
 */
const KEEPALIVE_REQUEST_INTERVAL = 25000;
/**
 * How long to wait before aborting a long-polling connection attempt.
 */
const LP_CONNECT_TIMEOUT = 30000;
/**
 * This class manages a single long-polling connection.
 */
class BrowserPollConnection {
    /**
     * @param connId An identifier for this connection, used for logging
     * @param repoInfo The info for the endpoint to send data to.
     * @param applicationId The Firebase App ID for this project.
     * @param appCheckToken The AppCheck token for this client.
     * @param authToken The AuthToken to use for this connection.
     * @param transportSessionId Optional transportSessionid if we are
     * reconnecting for an existing transport session
     * @param lastSessionId Optional lastSessionId if the PersistentConnection has
     * already created a connection previously
     */
    constructor(connId, repoInfo, applicationId, appCheckToken, authToken, transportSessionId, lastSessionId) {
        this.connId = connId;
        this.repoInfo = repoInfo;
        this.applicationId = applicationId;
        this.appCheckToken = appCheckToken;
        this.authToken = authToken;
        this.transportSessionId = transportSessionId;
        this.lastSessionId = lastSessionId;
        this.bytesSent = 0;
        this.bytesReceived = 0;
        this.everConnected_ = false;
        this.log_ = logWrapper(connId);
        this.stats_ = statsManagerGetCollection(repoInfo);
        this.urlFn = (params) => {
            // Always add the token if we have one.
            if (this.appCheckToken) {
                params[APP_CHECK_TOKEN_PARAM] = this.appCheckToken;
            }
            return repoInfoConnectionURL(repoInfo, LONG_POLLING, params);
        };
    }
    /**
     * @param onMessage - Callback when messages arrive
     * @param onDisconnect - Callback with connection lost.
     */
    open(onMessage, onDisconnect) {
        this.curSegmentNum = 0;
        this.onDisconnect_ = onDisconnect;
        this.myPacketOrderer = new PacketReceiver(onMessage);
        this.isClosed_ = false;
        this.connectTimeoutTimer_ = setTimeout(() => {
            this.log_('Timed out trying to connect.');
            // Make sure we clear the host cache
            this.onClosed_();
            this.connectTimeoutTimer_ = null;
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
        }, Math.floor(LP_CONNECT_TIMEOUT));
        // Ensure we delay the creation of the iframe until the DOM is loaded.
        executeWhenDOMReady(() => {
            if (this.isClosed_) {
                return;
            }
            //Set up a callback that gets triggered once a connection is set up.
            this.scriptTagHolder = new FirebaseIFrameScriptHolder((...args) => {
                const [command, arg1, arg2, arg3, arg4] = args;
                this.incrementIncomingBytes_(args);
                if (!this.scriptTagHolder) {
                    return; // we closed the connection.
                }
                if (this.connectTimeoutTimer_) {
                    clearTimeout(this.connectTimeoutTimer_);
                    this.connectTimeoutTimer_ = null;
                }
                this.everConnected_ = true;
                if (command === FIREBASE_LONGPOLL_START_PARAM) {
                    this.id = arg1;
                    this.password = arg2;
                }
                else if (command === FIREBASE_LONGPOLL_CLOSE_COMMAND) {
                    // Don't clear the host cache. We got a response from the server, so we know it's reachable
                    if (arg1) {
                        // We aren't expecting any more data (other than what the server's already in the process of sending us
                        // through our already open polls), so don't send any more.
                        this.scriptTagHolder.sendNewPolls = false;
                        // arg1 in this case is the last response number sent by the server. We should try to receive
                        // all of the responses up to this one before closing
                        this.myPacketOrderer.closeAfter(arg1, () => {
                            this.onClosed_();
                        });
                    }
                    else {
                        this.onClosed_();
                    }
                }
                else {
                    throw new Error('Unrecognized command received: ' + command);
                }
            }, (...args) => {
                const [pN, data] = args;
                this.incrementIncomingBytes_(args);
                this.myPacketOrderer.handleResponse(pN, data);
            }, () => {
                this.onClosed_();
            }, this.urlFn);
            //Send the initial request to connect. The serial number is simply to keep the browser from pulling previous results
            //from cache.
            const urlParams = {};
            urlParams[FIREBASE_LONGPOLL_START_PARAM] = 't';
            urlParams[FIREBASE_LONGPOLL_SERIAL_PARAM] = Math.floor(Math.random() * 100000000);
            if (this.scriptTagHolder.uniqueCallbackIdentifier) {
                urlParams[FIREBASE_LONGPOLL_CALLBACK_ID_PARAM] =
                    this.scriptTagHolder.uniqueCallbackIdentifier;
            }
            urlParams[VERSION_PARAM] = PROTOCOL_VERSION;
            if (this.transportSessionId) {
                urlParams[TRANSPORT_SESSION_PARAM] = this.transportSessionId;
            }
            if (this.lastSessionId) {
                urlParams[LAST_SESSION_PARAM] = this.lastSessionId;
            }
            if (this.applicationId) {
                urlParams[APPLICATION_ID_PARAM] = this.applicationId;
            }
            if (this.appCheckToken) {
                urlParams[APP_CHECK_TOKEN_PARAM] = this.appCheckToken;
            }
            if (typeof location !== 'undefined' &&
                location.hostname &&
                FORGE_DOMAIN_RE.test(location.hostname)) {
                urlParams[REFERER_PARAM] = FORGE_REF;
            }
            const connectURL = this.urlFn(urlParams);
            this.log_('Connecting via long-poll to ' + connectURL);
            this.scriptTagHolder.addTag(connectURL, () => {
                /* do nothing */
            });
        });
    }
    /**
     * Call this when a handshake has completed successfully and we want to consider the connection established
     */
    start() {
        this.scriptTagHolder.startLongPoll(this.id, this.password);
        this.addDisconnectPingFrame(this.id, this.password);
    }
    /**
     * Forces long polling to be considered as a potential transport
     */
    static forceAllow() {
        BrowserPollConnection.forceAllow_ = true;
    }
    /**
     * Forces longpolling to not be considered as a potential transport
     */
    static forceDisallow() {
        BrowserPollConnection.forceDisallow_ = true;
    }
    // Static method, use string literal so it can be accessed in a generic way
    static isAvailable() {
        if (util.isNodeSdk()) {
            return false;
        }
        else if (BrowserPollConnection.forceAllow_) {
            return true;
        }
        else {
            // NOTE: In React-Native there's normally no 'document', but if you debug a React-Native app in
            // the Chrome debugger, 'document' is defined, but document.createElement is null (2015/06/08).
            return (!BrowserPollConnection.forceDisallow_ &&
                typeof document !== 'undefined' &&
                document.createElement != null &&
                !isChromeExtensionContentScript() &&
                !isWindowsStoreApp());
        }
    }
    /**
     * No-op for polling
     */
    markConnectionHealthy() { }
    /**
     * Stops polling and cleans up the iframe
     */
    shutdown_() {
        this.isClosed_ = true;
        if (this.scriptTagHolder) {
            this.scriptTagHolder.close();
            this.scriptTagHolder = null;
        }
        //remove the disconnect frame, which will trigger an XHR call to the server to tell it we're leaving.
        if (this.myDisconnFrame) {
            document.body.removeChild(this.myDisconnFrame);
            this.myDisconnFrame = null;
        }
        if (this.connectTimeoutTimer_) {
            clearTimeout(this.connectTimeoutTimer_);
            this.connectTimeoutTimer_ = null;
        }
    }
    /**
     * Triggered when this transport is closed
     */
    onClosed_() {
        if (!this.isClosed_) {
            this.log_('Longpoll is closing itself');
            this.shutdown_();
            if (this.onDisconnect_) {
                this.onDisconnect_(this.everConnected_);
                this.onDisconnect_ = null;
            }
        }
    }
    /**
     * External-facing close handler. RealTime has requested we shut down. Kill our connection and tell the server
     * that we've left.
     */
    close() {
        if (!this.isClosed_) {
            this.log_('Longpoll is being closed.');
            this.shutdown_();
        }
    }
    /**
     * Send the JSON object down to the server. It will need to be stringified, base64 encoded, and then
     * broken into chunks (since URLs have a small maximum length).
     * @param data - The JSON data to transmit.
     */
    send(data) {
        const dataStr = util.stringify(data);
        this.bytesSent += dataStr.length;
        this.stats_.incrementCounter('bytes_sent', dataStr.length);
        //first, lets get the base64-encoded data
        const base64data = util.base64Encode(dataStr);
        //We can only fit a certain amount in each URL, so we need to split this request
        //up into multiple pieces if it doesn't fit in one request.
        const dataSegs = splitStringBySize(base64data, MAX_PAYLOAD_SIZE);
        //Enqueue each segment for transmission. We assign each chunk a sequential ID and a total number
        //of segments so that we can reassemble the packet on the server.
        for (let i = 0; i < dataSegs.length; i++) {
            this.scriptTagHolder.enqueueSegment(this.curSegmentNum, dataSegs.length, dataSegs[i]);
            this.curSegmentNum++;
        }
    }
    /**
     * This is how we notify the server that we're leaving.
     * We aren't able to send requests with DHTML on a window close event, but we can
     * trigger XHR requests in some browsers (everything but Opera basically).
     */
    addDisconnectPingFrame(id, pw) {
        if (util.isNodeSdk()) {
            return;
        }
        this.myDisconnFrame = document.createElement('iframe');
        const urlParams = {};
        urlParams[FIREBASE_LONGPOLL_DISCONN_FRAME_REQUEST_PARAM] = 't';
        urlParams[FIREBASE_LONGPOLL_ID_PARAM] = id;
        urlParams[FIREBASE_LONGPOLL_PW_PARAM] = pw;
        this.myDisconnFrame.src = this.urlFn(urlParams);
        this.myDisconnFrame.style.display = 'none';
        document.body.appendChild(this.myDisconnFrame);
    }
    /**
     * Used to track the bytes received by this client
     */
    incrementIncomingBytes_(args) {
        // TODO: This is an annoying perf hit just to track the number of incoming bytes.  Maybe it should be opt-in.
        const bytesReceived = util.stringify(args).length;
        this.bytesReceived += bytesReceived;
        this.stats_.incrementCounter('bytes_received', bytesReceived);
    }
}
/*********************************************************************************************
 * A wrapper around an iframe that is used as a long-polling script holder.
 *********************************************************************************************/
class FirebaseIFrameScriptHolder {
    /**
     * @param commandCB - The callback to be called when control commands are recevied from the server.
     * @param onMessageCB - The callback to be triggered when responses arrive from the server.
     * @param onDisconnect - The callback to be triggered when this tag holder is closed
     * @param urlFn - A function that provides the URL of the endpoint to send data to.
     */
    constructor(commandCB, onMessageCB, onDisconnect, urlFn) {
        this.onDisconnect = onDisconnect;
        this.urlFn = urlFn;
        //We maintain a count of all of the outstanding requests, because if we have too many active at once it can cause
        //problems in some browsers.
        this.outstandingRequests = new Set();
        //A queue of the pending segments waiting for transmission to the server.
        this.pendingSegs = [];
        //A serial number. We use this for two things:
        // 1) A way to ensure the browser doesn't cache responses to polls
        // 2) A way to make the server aware when long-polls arrive in a different order than we started them. The
        //    server needs to release both polls in this case or it will cause problems in Opera since Opera can only execute
        //    JSONP code in the order it was added to the iframe.
        this.currentSerial = Math.floor(Math.random() * 100000000);
        // This gets set to false when we're "closing down" the connection (e.g. we're switching transports but there's still
        // incoming data from the server that we're waiting for).
        this.sendNewPolls = true;
        if (!util.isNodeSdk()) {
            //Each script holder registers a couple of uniquely named callbacks with the window. These are called from the
            //iframes where we put the long-polling script tags. We have two callbacks:
            //   1) Command Callback - Triggered for control issues, like starting a connection.
            //   2) Message Callback - Triggered when new data arrives.
            this.uniqueCallbackIdentifier = LUIDGenerator();
            window[FIREBASE_LONGPOLL_COMMAND_CB_NAME + this.uniqueCallbackIdentifier] = commandCB;
            window[FIREBASE_LONGPOLL_DATA_CB_NAME + this.uniqueCallbackIdentifier] =
                onMessageCB;
            //Create an iframe for us to add script tags to.
            this.myIFrame = FirebaseIFrameScriptHolder.createIFrame_();
            // Set the iframe's contents.
            let script = '';
            // if we set a javascript url, it's IE and we need to set the document domain. The javascript url is sufficient
            // for ie9, but ie8 needs to do it again in the document itself.
            if (this.myIFrame.src &&
                this.myIFrame.src.substr(0, 'javascript:'.length) === 'javascript:') {
                const currentDomain = document.domain;
                script = '<script>document.domain="' + currentDomain + '";</script>';
            }
            const iframeContents = '<html><body>' + script + '</body></html>';
            try {
                this.myIFrame.doc.open();
                this.myIFrame.doc.write(iframeContents);
                this.myIFrame.doc.close();
            }
            catch (e) {
                log('frame writing exception');
                if (e.stack) {
                    log(e.stack);
                }
                log(e);
            }
        }
        else {
            this.commandCB = commandCB;
            this.onMessageCB = onMessageCB;
        }
    }
    /**
     * Each browser has its own funny way to handle iframes. Here we mush them all together into one object that I can
     * actually use.
     */
    static createIFrame_() {
        const iframe = document.createElement('iframe');
        iframe.style.display = 'none';
        // This is necessary in order to initialize the document inside the iframe
        if (document.body) {
            document.body.appendChild(iframe);
            try {
                // If document.domain has been modified in IE, this will throw an error, and we need to set the
                // domain of the iframe's document manually. We can do this via a javascript: url as the src attribute
                // Also note that we must do this *after* the iframe has been appended to the page. Otherwise it doesn't work.
                const a = iframe.contentWindow.document;
                if (!a) {
                    // Apologies for the log-spam, I need to do something to keep closure from optimizing out the assignment above.
                    log('No IE domain setting required');
                }
            }
            catch (e) {
                const domain = document.domain;
                iframe.src =
                    "javascript:void((function(){document.open();document.domain='" +
                        domain +
                        "';document.close();})())";
            }
        }
        else {
            // LongPollConnection attempts to delay initialization until the document is ready, so hopefully this
            // never gets hit.
            throw 'Document body has not initialized. Wait to initialize Firebase until after the document is ready.';
        }
        // Get the document of the iframe in a browser-specific way.
        if (iframe.contentDocument) {
            iframe.doc = iframe.contentDocument; // Firefox, Opera, Safari
        }
        else if (iframe.contentWindow) {
            iframe.doc = iframe.contentWindow.document; // Internet Explorer
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
        }
        else if (iframe.document) {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            iframe.doc = iframe.document; //others?
        }
        return iframe;
    }
    /**
     * Cancel all outstanding queries and remove the frame.
     */
    close() {
        //Mark this iframe as dead, so no new requests are sent.
        this.alive = false;
        if (this.myIFrame) {
            //We have to actually remove all of the html inside this iframe before removing it from the
            //window, or IE will continue loading and executing the script tags we've already added, which
            //can lead to some errors being thrown. Setting textContent seems to be the safest way to do this.
            this.myIFrame.doc.body.textContent = '';
            setTimeout(() => {
                if (this.myIFrame !== null) {
                    document.body.removeChild(this.myIFrame);
                    this.myIFrame = null;
                }
            }, Math.floor(0));
        }
        // Protect from being called recursively.
        const onDisconnect = this.onDisconnect;
        if (onDisconnect) {
            this.onDisconnect = null;
            onDisconnect();
        }
    }
    /**
     * Actually start the long-polling session by adding the first script tag(s) to the iframe.
     * @param id - The ID of this connection
     * @param pw - The password for this connection
     */
    startLongPoll(id, pw) {
        this.myID = id;
        this.myPW = pw;
        this.alive = true;
        //send the initial request. If there are requests queued, make sure that we transmit as many as we are currently able to.
        while (this.newRequest_()) { }
    }
    /**
     * This is called any time someone might want a script tag to be added. It adds a script tag when there aren't
     * too many outstanding requests and we are still alive.
     *
     * If there are outstanding packet segments to send, it sends one. If there aren't, it sends a long-poll anyways if
     * needed.
     */
    newRequest_() {
        // We keep one outstanding request open all the time to receive data, but if we need to send data
        // (pendingSegs.length > 0) then we create a new request to send the data.  The server will automatically
        // close the old request.
        if (this.alive &&
            this.sendNewPolls &&
            this.outstandingRequests.size < (this.pendingSegs.length > 0 ? 2 : 1)) {
            //construct our url
            this.currentSerial++;
            const urlParams = {};
            urlParams[FIREBASE_LONGPOLL_ID_PARAM] = this.myID;
            urlParams[FIREBASE_LONGPOLL_PW_PARAM] = this.myPW;
            urlParams[FIREBASE_LONGPOLL_SERIAL_PARAM] = this.currentSerial;
            let theURL = this.urlFn(urlParams);
            //Now add as much data as we can.
            let curDataString = '';
            let i = 0;
            while (this.pendingSegs.length > 0) {
                //first, lets see if the next segment will fit.
                const nextSeg = this.pendingSegs[0];
                if (nextSeg.d.length +
                    SEG_HEADER_SIZE +
                    curDataString.length <=
                    MAX_URL_DATA_SIZE) {
                    //great, the segment will fit. Lets append it.
                    const theSeg = this.pendingSegs.shift();
                    curDataString =
                        curDataString +
                            '&' +
                            FIREBASE_LONGPOLL_SEGMENT_NUM_PARAM +
                            i +
                            '=' +
                            theSeg.seg +
                            '&' +
                            FIREBASE_LONGPOLL_SEGMENTS_IN_PACKET +
                            i +
                            '=' +
                            theSeg.ts +
                            '&' +
                            FIREBASE_LONGPOLL_DATA_PARAM +
                            i +
                            '=' +
                            theSeg.d;
                    i++;
                }
                else {
                    break;
                }
            }
            theURL = theURL + curDataString;
            this.addLongPollTag_(theURL, this.currentSerial);
            return true;
        }
        else {
            return false;
        }
    }
    /**
     * Queue a packet for transmission to the server.
     * @param segnum - A sequential id for this packet segment used for reassembly
     * @param totalsegs - The total number of segments in this packet
     * @param data - The data for this segment.
     */
    enqueueSegment(segnum, totalsegs, data) {
        //add this to the queue of segments to send.
        this.pendingSegs.push({ seg: segnum, ts: totalsegs, d: data });
        //send the data immediately if there isn't already data being transmitted, unless
        //startLongPoll hasn't been called yet.
        if (this.alive) {
            this.newRequest_();
        }
    }
    /**
     * Add a script tag for a regular long-poll request.
     * @param url - The URL of the script tag.
     * @param serial - The serial number of the request.
     */
    addLongPollTag_(url, serial) {
        //remember that we sent this request.
        this.outstandingRequests.add(serial);
        const doNewRequest = () => {
            this.outstandingRequests.delete(serial);
            this.newRequest_();
        };
        // If this request doesn't return on its own accord (by the server sending us some data), we'll
        // create a new one after the KEEPALIVE interval to make sure we always keep a fresh request open.
        const keepaliveTimeout = setTimeout(doNewRequest, Math.floor(KEEPALIVE_REQUEST_INTERVAL));
        const readyStateCB = () => {
            // Request completed.  Cancel the keepalive.
            clearTimeout(keepaliveTimeout);
            // Trigger a new request so we can continue receiving data.
            doNewRequest();
        };
        this.addTag(url, readyStateCB);
    }
    /**
     * Add an arbitrary script tag to the iframe.
     * @param url - The URL for the script tag source.
     * @param loadCB - A callback to be triggered once the script has loaded.
     */
    addTag(url, loadCB) {
        if (util.isNodeSdk()) {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            this.doNodeLongPoll(url, loadCB);
        }
        else {
            setTimeout(() => {
                try {
                    // if we're already closed, don't add this poll
                    if (!this.sendNewPolls) {
                        return;
                    }
                    const newScript = this.myIFrame.doc.createElement('script');
                    newScript.type = 'text/javascript';
                    newScript.async = true;
                    newScript.src = url;
                    // eslint-disable-next-line @typescript-eslint/no-explicit-any
                    newScript.onload = newScript.onreadystatechange =
                        function () {
                            // eslint-disable-next-line @typescript-eslint/no-explicit-any
                            const rstate = newScript.readyState;
                            if (!rstate || rstate === 'loaded' || rstate === 'complete') {
                                // eslint-disable-next-line @typescript-eslint/no-explicit-any
                                newScript.onload = newScript.onreadystatechange = null;
                                if (newScript.parentNode) {
                                    newScript.parentNode.removeChild(newScript);
                                }
                                loadCB();
                            }
                        };
                    newScript.onerror = () => {
                        log('Long-poll script failed to load: ' + url);
                        this.sendNewPolls = false;
                        this.close();
                    };
                    this.myIFrame.doc.body.appendChild(newScript);
                }
                catch (e) {
                    // TODO: we should make this error visible somehow
                }
            }, Math.floor(1));
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const WEBSOCKET_MAX_FRAME_SIZE = 16384;
const WEBSOCKET_KEEPALIVE_INTERVAL = 45000;
let WebSocketImpl = null;
if (typeof MozWebSocket !== 'undefined') {
    WebSocketImpl = MozWebSocket;
}
else if (typeof WebSocket !== 'undefined') {
    WebSocketImpl = WebSocket;
}
/**
 * Create a new websocket connection with the given callbacks.
 */
class WebSocketConnection {
    /**
     * @param connId identifier for this transport
     * @param repoInfo The info for the websocket endpoint.
     * @param applicationId The Firebase App ID for this project.
     * @param appCheckToken The App Check Token for this client.
     * @param authToken The Auth Token for this client.
     * @param transportSessionId Optional transportSessionId if this is connecting
     * to an existing transport session
     * @param lastSessionId Optional lastSessionId if there was a previous
     * connection
     */
    constructor(connId, repoInfo, applicationId, appCheckToken, authToken, transportSessionId, lastSessionId) {
        this.connId = connId;
        this.applicationId = applicationId;
        this.appCheckToken = appCheckToken;
        this.authToken = authToken;
        this.keepaliveTimer = null;
        this.frames = null;
        this.totalFrames = 0;
        this.bytesSent = 0;
        this.bytesReceived = 0;
        this.log_ = logWrapper(this.connId);
        this.stats_ = statsManagerGetCollection(repoInfo);
        this.connURL = WebSocketConnection.connectionURL_(repoInfo, transportSessionId, lastSessionId, appCheckToken, applicationId);
        this.nodeAdmin = repoInfo.nodeAdmin;
    }
    /**
     * @param repoInfo - The info for the websocket endpoint.
     * @param transportSessionId - Optional transportSessionId if this is connecting to an existing transport
     *                                         session
     * @param lastSessionId - Optional lastSessionId if there was a previous connection
     * @returns connection url
     */
    static connectionURL_(repoInfo, transportSessionId, lastSessionId, appCheckToken, applicationId) {
        const urlParams = {};
        urlParams[VERSION_PARAM] = PROTOCOL_VERSION;
        if (!util.isNodeSdk() &&
            typeof location !== 'undefined' &&
            location.hostname &&
            FORGE_DOMAIN_RE.test(location.hostname)) {
            urlParams[REFERER_PARAM] = FORGE_REF;
        }
        if (transportSessionId) {
            urlParams[TRANSPORT_SESSION_PARAM] = transportSessionId;
        }
        if (lastSessionId) {
            urlParams[LAST_SESSION_PARAM] = lastSessionId;
        }
        if (appCheckToken) {
            urlParams[APP_CHECK_TOKEN_PARAM] = appCheckToken;
        }
        if (applicationId) {
            urlParams[APPLICATION_ID_PARAM] = applicationId;
        }
        return repoInfoConnectionURL(repoInfo, WEBSOCKET, urlParams);
    }
    /**
     * @param onMessage - Callback when messages arrive
     * @param onDisconnect - Callback with connection lost.
     */
    open(onMessage, onDisconnect) {
        this.onDisconnect = onDisconnect;
        this.onMessage = onMessage;
        this.log_('Websocket connecting to ' + this.connURL);
        this.everConnected_ = false;
        // Assume failure until proven otherwise.
        PersistentStorage.set('previous_websocket_failure', true);
        try {
            let options;
            if (util.isNodeSdk()) {
                const device = this.nodeAdmin ? 'AdminNode' : 'Node';
                // UA Format: Firebase/<wire_protocol>/<sdk_version>/<platform>/<device>
                options = {
                    headers: {
                        'User-Agent': `Firebase/${PROTOCOL_VERSION}/${SDK_VERSION}/${process.platform}/${device}`,
                        'X-Firebase-GMPID': this.applicationId || ''
                    }
                };
                // If using Node with admin creds, AppCheck-related checks are unnecessary.
                // Note that we send the credentials here even if they aren't admin credentials, which is
                // not a problem.
                // Note that this header is just used to bypass appcheck, and the token should still be sent
                // through the websocket connection once it is established.
                if (this.authToken) {
                    options.headers['Authorization'] = `Bearer ${this.authToken}`;
                }
                if (this.appCheckToken) {
                    options.headers['X-Firebase-AppCheck'] = this.appCheckToken;
                }
                // Plumb appropriate http_proxy environment variable into faye-websocket if it exists.
                const env = process['env'];
                const proxy = this.connURL.indexOf('wss://') === 0
                    ? env['HTTPS_PROXY'] || env['https_proxy']
                    : env['HTTP_PROXY'] || env['http_proxy'];
                if (proxy) {
                    options['proxy'] = { origin: proxy };
                }
            }
            this.mySock = new WebSocketImpl(this.connURL, [], options);
        }
        catch (e) {
            this.log_('Error instantiating WebSocket.');
            const error = e.message || e.data;
            if (error) {
                this.log_(error);
            }
            this.onClosed_();
            return;
        }
        this.mySock.onopen = () => {
            this.log_('Websocket connected.');
            this.everConnected_ = true;
        };
        this.mySock.onclose = () => {
            this.log_('Websocket connection was disconnected.');
            this.mySock = null;
            this.onClosed_();
        };
        this.mySock.onmessage = m => {
            this.handleIncomingFrame(m);
        };
        this.mySock.onerror = e => {
            this.log_('WebSocket error.  Closing connection.');
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const error = e.message || e.data;
            if (error) {
                this.log_(error);
            }
            this.onClosed_();
        };
    }
    /**
     * No-op for websockets, we don't need to do anything once the connection is confirmed as open
     */
    start() { }
    static forceDisallow() {
        WebSocketConnection.forceDisallow_ = true;
    }
    static isAvailable() {
        let isOldAndroid = false;
        if (typeof navigator !== 'undefined' && navigator.userAgent) {
            const oldAndroidRegex = /Android ([0-9]{0,}\.[0-9]{0,})/;
            const oldAndroidMatch = navigator.userAgent.match(oldAndroidRegex);
            if (oldAndroidMatch && oldAndroidMatch.length > 1) {
                if (parseFloat(oldAndroidMatch[1]) < 4.4) {
                    isOldAndroid = true;
                }
            }
        }
        return (!isOldAndroid &&
            WebSocketImpl !== null &&
            !WebSocketConnection.forceDisallow_);
    }
    /**
     * Returns true if we previously failed to connect with this transport.
     */
    static previouslyFailed() {
        // If our persistent storage is actually only in-memory storage,
        // we default to assuming that it previously failed to be safe.
        return (PersistentStorage.isInMemoryStorage ||
            PersistentStorage.get('previous_websocket_failure') === true);
    }
    markConnectionHealthy() {
        PersistentStorage.remove('previous_websocket_failure');
    }
    appendFrame_(data) {
        this.frames.push(data);
        if (this.frames.length === this.totalFrames) {
            const fullMess = this.frames.join('');
            this.frames = null;
            const jsonMess = util.jsonEval(fullMess);
            //handle the message
            this.onMessage(jsonMess);
        }
    }
    /**
     * @param frameCount - The number of frames we are expecting from the server
     */
    handleNewFrameCount_(frameCount) {
        this.totalFrames = frameCount;
        this.frames = [];
    }
    /**
     * Attempts to parse a frame count out of some text. If it can't, assumes a value of 1
     * @returns Any remaining data to be process, or null if there is none
     */
    extractFrameCount_(data) {
        util.assert(this.frames === null, 'We already have a frame buffer');
        // TODO: The server is only supposed to send up to 9999 frames (i.e. length <= 4), but that isn't being enforced
        // currently.  So allowing larger frame counts (length <= 6).  See https://app.asana.com/0/search/8688598998380/8237608042508
        if (data.length <= 6) {
            const frameCount = Number(data);
            if (!isNaN(frameCount)) {
                this.handleNewFrameCount_(frameCount);
                return null;
            }
        }
        this.handleNewFrameCount_(1);
        return data;
    }
    /**
     * Process a websocket frame that has arrived from the server.
     * @param mess - The frame data
     */
    handleIncomingFrame(mess) {
        if (this.mySock === null) {
            return; // Chrome apparently delivers incoming packets even after we .close() the connection sometimes.
        }
        const data = mess['data'];
        this.bytesReceived += data.length;
        this.stats_.incrementCounter('bytes_received', data.length);
        this.resetKeepAlive();
        if (this.frames !== null) {
            // we're buffering
            this.appendFrame_(data);
        }
        else {
            // try to parse out a frame count, otherwise, assume 1 and process it
            const remainingData = this.extractFrameCount_(data);
            if (remainingData !== null) {
                this.appendFrame_(remainingData);
            }
        }
    }
    /**
     * Send a message to the server
     * @param data - The JSON object to transmit
     */
    send(data) {
        this.resetKeepAlive();
        const dataStr = util.stringify(data);
        this.bytesSent += dataStr.length;
        this.stats_.incrementCounter('bytes_sent', dataStr.length);
        //We can only fit a certain amount in each websocket frame, so we need to split this request
        //up into multiple pieces if it doesn't fit in one request.
        const dataSegs = splitStringBySize(dataStr, WEBSOCKET_MAX_FRAME_SIZE);
        //Send the length header
        if (dataSegs.length > 1) {
            this.sendString_(String(dataSegs.length));
        }
        //Send the actual data in segments.
        for (let i = 0; i < dataSegs.length; i++) {
            this.sendString_(dataSegs[i]);
        }
    }
    shutdown_() {
        this.isClosed_ = true;
        if (this.keepaliveTimer) {
            clearInterval(this.keepaliveTimer);
            this.keepaliveTimer = null;
        }
        if (this.mySock) {
            this.mySock.close();
            this.mySock = null;
        }
    }
    onClosed_() {
        if (!this.isClosed_) {
            this.log_('WebSocket is closing itself');
            this.shutdown_();
            // since this is an internal close, trigger the close listener
            if (this.onDisconnect) {
                this.onDisconnect(this.everConnected_);
                this.onDisconnect = null;
            }
        }
    }
    /**
     * External-facing close handler.
     * Close the websocket and kill the connection.
     */
    close() {
        if (!this.isClosed_) {
            this.log_('WebSocket is being closed');
            this.shutdown_();
        }
    }
    /**
     * Kill the current keepalive timer and start a new one, to ensure that it always fires N seconds after
     * the last activity.
     */
    resetKeepAlive() {
        clearInterval(this.keepaliveTimer);
        this.keepaliveTimer = setInterval(() => {
            //If there has been no websocket activity for a while, send a no-op
            if (this.mySock) {
                this.sendString_('0');
            }
            this.resetKeepAlive();
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
        }, Math.floor(WEBSOCKET_KEEPALIVE_INTERVAL));
    }
    /**
     * Send a string over the websocket.
     *
     * @param str - String to send.
     */
    sendString_(str) {
        // Firefox seems to sometimes throw exceptions (NS_ERROR_UNEXPECTED) from websocket .send()
        // calls for some unknown reason.  We treat these as an error and disconnect.
        // See https://app.asana.com/0/58926111402292/68021340250410
        try {
            this.mySock.send(str);
        }
        catch (e) {
            this.log_('Exception thrown from WebSocket.send():', e.message || e.data, 'Closing connection.');
            setTimeout(this.onClosed_.bind(this), 0);
        }
    }
}
/**
 * Number of response before we consider the connection "healthy."
 */
WebSocketConnection.responsesRequiredToBeHealthy = 2;
/**
 * Time to wait for the connection te become healthy before giving up.
 */
WebSocketConnection.healthyTimeout = 30000;

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Currently simplistic, this class manages what transport a Connection should use at various stages of its
 * lifecycle.
 *
 * It starts with longpolling in a browser, and httppolling on node. It then upgrades to websockets if
 * they are available.
 */
class TransportManager {
    /**
     * @param repoInfo - Metadata around the namespace we're connecting to
     */
    constructor(repoInfo) {
        this.initTransports_(repoInfo);
    }
    static get ALL_TRANSPORTS() {
        return [BrowserPollConnection, WebSocketConnection];
    }
    /**
     * Returns whether transport has been selected to ensure WebSocketConnection or BrowserPollConnection are not called after
     * TransportManager has already set up transports_
     */
    static get IS_TRANSPORT_INITIALIZED() {
        return this.globalTransportInitialized_;
    }
    initTransports_(repoInfo) {
        const isWebSocketsAvailable = WebSocketConnection && WebSocketConnection['isAvailable']();
        let isSkipPollConnection = isWebSocketsAvailable && !WebSocketConnection.previouslyFailed();
        if (repoInfo.webSocketOnly) {
            if (!isWebSocketsAvailable) {
                warn("wss:// URL used, but browser isn't known to support websockets.  Trying anyway.");
            }
            isSkipPollConnection = true;
        }
        if (isSkipPollConnection) {
            this.transports_ = [WebSocketConnection];
        }
        else {
            const transports = (this.transports_ = []);
            for (const transport of TransportManager.ALL_TRANSPORTS) {
                if (transport && transport['isAvailable']()) {
                    transports.push(transport);
                }
            }
            TransportManager.globalTransportInitialized_ = true;
        }
    }
    /**
     * @returns The constructor for the initial transport to use
     */
    initialTransport() {
        if (this.transports_.length > 0) {
            return this.transports_[0];
        }
        else {
            throw new Error('No transports available');
        }
    }
    /**
     * @returns The constructor for the next transport, or null
     */
    upgradeTransport() {
        if (this.transports_.length > 1) {
            return this.transports_[1];
        }
        else {
            return null;
        }
    }
}
// Keeps track of whether the TransportManager has already chosen a transport to use
TransportManager.globalTransportInitialized_ = false;

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Abort upgrade attempt if it takes longer than 60s.
const UPGRADE_TIMEOUT = 60000;
// For some transports (WebSockets), we need to "validate" the transport by exchanging a few requests and responses.
// If we haven't sent enough requests within 5s, we'll start sending noop ping requests.
const DELAY_BEFORE_SENDING_EXTRA_REQUESTS = 5000;
// If the initial data sent triggers a lot of bandwidth (i.e. it's a large put or a listen for a large amount of data)
// then we may not be able to exchange our ping/pong requests within the healthy timeout.  So if we reach the timeout
// but we've sent/received enough bytes, we don't cancel the connection.
const BYTES_SENT_HEALTHY_OVERRIDE = 10 * 1024;
const BYTES_RECEIVED_HEALTHY_OVERRIDE = 100 * 1024;
const MESSAGE_TYPE = 't';
const MESSAGE_DATA = 'd';
const CONTROL_SHUTDOWN = 's';
const CONTROL_RESET = 'r';
const CONTROL_ERROR = 'e';
const CONTROL_PONG = 'o';
const SWITCH_ACK = 'a';
const END_TRANSMISSION = 'n';
const PING = 'p';
const SERVER_HELLO = 'h';
/**
 * Creates a new real-time connection to the server using whichever method works
 * best in the current browser.
 */
class Connection {
    /**
     * @param id - an id for this connection
     * @param repoInfo_ - the info for the endpoint to connect to
     * @param applicationId_ - the Firebase App ID for this project
     * @param appCheckToken_ - The App Check Token for this device.
     * @param authToken_ - The auth token for this session.
     * @param onMessage_ - the callback to be triggered when a server-push message arrives
     * @param onReady_ - the callback to be triggered when this connection is ready to send messages.
     * @param onDisconnect_ - the callback to be triggered when a connection was lost
     * @param onKill_ - the callback to be triggered when this connection has permanently shut down.
     * @param lastSessionId - last session id in persistent connection. is used to clean up old session in real-time server
     */
    constructor(id, repoInfo_, applicationId_, appCheckToken_, authToken_, onMessage_, onReady_, onDisconnect_, onKill_, lastSessionId) {
        this.id = id;
        this.repoInfo_ = repoInfo_;
        this.applicationId_ = applicationId_;
        this.appCheckToken_ = appCheckToken_;
        this.authToken_ = authToken_;
        this.onMessage_ = onMessage_;
        this.onReady_ = onReady_;
        this.onDisconnect_ = onDisconnect_;
        this.onKill_ = onKill_;
        this.lastSessionId = lastSessionId;
        this.connectionCount = 0;
        this.pendingDataMessages = [];
        this.state_ = 0 /* RealtimeState.CONNECTING */;
        this.log_ = logWrapper('c:' + this.id + ':');
        this.transportManager_ = new TransportManager(repoInfo_);
        this.log_('Connection created');
        this.start_();
    }
    /**
     * Starts a connection attempt
     */
    start_() {
        const conn = this.transportManager_.initialTransport();
        this.conn_ = new conn(this.nextTransportId_(), this.repoInfo_, this.applicationId_, this.appCheckToken_, this.authToken_, null, this.lastSessionId);
        // For certain transports (WebSockets), we need to send and receive several messages back and forth before we
        // can consider the transport healthy.
        this.primaryResponsesRequired_ = conn['responsesRequiredToBeHealthy'] || 0;
        const onMessageReceived = this.connReceiver_(this.conn_);
        const onConnectionLost = this.disconnReceiver_(this.conn_);
        this.tx_ = this.conn_;
        this.rx_ = this.conn_;
        this.secondaryConn_ = null;
        this.isHealthy_ = false;
        /*
         * Firefox doesn't like when code from one iframe tries to create another iframe by way of the parent frame.
         * This can occur in the case of a redirect, i.e. we guessed wrong on what server to connect to and received a reset.
         * Somehow, setTimeout seems to make this ok. That doesn't make sense from a security perspective, since you should
         * still have the context of your originating frame.
         */
        setTimeout(() => {
            // this.conn_ gets set to null in some of the tests. Check to make sure it still exists before using it
            this.conn_ && this.conn_.open(onMessageReceived, onConnectionLost);
        }, Math.floor(0));
        const healthyTimeoutMS = conn['healthyTimeout'] || 0;
        if (healthyTimeoutMS > 0) {
            this.healthyTimeout_ = setTimeoutNonBlocking(() => {
                this.healthyTimeout_ = null;
                if (!this.isHealthy_) {
                    if (this.conn_ &&
                        this.conn_.bytesReceived > BYTES_RECEIVED_HEALTHY_OVERRIDE) {
                        this.log_('Connection exceeded healthy timeout but has received ' +
                            this.conn_.bytesReceived +
                            ' bytes.  Marking connection healthy.');
                        this.isHealthy_ = true;
                        this.conn_.markConnectionHealthy();
                    }
                    else if (this.conn_ &&
                        this.conn_.bytesSent > BYTES_SENT_HEALTHY_OVERRIDE) {
                        this.log_('Connection exceeded healthy timeout but has sent ' +
                            this.conn_.bytesSent +
                            ' bytes.  Leaving connection alive.');
                        // NOTE: We don't want to mark it healthy, since we have no guarantee that the bytes have made it to
                        // the server.
                    }
                    else {
                        this.log_('Closing unhealthy connection after timeout.');
                        this.close();
                    }
                }
                // eslint-disable-next-line @typescript-eslint/no-explicit-any
            }, Math.floor(healthyTimeoutMS));
        }
    }
    nextTransportId_() {
        return 'c:' + this.id + ':' + this.connectionCount++;
    }
    disconnReceiver_(conn) {
        return everConnected => {
            if (conn === this.conn_) {
                this.onConnectionLost_(everConnected);
            }
            else if (conn === this.secondaryConn_) {
                this.log_('Secondary connection lost.');
                this.onSecondaryConnectionLost_();
            }
            else {
                this.log_('closing an old connection');
            }
        };
    }
    connReceiver_(conn) {
        return (message) => {
            if (this.state_ !== 2 /* RealtimeState.DISCONNECTED */) {
                if (conn === this.rx_) {
                    this.onPrimaryMessageReceived_(message);
                }
                else if (conn === this.secondaryConn_) {
                    this.onSecondaryMessageReceived_(message);
                }
                else {
                    this.log_('message on old connection');
                }
            }
        };
    }
    /**
     * @param dataMsg - An arbitrary data message to be sent to the server
     */
    sendRequest(dataMsg) {
        // wrap in a data message envelope and send it on
        const msg = { t: 'd', d: dataMsg };
        this.sendData_(msg);
    }
    tryCleanupConnection() {
        if (this.tx_ === this.secondaryConn_ && this.rx_ === this.secondaryConn_) {
            this.log_('cleaning up and promoting a connection: ' + this.secondaryConn_.connId);
            this.conn_ = this.secondaryConn_;
            this.secondaryConn_ = null;
            // the server will shutdown the old connection
        }
    }
    onSecondaryControl_(controlData) {
        if (MESSAGE_TYPE in controlData) {
            const cmd = controlData[MESSAGE_TYPE];
            if (cmd === SWITCH_ACK) {
                this.upgradeIfSecondaryHealthy_();
            }
            else if (cmd === CONTROL_RESET) {
                // Most likely the session wasn't valid. Abandon the switch attempt
                this.log_('Got a reset on secondary, closing it');
                this.secondaryConn_.close();
                // If we were already using this connection for something, than we need to fully close
                if (this.tx_ === this.secondaryConn_ ||
                    this.rx_ === this.secondaryConn_) {
                    this.close();
                }
            }
            else if (cmd === CONTROL_PONG) {
                this.log_('got pong on secondary.');
                this.secondaryResponsesRequired_--;
                this.upgradeIfSecondaryHealthy_();
            }
        }
    }
    onSecondaryMessageReceived_(parsedData) {
        const layer = requireKey('t', parsedData);
        const data = requireKey('d', parsedData);
        if (layer === 'c') {
            this.onSecondaryControl_(data);
        }
        else if (layer === 'd') {
            // got a data message, but we're still second connection. Need to buffer it up
            this.pendingDataMessages.push(data);
        }
        else {
            throw new Error('Unknown protocol layer: ' + layer);
        }
    }
    upgradeIfSecondaryHealthy_() {
        if (this.secondaryResponsesRequired_ <= 0) {
            this.log_('Secondary connection is healthy.');
            this.isHealthy_ = true;
            this.secondaryConn_.markConnectionHealthy();
            this.proceedWithUpgrade_();
        }
        else {
            // Send a ping to make sure the connection is healthy.
            this.log_('sending ping on secondary.');
            this.secondaryConn_.send({ t: 'c', d: { t: PING, d: {} } });
        }
    }
    proceedWithUpgrade_() {
        // tell this connection to consider itself open
        this.secondaryConn_.start();
        // send ack
        this.log_('sending client ack on secondary');
        this.secondaryConn_.send({ t: 'c', d: { t: SWITCH_ACK, d: {} } });
        // send end packet on primary transport, switch to sending on this one
        // can receive on this one, buffer responses until end received on primary transport
        this.log_('Ending transmission on primary');
        this.conn_.send({ t: 'c', d: { t: END_TRANSMISSION, d: {} } });
        this.tx_ = this.secondaryConn_;
        this.tryCleanupConnection();
    }
    onPrimaryMessageReceived_(parsedData) {
        // Must refer to parsedData properties in quotes, so closure doesn't touch them.
        const layer = requireKey('t', parsedData);
        const data = requireKey('d', parsedData);
        if (layer === 'c') {
            this.onControl_(data);
        }
        else if (layer === 'd') {
            this.onDataMessage_(data);
        }
    }
    onDataMessage_(message) {
        this.onPrimaryResponse_();
        // We don't do anything with data messages, just kick them up a level
        this.onMessage_(message);
    }
    onPrimaryResponse_() {
        if (!this.isHealthy_) {
            this.primaryResponsesRequired_--;
            if (this.primaryResponsesRequired_ <= 0) {
                this.log_('Primary connection is healthy.');
                this.isHealthy_ = true;
                this.conn_.markConnectionHealthy();
            }
        }
    }
    onControl_(controlData) {
        const cmd = requireKey(MESSAGE_TYPE, controlData);
        if (MESSAGE_DATA in controlData) {
            const payload = controlData[MESSAGE_DATA];
            if (cmd === SERVER_HELLO) {
                const handshakePayload = Object.assign({}, payload);
                if (this.repoInfo_.isUsingEmulator) {
                    // Upon connecting, the emulator will pass the hostname that it's aware of, but we prefer the user's set hostname via `connectDatabaseEmulator` over what the emulator passes.
                    handshakePayload.h = this.repoInfo_.host;
                }
                this.onHandshake_(handshakePayload);
            }
            else if (cmd === END_TRANSMISSION) {
                this.log_('recvd end transmission on primary');
                this.rx_ = this.secondaryConn_;
                for (let i = 0; i < this.pendingDataMessages.length; ++i) {
                    this.onDataMessage_(this.pendingDataMessages[i]);
                }
                this.pendingDataMessages = [];
                this.tryCleanupConnection();
            }
            else if (cmd === CONTROL_SHUTDOWN) {
                // This was previously the 'onKill' callback passed to the lower-level connection
                // payload in this case is the reason for the shutdown. Generally a human-readable error
                this.onConnectionShutdown_(payload);
            }
            else if (cmd === CONTROL_RESET) {
                // payload in this case is the host we should contact
                this.onReset_(payload);
            }
            else if (cmd === CONTROL_ERROR) {
                error('Server Error: ' + payload);
            }
            else if (cmd === CONTROL_PONG) {
                this.log_('got pong on primary.');
                this.onPrimaryResponse_();
                this.sendPingOnPrimaryIfNecessary_();
            }
            else {
                error('Unknown control packet command: ' + cmd);
            }
        }
    }
    /**
     * @param handshake - The handshake data returned from the server
     */
    onHandshake_(handshake) {
        const timestamp = handshake.ts;
        const version = handshake.v;
        const host = handshake.h;
        this.sessionId = handshake.s;
        this.repoInfo_.host = host;
        // if we've already closed the connection, then don't bother trying to progress further
        if (this.state_ === 0 /* RealtimeState.CONNECTING */) {
            this.conn_.start();
            this.onConnectionEstablished_(this.conn_, timestamp);
            if (PROTOCOL_VERSION !== version) {
                warn('Protocol version mismatch detected');
            }
            // TODO: do we want to upgrade? when? maybe a delay?
            this.tryStartUpgrade_();
        }
    }
    tryStartUpgrade_() {
        const conn = this.transportManager_.upgradeTransport();
        if (conn) {
            this.startUpgrade_(conn);
        }
    }
    startUpgrade_(conn) {
        this.secondaryConn_ = new conn(this.nextTransportId_(), this.repoInfo_, this.applicationId_, this.appCheckToken_, this.authToken_, this.sessionId);
        // For certain transports (WebSockets), we need to send and receive several messages back and forth before we
        // can consider the transport healthy.
        this.secondaryResponsesRequired_ =
            conn['responsesRequiredToBeHealthy'] || 0;
        const onMessage = this.connReceiver_(this.secondaryConn_);
        const onDisconnect = this.disconnReceiver_(this.secondaryConn_);
        this.secondaryConn_.open(onMessage, onDisconnect);
        // If we haven't successfully upgraded after UPGRADE_TIMEOUT, give up and kill the secondary.
        setTimeoutNonBlocking(() => {
            if (this.secondaryConn_) {
                this.log_('Timed out trying to upgrade.');
                this.secondaryConn_.close();
            }
        }, Math.floor(UPGRADE_TIMEOUT));
    }
    onReset_(host) {
        this.log_('Reset packet received.  New host: ' + host);
        this.repoInfo_.host = host;
        // TODO: if we're already "connected", we need to trigger a disconnect at the next layer up.
        // We don't currently support resets after the connection has already been established
        if (this.state_ === 1 /* RealtimeState.CONNECTED */) {
            this.close();
        }
        else {
            // Close whatever connections we have open and start again.
            this.closeConnections_();
            this.start_();
        }
    }
    onConnectionEstablished_(conn, timestamp) {
        this.log_('Realtime connection established.');
        this.conn_ = conn;
        this.state_ = 1 /* RealtimeState.CONNECTED */;
        if (this.onReady_) {
            this.onReady_(timestamp, this.sessionId);
            this.onReady_ = null;
        }
        // If after 5 seconds we haven't sent enough requests to the server to get the connection healthy,
        // send some pings.
        if (this.primaryResponsesRequired_ === 0) {
            this.log_('Primary connection is healthy.');
            this.isHealthy_ = true;
        }
        else {
            setTimeoutNonBlocking(() => {
                this.sendPingOnPrimaryIfNecessary_();
            }, Math.floor(DELAY_BEFORE_SENDING_EXTRA_REQUESTS));
        }
    }
    sendPingOnPrimaryIfNecessary_() {
        // If the connection isn't considered healthy yet, we'll send a noop ping packet request.
        if (!this.isHealthy_ && this.state_ === 1 /* RealtimeState.CONNECTED */) {
            this.log_('sending ping on primary.');
            this.sendData_({ t: 'c', d: { t: PING, d: {} } });
        }
    }
    onSecondaryConnectionLost_() {
        const conn = this.secondaryConn_;
        this.secondaryConn_ = null;
        if (this.tx_ === conn || this.rx_ === conn) {
            // we are relying on this connection already in some capacity. Therefore, a failure is real
            this.close();
        }
    }
    /**
     * @param everConnected - Whether or not the connection ever reached a server. Used to determine if
     * we should flush the host cache
     */
    onConnectionLost_(everConnected) {
        this.conn_ = null;
        // NOTE: IF you're seeing a Firefox error for this line, I think it might be because it's getting
        // called on window close and RealtimeState.CONNECTING is no longer defined.  Just a guess.
        if (!everConnected && this.state_ === 0 /* RealtimeState.CONNECTING */) {
            this.log_('Realtime connection failed.');
            // Since we failed to connect at all, clear any cached entry for this namespace in case the machine went away
            if (this.repoInfo_.isCacheableHost()) {
                PersistentStorage.remove('host:' + this.repoInfo_.host);
                // reset the internal host to what we would show the user, i.e. <ns>.firebaseio.com
                this.repoInfo_.internalHost = this.repoInfo_.host;
            }
        }
        else if (this.state_ === 1 /* RealtimeState.CONNECTED */) {
            this.log_('Realtime connection lost.');
        }
        this.close();
    }
    onConnectionShutdown_(reason) {
        this.log_('Connection shutdown command received. Shutting down...');
        if (this.onKill_) {
            this.onKill_(reason);
            this.onKill_ = null;
        }
        // We intentionally don't want to fire onDisconnect (kill is a different case),
        // so clear the callback.
        this.onDisconnect_ = null;
        this.close();
    }
    sendData_(data) {
        if (this.state_ !== 1 /* RealtimeState.CONNECTED */) {
            throw 'Connection is not connected';
        }
        else {
            this.tx_.send(data);
        }
    }
    /**
     * Cleans up this connection, calling the appropriate callbacks
     */
    close() {
        if (this.state_ !== 2 /* RealtimeState.DISCONNECTED */) {
            this.log_('Closing realtime connection.');
            this.state_ = 2 /* RealtimeState.DISCONNECTED */;
            this.closeConnections_();
            if (this.onDisconnect_) {
                this.onDisconnect_();
                this.onDisconnect_ = null;
            }
        }
    }
    closeConnections_() {
        this.log_('Shutting down all connections');
        if (this.conn_) {
            this.conn_.close();
            this.conn_ = null;
        }
        if (this.secondaryConn_) {
            this.secondaryConn_.close();
            this.secondaryConn_ = null;
        }
        if (this.healthyTimeout_) {
            clearTimeout(this.healthyTimeout_);
            this.healthyTimeout_ = null;
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Interface defining the set of actions that can be performed against the Firebase server
 * (basically corresponds to our wire protocol).
 *
 * @interface
 */
class ServerActions {
    put(pathString, data, onComplete, hash) { }
    merge(pathString, data, onComplete, hash) { }
    /**
     * Refreshes the auth token for the current connection.
     * @param token - The authentication token
     */
    refreshAuthToken(token) { }
    /**
     * Refreshes the app check token for the current connection.
     * @param token The app check token
     */
    refreshAppCheckToken(token) { }
    onDisconnectPut(pathString, data, onComplete) { }
    onDisconnectMerge(pathString, data, onComplete) { }
    onDisconnectCancel(pathString, onComplete) { }
    reportStats(stats) { }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Base class to be used if you want to emit events. Call the constructor with
 * the set of allowed event names.
 */
class EventEmitter {
    constructor(allowedEvents_) {
        this.allowedEvents_ = allowedEvents_;
        this.listeners_ = {};
        util.assert(Array.isArray(allowedEvents_) && allowedEvents_.length > 0, 'Requires a non-empty array');
    }
    /**
     * To be called by derived classes to trigger events.
     */
    trigger(eventType, ...varArgs) {
        if (Array.isArray(this.listeners_[eventType])) {
            // Clone the list, since callbacks could add/remove listeners.
            const listeners = [...this.listeners_[eventType]];
            for (let i = 0; i < listeners.length; i++) {
                listeners[i].callback.apply(listeners[i].context, varArgs);
            }
        }
    }
    on(eventType, callback, context) {
        this.validateEventType_(eventType);
        this.listeners_[eventType] = this.listeners_[eventType] || [];
        this.listeners_[eventType].push({ callback, context });
        const eventData = this.getInitialEvent(eventType);
        if (eventData) {
            callback.apply(context, eventData);
        }
    }
    off(eventType, callback, context) {
        this.validateEventType_(eventType);
        const listeners = this.listeners_[eventType] || [];
        for (let i = 0; i < listeners.length; i++) {
            if (listeners[i].callback === callback &&
                (!context || context === listeners[i].context)) {
                listeners.splice(i, 1);
                return;
            }
        }
    }
    validateEventType_(eventType) {
        util.assert(this.allowedEvents_.find(et => {
            return et === eventType;
        }), 'Unknown event: ' + eventType);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Monitors online state (as reported by window.online/offline events).
 *
 * The expectation is that this could have many false positives (thinks we are online
 * when we're not), but no false negatives.  So we can safely use it to determine when
 * we definitely cannot reach the internet.
 */
class OnlineMonitor extends EventEmitter {
    constructor() {
        super(['online']);
        this.online_ = true;
        // We've had repeated complaints that Cordova apps can get stuck "offline", e.g.
        // https://forum.ionicframework.com/t/firebase-connection-is-lost-and-never-come-back/43810
        // It would seem that the 'online' event does not always fire consistently. So we disable it
        // for Cordova.
        if (typeof window !== 'undefined' &&
            typeof window.addEventListener !== 'undefined' &&
            !util.isMobileCordova()) {
            window.addEventListener('online', () => {
                if (!this.online_) {
                    this.online_ = true;
                    this.trigger('online', true);
                }
            }, false);
            window.addEventListener('offline', () => {
                if (this.online_) {
                    this.online_ = false;
                    this.trigger('online', false);
                }
            }, false);
        }
    }
    static getInstance() {
        return new OnlineMonitor();
    }
    getInitialEvent(eventType) {
        util.assert(eventType === 'online', 'Unknown event type: ' + eventType);
        return [this.online_];
    }
    currentlyOnline() {
        return this.online_;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/** Maximum key depth. */
const MAX_PATH_DEPTH = 32;
/** Maximum number of (UTF8) bytes in a Firebase path. */
const MAX_PATH_LENGTH_BYTES = 768;
/**
 * An immutable object representing a parsed path.  It's immutable so that you
 * can pass them around to other functions without worrying about them changing
 * it.
 */
class Path {
    /**
     * @param pathOrString - Path string to parse, or another path, or the raw
     * tokens array
     */
    constructor(pathOrString, pieceNum) {
        if (pieceNum === void 0) {
            this.pieces_ = pathOrString.split('/');
            // Remove empty pieces.
            let copyTo = 0;
            for (let i = 0; i < this.pieces_.length; i++) {
                if (this.pieces_[i].length > 0) {
                    this.pieces_[copyTo] = this.pieces_[i];
                    copyTo++;
                }
            }
            this.pieces_.length = copyTo;
            this.pieceNum_ = 0;
        }
        else {
            this.pieces_ = pathOrString;
            this.pieceNum_ = pieceNum;
        }
    }
    toString() {
        let pathString = '';
        for (let i = this.pieceNum_; i < this.pieces_.length; i++) {
            if (this.pieces_[i] !== '') {
                pathString += '/' + this.pieces_[i];
            }
        }
        return pathString || '/';
    }
}
function newEmptyPath() {
    return new Path('');
}
function pathGetFront(path) {
    if (path.pieceNum_ >= path.pieces_.length) {
        return null;
    }
    return path.pieces_[path.pieceNum_];
}
/**
 * @returns The number of segments in this path
 */
function pathGetLength(path) {
    return path.pieces_.length - path.pieceNum_;
}
function pathPopFront(path) {
    let pieceNum = path.pieceNum_;
    if (pieceNum < path.pieces_.length) {
        pieceNum++;
    }
    return new Path(path.pieces_, pieceNum);
}
function pathGetBack(path) {
    if (path.pieceNum_ < path.pieces_.length) {
        return path.pieces_[path.pieces_.length - 1];
    }
    return null;
}
function pathToUrlEncodedString(path) {
    let pathString = '';
    for (let i = path.pieceNum_; i < path.pieces_.length; i++) {
        if (path.pieces_[i] !== '') {
            pathString += '/' + encodeURIComponent(String(path.pieces_[i]));
        }
    }
    return pathString || '/';
}
/**
 * Shallow copy of the parts of the path.
 *
 */
function pathSlice(path, begin = 0) {
    return path.pieces_.slice(path.pieceNum_ + begin);
}
function pathParent(path) {
    if (path.pieceNum_ >= path.pieces_.length) {
        return null;
    }
    const pieces = [];
    for (let i = path.pieceNum_; i < path.pieces_.length - 1; i++) {
        pieces.push(path.pieces_[i]);
    }
    return new Path(pieces, 0);
}
function pathChild(path, childPathObj) {
    const pieces = [];
    for (let i = path.pieceNum_; i < path.pieces_.length; i++) {
        pieces.push(path.pieces_[i]);
    }
    if (childPathObj instanceof Path) {
        for (let i = childPathObj.pieceNum_; i < childPathObj.pieces_.length; i++) {
            pieces.push(childPathObj.pieces_[i]);
        }
    }
    else {
        const childPieces = childPathObj.split('/');
        for (let i = 0; i < childPieces.length; i++) {
            if (childPieces[i].length > 0) {
                pieces.push(childPieces[i]);
            }
        }
    }
    return new Path(pieces, 0);
}
/**
 * @returns True if there are no segments in this path
 */
function pathIsEmpty(path) {
    return path.pieceNum_ >= path.pieces_.length;
}
/**
 * @returns The path from outerPath to innerPath
 */
function newRelativePath(outerPath, innerPath) {
    const outer = pathGetFront(outerPath), inner = pathGetFront(innerPath);
    if (outer === null) {
        return innerPath;
    }
    else if (outer === inner) {
        return newRelativePath(pathPopFront(outerPath), pathPopFront(innerPath));
    }
    else {
        throw new Error('INTERNAL ERROR: innerPath (' +
            innerPath +
            ') is not within ' +
            'outerPath (' +
            outerPath +
            ')');
    }
}
/**
 * @returns -1, 0, 1 if left is less, equal, or greater than the right.
 */
function pathCompare(left, right) {
    const leftKeys = pathSlice(left, 0);
    const rightKeys = pathSlice(right, 0);
    for (let i = 0; i < leftKeys.length && i < rightKeys.length; i++) {
        const cmp = nameCompare(leftKeys[i], rightKeys[i]);
        if (cmp !== 0) {
            return cmp;
        }
    }
    if (leftKeys.length === rightKeys.length) {
        return 0;
    }
    return leftKeys.length < rightKeys.length ? -1 : 1;
}
/**
 * @returns true if paths are the same.
 */
function pathEquals(path, other) {
    if (pathGetLength(path) !== pathGetLength(other)) {
        return false;
    }
    for (let i = path.pieceNum_, j = other.pieceNum_; i <= path.pieces_.length; i++, j++) {
        if (path.pieces_[i] !== other.pieces_[j]) {
            return false;
        }
    }
    return true;
}
/**
 * @returns True if this path is a parent of (or the same as) other
 */
function pathContains(path, other) {
    let i = path.pieceNum_;
    let j = other.pieceNum_;
    if (pathGetLength(path) > pathGetLength(other)) {
        return false;
    }
    while (i < path.pieces_.length) {
        if (path.pieces_[i] !== other.pieces_[j]) {
            return false;
        }
        ++i;
        ++j;
    }
    return true;
}
/**
 * Dynamic (mutable) path used to count path lengths.
 *
 * This class is used to efficiently check paths for valid
 * length (in UTF8 bytes) and depth (used in path validation).
 *
 * Throws Error exception if path is ever invalid.
 *
 * The definition of a path always begins with '/'.
 */
class ValidationPath {
    /**
     * @param path - Initial Path.
     * @param errorPrefix_ - Prefix for any error messages.
     */
    constructor(path, errorPrefix_) {
        this.errorPrefix_ = errorPrefix_;
        this.parts_ = pathSlice(path, 0);
        /** Initialize to number of '/' chars needed in path. */
        this.byteLength_ = Math.max(1, this.parts_.length);
        for (let i = 0; i < this.parts_.length; i++) {
            this.byteLength_ += util.stringLength(this.parts_[i]);
        }
        validationPathCheckValid(this);
    }
}
function validationPathPush(validationPath, child) {
    // Count the needed '/'
    if (validationPath.parts_.length > 0) {
        validationPath.byteLength_ += 1;
    }
    validationPath.parts_.push(child);
    validationPath.byteLength_ += util.stringLength(child);
    validationPathCheckValid(validationPath);
}
function validationPathPop(validationPath) {
    const last = validationPath.parts_.pop();
    validationPath.byteLength_ -= util.stringLength(last);
    // Un-count the previous '/'
    if (validationPath.parts_.length > 0) {
        validationPath.byteLength_ -= 1;
    }
}
function validationPathCheckValid(validationPath) {
    if (validationPath.byteLength_ > MAX_PATH_LENGTH_BYTES) {
        throw new Error(validationPath.errorPrefix_ +
            'has a key path longer than ' +
            MAX_PATH_LENGTH_BYTES +
            ' bytes (' +
            validationPath.byteLength_ +
            ').');
    }
    if (validationPath.parts_.length > MAX_PATH_DEPTH) {
        throw new Error(validationPath.errorPrefix_ +
            'path specified exceeds the maximum depth that can be written (' +
            MAX_PATH_DEPTH +
            ') or object contains a cycle ' +
            validationPathToErrorString(validationPath));
    }
}
/**
 * String for use in error messages - uses '.' notation for path.
 */
function validationPathToErrorString(validationPath) {
    if (validationPath.parts_.length === 0) {
        return '';
    }
    return "in property '" + validationPath.parts_.join('.') + "'";
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class VisibilityMonitor extends EventEmitter {
    constructor() {
        super(['visible']);
        let hidden;
        let visibilityChange;
        if (typeof document !== 'undefined' &&
            typeof document.addEventListener !== 'undefined') {
            if (typeof document['hidden'] !== 'undefined') {
                // Opera 12.10 and Firefox 18 and later support
                visibilityChange = 'visibilitychange';
                hidden = 'hidden';
            }
            else if (typeof document['mozHidden'] !== 'undefined') {
                visibilityChange = 'mozvisibilitychange';
                hidden = 'mozHidden';
            }
            else if (typeof document['msHidden'] !== 'undefined') {
                visibilityChange = 'msvisibilitychange';
                hidden = 'msHidden';
            }
            else if (typeof document['webkitHidden'] !== 'undefined') {
                visibilityChange = 'webkitvisibilitychange';
                hidden = 'webkitHidden';
            }
        }
        // Initially, we always assume we are visible. This ensures that in browsers
        // without page visibility support or in cases where we are never visible
        // (e.g. chrome extension), we act as if we are visible, i.e. don't delay
        // reconnects
        this.visible_ = true;
        if (visibilityChange) {
            document.addEventListener(visibilityChange, () => {
                const visible = !document[hidden];
                if (visible !== this.visible_) {
                    this.visible_ = visible;
                    this.trigger('visible', visible);
                }
            }, false);
        }
    }
    static getInstance() {
        return new VisibilityMonitor();
    }
    getInitialEvent(eventType) {
        util.assert(eventType === 'visible', 'Unknown event type: ' + eventType);
        return [this.visible_];
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const RECONNECT_MIN_DELAY = 1000;
const RECONNECT_MAX_DELAY_DEFAULT = 60 * 5 * 1000; // 5 minutes in milliseconds (Case: 1858)
const RECONNECT_MAX_DELAY_FOR_ADMINS = 30 * 1000; // 30 seconds for admin clients (likely to be a backend server)
const RECONNECT_DELAY_MULTIPLIER = 1.3;
const RECONNECT_DELAY_RESET_TIMEOUT = 30000; // Reset delay back to MIN_DELAY after being connected for 30sec.
const SERVER_KILL_INTERRUPT_REASON = 'server_kill';
// If auth fails repeatedly, we'll assume something is wrong and log a warning / back off.
const INVALID_TOKEN_THRESHOLD = 3;
/**
 * Firebase connection.  Abstracts wire protocol and handles reconnecting.
 *
 * NOTE: All JSON objects sent to the realtime connection must have property names enclosed
 * in quotes to make sure the closure compiler does not minify them.
 */
class PersistentConnection extends ServerActions {
    /**
     * @param repoInfo_ - Data about the namespace we are connecting to
     * @param applicationId_ - The Firebase App ID for this project
     * @param onDataUpdate_ - A callback for new data from the server
     */
    constructor(repoInfo_, applicationId_, onDataUpdate_, onConnectStatus_, onServerInfoUpdate_, authTokenProvider_, appCheckTokenProvider_, authOverride_) {
        super();
        this.repoInfo_ = repoInfo_;
        this.applicationId_ = applicationId_;
        this.onDataUpdate_ = onDataUpdate_;
        this.onConnectStatus_ = onConnectStatus_;
        this.onServerInfoUpdate_ = onServerInfoUpdate_;
        this.authTokenProvider_ = authTokenProvider_;
        this.appCheckTokenProvider_ = appCheckTokenProvider_;
        this.authOverride_ = authOverride_;
        // Used for diagnostic logging.
        this.id = PersistentConnection.nextPersistentConnectionId_++;
        this.log_ = logWrapper('p:' + this.id + ':');
        this.interruptReasons_ = {};
        this.listens = new Map();
        this.outstandingPuts_ = [];
        this.outstandingGets_ = [];
        this.outstandingPutCount_ = 0;
        this.outstandingGetCount_ = 0;
        this.onDisconnectRequestQueue_ = [];
        this.connected_ = false;
        this.reconnectDelay_ = RECONNECT_MIN_DELAY;
        this.maxReconnectDelay_ = RECONNECT_MAX_DELAY_DEFAULT;
        this.securityDebugCallback_ = null;
        this.lastSessionId = null;
        this.establishConnectionTimer_ = null;
        this.visible_ = false;
        // Before we get connected, we keep a queue of pending messages to send.
        this.requestCBHash_ = {};
        this.requestNumber_ = 0;
        this.realtime_ = null;
        this.authToken_ = null;
        this.appCheckToken_ = null;
        this.forceTokenRefresh_ = false;
        this.invalidAuthTokenCount_ = 0;
        this.invalidAppCheckTokenCount_ = 0;
        this.firstConnection_ = true;
        this.lastConnectionAttemptTime_ = null;
        this.lastConnectionEstablishedTime_ = null;
        if (authOverride_ && !util.isNodeSdk()) {
            throw new Error('Auth override specified in options, but not supported on non Node.js platforms');
        }
        VisibilityMonitor.getInstance().on('visible', this.onVisible_, this);
        if (repoInfo_.host.indexOf('fblocal') === -1) {
            OnlineMonitor.getInstance().on('online', this.onOnline_, this);
        }
    }
    sendRequest(action, body, onResponse) {
        const curReqNum = ++this.requestNumber_;
        const msg = { r: curReqNum, a: action, b: body };
        this.log_(util.stringify(msg));
        util.assert(this.connected_, "sendRequest call when we're not connected not allowed.");
        this.realtime_.sendRequest(msg);
        if (onResponse) {
            this.requestCBHash_[curReqNum] = onResponse;
        }
    }
    get(query) {
        this.initConnection_();
        const deferred = new util.Deferred();
        const request = {
            p: query._path.toString(),
            q: query._queryObject
        };
        const outstandingGet = {
            action: 'g',
            request,
            onComplete: (message) => {
                const payload = message['d'];
                if (message['s'] === 'ok') {
                    deferred.resolve(payload);
                }
                else {
                    deferred.reject(payload);
                }
            }
        };
        this.outstandingGets_.push(outstandingGet);
        this.outstandingGetCount_++;
        const index = this.outstandingGets_.length - 1;
        if (this.connected_) {
            this.sendGet_(index);
        }
        return deferred.promise;
    }
    listen(query, currentHashFn, tag, onComplete) {
        this.initConnection_();
        const queryId = query._queryIdentifier;
        const pathString = query._path.toString();
        this.log_('Listen called for ' + pathString + ' ' + queryId);
        if (!this.listens.has(pathString)) {
            this.listens.set(pathString, new Map());
        }
        util.assert(query._queryParams.isDefault() || !query._queryParams.loadsAllData(), 'listen() called for non-default but complete query');
        util.assert(!this.listens.get(pathString).has(queryId), `listen() called twice for same path/queryId.`);
        const listenSpec = {
            onComplete,
            hashFn: currentHashFn,
            query,
            tag
        };
        this.listens.get(pathString).set(queryId, listenSpec);
        if (this.connected_) {
            this.sendListen_(listenSpec);
        }
    }
    sendGet_(index) {
        const get = this.outstandingGets_[index];
        this.sendRequest('g', get.request, (message) => {
            delete this.outstandingGets_[index];
            this.outstandingGetCount_--;
            if (this.outstandingGetCount_ === 0) {
                this.outstandingGets_ = [];
            }
            if (get.onComplete) {
                get.onComplete(message);
            }
        });
    }
    sendListen_(listenSpec) {
        const query = listenSpec.query;
        const pathString = query._path.toString();
        const queryId = query._queryIdentifier;
        this.log_('Listen on ' + pathString + ' for ' + queryId);
        const req = { /*path*/ p: pathString };
        const action = 'q';
        // Only bother to send query if it's non-default.
        if (listenSpec.tag) {
            req['q'] = query._queryObject;
            req['t'] = listenSpec.tag;
        }
        req[ /*hash*/'h'] = listenSpec.hashFn();
        this.sendRequest(action, req, (message) => {
            const payload = message[ /*data*/'d'];
            const status = message[ /*status*/'s'];
            // print warnings in any case...
            PersistentConnection.warnOnListenWarnings_(payload, query);
            const currentListenSpec = this.listens.get(pathString) &&
                this.listens.get(pathString).get(queryId);
            // only trigger actions if the listen hasn't been removed and readded
            if (currentListenSpec === listenSpec) {
                this.log_('listen response', message);
                if (status !== 'ok') {
                    this.removeListen_(pathString, queryId);
                }
                if (listenSpec.onComplete) {
                    listenSpec.onComplete(status, payload);
                }
            }
        });
    }
    static warnOnListenWarnings_(payload, query) {
        if (payload && typeof payload === 'object' && util.contains(payload, 'w')) {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const warnings = util.safeGet(payload, 'w');
            if (Array.isArray(warnings) && ~warnings.indexOf('no_index')) {
                const indexSpec = '".indexOn": "' + query._queryParams.getIndex().toString() + '"';
                const indexPath = query._path.toString();
                warn(`Using an unspecified index. Your data will be downloaded and ` +
                    `filtered on the client. Consider adding ${indexSpec} at ` +
                    `${indexPath} to your security rules for better performance.`);
            }
        }
    }
    refreshAuthToken(token) {
        this.authToken_ = token;
        this.log_('Auth token refreshed');
        if (this.authToken_) {
            this.tryAuth();
        }
        else {
            //If we're connected we want to let the server know to unauthenticate us. If we're not connected, simply delete
            //the credential so we dont become authenticated next time we connect.
            if (this.connected_) {
                this.sendRequest('unauth', {}, () => { });
            }
        }
        this.reduceReconnectDelayIfAdminCredential_(token);
    }
    reduceReconnectDelayIfAdminCredential_(credential) {
        // NOTE: This isn't intended to be bulletproof (a malicious developer can always just modify the client).
        // Additionally, we don't bother resetting the max delay back to the default if auth fails / expires.
        const isFirebaseSecret = credential && credential.length === 40;
        if (isFirebaseSecret || util.isAdmin(credential)) {
            this.log_('Admin auth credential detected.  Reducing max reconnect time.');
            this.maxReconnectDelay_ = RECONNECT_MAX_DELAY_FOR_ADMINS;
        }
    }
    refreshAppCheckToken(token) {
        this.appCheckToken_ = token;
        this.log_('App check token refreshed');
        if (this.appCheckToken_) {
            this.tryAppCheck();
        }
        else {
            //If we're connected we want to let the server know to unauthenticate us.
            //If we're not connected, simply delete the credential so we dont become
            // authenticated next time we connect.
            if (this.connected_) {
                this.sendRequest('unappeck', {}, () => { });
            }
        }
    }
    /**
     * Attempts to authenticate with the given credentials. If the authentication attempt fails, it's triggered like
     * a auth revoked (the connection is closed).
     */
    tryAuth() {
        if (this.connected_ && this.authToken_) {
            const token = this.authToken_;
            const authMethod = util.isValidFormat(token) ? 'auth' : 'gauth';
            const requestData = { cred: token };
            if (this.authOverride_ === null) {
                requestData['noauth'] = true;
            }
            else if (typeof this.authOverride_ === 'object') {
                requestData['authvar'] = this.authOverride_;
            }
            this.sendRequest(authMethod, requestData, (res) => {
                const status = res[ /*status*/'s'];
                const data = res[ /*data*/'d'] || 'error';
                if (this.authToken_ === token) {
                    if (status === 'ok') {
                        this.invalidAuthTokenCount_ = 0;
                    }
                    else {
                        // Triggers reconnect and force refresh for auth token
                        this.onAuthRevoked_(status, data);
                    }
                }
            });
        }
    }
    /**
     * Attempts to authenticate with the given token. If the authentication
     * attempt fails, it's triggered like the token was revoked (the connection is
     * closed).
     */
    tryAppCheck() {
        if (this.connected_ && this.appCheckToken_) {
            this.sendRequest('appcheck', { 'token': this.appCheckToken_ }, (res) => {
                const status = res[ /*status*/'s'];
                const data = res[ /*data*/'d'] || 'error';
                if (status === 'ok') {
                    this.invalidAppCheckTokenCount_ = 0;
                }
                else {
                    this.onAppCheckRevoked_(status, data);
                }
            });
        }
    }
    /**
     * @inheritDoc
     */
    unlisten(query, tag) {
        const pathString = query._path.toString();
        const queryId = query._queryIdentifier;
        this.log_('Unlisten called for ' + pathString + ' ' + queryId);
        util.assert(query._queryParams.isDefault() || !query._queryParams.loadsAllData(), 'unlisten() called for non-default but complete query');
        const listen = this.removeListen_(pathString, queryId);
        if (listen && this.connected_) {
            this.sendUnlisten_(pathString, queryId, query._queryObject, tag);
        }
    }
    sendUnlisten_(pathString, queryId, queryObj, tag) {
        this.log_('Unlisten on ' + pathString + ' for ' + queryId);
        const req = { /*path*/ p: pathString };
        const action = 'n';
        // Only bother sending queryId if it's non-default.
        if (tag) {
            req['q'] = queryObj;
            req['t'] = tag;
        }
        this.sendRequest(action, req);
    }
    onDisconnectPut(pathString, data, onComplete) {
        this.initConnection_();
        if (this.connected_) {
            this.sendOnDisconnect_('o', pathString, data, onComplete);
        }
        else {
            this.onDisconnectRequestQueue_.push({
                pathString,
                action: 'o',
                data,
                onComplete
            });
        }
    }
    onDisconnectMerge(pathString, data, onComplete) {
        this.initConnection_();
        if (this.connected_) {
            this.sendOnDisconnect_('om', pathString, data, onComplete);
        }
        else {
            this.onDisconnectRequestQueue_.push({
                pathString,
                action: 'om',
                data,
                onComplete
            });
        }
    }
    onDisconnectCancel(pathString, onComplete) {
        this.initConnection_();
        if (this.connected_) {
            this.sendOnDisconnect_('oc', pathString, null, onComplete);
        }
        else {
            this.onDisconnectRequestQueue_.push({
                pathString,
                action: 'oc',
                data: null,
                onComplete
            });
        }
    }
    sendOnDisconnect_(action, pathString, data, onComplete) {
        const request = { /*path*/ p: pathString, /*data*/ d: data };
        this.log_('onDisconnect ' + action, request);
        this.sendRequest(action, request, (response) => {
            if (onComplete) {
                setTimeout(() => {
                    onComplete(response[ /*status*/'s'], response[ /* data */'d']);
                }, Math.floor(0));
            }
        });
    }
    put(pathString, data, onComplete, hash) {
        this.putInternal('p', pathString, data, onComplete, hash);
    }
    merge(pathString, data, onComplete, hash) {
        this.putInternal('m', pathString, data, onComplete, hash);
    }
    putInternal(action, pathString, data, onComplete, hash) {
        this.initConnection_();
        const request = {
            /*path*/ p: pathString,
            /*data*/ d: data
        };
        if (hash !== undefined) {
            request[ /*hash*/'h'] = hash;
        }
        // TODO: Only keep track of the most recent put for a given path?
        this.outstandingPuts_.push({
            action,
            request,
            onComplete
        });
        this.outstandingPutCount_++;
        const index = this.outstandingPuts_.length - 1;
        if (this.connected_) {
            this.sendPut_(index);
        }
        else {
            this.log_('Buffering put: ' + pathString);
        }
    }
    sendPut_(index) {
        const action = this.outstandingPuts_[index].action;
        const request = this.outstandingPuts_[index].request;
        const onComplete = this.outstandingPuts_[index].onComplete;
        this.outstandingPuts_[index].queued = this.connected_;
        this.sendRequest(action, request, (message) => {
            this.log_(action + ' response', message);
            delete this.outstandingPuts_[index];
            this.outstandingPutCount_--;
            // Clean up array occasionally.
            if (this.outstandingPutCount_ === 0) {
                this.outstandingPuts_ = [];
            }
            if (onComplete) {
                onComplete(message[ /*status*/'s'], message[ /* data */'d']);
            }
        });
    }
    reportStats(stats) {
        // If we're not connected, we just drop the stats.
        if (this.connected_) {
            const request = { /*counters*/ c: stats };
            this.log_('reportStats', request);
            this.sendRequest(/*stats*/ 's', request, result => {
                const status = result[ /*status*/'s'];
                if (status !== 'ok') {
                    const errorReason = result[ /* data */'d'];
                    this.log_('reportStats', 'Error sending stats: ' + errorReason);
                }
            });
        }
    }
    onDataMessage_(message) {
        if ('r' in message) {
            // this is a response
            this.log_('from server: ' + util.stringify(message));
            const reqNum = message['r'];
            const onResponse = this.requestCBHash_[reqNum];
            if (onResponse) {
                delete this.requestCBHash_[reqNum];
                onResponse(message[ /*body*/'b']);
            }
        }
        else if ('error' in message) {
            throw 'A server-side error has occurred: ' + message['error'];
        }
        else if ('a' in message) {
            // a and b are action and body, respectively
            this.onDataPush_(message['a'], message['b']);
        }
    }
    onDataPush_(action, body) {
        this.log_('handleServerMessage', action, body);
        if (action === 'd') {
            this.onDataUpdate_(body[ /*path*/'p'], body[ /*data*/'d'], 
            /*isMerge*/ false, body['t']);
        }
        else if (action === 'm') {
            this.onDataUpdate_(body[ /*path*/'p'], body[ /*data*/'d'], 
            /*isMerge=*/ true, body['t']);
        }
        else if (action === 'c') {
            this.onListenRevoked_(body[ /*path*/'p'], body[ /*query*/'q']);
        }
        else if (action === 'ac') {
            this.onAuthRevoked_(body[ /*status code*/'s'], body[ /* explanation */'d']);
        }
        else if (action === 'apc') {
            this.onAppCheckRevoked_(body[ /*status code*/'s'], body[ /* explanation */'d']);
        }
        else if (action === 'sd') {
            this.onSecurityDebugPacket_(body);
        }
        else {
            error('Unrecognized action received from server: ' +
                util.stringify(action) +
                '\nAre you using the latest client?');
        }
    }
    onReady_(timestamp, sessionId) {
        this.log_('connection ready');
        this.connected_ = true;
        this.lastConnectionEstablishedTime_ = new Date().getTime();
        this.handleTimestamp_(timestamp);
        this.lastSessionId = sessionId;
        if (this.firstConnection_) {
            this.sendConnectStats_();
        }
        this.restoreState_();
        this.firstConnection_ = false;
        this.onConnectStatus_(true);
    }
    scheduleConnect_(timeout) {
        util.assert(!this.realtime_, "Scheduling a connect when we're already connected/ing?");
        if (this.establishConnectionTimer_) {
            clearTimeout(this.establishConnectionTimer_);
        }
        // NOTE: Even when timeout is 0, it's important to do a setTimeout to work around an infuriating "Security Error" in
        // Firefox when trying to write to our long-polling iframe in some scenarios (e.g. Forge or our unit tests).
        this.establishConnectionTimer_ = setTimeout(() => {
            this.establishConnectionTimer_ = null;
            this.establishConnection_();
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
        }, Math.floor(timeout));
    }
    initConnection_() {
        if (!this.realtime_ && this.firstConnection_) {
            this.scheduleConnect_(0);
        }
    }
    onVisible_(visible) {
        // NOTE: Tabbing away and back to a window will defeat our reconnect backoff, but I think that's fine.
        if (visible &&
            !this.visible_ &&
            this.reconnectDelay_ === this.maxReconnectDelay_) {
            this.log_('Window became visible.  Reducing delay.');
            this.reconnectDelay_ = RECONNECT_MIN_DELAY;
            if (!this.realtime_) {
                this.scheduleConnect_(0);
            }
        }
        this.visible_ = visible;
    }
    onOnline_(online) {
        if (online) {
            this.log_('Browser went online.');
            this.reconnectDelay_ = RECONNECT_MIN_DELAY;
            if (!this.realtime_) {
                this.scheduleConnect_(0);
            }
        }
        else {
            this.log_('Browser went offline.  Killing connection.');
            if (this.realtime_) {
                this.realtime_.close();
            }
        }
    }
    onRealtimeDisconnect_() {
        this.log_('data client disconnected');
        this.connected_ = false;
        this.realtime_ = null;
        // Since we don't know if our sent transactions succeeded or not, we need to cancel them.
        this.cancelSentTransactions_();
        // Clear out the pending requests.
        this.requestCBHash_ = {};
        if (this.shouldReconnect_()) {
            if (!this.visible_) {
                this.log_("Window isn't visible.  Delaying reconnect.");
                this.reconnectDelay_ = this.maxReconnectDelay_;
                this.lastConnectionAttemptTime_ = new Date().getTime();
            }
            else if (this.lastConnectionEstablishedTime_) {
                // If we've been connected long enough, reset reconnect delay to minimum.
                const timeSinceLastConnectSucceeded = new Date().getTime() - this.lastConnectionEstablishedTime_;
                if (timeSinceLastConnectSucceeded > RECONNECT_DELAY_RESET_TIMEOUT) {
                    this.reconnectDelay_ = RECONNECT_MIN_DELAY;
                }
                this.lastConnectionEstablishedTime_ = null;
            }
            const timeSinceLastConnectAttempt = new Date().getTime() - this.lastConnectionAttemptTime_;
            let reconnectDelay = Math.max(0, this.reconnectDelay_ - timeSinceLastConnectAttempt);
            reconnectDelay = Math.random() * reconnectDelay;
            this.log_('Trying to reconnect in ' + reconnectDelay + 'ms');
            this.scheduleConnect_(reconnectDelay);
            // Adjust reconnect delay for next time.
            this.reconnectDelay_ = Math.min(this.maxReconnectDelay_, this.reconnectDelay_ * RECONNECT_DELAY_MULTIPLIER);
        }
        this.onConnectStatus_(false);
    }
    async establishConnection_() {
        if (this.shouldReconnect_()) {
            this.log_('Making a connection attempt');
            this.lastConnectionAttemptTime_ = new Date().getTime();
            this.lastConnectionEstablishedTime_ = null;
            const onDataMessage = this.onDataMessage_.bind(this);
            const onReady = this.onReady_.bind(this);
            const onDisconnect = this.onRealtimeDisconnect_.bind(this);
            const connId = this.id + ':' + PersistentConnection.nextConnectionId_++;
            const lastSessionId = this.lastSessionId;
            let canceled = false;
            let connection = null;
            const closeFn = function () {
                if (connection) {
                    connection.close();
                }
                else {
                    canceled = true;
                    onDisconnect();
                }
            };
            const sendRequestFn = function (msg) {
                util.assert(connection, "sendRequest call when we're not connected not allowed.");
                connection.sendRequest(msg);
            };
            this.realtime_ = {
                close: closeFn,
                sendRequest: sendRequestFn
            };
            const forceRefresh = this.forceTokenRefresh_;
            this.forceTokenRefresh_ = false;
            try {
                // First fetch auth and app check token, and establish connection after
                // fetching the token was successful
                const [authToken, appCheckToken] = await Promise.all([
                    this.authTokenProvider_.getToken(forceRefresh),
                    this.appCheckTokenProvider_.getToken(forceRefresh)
                ]);
                if (!canceled) {
                    log('getToken() completed. Creating connection.');
                    this.authToken_ = authToken && authToken.accessToken;
                    this.appCheckToken_ = appCheckToken && appCheckToken.token;
                    connection = new Connection(connId, this.repoInfo_, this.applicationId_, this.appCheckToken_, this.authToken_, onDataMessage, onReady, onDisconnect, 
                    /* onKill= */ reason => {
                        warn(reason + ' (' + this.repoInfo_.toString() + ')');
                        this.interrupt(SERVER_KILL_INTERRUPT_REASON);
                    }, lastSessionId);
                }
                else {
                    log('getToken() completed but was canceled');
                }
            }
            catch (error) {
                this.log_('Failed to get token: ' + error);
                if (!canceled) {
                    if (this.repoInfo_.nodeAdmin) {
                        // This may be a critical error for the Admin Node.js SDK, so log a warning.
                        // But getToken() may also just have temporarily failed, so we still want to
                        // continue retrying.
                        warn(error);
                    }
                    closeFn();
                }
            }
        }
    }
    interrupt(reason) {
        log('Interrupting connection for reason: ' + reason);
        this.interruptReasons_[reason] = true;
        if (this.realtime_) {
            this.realtime_.close();
        }
        else {
            if (this.establishConnectionTimer_) {
                clearTimeout(this.establishConnectionTimer_);
                this.establishConnectionTimer_ = null;
            }
            if (this.connected_) {
                this.onRealtimeDisconnect_();
            }
        }
    }
    resume(reason) {
        log('Resuming connection for reason: ' + reason);
        delete this.interruptReasons_[reason];
        if (util.isEmpty(this.interruptReasons_)) {
            this.reconnectDelay_ = RECONNECT_MIN_DELAY;
            if (!this.realtime_) {
                this.scheduleConnect_(0);
            }
        }
    }
    handleTimestamp_(timestamp) {
        const delta = timestamp - new Date().getTime();
        this.onServerInfoUpdate_({ serverTimeOffset: delta });
    }
    cancelSentTransactions_() {
        for (let i = 0; i < this.outstandingPuts_.length; i++) {
            const put = this.outstandingPuts_[i];
            if (put && /*hash*/ 'h' in put.request && put.queued) {
                if (put.onComplete) {
                    put.onComplete('disconnect');
                }
                delete this.outstandingPuts_[i];
                this.outstandingPutCount_--;
            }
        }
        // Clean up array occasionally.
        if (this.outstandingPutCount_ === 0) {
            this.outstandingPuts_ = [];
        }
    }
    onListenRevoked_(pathString, query) {
        // Remove the listen and manufacture a "permission_denied" error for the failed listen.
        let queryId;
        if (!query) {
            queryId = 'default';
        }
        else {
            queryId = query.map(q => ObjectToUniqueKey(q)).join('$');
        }
        const listen = this.removeListen_(pathString, queryId);
        if (listen && listen.onComplete) {
            listen.onComplete('permission_denied');
        }
    }
    removeListen_(pathString, queryId) {
        const normalizedPathString = new Path(pathString).toString(); // normalize path.
        let listen;
        if (this.listens.has(normalizedPathString)) {
            const map = this.listens.get(normalizedPathString);
            listen = map.get(queryId);
            map.delete(queryId);
            if (map.size === 0) {
                this.listens.delete(normalizedPathString);
            }
        }
        else {
            // all listens for this path has already been removed
            listen = undefined;
        }
        return listen;
    }
    onAuthRevoked_(statusCode, explanation) {
        log('Auth token revoked: ' + statusCode + '/' + explanation);
        this.authToken_ = null;
        this.forceTokenRefresh_ = true;
        this.realtime_.close();
        if (statusCode === 'invalid_token' || statusCode === 'permission_denied') {
            // We'll wait a couple times before logging the warning / increasing the
            // retry period since oauth tokens will report as "invalid" if they're
            // just expired. Plus there may be transient issues that resolve themselves.
            this.invalidAuthTokenCount_++;
            if (this.invalidAuthTokenCount_ >= INVALID_TOKEN_THRESHOLD) {
                // Set a long reconnect delay because recovery is unlikely
                this.reconnectDelay_ = RECONNECT_MAX_DELAY_FOR_ADMINS;
                // Notify the auth token provider that the token is invalid, which will log
                // a warning
                this.authTokenProvider_.notifyForInvalidToken();
            }
        }
    }
    onAppCheckRevoked_(statusCode, explanation) {
        log('App check token revoked: ' + statusCode + '/' + explanation);
        this.appCheckToken_ = null;
        this.forceTokenRefresh_ = true;
        // Note: We don't close the connection as the developer may not have
        // enforcement enabled. The backend closes connections with enforcements.
        if (statusCode === 'invalid_token' || statusCode === 'permission_denied') {
            // We'll wait a couple times before logging the warning / increasing the
            // retry period since oauth tokens will report as "invalid" if they're
            // just expired. Plus there may be transient issues that resolve themselves.
            this.invalidAppCheckTokenCount_++;
            if (this.invalidAppCheckTokenCount_ >= INVALID_TOKEN_THRESHOLD) {
                this.appCheckTokenProvider_.notifyForInvalidToken();
            }
        }
    }
    onSecurityDebugPacket_(body) {
        if (this.securityDebugCallback_) {
            this.securityDebugCallback_(body);
        }
        else {
            if ('msg' in body) {
                console.log('FIREBASE: ' + body['msg'].replace('\n', '\nFIREBASE: '));
            }
        }
    }
    restoreState_() {
        //Re-authenticate ourselves if we have a credential stored.
        this.tryAuth();
        this.tryAppCheck();
        // Puts depend on having received the corresponding data update from the server before they complete, so we must
        // make sure to send listens before puts.
        for (const queries of this.listens.values()) {
            for (const listenSpec of queries.values()) {
                this.sendListen_(listenSpec);
            }
        }
        for (let i = 0; i < this.outstandingPuts_.length; i++) {
            if (this.outstandingPuts_[i]) {
                this.sendPut_(i);
            }
        }
        while (this.onDisconnectRequestQueue_.length) {
            const request = this.onDisconnectRequestQueue_.shift();
            this.sendOnDisconnect_(request.action, request.pathString, request.data, request.onComplete);
        }
        for (let i = 0; i < this.outstandingGets_.length; i++) {
            if (this.outstandingGets_[i]) {
                this.sendGet_(i);
            }
        }
    }
    /**
     * Sends client stats for first connection
     */
    sendConnectStats_() {
        const stats = {};
        let clientName = 'js';
        if (util.isNodeSdk()) {
            if (this.repoInfo_.nodeAdmin) {
                clientName = 'admin_node';
            }
            else {
                clientName = 'node';
            }
        }
        stats['sdk.' + clientName + '.' + SDK_VERSION.replace(/\./g, '-')] = 1;
        if (util.isMobileCordova()) {
            stats['framework.cordova'] = 1;
        }
        else if (util.isReactNative()) {
            stats['framework.reactnative'] = 1;
        }
        this.reportStats(stats);
    }
    shouldReconnect_() {
        const online = OnlineMonitor.getInstance().currentlyOnline();
        return util.isEmpty(this.interruptReasons_) && online;
    }
}
PersistentConnection.nextPersistentConnectionId_ = 0;
/**
 * Counter for number of connections created. Mainly used for tagging in the logs
 */
PersistentConnection.nextConnectionId_ = 0;

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class NamedNode {
    constructor(name, node) {
        this.name = name;
        this.node = node;
    }
    static Wrap(name, node) {
        return new NamedNode(name, node);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class Index {
    /**
     * @returns A standalone comparison function for
     * this index
     */
    getCompare() {
        return this.compare.bind(this);
    }
    /**
     * Given a before and after value for a node, determine if the indexed value has changed. Even if they are different,
     * it's possible that the changes are isolated to parts of the snapshot that are not indexed.
     *
     *
     * @returns True if the portion of the snapshot being indexed changed between oldNode and newNode
     */
    indexedValueChanged(oldNode, newNode) {
        const oldWrapped = new NamedNode(MIN_NAME, oldNode);
        const newWrapped = new NamedNode(MIN_NAME, newNode);
        return this.compare(oldWrapped, newWrapped) !== 0;
    }
    /**
     * @returns a node wrapper that will sort equal to or less than
     * any other node wrapper, using this index
     */
    minPost() {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return NamedNode.MIN;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let __EMPTY_NODE;
class KeyIndex extends Index {
    static get __EMPTY_NODE() {
        return __EMPTY_NODE;
    }
    static set __EMPTY_NODE(val) {
        __EMPTY_NODE = val;
    }
    compare(a, b) {
        return nameCompare(a.name, b.name);
    }
    isDefinedOn(node) {
        // We could probably return true here (since every node has a key), but it's never called
        // so just leaving unimplemented for now.
        throw util.assertionError('KeyIndex.isDefinedOn not expected to be called.');
    }
    indexedValueChanged(oldNode, newNode) {
        return false; // The key for a node never changes.
    }
    minPost() {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return NamedNode.MIN;
    }
    maxPost() {
        // TODO: This should really be created once and cached in a static property, but
        // NamedNode isn't defined yet, so I can't use it in a static.  Bleh.
        return new NamedNode(MAX_NAME, __EMPTY_NODE);
    }
    makePost(indexValue, name) {
        util.assert(typeof indexValue === 'string', 'KeyIndex indexValue must always be a string.');
        // We just use empty node, but it'll never be compared, since our comparator only looks at name.
        return new NamedNode(indexValue, __EMPTY_NODE);
    }
    /**
     * @returns String representation for inclusion in a query spec
     */
    toString() {
        return '.key';
    }
}
const KEY_INDEX = new KeyIndex();

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An iterator over an LLRBNode.
 */
class SortedMapIterator {
    /**
     * @param node - Node to iterate.
     * @param isReverse_ - Whether or not to iterate in reverse
     */
    constructor(node, startKey, comparator, isReverse_, resultGenerator_ = null) {
        this.isReverse_ = isReverse_;
        this.resultGenerator_ = resultGenerator_;
        this.nodeStack_ = [];
        let cmp = 1;
        while (!node.isEmpty()) {
            node = node;
            cmp = startKey ? comparator(node.key, startKey) : 1;
            // flip the comparison if we're going in reverse
            if (isReverse_) {
                cmp *= -1;
            }
            if (cmp < 0) {
                // This node is less than our start key. ignore it
                if (this.isReverse_) {
                    node = node.left;
                }
                else {
                    node = node.right;
                }
            }
            else if (cmp === 0) {
                // This node is exactly equal to our start key. Push it on the stack, but stop iterating;
                this.nodeStack_.push(node);
                break;
            }
            else {
                // This node is greater than our start key, add it to the stack and move to the next one
                this.nodeStack_.push(node);
                if (this.isReverse_) {
                    node = node.right;
                }
                else {
                    node = node.left;
                }
            }
        }
    }
    getNext() {
        if (this.nodeStack_.length === 0) {
            return null;
        }
        let node = this.nodeStack_.pop();
        let result;
        if (this.resultGenerator_) {
            result = this.resultGenerator_(node.key, node.value);
        }
        else {
            result = { key: node.key, value: node.value };
        }
        if (this.isReverse_) {
            node = node.left;
            while (!node.isEmpty()) {
                this.nodeStack_.push(node);
                node = node.right;
            }
        }
        else {
            node = node.right;
            while (!node.isEmpty()) {
                this.nodeStack_.push(node);
                node = node.left;
            }
        }
        return result;
    }
    hasNext() {
        return this.nodeStack_.length > 0;
    }
    peek() {
        if (this.nodeStack_.length === 0) {
            return null;
        }
        const node = this.nodeStack_[this.nodeStack_.length - 1];
        if (this.resultGenerator_) {
            return this.resultGenerator_(node.key, node.value);
        }
        else {
            return { key: node.key, value: node.value };
        }
    }
}
/**
 * Represents a node in a Left-leaning Red-Black tree.
 */
class LLRBNode {
    /**
     * @param key - Key associated with this node.
     * @param value - Value associated with this node.
     * @param color - Whether this node is red.
     * @param left - Left child.
     * @param right - Right child.
     */
    constructor(key, value, color, left, right) {
        this.key = key;
        this.value = value;
        this.color = color != null ? color : LLRBNode.RED;
        this.left =
            left != null ? left : SortedMap.EMPTY_NODE;
        this.right =
            right != null ? right : SortedMap.EMPTY_NODE;
    }
    /**
     * Returns a copy of the current node, optionally replacing pieces of it.
     *
     * @param key - New key for the node, or null.
     * @param value - New value for the node, or null.
     * @param color - New color for the node, or null.
     * @param left - New left child for the node, or null.
     * @param right - New right child for the node, or null.
     * @returns The node copy.
     */
    copy(key, value, color, left, right) {
        return new LLRBNode(key != null ? key : this.key, value != null ? value : this.value, color != null ? color : this.color, left != null ? left : this.left, right != null ? right : this.right);
    }
    /**
     * @returns The total number of nodes in the tree.
     */
    count() {
        return this.left.count() + 1 + this.right.count();
    }
    /**
     * @returns True if the tree is empty.
     */
    isEmpty() {
        return false;
    }
    /**
     * Traverses the tree in key order and calls the specified action function
     * for each node.
     *
     * @param action - Callback function to be called for each
     *   node.  If it returns true, traversal is aborted.
     * @returns The first truthy value returned by action, or the last falsey
     *   value returned by action
     */
    inorderTraversal(action) {
        return (this.left.inorderTraversal(action) ||
            !!action(this.key, this.value) ||
            this.right.inorderTraversal(action));
    }
    /**
     * Traverses the tree in reverse key order and calls the specified action function
     * for each node.
     *
     * @param action - Callback function to be called for each
     * node.  If it returns true, traversal is aborted.
     * @returns True if traversal was aborted.
     */
    reverseTraversal(action) {
        return (this.right.reverseTraversal(action) ||
            action(this.key, this.value) ||
            this.left.reverseTraversal(action));
    }
    /**
     * @returns The minimum node in the tree.
     */
    min_() {
        if (this.left.isEmpty()) {
            return this;
        }
        else {
            return this.left.min_();
        }
    }
    /**
     * @returns The maximum key in the tree.
     */
    minKey() {
        return this.min_().key;
    }
    /**
     * @returns The maximum key in the tree.
     */
    maxKey() {
        if (this.right.isEmpty()) {
            return this.key;
        }
        else {
            return this.right.maxKey();
        }
    }
    /**
     * @param key - Key to insert.
     * @param value - Value to insert.
     * @param comparator - Comparator.
     * @returns New tree, with the key/value added.
     */
    insert(key, value, comparator) {
        let n = this;
        const cmp = comparator(key, n.key);
        if (cmp < 0) {
            n = n.copy(null, null, null, n.left.insert(key, value, comparator), null);
        }
        else if (cmp === 0) {
            n = n.copy(null, value, null, null, null);
        }
        else {
            n = n.copy(null, null, null, null, n.right.insert(key, value, comparator));
        }
        return n.fixUp_();
    }
    /**
     * @returns New tree, with the minimum key removed.
     */
    removeMin_() {
        if (this.left.isEmpty()) {
            return SortedMap.EMPTY_NODE;
        }
        let n = this;
        if (!n.left.isRed_() && !n.left.left.isRed_()) {
            n = n.moveRedLeft_();
        }
        n = n.copy(null, null, null, n.left.removeMin_(), null);
        return n.fixUp_();
    }
    /**
     * @param key - The key of the item to remove.
     * @param comparator - Comparator.
     * @returns New tree, with the specified item removed.
     */
    remove(key, comparator) {
        let n, smallest;
        n = this;
        if (comparator(key, n.key) < 0) {
            if (!n.left.isEmpty() && !n.left.isRed_() && !n.left.left.isRed_()) {
                n = n.moveRedLeft_();
            }
            n = n.copy(null, null, null, n.left.remove(key, comparator), null);
        }
        else {
            if (n.left.isRed_()) {
                n = n.rotateRight_();
            }
            if (!n.right.isEmpty() && !n.right.isRed_() && !n.right.left.isRed_()) {
                n = n.moveRedRight_();
            }
            if (comparator(key, n.key) === 0) {
                if (n.right.isEmpty()) {
                    return SortedMap.EMPTY_NODE;
                }
                else {
                    smallest = n.right.min_();
                    n = n.copy(smallest.key, smallest.value, null, null, n.right.removeMin_());
                }
            }
            n = n.copy(null, null, null, null, n.right.remove(key, comparator));
        }
        return n.fixUp_();
    }
    /**
     * @returns Whether this is a RED node.
     */
    isRed_() {
        return this.color;
    }
    /**
     * @returns New tree after performing any needed rotations.
     */
    fixUp_() {
        let n = this;
        if (n.right.isRed_() && !n.left.isRed_()) {
            n = n.rotateLeft_();
        }
        if (n.left.isRed_() && n.left.left.isRed_()) {
            n = n.rotateRight_();
        }
        if (n.left.isRed_() && n.right.isRed_()) {
            n = n.colorFlip_();
        }
        return n;
    }
    /**
     * @returns New tree, after moveRedLeft.
     */
    moveRedLeft_() {
        let n = this.colorFlip_();
        if (n.right.left.isRed_()) {
            n = n.copy(null, null, null, null, n.right.rotateRight_());
            n = n.rotateLeft_();
            n = n.colorFlip_();
        }
        return n;
    }
    /**
     * @returns New tree, after moveRedRight.
     */
    moveRedRight_() {
        let n = this.colorFlip_();
        if (n.left.left.isRed_()) {
            n = n.rotateRight_();
            n = n.colorFlip_();
        }
        return n;
    }
    /**
     * @returns New tree, after rotateLeft.
     */
    rotateLeft_() {
        const nl = this.copy(null, null, LLRBNode.RED, null, this.right.left);
        return this.right.copy(null, null, this.color, nl, null);
    }
    /**
     * @returns New tree, after rotateRight.
     */
    rotateRight_() {
        const nr = this.copy(null, null, LLRBNode.RED, this.left.right, null);
        return this.left.copy(null, null, this.color, null, nr);
    }
    /**
     * @returns Newt ree, after colorFlip.
     */
    colorFlip_() {
        const left = this.left.copy(null, null, !this.left.color, null, null);
        const right = this.right.copy(null, null, !this.right.color, null, null);
        return this.copy(null, null, !this.color, left, right);
    }
    /**
     * For testing.
     *
     * @returns True if all is well.
     */
    checkMaxDepth_() {
        const blackDepth = this.check_();
        return Math.pow(2.0, blackDepth) <= this.count() + 1;
    }
    check_() {
        if (this.isRed_() && this.left.isRed_()) {
            throw new Error('Red node has red child(' + this.key + ',' + this.value + ')');
        }
        if (this.right.isRed_()) {
            throw new Error('Right child of (' + this.key + ',' + this.value + ') is red');
        }
        const blackDepth = this.left.check_();
        if (blackDepth !== this.right.check_()) {
            throw new Error('Black depths differ');
        }
        else {
            return blackDepth + (this.isRed_() ? 0 : 1);
        }
    }
}
LLRBNode.RED = true;
LLRBNode.BLACK = false;
/**
 * Represents an empty node (a leaf node in the Red-Black Tree).
 */
class LLRBEmptyNode {
    /**
     * Returns a copy of the current node.
     *
     * @returns The node copy.
     */
    copy(key, value, color, left, right) {
        return this;
    }
    /**
     * Returns a copy of the tree, with the specified key/value added.
     *
     * @param key - Key to be added.
     * @param value - Value to be added.
     * @param comparator - Comparator.
     * @returns New tree, with item added.
     */
    insert(key, value, comparator) {
        return new LLRBNode(key, value, null);
    }
    /**
     * Returns a copy of the tree, with the specified key removed.
     *
     * @param key - The key to remove.
     * @param comparator - Comparator.
     * @returns New tree, with item removed.
     */
    remove(key, comparator) {
        return this;
    }
    /**
     * @returns The total number of nodes in the tree.
     */
    count() {
        return 0;
    }
    /**
     * @returns True if the tree is empty.
     */
    isEmpty() {
        return true;
    }
    /**
     * Traverses the tree in key order and calls the specified action function
     * for each node.
     *
     * @param action - Callback function to be called for each
     * node.  If it returns true, traversal is aborted.
     * @returns True if traversal was aborted.
     */
    inorderTraversal(action) {
        return false;
    }
    /**
     * Traverses the tree in reverse key order and calls the specified action function
     * for each node.
     *
     * @param action - Callback function to be called for each
     * node.  If it returns true, traversal is aborted.
     * @returns True if traversal was aborted.
     */
    reverseTraversal(action) {
        return false;
    }
    minKey() {
        return null;
    }
    maxKey() {
        return null;
    }
    check_() {
        return 0;
    }
    /**
     * @returns Whether this node is red.
     */
    isRed_() {
        return false;
    }
}
/**
 * An immutable sorted map implementation, based on a Left-leaning Red-Black
 * tree.
 */
class SortedMap {
    /**
     * @param comparator_ - Key comparator.
     * @param root_ - Optional root node for the map.
     */
    constructor(comparator_, root_ = SortedMap.EMPTY_NODE) {
        this.comparator_ = comparator_;
        this.root_ = root_;
    }
    /**
     * Returns a copy of the map, with the specified key/value added or replaced.
     * (TODO: We should perhaps rename this method to 'put')
     *
     * @param key - Key to be added.
     * @param value - Value to be added.
     * @returns New map, with item added.
     */
    insert(key, value) {
        return new SortedMap(this.comparator_, this.root_
            .insert(key, value, this.comparator_)
            .copy(null, null, LLRBNode.BLACK, null, null));
    }
    /**
     * Returns a copy of the map, with the specified key removed.
     *
     * @param key - The key to remove.
     * @returns New map, with item removed.
     */
    remove(key) {
        return new SortedMap(this.comparator_, this.root_
            .remove(key, this.comparator_)
            .copy(null, null, LLRBNode.BLACK, null, null));
    }
    /**
     * Returns the value of the node with the given key, or null.
     *
     * @param key - The key to look up.
     * @returns The value of the node with the given key, or null if the
     * key doesn't exist.
     */
    get(key) {
        let cmp;
        let node = this.root_;
        while (!node.isEmpty()) {
            cmp = this.comparator_(key, node.key);
            if (cmp === 0) {
                return node.value;
            }
            else if (cmp < 0) {
                node = node.left;
            }
            else if (cmp > 0) {
                node = node.right;
            }
        }
        return null;
    }
    /**
     * Returns the key of the item *before* the specified key, or null if key is the first item.
     * @param key - The key to find the predecessor of
     * @returns The predecessor key.
     */
    getPredecessorKey(key) {
        let cmp, node = this.root_, rightParent = null;
        while (!node.isEmpty()) {
            cmp = this.comparator_(key, node.key);
            if (cmp === 0) {
                if (!node.left.isEmpty()) {
                    node = node.left;
                    while (!node.right.isEmpty()) {
                        node = node.right;
                    }
                    return node.key;
                }
                else if (rightParent) {
                    return rightParent.key;
                }
                else {
                    return null; // first item.
                }
            }
            else if (cmp < 0) {
                node = node.left;
            }
            else if (cmp > 0) {
                rightParent = node;
                node = node.right;
            }
        }
        throw new Error('Attempted to find predecessor key for a nonexistent key.  What gives?');
    }
    /**
     * @returns True if the map is empty.
     */
    isEmpty() {
        return this.root_.isEmpty();
    }
    /**
     * @returns The total number of nodes in the map.
     */
    count() {
        return this.root_.count();
    }
    /**
     * @returns The minimum key in the map.
     */
    minKey() {
        return this.root_.minKey();
    }
    /**
     * @returns The maximum key in the map.
     */
    maxKey() {
        return this.root_.maxKey();
    }
    /**
     * Traverses the map in key order and calls the specified action function
     * for each key/value pair.
     *
     * @param action - Callback function to be called
     * for each key/value pair.  If action returns true, traversal is aborted.
     * @returns The first truthy value returned by action, or the last falsey
     *   value returned by action
     */
    inorderTraversal(action) {
        return this.root_.inorderTraversal(action);
    }
    /**
     * Traverses the map in reverse key order and calls the specified action function
     * for each key/value pair.
     *
     * @param action - Callback function to be called
     * for each key/value pair.  If action returns true, traversal is aborted.
     * @returns True if the traversal was aborted.
     */
    reverseTraversal(action) {
        return this.root_.reverseTraversal(action);
    }
    /**
     * Returns an iterator over the SortedMap.
     * @returns The iterator.
     */
    getIterator(resultGenerator) {
        return new SortedMapIterator(this.root_, null, this.comparator_, false, resultGenerator);
    }
    getIteratorFrom(key, resultGenerator) {
        return new SortedMapIterator(this.root_, key, this.comparator_, false, resultGenerator);
    }
    getReverseIteratorFrom(key, resultGenerator) {
        return new SortedMapIterator(this.root_, key, this.comparator_, true, resultGenerator);
    }
    getReverseIterator(resultGenerator) {
        return new SortedMapIterator(this.root_, null, this.comparator_, true, resultGenerator);
    }
}
/**
 * Always use the same empty node, to reduce memory.
 */
SortedMap.EMPTY_NODE = new LLRBEmptyNode();

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function NAME_ONLY_COMPARATOR(left, right) {
    return nameCompare(left.name, right.name);
}
function NAME_COMPARATOR(left, right) {
    return nameCompare(left, right);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let MAX_NODE$2;
function setMaxNode$1(val) {
    MAX_NODE$2 = val;
}
const priorityHashText = function (priority) {
    if (typeof priority === 'number') {
        return 'number:' + doubleToIEEE754String(priority);
    }
    else {
        return 'string:' + priority;
    }
};
/**
 * Validates that a priority snapshot Node is valid.
 */
const validatePriorityNode = function (priorityNode) {
    if (priorityNode.isLeafNode()) {
        const val = priorityNode.val();
        util.assert(typeof val === 'string' ||
            typeof val === 'number' ||
            (typeof val === 'object' && util.contains(val, '.sv')), 'Priority must be a string or number.');
    }
    else {
        util.assert(priorityNode === MAX_NODE$2 || priorityNode.isEmpty(), 'priority of unexpected type.');
    }
    // Don't call getPriority() on MAX_NODE to avoid hitting assertion.
    util.assert(priorityNode === MAX_NODE$2 || priorityNode.getPriority().isEmpty(), "Priority nodes can't have a priority of their own.");
};

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let __childrenNodeConstructor;
/**
 * LeafNode is a class for storing leaf nodes in a DataSnapshot.  It
 * implements Node and stores the value of the node (a string,
 * number, or boolean) accessible via getValue().
 */
class LeafNode {
    /**
     * @param value_ - The value to store in this leaf node. The object type is
     * possible in the event of a deferred value
     * @param priorityNode_ - The priority of this node.
     */
    constructor(value_, priorityNode_ = LeafNode.__childrenNodeConstructor.EMPTY_NODE) {
        this.value_ = value_;
        this.priorityNode_ = priorityNode_;
        this.lazyHash_ = null;
        util.assert(this.value_ !== undefined && this.value_ !== null, "LeafNode shouldn't be created with null/undefined value.");
        validatePriorityNode(this.priorityNode_);
    }
    static set __childrenNodeConstructor(val) {
        __childrenNodeConstructor = val;
    }
    static get __childrenNodeConstructor() {
        return __childrenNodeConstructor;
    }
    /** @inheritDoc */
    isLeafNode() {
        return true;
    }
    /** @inheritDoc */
    getPriority() {
        return this.priorityNode_;
    }
    /** @inheritDoc */
    updatePriority(newPriorityNode) {
        return new LeafNode(this.value_, newPriorityNode);
    }
    /** @inheritDoc */
    getImmediateChild(childName) {
        // Hack to treat priority as a regular child
        if (childName === '.priority') {
            return this.priorityNode_;
        }
        else {
            return LeafNode.__childrenNodeConstructor.EMPTY_NODE;
        }
    }
    /** @inheritDoc */
    getChild(path) {
        if (pathIsEmpty(path)) {
            return this;
        }
        else if (pathGetFront(path) === '.priority') {
            return this.priorityNode_;
        }
        else {
            return LeafNode.__childrenNodeConstructor.EMPTY_NODE;
        }
    }
    hasChild() {
        return false;
    }
    /** @inheritDoc */
    getPredecessorChildName(childName, childNode) {
        return null;
    }
    /** @inheritDoc */
    updateImmediateChild(childName, newChildNode) {
        if (childName === '.priority') {
            return this.updatePriority(newChildNode);
        }
        else if (newChildNode.isEmpty() && childName !== '.priority') {
            return this;
        }
        else {
            return LeafNode.__childrenNodeConstructor.EMPTY_NODE.updateImmediateChild(childName, newChildNode).updatePriority(this.priorityNode_);
        }
    }
    /** @inheritDoc */
    updateChild(path, newChildNode) {
        const front = pathGetFront(path);
        if (front === null) {
            return newChildNode;
        }
        else if (newChildNode.isEmpty() && front !== '.priority') {
            return this;
        }
        else {
            util.assert(front !== '.priority' || pathGetLength(path) === 1, '.priority must be the last token in a path');
            return this.updateImmediateChild(front, LeafNode.__childrenNodeConstructor.EMPTY_NODE.updateChild(pathPopFront(path), newChildNode));
        }
    }
    /** @inheritDoc */
    isEmpty() {
        return false;
    }
    /** @inheritDoc */
    numChildren() {
        return 0;
    }
    /** @inheritDoc */
    forEachChild(index, action) {
        return false;
    }
    val(exportFormat) {
        if (exportFormat && !this.getPriority().isEmpty()) {
            return {
                '.value': this.getValue(),
                '.priority': this.getPriority().val()
            };
        }
        else {
            return this.getValue();
        }
    }
    /** @inheritDoc */
    hash() {
        if (this.lazyHash_ === null) {
            let toHash = '';
            if (!this.priorityNode_.isEmpty()) {
                toHash +=
                    'priority:' +
                        priorityHashText(this.priorityNode_.val()) +
                        ':';
            }
            const type = typeof this.value_;
            toHash += type + ':';
            if (type === 'number') {
                toHash += doubleToIEEE754String(this.value_);
            }
            else {
                toHash += this.value_;
            }
            this.lazyHash_ = sha1(toHash);
        }
        return this.lazyHash_;
    }
    /**
     * Returns the value of the leaf node.
     * @returns The value of the node.
     */
    getValue() {
        return this.value_;
    }
    compareTo(other) {
        if (other === LeafNode.__childrenNodeConstructor.EMPTY_NODE) {
            return 1;
        }
        else if (other instanceof LeafNode.__childrenNodeConstructor) {
            return -1;
        }
        else {
            util.assert(other.isLeafNode(), 'Unknown node type');
            return this.compareToLeafNode_(other);
        }
    }
    /**
     * Comparison specifically for two leaf nodes
     */
    compareToLeafNode_(otherLeaf) {
        const otherLeafType = typeof otherLeaf.value_;
        const thisLeafType = typeof this.value_;
        const otherIndex = LeafNode.VALUE_TYPE_ORDER.indexOf(otherLeafType);
        const thisIndex = LeafNode.VALUE_TYPE_ORDER.indexOf(thisLeafType);
        util.assert(otherIndex >= 0, 'Unknown leaf type: ' + otherLeafType);
        util.assert(thisIndex >= 0, 'Unknown leaf type: ' + thisLeafType);
        if (otherIndex === thisIndex) {
            // Same type, compare values
            if (thisLeafType === 'object') {
                // Deferred value nodes are all equal, but we should also never get to this point...
                return 0;
            }
            else {
                // Note that this works because true > false, all others are number or string comparisons
                if (this.value_ < otherLeaf.value_) {
                    return -1;
                }
                else if (this.value_ === otherLeaf.value_) {
                    return 0;
                }
                else {
                    return 1;
                }
            }
        }
        else {
            return thisIndex - otherIndex;
        }
    }
    withIndex() {
        return this;
    }
    isIndexed() {
        return true;
    }
    equals(other) {
        if (other === this) {
            return true;
        }
        else if (other.isLeafNode()) {
            const otherLeaf = other;
            return (this.value_ === otherLeaf.value_ &&
                this.priorityNode_.equals(otherLeaf.priorityNode_));
        }
        else {
            return false;
        }
    }
}
/**
 * The sort order for comparing leaf nodes of different types. If two leaf nodes have
 * the same type, the comparison falls back to their value
 */
LeafNode.VALUE_TYPE_ORDER = ['object', 'boolean', 'number', 'string'];

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let nodeFromJSON$1;
let MAX_NODE$1;
function setNodeFromJSON(val) {
    nodeFromJSON$1 = val;
}
function setMaxNode(val) {
    MAX_NODE$1 = val;
}
class PriorityIndex extends Index {
    compare(a, b) {
        const aPriority = a.node.getPriority();
        const bPriority = b.node.getPriority();
        const indexCmp = aPriority.compareTo(bPriority);
        if (indexCmp === 0) {
            return nameCompare(a.name, b.name);
        }
        else {
            return indexCmp;
        }
    }
    isDefinedOn(node) {
        return !node.getPriority().isEmpty();
    }
    indexedValueChanged(oldNode, newNode) {
        return !oldNode.getPriority().equals(newNode.getPriority());
    }
    minPost() {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return NamedNode.MIN;
    }
    maxPost() {
        return new NamedNode(MAX_NAME, new LeafNode('[PRIORITY-POST]', MAX_NODE$1));
    }
    makePost(indexValue, name) {
        const priorityNode = nodeFromJSON$1(indexValue);
        return new NamedNode(name, new LeafNode('[PRIORITY-POST]', priorityNode));
    }
    /**
     * @returns String representation for inclusion in a query spec
     */
    toString() {
        return '.priority';
    }
}
const PRIORITY_INDEX = new PriorityIndex();

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const LOG_2 = Math.log(2);
class Base12Num {
    constructor(length) {
        const logBase2 = (num) => 
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        parseInt((Math.log(num) / LOG_2), 10);
        const bitMask = (bits) => parseInt(Array(bits + 1).join('1'), 2);
        this.count = logBase2(length + 1);
        this.current_ = this.count - 1;
        const mask = bitMask(this.count);
        this.bits_ = (length + 1) & mask;
    }
    nextBitIsOne() {
        //noinspection JSBitwiseOperatorUsage
        const result = !(this.bits_ & (0x1 << this.current_));
        this.current_--;
        return result;
    }
}
/**
 * Takes a list of child nodes and constructs a SortedSet using the given comparison
 * function
 *
 * Uses the algorithm described in the paper linked here:
 * http://citeseerx.ist.psu.edu/viewdoc/summary?doi=10.1.1.46.1458
 *
 * @param childList - Unsorted list of children
 * @param cmp - The comparison method to be used
 * @param keyFn - An optional function to extract K from a node wrapper, if K's
 * type is not NamedNode
 * @param mapSortFn - An optional override for comparator used by the generated sorted map
 */
const buildChildSet = function (childList, cmp, keyFn, mapSortFn) {
    childList.sort(cmp);
    const buildBalancedTree = function (low, high) {
        const length = high - low;
        let namedNode;
        let key;
        if (length === 0) {
            return null;
        }
        else if (length === 1) {
            namedNode = childList[low];
            key = keyFn ? keyFn(namedNode) : namedNode;
            return new LLRBNode(key, namedNode.node, LLRBNode.BLACK, null, null);
        }
        else {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const middle = parseInt((length / 2), 10) + low;
            const left = buildBalancedTree(low, middle);
            const right = buildBalancedTree(middle + 1, high);
            namedNode = childList[middle];
            key = keyFn ? keyFn(namedNode) : namedNode;
            return new LLRBNode(key, namedNode.node, LLRBNode.BLACK, left, right);
        }
    };
    const buildFrom12Array = function (base12) {
        let node = null;
        let root = null;
        let index = childList.length;
        const buildPennant = function (chunkSize, color) {
            const low = index - chunkSize;
            const high = index;
            index -= chunkSize;
            const childTree = buildBalancedTree(low + 1, high);
            const namedNode = childList[low];
            const key = keyFn ? keyFn(namedNode) : namedNode;
            attachPennant(new LLRBNode(key, namedNode.node, color, null, childTree));
        };
        const attachPennant = function (pennant) {
            if (node) {
                node.left = pennant;
                node = pennant;
            }
            else {
                root = pennant;
                node = pennant;
            }
        };
        for (let i = 0; i < base12.count; ++i) {
            const isOne = base12.nextBitIsOne();
            // The number of nodes taken in each slice is 2^(arr.length - (i + 1))
            const chunkSize = Math.pow(2, base12.count - (i + 1));
            if (isOne) {
                buildPennant(chunkSize, LLRBNode.BLACK);
            }
            else {
                // current == 2
                buildPennant(chunkSize, LLRBNode.BLACK);
                buildPennant(chunkSize, LLRBNode.RED);
            }
        }
        return root;
    };
    const base12 = new Base12Num(childList.length);
    const root = buildFrom12Array(base12);
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    return new SortedMap(mapSortFn || cmp, root);
};

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let _defaultIndexMap;
const fallbackObject = {};
class IndexMap {
    constructor(indexes_, indexSet_) {
        this.indexes_ = indexes_;
        this.indexSet_ = indexSet_;
    }
    /**
     * The default IndexMap for nodes without a priority
     */
    static get Default() {
        util.assert(fallbackObject && PRIORITY_INDEX, 'ChildrenNode.ts has not been loaded');
        _defaultIndexMap =
            _defaultIndexMap ||
                new IndexMap({ '.priority': fallbackObject }, { '.priority': PRIORITY_INDEX });
        return _defaultIndexMap;
    }
    get(indexKey) {
        const sortedMap = util.safeGet(this.indexes_, indexKey);
        if (!sortedMap) {
            throw new Error('No index defined for ' + indexKey);
        }
        if (sortedMap instanceof SortedMap) {
            return sortedMap;
        }
        else {
            // The index exists, but it falls back to just name comparison. Return null so that the calling code uses the
            // regular child map
            return null;
        }
    }
    hasIndex(indexDefinition) {
        return util.contains(this.indexSet_, indexDefinition.toString());
    }
    addIndex(indexDefinition, existingChildren) {
        util.assert(indexDefinition !== KEY_INDEX, "KeyIndex always exists and isn't meant to be added to the IndexMap.");
        const childList = [];
        let sawIndexedValue = false;
        const iter = existingChildren.getIterator(NamedNode.Wrap);
        let next = iter.getNext();
        while (next) {
            sawIndexedValue =
                sawIndexedValue || indexDefinition.isDefinedOn(next.node);
            childList.push(next);
            next = iter.getNext();
        }
        let newIndex;
        if (sawIndexedValue) {
            newIndex = buildChildSet(childList, indexDefinition.getCompare());
        }
        else {
            newIndex = fallbackObject;
        }
        const indexName = indexDefinition.toString();
        const newIndexSet = Object.assign({}, this.indexSet_);
        newIndexSet[indexName] = indexDefinition;
        const newIndexes = Object.assign({}, this.indexes_);
        newIndexes[indexName] = newIndex;
        return new IndexMap(newIndexes, newIndexSet);
    }
    /**
     * Ensure that this node is properly tracked in any indexes that we're maintaining
     */
    addToIndexes(namedNode, existingChildren) {
        const newIndexes = util.map(this.indexes_, (indexedChildren, indexName) => {
            const index = util.safeGet(this.indexSet_, indexName);
            util.assert(index, 'Missing index implementation for ' + indexName);
            if (indexedChildren === fallbackObject) {
                // Check to see if we need to index everything
                if (index.isDefinedOn(namedNode.node)) {
                    // We need to build this index
                    const childList = [];
                    const iter = existingChildren.getIterator(NamedNode.Wrap);
                    let next = iter.getNext();
                    while (next) {
                        if (next.name !== namedNode.name) {
                            childList.push(next);
                        }
                        next = iter.getNext();
                    }
                    childList.push(namedNode);
                    return buildChildSet(childList, index.getCompare());
                }
                else {
                    // No change, this remains a fallback
                    return fallbackObject;
                }
            }
            else {
                const existingSnap = existingChildren.get(namedNode.name);
                let newChildren = indexedChildren;
                if (existingSnap) {
                    newChildren = newChildren.remove(new NamedNode(namedNode.name, existingSnap));
                }
                return newChildren.insert(namedNode, namedNode.node);
            }
        });
        return new IndexMap(newIndexes, this.indexSet_);
    }
    /**
     * Create a new IndexMap instance with the given value removed
     */
    removeFromIndexes(namedNode, existingChildren) {
        const newIndexes = util.map(this.indexes_, (indexedChildren) => {
            if (indexedChildren === fallbackObject) {
                // This is the fallback. Just return it, nothing to do in this case
                return indexedChildren;
            }
            else {
                const existingSnap = existingChildren.get(namedNode.name);
                if (existingSnap) {
                    return indexedChildren.remove(new NamedNode(namedNode.name, existingSnap));
                }
                else {
                    // No record of this child
                    return indexedChildren;
                }
            }
        });
        return new IndexMap(newIndexes, this.indexSet_);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// TODO: For memory savings, don't store priorityNode_ if it's empty.
let EMPTY_NODE;
/**
 * ChildrenNode is a class for storing internal nodes in a DataSnapshot
 * (i.e. nodes with children).  It implements Node and stores the
 * list of children in the children property, sorted by child name.
 */
class ChildrenNode {
    /**
     * @param children_ - List of children of this node..
     * @param priorityNode_ - The priority of this node (as a snapshot node).
     */
    constructor(children_, priorityNode_, indexMap_) {
        this.children_ = children_;
        this.priorityNode_ = priorityNode_;
        this.indexMap_ = indexMap_;
        this.lazyHash_ = null;
        /**
         * Note: The only reason we allow null priority is for EMPTY_NODE, since we can't use
         * EMPTY_NODE as the priority of EMPTY_NODE.  We might want to consider making EMPTY_NODE its own
         * class instead of an empty ChildrenNode.
         */
        if (this.priorityNode_) {
            validatePriorityNode(this.priorityNode_);
        }
        if (this.children_.isEmpty()) {
            util.assert(!this.priorityNode_ || this.priorityNode_.isEmpty(), 'An empty node cannot have a priority');
        }
    }
    static get EMPTY_NODE() {
        return (EMPTY_NODE ||
            (EMPTY_NODE = new ChildrenNode(new SortedMap(NAME_COMPARATOR), null, IndexMap.Default)));
    }
    /** @inheritDoc */
    isLeafNode() {
        return false;
    }
    /** @inheritDoc */
    getPriority() {
        return this.priorityNode_ || EMPTY_NODE;
    }
    /** @inheritDoc */
    updatePriority(newPriorityNode) {
        if (this.children_.isEmpty()) {
            // Don't allow priorities on empty nodes
            return this;
        }
        else {
            return new ChildrenNode(this.children_, newPriorityNode, this.indexMap_);
        }
    }
    /** @inheritDoc */
    getImmediateChild(childName) {
        // Hack to treat priority as a regular child
        if (childName === '.priority') {
            return this.getPriority();
        }
        else {
            const child = this.children_.get(childName);
            return child === null ? EMPTY_NODE : child;
        }
    }
    /** @inheritDoc */
    getChild(path) {
        const front = pathGetFront(path);
        if (front === null) {
            return this;
        }
        return this.getImmediateChild(front).getChild(pathPopFront(path));
    }
    /** @inheritDoc */
    hasChild(childName) {
        return this.children_.get(childName) !== null;
    }
    /** @inheritDoc */
    updateImmediateChild(childName, newChildNode) {
        util.assert(newChildNode, 'We should always be passing snapshot nodes');
        if (childName === '.priority') {
            return this.updatePriority(newChildNode);
        }
        else {
            const namedNode = new NamedNode(childName, newChildNode);
            let newChildren, newIndexMap;
            if (newChildNode.isEmpty()) {
                newChildren = this.children_.remove(childName);
                newIndexMap = this.indexMap_.removeFromIndexes(namedNode, this.children_);
            }
            else {
                newChildren = this.children_.insert(childName, newChildNode);
                newIndexMap = this.indexMap_.addToIndexes(namedNode, this.children_);
            }
            const newPriority = newChildren.isEmpty()
                ? EMPTY_NODE
                : this.priorityNode_;
            return new ChildrenNode(newChildren, newPriority, newIndexMap);
        }
    }
    /** @inheritDoc */
    updateChild(path, newChildNode) {
        const front = pathGetFront(path);
        if (front === null) {
            return newChildNode;
        }
        else {
            util.assert(pathGetFront(path) !== '.priority' || pathGetLength(path) === 1, '.priority must be the last token in a path');
            const newImmediateChild = this.getImmediateChild(front).updateChild(pathPopFront(path), newChildNode);
            return this.updateImmediateChild(front, newImmediateChild);
        }
    }
    /** @inheritDoc */
    isEmpty() {
        return this.children_.isEmpty();
    }
    /** @inheritDoc */
    numChildren() {
        return this.children_.count();
    }
    /** @inheritDoc */
    val(exportFormat) {
        if (this.isEmpty()) {
            return null;
        }
        const obj = {};
        let numKeys = 0, maxKey = 0, allIntegerKeys = true;
        this.forEachChild(PRIORITY_INDEX, (key, childNode) => {
            obj[key] = childNode.val(exportFormat);
            numKeys++;
            if (allIntegerKeys && ChildrenNode.INTEGER_REGEXP_.test(key)) {
                maxKey = Math.max(maxKey, Number(key));
            }
            else {
                allIntegerKeys = false;
            }
        });
        if (!exportFormat && allIntegerKeys && maxKey < 2 * numKeys) {
            // convert to array.
            const array = [];
            // eslint-disable-next-line guard-for-in
            for (const key in obj) {
                array[key] = obj[key];
            }
            return array;
        }
        else {
            if (exportFormat && !this.getPriority().isEmpty()) {
                obj['.priority'] = this.getPriority().val();
            }
            return obj;
        }
    }
    /** @inheritDoc */
    hash() {
        if (this.lazyHash_ === null) {
            let toHash = '';
            if (!this.getPriority().isEmpty()) {
                toHash +=
                    'priority:' +
                        priorityHashText(this.getPriority().val()) +
                        ':';
            }
            this.forEachChild(PRIORITY_INDEX, (key, childNode) => {
                const childHash = childNode.hash();
                if (childHash !== '') {
                    toHash += ':' + key + ':' + childHash;
                }
            });
            this.lazyHash_ = toHash === '' ? '' : sha1(toHash);
        }
        return this.lazyHash_;
    }
    /** @inheritDoc */
    getPredecessorChildName(childName, childNode, index) {
        const idx = this.resolveIndex_(index);
        if (idx) {
            const predecessor = idx.getPredecessorKey(new NamedNode(childName, childNode));
            return predecessor ? predecessor.name : null;
        }
        else {
            return this.children_.getPredecessorKey(childName);
        }
    }
    getFirstChildName(indexDefinition) {
        const idx = this.resolveIndex_(indexDefinition);
        if (idx) {
            const minKey = idx.minKey();
            return minKey && minKey.name;
        }
        else {
            return this.children_.minKey();
        }
    }
    getFirstChild(indexDefinition) {
        const minKey = this.getFirstChildName(indexDefinition);
        if (minKey) {
            return new NamedNode(minKey, this.children_.get(minKey));
        }
        else {
            return null;
        }
    }
    /**
     * Given an index, return the key name of the largest value we have, according to that index
     */
    getLastChildName(indexDefinition) {
        const idx = this.resolveIndex_(indexDefinition);
        if (idx) {
            const maxKey = idx.maxKey();
            return maxKey && maxKey.name;
        }
        else {
            return this.children_.maxKey();
        }
    }
    getLastChild(indexDefinition) {
        const maxKey = this.getLastChildName(indexDefinition);
        if (maxKey) {
            return new NamedNode(maxKey, this.children_.get(maxKey));
        }
        else {
            return null;
        }
    }
    forEachChild(index, action) {
        const idx = this.resolveIndex_(index);
        if (idx) {
            return idx.inorderTraversal(wrappedNode => {
                return action(wrappedNode.name, wrappedNode.node);
            });
        }
        else {
            return this.children_.inorderTraversal(action);
        }
    }
    getIterator(indexDefinition) {
        return this.getIteratorFrom(indexDefinition.minPost(), indexDefinition);
    }
    getIteratorFrom(startPost, indexDefinition) {
        const idx = this.resolveIndex_(indexDefinition);
        if (idx) {
            return idx.getIteratorFrom(startPost, key => key);
        }
        else {
            const iterator = this.children_.getIteratorFrom(startPost.name, NamedNode.Wrap);
            let next = iterator.peek();
            while (next != null && indexDefinition.compare(next, startPost) < 0) {
                iterator.getNext();
                next = iterator.peek();
            }
            return iterator;
        }
    }
    getReverseIterator(indexDefinition) {
        return this.getReverseIteratorFrom(indexDefinition.maxPost(), indexDefinition);
    }
    getReverseIteratorFrom(endPost, indexDefinition) {
        const idx = this.resolveIndex_(indexDefinition);
        if (idx) {
            return idx.getReverseIteratorFrom(endPost, key => {
                return key;
            });
        }
        else {
            const iterator = this.children_.getReverseIteratorFrom(endPost.name, NamedNode.Wrap);
            let next = iterator.peek();
            while (next != null && indexDefinition.compare(next, endPost) > 0) {
                iterator.getNext();
                next = iterator.peek();
            }
            return iterator;
        }
    }
    compareTo(other) {
        if (this.isEmpty()) {
            if (other.isEmpty()) {
                return 0;
            }
            else {
                return -1;
            }
        }
        else if (other.isLeafNode() || other.isEmpty()) {
            return 1;
        }
        else if (other === MAX_NODE) {
            return -1;
        }
        else {
            // Must be another node with children.
            return 0;
        }
    }
    withIndex(indexDefinition) {
        if (indexDefinition === KEY_INDEX ||
            this.indexMap_.hasIndex(indexDefinition)) {
            return this;
        }
        else {
            const newIndexMap = this.indexMap_.addIndex(indexDefinition, this.children_);
            return new ChildrenNode(this.children_, this.priorityNode_, newIndexMap);
        }
    }
    isIndexed(index) {
        return index === KEY_INDEX || this.indexMap_.hasIndex(index);
    }
    equals(other) {
        if (other === this) {
            return true;
        }
        else if (other.isLeafNode()) {
            return false;
        }
        else {
            const otherChildrenNode = other;
            if (!this.getPriority().equals(otherChildrenNode.getPriority())) {
                return false;
            }
            else if (this.children_.count() === otherChildrenNode.children_.count()) {
                const thisIter = this.getIterator(PRIORITY_INDEX);
                const otherIter = otherChildrenNode.getIterator(PRIORITY_INDEX);
                let thisCurrent = thisIter.getNext();
                let otherCurrent = otherIter.getNext();
                while (thisCurrent && otherCurrent) {
                    if (thisCurrent.name !== otherCurrent.name ||
                        !thisCurrent.node.equals(otherCurrent.node)) {
                        return false;
                    }
                    thisCurrent = thisIter.getNext();
                    otherCurrent = otherIter.getNext();
                }
                return thisCurrent === null && otherCurrent === null;
            }
            else {
                return false;
            }
        }
    }
    /**
     * Returns a SortedMap ordered by index, or null if the default (by-key) ordering can be used
     * instead.
     *
     */
    resolveIndex_(indexDefinition) {
        if (indexDefinition === KEY_INDEX) {
            return null;
        }
        else {
            return this.indexMap_.get(indexDefinition.toString());
        }
    }
}
ChildrenNode.INTEGER_REGEXP_ = /^(0|[1-9]\d*)$/;
class MaxNode extends ChildrenNode {
    constructor() {
        super(new SortedMap(NAME_COMPARATOR), ChildrenNode.EMPTY_NODE, IndexMap.Default);
    }
    compareTo(other) {
        if (other === this) {
            return 0;
        }
        else {
            return 1;
        }
    }
    equals(other) {
        // Not that we every compare it, but MAX_NODE is only ever equal to itself
        return other === this;
    }
    getPriority() {
        return this;
    }
    getImmediateChild(childName) {
        return ChildrenNode.EMPTY_NODE;
    }
    isEmpty() {
        return false;
    }
}
/**
 * Marker that will sort higher than any other snapshot.
 */
const MAX_NODE = new MaxNode();
Object.defineProperties(NamedNode, {
    MIN: {
        value: new NamedNode(MIN_NAME, ChildrenNode.EMPTY_NODE)
    },
    MAX: {
        value: new NamedNode(MAX_NAME, MAX_NODE)
    }
});
/**
 * Reference Extensions
 */
KeyIndex.__EMPTY_NODE = ChildrenNode.EMPTY_NODE;
LeafNode.__childrenNodeConstructor = ChildrenNode;
setMaxNode$1(MAX_NODE);
setMaxNode(MAX_NODE);

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const USE_HINZE = true;
/**
 * Constructs a snapshot node representing the passed JSON and returns it.
 * @param json - JSON to create a node for.
 * @param priority - Optional priority to use.  This will be ignored if the
 * passed JSON contains a .priority property.
 */
function nodeFromJSON(json, priority = null) {
    if (json === null) {
        return ChildrenNode.EMPTY_NODE;
    }
    if (typeof json === 'object' && '.priority' in json) {
        priority = json['.priority'];
    }
    util.assert(priority === null ||
        typeof priority === 'string' ||
        typeof priority === 'number' ||
        (typeof priority === 'object' && '.sv' in priority), 'Invalid priority type found: ' + typeof priority);
    if (typeof json === 'object' && '.value' in json && json['.value'] !== null) {
        json = json['.value'];
    }
    // Valid leaf nodes include non-objects or server-value wrapper objects
    if (typeof json !== 'object' || '.sv' in json) {
        const jsonLeaf = json;
        return new LeafNode(jsonLeaf, nodeFromJSON(priority));
    }
    if (!(json instanceof Array) && USE_HINZE) {
        const children = [];
        let childrenHavePriority = false;
        const hinzeJsonObj = json;
        each(hinzeJsonObj, (key, child) => {
            if (key.substring(0, 1) !== '.') {
                // Ignore metadata nodes
                const childNode = nodeFromJSON(child);
                if (!childNode.isEmpty()) {
                    childrenHavePriority =
                        childrenHavePriority || !childNode.getPriority().isEmpty();
                    children.push(new NamedNode(key, childNode));
                }
            }
        });
        if (children.length === 0) {
            return ChildrenNode.EMPTY_NODE;
        }
        const childSet = buildChildSet(children, NAME_ONLY_COMPARATOR, namedNode => namedNode.name, NAME_COMPARATOR);
        if (childrenHavePriority) {
            const sortedChildSet = buildChildSet(children, PRIORITY_INDEX.getCompare());
            return new ChildrenNode(childSet, nodeFromJSON(priority), new IndexMap({ '.priority': sortedChildSet }, { '.priority': PRIORITY_INDEX }));
        }
        else {
            return new ChildrenNode(childSet, nodeFromJSON(priority), IndexMap.Default);
        }
    }
    else {
        let node = ChildrenNode.EMPTY_NODE;
        each(json, (key, childData) => {
            if (util.contains(json, key)) {
                if (key.substring(0, 1) !== '.') {
                    // ignore metadata nodes.
                    const childNode = nodeFromJSON(childData);
                    if (childNode.isLeafNode() || !childNode.isEmpty()) {
                        node = node.updateImmediateChild(key, childNode);
                    }
                }
            }
        });
        return node.updatePriority(nodeFromJSON(priority));
    }
}
setNodeFromJSON(nodeFromJSON);

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class PathIndex extends Index {
    constructor(indexPath_) {
        super();
        this.indexPath_ = indexPath_;
        util.assert(!pathIsEmpty(indexPath_) && pathGetFront(indexPath_) !== '.priority', "Can't create PathIndex with empty path or .priority key");
    }
    extractChild(snap) {
        return snap.getChild(this.indexPath_);
    }
    isDefinedOn(node) {
        return !node.getChild(this.indexPath_).isEmpty();
    }
    compare(a, b) {
        const aChild = this.extractChild(a.node);
        const bChild = this.extractChild(b.node);
        const indexCmp = aChild.compareTo(bChild);
        if (indexCmp === 0) {
            return nameCompare(a.name, b.name);
        }
        else {
            return indexCmp;
        }
    }
    makePost(indexValue, name) {
        const valueNode = nodeFromJSON(indexValue);
        const node = ChildrenNode.EMPTY_NODE.updateChild(this.indexPath_, valueNode);
        return new NamedNode(name, node);
    }
    maxPost() {
        const node = ChildrenNode.EMPTY_NODE.updateChild(this.indexPath_, MAX_NODE);
        return new NamedNode(MAX_NAME, node);
    }
    toString() {
        return pathSlice(this.indexPath_, 0).join('/');
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class ValueIndex extends Index {
    compare(a, b) {
        const indexCmp = a.node.compareTo(b.node);
        if (indexCmp === 0) {
            return nameCompare(a.name, b.name);
        }
        else {
            return indexCmp;
        }
    }
    isDefinedOn(node) {
        return true;
    }
    indexedValueChanged(oldNode, newNode) {
        return !oldNode.equals(newNode);
    }
    minPost() {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return NamedNode.MIN;
    }
    maxPost() {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        return NamedNode.MAX;
    }
    makePost(indexValue, name) {
        const valueNode = nodeFromJSON(indexValue);
        return new NamedNode(name, valueNode);
    }
    /**
     * @returns String representation for inclusion in a query spec
     */
    toString() {
        return '.value';
    }
}
const VALUE_INDEX = new ValueIndex();

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function changeValue(snapshotNode) {
    return { type: "value" /* ChangeType.VALUE */, snapshotNode };
}
function changeChildAdded(childName, snapshotNode) {
    return { type: "child_added" /* ChangeType.CHILD_ADDED */, snapshotNode, childName };
}
function changeChildRemoved(childName, snapshotNode) {
    return { type: "child_removed" /* ChangeType.CHILD_REMOVED */, snapshotNode, childName };
}
function changeChildChanged(childName, snapshotNode, oldSnap) {
    return {
        type: "child_changed" /* ChangeType.CHILD_CHANGED */,
        snapshotNode,
        childName,
        oldSnap
    };
}
function changeChildMoved(childName, snapshotNode) {
    return { type: "child_moved" /* ChangeType.CHILD_MOVED */, snapshotNode, childName };
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Doesn't really filter nodes but applies an index to the node and keeps track of any changes
 */
class IndexedFilter {
    constructor(index_) {
        this.index_ = index_;
    }
    updateChild(snap, key, newChild, affectedPath, source, optChangeAccumulator) {
        util.assert(snap.isIndexed(this.index_), 'A node must be indexed if only a child is updated');
        const oldChild = snap.getImmediateChild(key);
        // Check if anything actually changed.
        if (oldChild.getChild(affectedPath).equals(newChild.getChild(affectedPath))) {
            // There's an edge case where a child can enter or leave the view because affectedPath was set to null.
            // In this case, affectedPath will appear null in both the old and new snapshots.  So we need
            // to avoid treating these cases as "nothing changed."
            if (oldChild.isEmpty() === newChild.isEmpty()) {
                // Nothing changed.
                // This assert should be valid, but it's expensive (can dominate perf testing) so don't actually do it.
                //assert(oldChild.equals(newChild), 'Old and new snapshots should be equal.');
                return snap;
            }
        }
        if (optChangeAccumulator != null) {
            if (newChild.isEmpty()) {
                if (snap.hasChild(key)) {
                    optChangeAccumulator.trackChildChange(changeChildRemoved(key, oldChild));
                }
                else {
                    util.assert(snap.isLeafNode(), 'A child remove without an old child only makes sense on a leaf node');
                }
            }
            else if (oldChild.isEmpty()) {
                optChangeAccumulator.trackChildChange(changeChildAdded(key, newChild));
            }
            else {
                optChangeAccumulator.trackChildChange(changeChildChanged(key, newChild, oldChild));
            }
        }
        if (snap.isLeafNode() && newChild.isEmpty()) {
            return snap;
        }
        else {
            // Make sure the node is indexed
            return snap.updateImmediateChild(key, newChild).withIndex(this.index_);
        }
    }
    updateFullNode(oldSnap, newSnap, optChangeAccumulator) {
        if (optChangeAccumulator != null) {
            if (!oldSnap.isLeafNode()) {
                oldSnap.forEachChild(PRIORITY_INDEX, (key, childNode) => {
                    if (!newSnap.hasChild(key)) {
                        optChangeAccumulator.trackChildChange(changeChildRemoved(key, childNode));
                    }
                });
            }
            if (!newSnap.isLeafNode()) {
                newSnap.forEachChild(PRIORITY_INDEX, (key, childNode) => {
                    if (oldSnap.hasChild(key)) {
                        const oldChild = oldSnap.getImmediateChild(key);
                        if (!oldChild.equals(childNode)) {
                            optChangeAccumulator.trackChildChange(changeChildChanged(key, childNode, oldChild));
                        }
                    }
                    else {
                        optChangeAccumulator.trackChildChange(changeChildAdded(key, childNode));
                    }
                });
            }
        }
        return newSnap.withIndex(this.index_);
    }
    updatePriority(oldSnap, newPriority) {
        if (oldSnap.isEmpty()) {
            return ChildrenNode.EMPTY_NODE;
        }
        else {
            return oldSnap.updatePriority(newPriority);
        }
    }
    filtersNodes() {
        return false;
    }
    getIndexedFilter() {
        return this;
    }
    getIndex() {
        return this.index_;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Filters nodes by range and uses an IndexFilter to track any changes after filtering the node
 */
class RangedFilter {
    constructor(params) {
        this.indexedFilter_ = new IndexedFilter(params.getIndex());
        this.index_ = params.getIndex();
        this.startPost_ = RangedFilter.getStartPost_(params);
        this.endPost_ = RangedFilter.getEndPost_(params);
        this.startIsInclusive_ = !params.startAfterSet_;
        this.endIsInclusive_ = !params.endBeforeSet_;
    }
    getStartPost() {
        return this.startPost_;
    }
    getEndPost() {
        return this.endPost_;
    }
    matches(node) {
        const isWithinStart = this.startIsInclusive_
            ? this.index_.compare(this.getStartPost(), node) <= 0
            : this.index_.compare(this.getStartPost(), node) < 0;
        const isWithinEnd = this.endIsInclusive_
            ? this.index_.compare(node, this.getEndPost()) <= 0
            : this.index_.compare(node, this.getEndPost()) < 0;
        return isWithinStart && isWithinEnd;
    }
    updateChild(snap, key, newChild, affectedPath, source, optChangeAccumulator) {
        if (!this.matches(new NamedNode(key, newChild))) {
            newChild = ChildrenNode.EMPTY_NODE;
        }
        return this.indexedFilter_.updateChild(snap, key, newChild, affectedPath, source, optChangeAccumulator);
    }
    updateFullNode(oldSnap, newSnap, optChangeAccumulator) {
        if (newSnap.isLeafNode()) {
            // Make sure we have a children node with the correct index, not a leaf node;
            newSnap = ChildrenNode.EMPTY_NODE;
        }
        let filtered = newSnap.withIndex(this.index_);
        // Don't support priorities on queries
        filtered = filtered.updatePriority(ChildrenNode.EMPTY_NODE);
        const self = this;
        newSnap.forEachChild(PRIORITY_INDEX, (key, childNode) => {
            if (!self.matches(new NamedNode(key, childNode))) {
                filtered = filtered.updateImmediateChild(key, ChildrenNode.EMPTY_NODE);
            }
        });
        return this.indexedFilter_.updateFullNode(oldSnap, filtered, optChangeAccumulator);
    }
    updatePriority(oldSnap, newPriority) {
        // Don't support priorities on queries
        return oldSnap;
    }
    filtersNodes() {
        return true;
    }
    getIndexedFilter() {
        return this.indexedFilter_;
    }
    getIndex() {
        return this.index_;
    }
    static getStartPost_(params) {
        if (params.hasStart()) {
            const startName = params.getIndexStartName();
            return params.getIndex().makePost(params.getIndexStartValue(), startName);
        }
        else {
            return params.getIndex().minPost();
        }
    }
    static getEndPost_(params) {
        if (params.hasEnd()) {
            const endName = params.getIndexEndName();
            return params.getIndex().makePost(params.getIndexEndValue(), endName);
        }
        else {
            return params.getIndex().maxPost();
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Applies a limit and a range to a node and uses RangedFilter to do the heavy lifting where possible
 */
class LimitedFilter {
    constructor(params) {
        this.withinDirectionalStart = (node) => this.reverse_ ? this.withinEndPost(node) : this.withinStartPost(node);
        this.withinDirectionalEnd = (node) => this.reverse_ ? this.withinStartPost(node) : this.withinEndPost(node);
        this.withinStartPost = (node) => {
            const compareRes = this.index_.compare(this.rangedFilter_.getStartPost(), node);
            return this.startIsInclusive_ ? compareRes <= 0 : compareRes < 0;
        };
        this.withinEndPost = (node) => {
            const compareRes = this.index_.compare(node, this.rangedFilter_.getEndPost());
            return this.endIsInclusive_ ? compareRes <= 0 : compareRes < 0;
        };
        this.rangedFilter_ = new RangedFilter(params);
        this.index_ = params.getIndex();
        this.limit_ = params.getLimit();
        this.reverse_ = !params.isViewFromLeft();
        this.startIsInclusive_ = !params.startAfterSet_;
        this.endIsInclusive_ = !params.endBeforeSet_;
    }
    updateChild(snap, key, newChild, affectedPath, source, optChangeAccumulator) {
        if (!this.rangedFilter_.matches(new NamedNode(key, newChild))) {
            newChild = ChildrenNode.EMPTY_NODE;
        }
        if (snap.getImmediateChild(key).equals(newChild)) {
            // No change
            return snap;
        }
        else if (snap.numChildren() < this.limit_) {
            return this.rangedFilter_
                .getIndexedFilter()
                .updateChild(snap, key, newChild, affectedPath, source, optChangeAccumulator);
        }
        else {
            return this.fullLimitUpdateChild_(snap, key, newChild, source, optChangeAccumulator);
        }
    }
    updateFullNode(oldSnap, newSnap, optChangeAccumulator) {
        let filtered;
        if (newSnap.isLeafNode() || newSnap.isEmpty()) {
            // Make sure we have a children node with the correct index, not a leaf node;
            filtered = ChildrenNode.EMPTY_NODE.withIndex(this.index_);
        }
        else {
            if (this.limit_ * 2 < newSnap.numChildren() &&
                newSnap.isIndexed(this.index_)) {
                // Easier to build up a snapshot, since what we're given has more than twice the elements we want
                filtered = ChildrenNode.EMPTY_NODE.withIndex(this.index_);
                // anchor to the startPost, endPost, or last element as appropriate
                let iterator;
                if (this.reverse_) {
                    iterator = newSnap.getReverseIteratorFrom(this.rangedFilter_.getEndPost(), this.index_);
                }
                else {
                    iterator = newSnap.getIteratorFrom(this.rangedFilter_.getStartPost(), this.index_);
                }
                let count = 0;
                while (iterator.hasNext() && count < this.limit_) {
                    const next = iterator.getNext();
                    if (!this.withinDirectionalStart(next)) {
                        // if we have not reached the start, skip to the next element
                        continue;
                    }
                    else if (!this.withinDirectionalEnd(next)) {
                        // if we have reached the end, stop adding elements
                        break;
                    }
                    else {
                        filtered = filtered.updateImmediateChild(next.name, next.node);
                        count++;
                    }
                }
            }
            else {
                // The snap contains less than twice the limit. Faster to delete from the snap than build up a new one
                filtered = newSnap.withIndex(this.index_);
                // Don't support priorities on queries
                filtered = filtered.updatePriority(ChildrenNode.EMPTY_NODE);
                let iterator;
                if (this.reverse_) {
                    iterator = filtered.getReverseIterator(this.index_);
                }
                else {
                    iterator = filtered.getIterator(this.index_);
                }
                let count = 0;
                while (iterator.hasNext()) {
                    const next = iterator.getNext();
                    const inRange = count < this.limit_ &&
                        this.withinDirectionalStart(next) &&
                        this.withinDirectionalEnd(next);
                    if (inRange) {
                        count++;
                    }
                    else {
                        filtered = filtered.updateImmediateChild(next.name, ChildrenNode.EMPTY_NODE);
                    }
                }
            }
        }
        return this.rangedFilter_
            .getIndexedFilter()
            .updateFullNode(oldSnap, filtered, optChangeAccumulator);
    }
    updatePriority(oldSnap, newPriority) {
        // Don't support priorities on queries
        return oldSnap;
    }
    filtersNodes() {
        return true;
    }
    getIndexedFilter() {
        return this.rangedFilter_.getIndexedFilter();
    }
    getIndex() {
        return this.index_;
    }
    fullLimitUpdateChild_(snap, childKey, childSnap, source, changeAccumulator) {
        // TODO: rename all cache stuff etc to general snap terminology
        let cmp;
        if (this.reverse_) {
            const indexCmp = this.index_.getCompare();
            cmp = (a, b) => indexCmp(b, a);
        }
        else {
            cmp = this.index_.getCompare();
        }
        const oldEventCache = snap;
        util.assert(oldEventCache.numChildren() === this.limit_, '');
        const newChildNamedNode = new NamedNode(childKey, childSnap);
        const windowBoundary = this.reverse_
            ? oldEventCache.getFirstChild(this.index_)
            : oldEventCache.getLastChild(this.index_);
        const inRange = this.rangedFilter_.matches(newChildNamedNode);
        if (oldEventCache.hasChild(childKey)) {
            const oldChildSnap = oldEventCache.getImmediateChild(childKey);
            let nextChild = source.getChildAfterChild(this.index_, windowBoundary, this.reverse_);
            while (nextChild != null &&
                (nextChild.name === childKey || oldEventCache.hasChild(nextChild.name))) {
                // There is a weird edge case where a node is updated as part of a merge in the write tree, but hasn't
                // been applied to the limited filter yet. Ignore this next child which will be updated later in
                // the limited filter...
                nextChild = source.getChildAfterChild(this.index_, nextChild, this.reverse_);
            }
            const compareNext = nextChild == null ? 1 : cmp(nextChild, newChildNamedNode);
            const remainsInWindow = inRange && !childSnap.isEmpty() && compareNext >= 0;
            if (remainsInWindow) {
                if (changeAccumulator != null) {
                    changeAccumulator.trackChildChange(changeChildChanged(childKey, childSnap, oldChildSnap));
                }
                return oldEventCache.updateImmediateChild(childKey, childSnap);
            }
            else {
                if (changeAccumulator != null) {
                    changeAccumulator.trackChildChange(changeChildRemoved(childKey, oldChildSnap));
                }
                const newEventCache = oldEventCache.updateImmediateChild(childKey, ChildrenNode.EMPTY_NODE);
                const nextChildInRange = nextChild != null && this.rangedFilter_.matches(nextChild);
                if (nextChildInRange) {
                    if (changeAccumulator != null) {
                        changeAccumulator.trackChildChange(changeChildAdded(nextChild.name, nextChild.node));
                    }
                    return newEventCache.updateImmediateChild(nextChild.name, nextChild.node);
                }
                else {
                    return newEventCache;
                }
            }
        }
        else if (childSnap.isEmpty()) {
            // we're deleting a node, but it was not in the window, so ignore it
            return snap;
        }
        else if (inRange) {
            if (cmp(windowBoundary, newChildNamedNode) >= 0) {
                if (changeAccumulator != null) {
                    changeAccumulator.trackChildChange(changeChildRemoved(windowBoundary.name, windowBoundary.node));
                    changeAccumulator.trackChildChange(changeChildAdded(childKey, childSnap));
                }
                return oldEventCache
                    .updateImmediateChild(childKey, childSnap)
                    .updateImmediateChild(windowBoundary.name, ChildrenNode.EMPTY_NODE);
            }
            else {
                return snap;
            }
        }
        else {
            return snap;
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * This class is an immutable-from-the-public-api struct containing a set of query parameters defining a
 * range to be returned for a particular location. It is assumed that validation of parameters is done at the
 * user-facing API level, so it is not done here.
 *
 * @internal
 */
class QueryParams {
    constructor() {
        this.limitSet_ = false;
        this.startSet_ = false;
        this.startNameSet_ = false;
        this.startAfterSet_ = false; // can only be true if startSet_ is true
        this.endSet_ = false;
        this.endNameSet_ = false;
        this.endBeforeSet_ = false; // can only be true if endSet_ is true
        this.limit_ = 0;
        this.viewFrom_ = '';
        this.indexStartValue_ = null;
        this.indexStartName_ = '';
        this.indexEndValue_ = null;
        this.indexEndName_ = '';
        this.index_ = PRIORITY_INDEX;
    }
    hasStart() {
        return this.startSet_;
    }
    /**
     * @returns True if it would return from left.
     */
    isViewFromLeft() {
        if (this.viewFrom_ === '') {
            // limit(), rather than limitToFirst or limitToLast was called.
            // This means that only one of startSet_ and endSet_ is true. Use them
            // to calculate which side of the view to anchor to. If neither is set,
            // anchor to the end.
            return this.startSet_;
        }
        else {
            return this.viewFrom_ === "l" /* WIRE_PROTOCOL_CONSTANTS.VIEW_FROM_LEFT */;
        }
    }
    /**
     * Only valid to call if hasStart() returns true
     */
    getIndexStartValue() {
        util.assert(this.startSet_, 'Only valid if start has been set');
        return this.indexStartValue_;
    }
    /**
     * Only valid to call if hasStart() returns true.
     * Returns the starting key name for the range defined by these query parameters
     */
    getIndexStartName() {
        util.assert(this.startSet_, 'Only valid if start has been set');
        if (this.startNameSet_) {
            return this.indexStartName_;
        }
        else {
            return MIN_NAME;
        }
    }
    hasEnd() {
        return this.endSet_;
    }
    /**
     * Only valid to call if hasEnd() returns true.
     */
    getIndexEndValue() {
        util.assert(this.endSet_, 'Only valid if end has been set');
        return this.indexEndValue_;
    }
    /**
     * Only valid to call if hasEnd() returns true.
     * Returns the end key name for the range defined by these query parameters
     */
    getIndexEndName() {
        util.assert(this.endSet_, 'Only valid if end has been set');
        if (this.endNameSet_) {
            return this.indexEndName_;
        }
        else {
            return MAX_NAME;
        }
    }
    hasLimit() {
        return this.limitSet_;
    }
    /**
     * @returns True if a limit has been set and it has been explicitly anchored
     */
    hasAnchoredLimit() {
        return this.limitSet_ && this.viewFrom_ !== '';
    }
    /**
     * Only valid to call if hasLimit() returns true
     */
    getLimit() {
        util.assert(this.limitSet_, 'Only valid if limit has been set');
        return this.limit_;
    }
    getIndex() {
        return this.index_;
    }
    loadsAllData() {
        return !(this.startSet_ || this.endSet_ || this.limitSet_);
    }
    isDefault() {
        return this.loadsAllData() && this.index_ === PRIORITY_INDEX;
    }
    copy() {
        const copy = new QueryParams();
        copy.limitSet_ = this.limitSet_;
        copy.limit_ = this.limit_;
        copy.startSet_ = this.startSet_;
        copy.startAfterSet_ = this.startAfterSet_;
        copy.indexStartValue_ = this.indexStartValue_;
        copy.startNameSet_ = this.startNameSet_;
        copy.indexStartName_ = this.indexStartName_;
        copy.endSet_ = this.endSet_;
        copy.endBeforeSet_ = this.endBeforeSet_;
        copy.indexEndValue_ = this.indexEndValue_;
        copy.endNameSet_ = this.endNameSet_;
        copy.indexEndName_ = this.indexEndName_;
        copy.index_ = this.index_;
        copy.viewFrom_ = this.viewFrom_;
        return copy;
    }
}
function queryParamsGetNodeFilter(queryParams) {
    if (queryParams.loadsAllData()) {
        return new IndexedFilter(queryParams.getIndex());
    }
    else if (queryParams.hasLimit()) {
        return new LimitedFilter(queryParams);
    }
    else {
        return new RangedFilter(queryParams);
    }
}
function queryParamsLimitToFirst(queryParams, newLimit) {
    const newParams = queryParams.copy();
    newParams.limitSet_ = true;
    newParams.limit_ = newLimit;
    newParams.viewFrom_ = "l" /* WIRE_PROTOCOL_CONSTANTS.VIEW_FROM_LEFT */;
    return newParams;
}
function queryParamsLimitToLast(queryParams, newLimit) {
    const newParams = queryParams.copy();
    newParams.limitSet_ = true;
    newParams.limit_ = newLimit;
    newParams.viewFrom_ = "r" /* WIRE_PROTOCOL_CONSTANTS.VIEW_FROM_RIGHT */;
    return newParams;
}
function queryParamsStartAt(queryParams, indexValue, key) {
    const newParams = queryParams.copy();
    newParams.startSet_ = true;
    if (indexValue === undefined) {
        indexValue = null;
    }
    newParams.indexStartValue_ = indexValue;
    if (key != null) {
        newParams.startNameSet_ = true;
        newParams.indexStartName_ = key;
    }
    else {
        newParams.startNameSet_ = false;
        newParams.indexStartName_ = '';
    }
    return newParams;
}
function queryParamsStartAfter(queryParams, indexValue, key) {
    let params;
    if (queryParams.index_ === KEY_INDEX || !!key) {
        params = queryParamsStartAt(queryParams, indexValue, key);
    }
    else {
        params = queryParamsStartAt(queryParams, indexValue, MAX_NAME);
    }
    params.startAfterSet_ = true;
    return params;
}
function queryParamsEndAt(queryParams, indexValue, key) {
    const newParams = queryParams.copy();
    newParams.endSet_ = true;
    if (indexValue === undefined) {
        indexValue = null;
    }
    newParams.indexEndValue_ = indexValue;
    if (key !== undefined) {
        newParams.endNameSet_ = true;
        newParams.indexEndName_ = key;
    }
    else {
        newParams.endNameSet_ = false;
        newParams.indexEndName_ = '';
    }
    return newParams;
}
function queryParamsEndBefore(queryParams, indexValue, key) {
    let params;
    if (queryParams.index_ === KEY_INDEX || !!key) {
        params = queryParamsEndAt(queryParams, indexValue, key);
    }
    else {
        params = queryParamsEndAt(queryParams, indexValue, MIN_NAME);
    }
    params.endBeforeSet_ = true;
    return params;
}
function queryParamsOrderBy(queryParams, index) {
    const newParams = queryParams.copy();
    newParams.index_ = index;
    return newParams;
}
/**
 * Returns a set of REST query string parameters representing this query.
 *
 * @returns query string parameters
 */
function queryParamsToRestQueryStringParameters(queryParams) {
    const qs = {};
    if (queryParams.isDefault()) {
        return qs;
    }
    let orderBy;
    if (queryParams.index_ === PRIORITY_INDEX) {
        orderBy = "$priority" /* REST_QUERY_CONSTANTS.PRIORITY_INDEX */;
    }
    else if (queryParams.index_ === VALUE_INDEX) {
        orderBy = "$value" /* REST_QUERY_CONSTANTS.VALUE_INDEX */;
    }
    else if (queryParams.index_ === KEY_INDEX) {
        orderBy = "$key" /* REST_QUERY_CONSTANTS.KEY_INDEX */;
    }
    else {
        util.assert(queryParams.index_ instanceof PathIndex, 'Unrecognized index type!');
        orderBy = queryParams.index_.toString();
    }
    qs["orderBy" /* REST_QUERY_CONSTANTS.ORDER_BY */] = util.stringify(orderBy);
    if (queryParams.startSet_) {
        const startParam = queryParams.startAfterSet_
            ? "startAfter" /* REST_QUERY_CONSTANTS.START_AFTER */
            : "startAt" /* REST_QUERY_CONSTANTS.START_AT */;
        qs[startParam] = util.stringify(queryParams.indexStartValue_);
        if (queryParams.startNameSet_) {
            qs[startParam] += ',' + util.stringify(queryParams.indexStartName_);
        }
    }
    if (queryParams.endSet_) {
        const endParam = queryParams.endBeforeSet_
            ? "endBefore" /* REST_QUERY_CONSTANTS.END_BEFORE */
            : "endAt" /* REST_QUERY_CONSTANTS.END_AT */;
        qs[endParam] = util.stringify(queryParams.indexEndValue_);
        if (queryParams.endNameSet_) {
            qs[endParam] += ',' + util.stringify(queryParams.indexEndName_);
        }
    }
    if (queryParams.limitSet_) {
        if (queryParams.isViewFromLeft()) {
            qs["limitToFirst" /* REST_QUERY_CONSTANTS.LIMIT_TO_FIRST */] = queryParams.limit_;
        }
        else {
            qs["limitToLast" /* REST_QUERY_CONSTANTS.LIMIT_TO_LAST */] = queryParams.limit_;
        }
    }
    return qs;
}
function queryParamsGetQueryObject(queryParams) {
    const obj = {};
    if (queryParams.startSet_) {
        obj["sp" /* WIRE_PROTOCOL_CONSTANTS.INDEX_START_VALUE */] =
            queryParams.indexStartValue_;
        if (queryParams.startNameSet_) {
            obj["sn" /* WIRE_PROTOCOL_CONSTANTS.INDEX_START_NAME */] =
                queryParams.indexStartName_;
        }
        obj["sin" /* WIRE_PROTOCOL_CONSTANTS.INDEX_START_IS_INCLUSIVE */] =
            !queryParams.startAfterSet_;
    }
    if (queryParams.endSet_) {
        obj["ep" /* WIRE_PROTOCOL_CONSTANTS.INDEX_END_VALUE */] = queryParams.indexEndValue_;
        if (queryParams.endNameSet_) {
            obj["en" /* WIRE_PROTOCOL_CONSTANTS.INDEX_END_NAME */] = queryParams.indexEndName_;
        }
        obj["ein" /* WIRE_PROTOCOL_CONSTANTS.INDEX_END_IS_INCLUSIVE */] =
            !queryParams.endBeforeSet_;
    }
    if (queryParams.limitSet_) {
        obj["l" /* WIRE_PROTOCOL_CONSTANTS.LIMIT */] = queryParams.limit_;
        let viewFrom = queryParams.viewFrom_;
        if (viewFrom === '') {
            if (queryParams.isViewFromLeft()) {
                viewFrom = "l" /* WIRE_PROTOCOL_CONSTANTS.VIEW_FROM_LEFT */;
            }
            else {
                viewFrom = "r" /* WIRE_PROTOCOL_CONSTANTS.VIEW_FROM_RIGHT */;
            }
        }
        obj["vf" /* WIRE_PROTOCOL_CONSTANTS.VIEW_FROM */] = viewFrom;
    }
    // For now, priority index is the default, so we only specify if it's some other index
    if (queryParams.index_ !== PRIORITY_INDEX) {
        obj["i" /* WIRE_PROTOCOL_CONSTANTS.INDEX */] = queryParams.index_.toString();
    }
    return obj;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An implementation of ServerActions that communicates with the server via REST requests.
 * This is mostly useful for compatibility with crawlers, where we don't want to spin up a full
 * persistent connection (using WebSockets or long-polling)
 */
class ReadonlyRestClient extends ServerActions {
    /**
     * @param repoInfo_ - Data about the namespace we are connecting to
     * @param onDataUpdate_ - A callback for new data from the server
     */
    constructor(repoInfo_, onDataUpdate_, authTokenProvider_, appCheckTokenProvider_) {
        super();
        this.repoInfo_ = repoInfo_;
        this.onDataUpdate_ = onDataUpdate_;
        this.authTokenProvider_ = authTokenProvider_;
        this.appCheckTokenProvider_ = appCheckTokenProvider_;
        /** @private {function(...[*])} */
        this.log_ = logWrapper('p:rest:');
        /**
         * We don't actually need to track listens, except to prevent us calling an onComplete for a listen
         * that's been removed. :-/
         */
        this.listens_ = {};
    }
    reportStats(stats) {
        throw new Error('Method not implemented.');
    }
    static getListenId_(query, tag) {
        if (tag !== undefined) {
            return 'tag$' + tag;
        }
        else {
            util.assert(query._queryParams.isDefault(), "should have a tag if it's not a default query.");
            return query._path.toString();
        }
    }
    /** @inheritDoc */
    listen(query, currentHashFn, tag, onComplete) {
        const pathString = query._path.toString();
        this.log_('Listen called for ' + pathString + ' ' + query._queryIdentifier);
        // Mark this listener so we can tell if it's removed.
        const listenId = ReadonlyRestClient.getListenId_(query, tag);
        const thisListen = {};
        this.listens_[listenId] = thisListen;
        const queryStringParameters = queryParamsToRestQueryStringParameters(query._queryParams);
        this.restRequest_(pathString + '.json', queryStringParameters, (error, result) => {
            let data = result;
            if (error === 404) {
                data = null;
                error = null;
            }
            if (error === null) {
                this.onDataUpdate_(pathString, data, /*isMerge=*/ false, tag);
            }
            if (util.safeGet(this.listens_, listenId) === thisListen) {
                let status;
                if (!error) {
                    status = 'ok';
                }
                else if (error === 401) {
                    status = 'permission_denied';
                }
                else {
                    status = 'rest_error:' + error;
                }
                onComplete(status, null);
            }
        });
    }
    /** @inheritDoc */
    unlisten(query, tag) {
        const listenId = ReadonlyRestClient.getListenId_(query, tag);
        delete this.listens_[listenId];
    }
    get(query) {
        const queryStringParameters = queryParamsToRestQueryStringParameters(query._queryParams);
        const pathString = query._path.toString();
        const deferred = new util.Deferred();
        this.restRequest_(pathString + '.json', queryStringParameters, (error, result) => {
            let data = result;
            if (error === 404) {
                data = null;
                error = null;
            }
            if (error === null) {
                this.onDataUpdate_(pathString, data, 
                /*isMerge=*/ false, 
                /*tag=*/ null);
                deferred.resolve(data);
            }
            else {
                deferred.reject(new Error(data));
            }
        });
        return deferred.promise;
    }
    /** @inheritDoc */
    refreshAuthToken(token) {
        // no-op since we just always call getToken.
    }
    /**
     * Performs a REST request to the given path, with the provided query string parameters,
     * and any auth credentials we have.
     */
    restRequest_(pathString, queryStringParameters = {}, callback) {
        queryStringParameters['format'] = 'export';
        return Promise.all([
            this.authTokenProvider_.getToken(/*forceRefresh=*/ false),
            this.appCheckTokenProvider_.getToken(/*forceRefresh=*/ false)
        ]).then(([authToken, appCheckToken]) => {
            if (authToken && authToken.accessToken) {
                queryStringParameters['auth'] = authToken.accessToken;
            }
            if (appCheckToken && appCheckToken.token) {
                queryStringParameters['ac'] = appCheckToken.token;
            }
            const url = (this.repoInfo_.secure ? 'https://' : 'http://') +
                this.repoInfo_.host +
                pathString +
                '?' +
                'ns=' +
                this.repoInfo_.namespace +
                util.querystring(queryStringParameters);
            this.log_('Sending REST request for ' + url);
            const xhr = new XMLHttpRequest();
            xhr.onreadystatechange = () => {
                if (callback && xhr.readyState === 4) {
                    this.log_('REST Response for ' + url + ' received. status:', xhr.status, 'response:', xhr.responseText);
                    let res = null;
                    if (xhr.status >= 200 && xhr.status < 300) {
                        try {
                            res = util.jsonEval(xhr.responseText);
                        }
                        catch (e) {
                            warn('Failed to parse JSON response for ' +
                                url +
                                ': ' +
                                xhr.responseText);
                        }
                        callback(null, res);
                    }
                    else {
                        // 401 and 404 are expected.
                        if (xhr.status !== 401 && xhr.status !== 404) {
                            warn('Got unsuccessful REST response for ' +
                                url +
                                ' Status: ' +
                                xhr.status);
                        }
                        callback(xhr.status);
                    }
                    callback = null;
                }
            };
            xhr.open('GET', url, /*asynchronous=*/ true);
            xhr.send();
        });
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Mutable object which basically just stores a reference to the "latest" immutable snapshot.
 */
class SnapshotHolder {
    constructor() {
        this.rootNode_ = ChildrenNode.EMPTY_NODE;
    }
    getNode(path) {
        return this.rootNode_.getChild(path);
    }
    updateSnapshot(path, newSnapshotNode) {
        this.rootNode_ = this.rootNode_.updateChild(path, newSnapshotNode);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function newSparseSnapshotTree() {
    return {
        value: null,
        children: new Map()
    };
}
/**
 * Stores the given node at the specified path. If there is already a node
 * at a shallower path, it merges the new data into that snapshot node.
 *
 * @param path - Path to look up snapshot for.
 * @param data - The new data, or null.
 */
function sparseSnapshotTreeRemember(sparseSnapshotTree, path, data) {
    if (pathIsEmpty(path)) {
        sparseSnapshotTree.value = data;
        sparseSnapshotTree.children.clear();
    }
    else if (sparseSnapshotTree.value !== null) {
        sparseSnapshotTree.value = sparseSnapshotTree.value.updateChild(path, data);
    }
    else {
        const childKey = pathGetFront(path);
        if (!sparseSnapshotTree.children.has(childKey)) {
            sparseSnapshotTree.children.set(childKey, newSparseSnapshotTree());
        }
        const child = sparseSnapshotTree.children.get(childKey);
        path = pathPopFront(path);
        sparseSnapshotTreeRemember(child, path, data);
    }
}
/**
 * Purge the data at path from the cache.
 *
 * @param path - Path to look up snapshot for.
 * @returns True if this node should now be removed.
 */
function sparseSnapshotTreeForget(sparseSnapshotTree, path) {
    if (pathIsEmpty(path)) {
        sparseSnapshotTree.value = null;
        sparseSnapshotTree.children.clear();
        return true;
    }
    else {
        if (sparseSnapshotTree.value !== null) {
            if (sparseSnapshotTree.value.isLeafNode()) {
                // We're trying to forget a node that doesn't exist
                return false;
            }
            else {
                const value = sparseSnapshotTree.value;
                sparseSnapshotTree.value = null;
                value.forEachChild(PRIORITY_INDEX, (key, tree) => {
                    sparseSnapshotTreeRemember(sparseSnapshotTree, new Path(key), tree);
                });
                return sparseSnapshotTreeForget(sparseSnapshotTree, path);
            }
        }
        else if (sparseSnapshotTree.children.size > 0) {
            const childKey = pathGetFront(path);
            path = pathPopFront(path);
            if (sparseSnapshotTree.children.has(childKey)) {
                const safeToRemove = sparseSnapshotTreeForget(sparseSnapshotTree.children.get(childKey), path);
                if (safeToRemove) {
                    sparseSnapshotTree.children.delete(childKey);
                }
            }
            return sparseSnapshotTree.children.size === 0;
        }
        else {
            return true;
        }
    }
}
/**
 * Recursively iterates through all of the stored tree and calls the
 * callback on each one.
 *
 * @param prefixPath - Path to look up node for.
 * @param func - The function to invoke for each tree.
 */
function sparseSnapshotTreeForEachTree(sparseSnapshotTree, prefixPath, func) {
    if (sparseSnapshotTree.value !== null) {
        func(prefixPath, sparseSnapshotTree.value);
    }
    else {
        sparseSnapshotTreeForEachChild(sparseSnapshotTree, (key, tree) => {
            const path = new Path(prefixPath.toString() + '/' + key);
            sparseSnapshotTreeForEachTree(tree, path, func);
        });
    }
}
/**
 * Iterates through each immediate child and triggers the callback.
 * Only seems to be used in tests.
 *
 * @param func - The function to invoke for each child.
 */
function sparseSnapshotTreeForEachChild(sparseSnapshotTree, func) {
    sparseSnapshotTree.children.forEach((tree, key) => {
        func(key, tree);
    });
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Returns the delta from the previous call to get stats.
 *
 * @param collection_ - The collection to "listen" to.
 */
class StatsListener {
    constructor(collection_) {
        this.collection_ = collection_;
        this.last_ = null;
    }
    get() {
        const newStats = this.collection_.get();
        const delta = Object.assign({}, newStats);
        if (this.last_) {
            each(this.last_, (stat, value) => {
                delta[stat] = delta[stat] - value;
            });
        }
        this.last_ = newStats;
        return delta;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Assuming some apps may have a short amount of time on page, and a bulk of firebase operations probably
// happen on page load, we try to report our first set of stats pretty quickly, but we wait at least 10
// seconds to try to ensure the Firebase connection is established / settled.
const FIRST_STATS_MIN_TIME = 10 * 1000;
const FIRST_STATS_MAX_TIME = 30 * 1000;
// We'll continue to report stats on average every 5 minutes.
const REPORT_STATS_INTERVAL = 5 * 60 * 1000;
class StatsReporter {
    constructor(collection, server_) {
        this.server_ = server_;
        this.statsToReport_ = {};
        this.statsListener_ = new StatsListener(collection);
        const timeout = FIRST_STATS_MIN_TIME +
            (FIRST_STATS_MAX_TIME - FIRST_STATS_MIN_TIME) * Math.random();
        setTimeoutNonBlocking(this.reportStats_.bind(this), Math.floor(timeout));
    }
    reportStats_() {
        const stats = this.statsListener_.get();
        const reportedStats = {};
        let haveStatsToReport = false;
        each(stats, (stat, value) => {
            if (value > 0 && util.contains(this.statsToReport_, stat)) {
                reportedStats[stat] = value;
                haveStatsToReport = true;
            }
        });
        if (haveStatsToReport) {
            this.server_.reportStats(reportedStats);
        }
        // queue our next run.
        setTimeoutNonBlocking(this.reportStats_.bind(this), Math.floor(Math.random() * 2 * REPORT_STATS_INTERVAL));
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 *
 * @enum
 */
var OperationType;
(function (OperationType) {
    OperationType[OperationType["OVERWRITE"] = 0] = "OVERWRITE";
    OperationType[OperationType["MERGE"] = 1] = "MERGE";
    OperationType[OperationType["ACK_USER_WRITE"] = 2] = "ACK_USER_WRITE";
    OperationType[OperationType["LISTEN_COMPLETE"] = 3] = "LISTEN_COMPLETE";
})(OperationType || (OperationType = {}));
function newOperationSourceUser() {
    return {
        fromUser: true,
        fromServer: false,
        queryId: null,
        tagged: false
    };
}
function newOperationSourceServer() {
    return {
        fromUser: false,
        fromServer: true,
        queryId: null,
        tagged: false
    };
}
function newOperationSourceServerTaggedQuery(queryId) {
    return {
        fromUser: false,
        fromServer: true,
        queryId,
        tagged: true
    };
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class AckUserWrite {
    /**
     * @param affectedTree - A tree containing true for each affected path. Affected paths can't overlap.
     */
    constructor(
    /** @inheritDoc */ path, 
    /** @inheritDoc */ affectedTree, 
    /** @inheritDoc */ revert) {
        this.path = path;
        this.affectedTree = affectedTree;
        this.revert = revert;
        /** @inheritDoc */
        this.type = OperationType.ACK_USER_WRITE;
        /** @inheritDoc */
        this.source = newOperationSourceUser();
    }
    operationForChild(childName) {
        if (!pathIsEmpty(this.path)) {
            util.assert(pathGetFront(this.path) === childName, 'operationForChild called for unrelated child.');
            return new AckUserWrite(pathPopFront(this.path), this.affectedTree, this.revert);
        }
        else if (this.affectedTree.value != null) {
            util.assert(this.affectedTree.children.isEmpty(), 'affectedTree should not have overlapping affected paths.');
            // All child locations are affected as well; just return same operation.
            return this;
        }
        else {
            const childTree = this.affectedTree.subtree(new Path(childName));
            return new AckUserWrite(newEmptyPath(), childTree, this.revert);
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class ListenComplete {
    constructor(source, path) {
        this.source = source;
        this.path = path;
        /** @inheritDoc */
        this.type = OperationType.LISTEN_COMPLETE;
    }
    operationForChild(childName) {
        if (pathIsEmpty(this.path)) {
            return new ListenComplete(this.source, newEmptyPath());
        }
        else {
            return new ListenComplete(this.source, pathPopFront(this.path));
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class Overwrite {
    constructor(source, path, snap) {
        this.source = source;
        this.path = path;
        this.snap = snap;
        /** @inheritDoc */
        this.type = OperationType.OVERWRITE;
    }
    operationForChild(childName) {
        if (pathIsEmpty(this.path)) {
            return new Overwrite(this.source, newEmptyPath(), this.snap.getImmediateChild(childName));
        }
        else {
            return new Overwrite(this.source, pathPopFront(this.path), this.snap);
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class Merge {
    constructor(
    /** @inheritDoc */ source, 
    /** @inheritDoc */ path, 
    /** @inheritDoc */ children) {
        this.source = source;
        this.path = path;
        this.children = children;
        /** @inheritDoc */
        this.type = OperationType.MERGE;
    }
    operationForChild(childName) {
        if (pathIsEmpty(this.path)) {
            const childTree = this.children.subtree(new Path(childName));
            if (childTree.isEmpty()) {
                // This child is unaffected
                return null;
            }
            else if (childTree.value) {
                // We have a snapshot for the child in question.  This becomes an overwrite of the child.
                return new Overwrite(this.source, newEmptyPath(), childTree.value);
            }
            else {
                // This is a merge at a deeper level
                return new Merge(this.source, newEmptyPath(), childTree);
            }
        }
        else {
            util.assert(pathGetFront(this.path) === childName, "Can't get a merge for a child not on the path of the operation");
            return new Merge(this.source, pathPopFront(this.path), this.children);
        }
    }
    toString() {
        return ('Operation(' +
            this.path +
            ': ' +
            this.source.toString() +
            ' merge: ' +
            this.children.toString() +
            ')');
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A cache node only stores complete children. Additionally it holds a flag whether the node can be considered fully
 * initialized in the sense that we know at one point in time this represented a valid state of the world, e.g.
 * initialized with data from the server, or a complete overwrite by the client. The filtered flag also tracks
 * whether a node potentially had children removed due to a filter.
 */
class CacheNode {
    constructor(node_, fullyInitialized_, filtered_) {
        this.node_ = node_;
        this.fullyInitialized_ = fullyInitialized_;
        this.filtered_ = filtered_;
    }
    /**
     * Returns whether this node was fully initialized with either server data or a complete overwrite by the client
     */
    isFullyInitialized() {
        return this.fullyInitialized_;
    }
    /**
     * Returns whether this node is potentially missing children due to a filter applied to the node
     */
    isFiltered() {
        return this.filtered_;
    }
    isCompleteForPath(path) {
        if (pathIsEmpty(path)) {
            return this.isFullyInitialized() && !this.filtered_;
        }
        const childKey = pathGetFront(path);
        return this.isCompleteForChild(childKey);
    }
    isCompleteForChild(key) {
        return ((this.isFullyInitialized() && !this.filtered_) || this.node_.hasChild(key));
    }
    getNode() {
        return this.node_;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An EventGenerator is used to convert "raw" changes (Change) as computed by the
 * CacheDiffer into actual events (Event) that can be raised.  See generateEventsForChanges()
 * for details.
 *
 */
class EventGenerator {
    constructor(query_) {
        this.query_ = query_;
        this.index_ = this.query_._queryParams.getIndex();
    }
}
/**
 * Given a set of raw changes (no moved events and prevName not specified yet), and a set of
 * EventRegistrations that should be notified of these changes, generate the actual events to be raised.
 *
 * Notes:
 *  - child_moved events will be synthesized at this time for any child_changed events that affect
 *    our index.
 *  - prevName will be calculated based on the index ordering.
 */
function eventGeneratorGenerateEventsForChanges(eventGenerator, changes, eventCache, eventRegistrations) {
    const events = [];
    const moves = [];
    changes.forEach(change => {
        if (change.type === "child_changed" /* ChangeType.CHILD_CHANGED */ &&
            eventGenerator.index_.indexedValueChanged(change.oldSnap, change.snapshotNode)) {
            moves.push(changeChildMoved(change.childName, change.snapshotNode));
        }
    });
    eventGeneratorGenerateEventsForType(eventGenerator, events, "child_removed" /* ChangeType.CHILD_REMOVED */, changes, eventRegistrations, eventCache);
    eventGeneratorGenerateEventsForType(eventGenerator, events, "child_added" /* ChangeType.CHILD_ADDED */, changes, eventRegistrations, eventCache);
    eventGeneratorGenerateEventsForType(eventGenerator, events, "child_moved" /* ChangeType.CHILD_MOVED */, moves, eventRegistrations, eventCache);
    eventGeneratorGenerateEventsForType(eventGenerator, events, "child_changed" /* ChangeType.CHILD_CHANGED */, changes, eventRegistrations, eventCache);
    eventGeneratorGenerateEventsForType(eventGenerator, events, "value" /* ChangeType.VALUE */, changes, eventRegistrations, eventCache);
    return events;
}
/**
 * Given changes of a single change type, generate the corresponding events.
 */
function eventGeneratorGenerateEventsForType(eventGenerator, events, eventType, changes, registrations, eventCache) {
    const filteredChanges = changes.filter(change => change.type === eventType);
    filteredChanges.sort((a, b) => eventGeneratorCompareChanges(eventGenerator, a, b));
    filteredChanges.forEach(change => {
        const materializedChange = eventGeneratorMaterializeSingleChange(eventGenerator, change, eventCache);
        registrations.forEach(registration => {
            if (registration.respondsTo(change.type)) {
                events.push(registration.createEvent(materializedChange, eventGenerator.query_));
            }
        });
    });
}
function eventGeneratorMaterializeSingleChange(eventGenerator, change, eventCache) {
    if (change.type === 'value' || change.type === 'child_removed') {
        return change;
    }
    else {
        change.prevName = eventCache.getPredecessorChildName(change.childName, change.snapshotNode, eventGenerator.index_);
        return change;
    }
}
function eventGeneratorCompareChanges(eventGenerator, a, b) {
    if (a.childName == null || b.childName == null) {
        throw util.assertionError('Should only compare child_ events.');
    }
    const aWrapped = new NamedNode(a.childName, a.snapshotNode);
    const bWrapped = new NamedNode(b.childName, b.snapshotNode);
    return eventGenerator.index_.compare(aWrapped, bWrapped);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function newViewCache(eventCache, serverCache) {
    return { eventCache, serverCache };
}
function viewCacheUpdateEventSnap(viewCache, eventSnap, complete, filtered) {
    return newViewCache(new CacheNode(eventSnap, complete, filtered), viewCache.serverCache);
}
function viewCacheUpdateServerSnap(viewCache, serverSnap, complete, filtered) {
    return newViewCache(viewCache.eventCache, new CacheNode(serverSnap, complete, filtered));
}
function viewCacheGetCompleteEventSnap(viewCache) {
    return viewCache.eventCache.isFullyInitialized()
        ? viewCache.eventCache.getNode()
        : null;
}
function viewCacheGetCompleteServerSnap(viewCache) {
    return viewCache.serverCache.isFullyInitialized()
        ? viewCache.serverCache.getNode()
        : null;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let emptyChildrenSingleton;
/**
 * Singleton empty children collection.
 *
 */
const EmptyChildren = () => {
    if (!emptyChildrenSingleton) {
        emptyChildrenSingleton = new SortedMap(stringCompare);
    }
    return emptyChildrenSingleton;
};
/**
 * A tree with immutable elements.
 */
class ImmutableTree {
    constructor(value, children = EmptyChildren()) {
        this.value = value;
        this.children = children;
    }
    static fromObject(obj) {
        let tree = new ImmutableTree(null);
        each(obj, (childPath, childSnap) => {
            tree = tree.set(new Path(childPath), childSnap);
        });
        return tree;
    }
    /**
     * True if the value is empty and there are no children
     */
    isEmpty() {
        return this.value === null && this.children.isEmpty();
    }
    /**
     * Given a path and predicate, return the first node and the path to that node
     * where the predicate returns true.
     *
     * TODO Do a perf test -- If we're creating a bunch of `{path: value:}`
     * objects on the way back out, it may be better to pass down a pathSoFar obj.
     *
     * @param relativePath - The remainder of the path
     * @param predicate - The predicate to satisfy to return a node
     */
    findRootMostMatchingPathAndValue(relativePath, predicate) {
        if (this.value != null && predicate(this.value)) {
            return { path: newEmptyPath(), value: this.value };
        }
        else {
            if (pathIsEmpty(relativePath)) {
                return null;
            }
            else {
                const front = pathGetFront(relativePath);
                const child = this.children.get(front);
                if (child !== null) {
                    const childExistingPathAndValue = child.findRootMostMatchingPathAndValue(pathPopFront(relativePath), predicate);
                    if (childExistingPathAndValue != null) {
                        const fullPath = pathChild(new Path(front), childExistingPathAndValue.path);
                        return { path: fullPath, value: childExistingPathAndValue.value };
                    }
                    else {
                        return null;
                    }
                }
                else {
                    return null;
                }
            }
        }
    }
    /**
     * Find, if it exists, the shortest subpath of the given path that points a defined
     * value in the tree
     */
    findRootMostValueAndPath(relativePath) {
        return this.findRootMostMatchingPathAndValue(relativePath, () => true);
    }
    /**
     * @returns The subtree at the given path
     */
    subtree(relativePath) {
        if (pathIsEmpty(relativePath)) {
            return this;
        }
        else {
            const front = pathGetFront(relativePath);
            const childTree = this.children.get(front);
            if (childTree !== null) {
                return childTree.subtree(pathPopFront(relativePath));
            }
            else {
                return new ImmutableTree(null);
            }
        }
    }
    /**
     * Sets a value at the specified path.
     *
     * @param relativePath - Path to set value at.
     * @param toSet - Value to set.
     * @returns Resulting tree.
     */
    set(relativePath, toSet) {
        if (pathIsEmpty(relativePath)) {
            return new ImmutableTree(toSet, this.children);
        }
        else {
            const front = pathGetFront(relativePath);
            const child = this.children.get(front) || new ImmutableTree(null);
            const newChild = child.set(pathPopFront(relativePath), toSet);
            const newChildren = this.children.insert(front, newChild);
            return new ImmutableTree(this.value, newChildren);
        }
    }
    /**
     * Removes the value at the specified path.
     *
     * @param relativePath - Path to value to remove.
     * @returns Resulting tree.
     */
    remove(relativePath) {
        if (pathIsEmpty(relativePath)) {
            if (this.children.isEmpty()) {
                return new ImmutableTree(null);
            }
            else {
                return new ImmutableTree(null, this.children);
            }
        }
        else {
            const front = pathGetFront(relativePath);
            const child = this.children.get(front);
            if (child) {
                const newChild = child.remove(pathPopFront(relativePath));
                let newChildren;
                if (newChild.isEmpty()) {
                    newChildren = this.children.remove(front);
                }
                else {
                    newChildren = this.children.insert(front, newChild);
                }
                if (this.value === null && newChildren.isEmpty()) {
                    return new ImmutableTree(null);
                }
                else {
                    return new ImmutableTree(this.value, newChildren);
                }
            }
            else {
                return this;
            }
        }
    }
    /**
     * Gets a value from the tree.
     *
     * @param relativePath - Path to get value for.
     * @returns Value at path, or null.
     */
    get(relativePath) {
        if (pathIsEmpty(relativePath)) {
            return this.value;
        }
        else {
            const front = pathGetFront(relativePath);
            const child = this.children.get(front);
            if (child) {
                return child.get(pathPopFront(relativePath));
            }
            else {
                return null;
            }
        }
    }
    /**
     * Replace the subtree at the specified path with the given new tree.
     *
     * @param relativePath - Path to replace subtree for.
     * @param newTree - New tree.
     * @returns Resulting tree.
     */
    setTree(relativePath, newTree) {
        if (pathIsEmpty(relativePath)) {
            return newTree;
        }
        else {
            const front = pathGetFront(relativePath);
            const child = this.children.get(front) || new ImmutableTree(null);
            const newChild = child.setTree(pathPopFront(relativePath), newTree);
            let newChildren;
            if (newChild.isEmpty()) {
                newChildren = this.children.remove(front);
            }
            else {
                newChildren = this.children.insert(front, newChild);
            }
            return new ImmutableTree(this.value, newChildren);
        }
    }
    /**
     * Performs a depth first fold on this tree. Transforms a tree into a single
     * value, given a function that operates on the path to a node, an optional
     * current value, and a map of child names to folded subtrees
     */
    fold(fn) {
        return this.fold_(newEmptyPath(), fn);
    }
    /**
     * Recursive helper for public-facing fold() method
     */
    fold_(pathSoFar, fn) {
        const accum = {};
        this.children.inorderTraversal((childKey, childTree) => {
            accum[childKey] = childTree.fold_(pathChild(pathSoFar, childKey), fn);
        });
        return fn(pathSoFar, this.value, accum);
    }
    /**
     * Find the first matching value on the given path. Return the result of applying f to it.
     */
    findOnPath(path, f) {
        return this.findOnPath_(path, newEmptyPath(), f);
    }
    findOnPath_(pathToFollow, pathSoFar, f) {
        const result = this.value ? f(pathSoFar, this.value) : false;
        if (result) {
            return result;
        }
        else {
            if (pathIsEmpty(pathToFollow)) {
                return null;
            }
            else {
                const front = pathGetFront(pathToFollow);
                const nextChild = this.children.get(front);
                if (nextChild) {
                    return nextChild.findOnPath_(pathPopFront(pathToFollow), pathChild(pathSoFar, front), f);
                }
                else {
                    return null;
                }
            }
        }
    }
    foreachOnPath(path, f) {
        return this.foreachOnPath_(path, newEmptyPath(), f);
    }
    foreachOnPath_(pathToFollow, currentRelativePath, f) {
        if (pathIsEmpty(pathToFollow)) {
            return this;
        }
        else {
            if (this.value) {
                f(currentRelativePath, this.value);
            }
            const front = pathGetFront(pathToFollow);
            const nextChild = this.children.get(front);
            if (nextChild) {
                return nextChild.foreachOnPath_(pathPopFront(pathToFollow), pathChild(currentRelativePath, front), f);
            }
            else {
                return new ImmutableTree(null);
            }
        }
    }
    /**
     * Calls the given function for each node in the tree that has a value.
     *
     * @param f - A function to be called with the path from the root of the tree to
     * a node, and the value at that node. Called in depth-first order.
     */
    foreach(f) {
        this.foreach_(newEmptyPath(), f);
    }
    foreach_(currentRelativePath, f) {
        this.children.inorderTraversal((childName, childTree) => {
            childTree.foreach_(pathChild(currentRelativePath, childName), f);
        });
        if (this.value) {
            f(currentRelativePath, this.value);
        }
    }
    foreachChild(f) {
        this.children.inorderTraversal((childName, childTree) => {
            if (childTree.value) {
                f(childName, childTree.value);
            }
        });
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * This class holds a collection of writes that can be applied to nodes in unison. It abstracts away the logic with
 * dealing with priority writes and multiple nested writes. At any given path there is only allowed to be one write
 * modifying that path. Any write to an existing path or shadowing an existing path will modify that existing write
 * to reflect the write added.
 */
class CompoundWrite {
    constructor(writeTree_) {
        this.writeTree_ = writeTree_;
    }
    static empty() {
        return new CompoundWrite(new ImmutableTree(null));
    }
}
function compoundWriteAddWrite(compoundWrite, path, node) {
    if (pathIsEmpty(path)) {
        return new CompoundWrite(new ImmutableTree(node));
    }
    else {
        const rootmost = compoundWrite.writeTree_.findRootMostValueAndPath(path);
        if (rootmost != null) {
            const rootMostPath = rootmost.path;
            let value = rootmost.value;
            const relativePath = newRelativePath(rootMostPath, path);
            value = value.updateChild(relativePath, node);
            return new CompoundWrite(compoundWrite.writeTree_.set(rootMostPath, value));
        }
        else {
            const subtree = new ImmutableTree(node);
            const newWriteTree = compoundWrite.writeTree_.setTree(path, subtree);
            return new CompoundWrite(newWriteTree);
        }
    }
}
function compoundWriteAddWrites(compoundWrite, path, updates) {
    let newWrite = compoundWrite;
    each(updates, (childKey, node) => {
        newWrite = compoundWriteAddWrite(newWrite, pathChild(path, childKey), node);
    });
    return newWrite;
}
/**
 * Will remove a write at the given path and deeper paths. This will <em>not</em> modify a write at a higher
 * location, which must be removed by calling this method with that path.
 *
 * @param compoundWrite - The CompoundWrite to remove.
 * @param path - The path at which a write and all deeper writes should be removed
 * @returns The new CompoundWrite with the removed path
 */
function compoundWriteRemoveWrite(compoundWrite, path) {
    if (pathIsEmpty(path)) {
        return CompoundWrite.empty();
    }
    else {
        const newWriteTree = compoundWrite.writeTree_.setTree(path, new ImmutableTree(null));
        return new CompoundWrite(newWriteTree);
    }
}
/**
 * Returns whether this CompoundWrite will fully overwrite a node at a given location and can therefore be
 * considered "complete".
 *
 * @param compoundWrite - The CompoundWrite to check.
 * @param path - The path to check for
 * @returns Whether there is a complete write at that path
 */
function compoundWriteHasCompleteWrite(compoundWrite, path) {
    return compoundWriteGetCompleteNode(compoundWrite, path) != null;
}
/**
 * Returns a node for a path if and only if the node is a "complete" overwrite at that path. This will not aggregate
 * writes from deeper paths, but will return child nodes from a more shallow path.
 *
 * @param compoundWrite - The CompoundWrite to get the node from.
 * @param path - The path to get a complete write
 * @returns The node if complete at that path, or null otherwise.
 */
function compoundWriteGetCompleteNode(compoundWrite, path) {
    const rootmost = compoundWrite.writeTree_.findRootMostValueAndPath(path);
    if (rootmost != null) {
        return compoundWrite.writeTree_
            .get(rootmost.path)
            .getChild(newRelativePath(rootmost.path, path));
    }
    else {
        return null;
    }
}
/**
 * Returns all children that are guaranteed to be a complete overwrite.
 *
 * @param compoundWrite - The CompoundWrite to get children from.
 * @returns A list of all complete children.
 */
function compoundWriteGetCompleteChildren(compoundWrite) {
    const children = [];
    const node = compoundWrite.writeTree_.value;
    if (node != null) {
        // If it's a leaf node, it has no children; so nothing to do.
        if (!node.isLeafNode()) {
            node.forEachChild(PRIORITY_INDEX, (childName, childNode) => {
                children.push(new NamedNode(childName, childNode));
            });
        }
    }
    else {
        compoundWrite.writeTree_.children.inorderTraversal((childName, childTree) => {
            if (childTree.value != null) {
                children.push(new NamedNode(childName, childTree.value));
            }
        });
    }
    return children;
}
function compoundWriteChildCompoundWrite(compoundWrite, path) {
    if (pathIsEmpty(path)) {
        return compoundWrite;
    }
    else {
        const shadowingNode = compoundWriteGetCompleteNode(compoundWrite, path);
        if (shadowingNode != null) {
            return new CompoundWrite(new ImmutableTree(shadowingNode));
        }
        else {
            return new CompoundWrite(compoundWrite.writeTree_.subtree(path));
        }
    }
}
/**
 * Returns true if this CompoundWrite is empty and therefore does not modify any nodes.
 * @returns Whether this CompoundWrite is empty
 */
function compoundWriteIsEmpty(compoundWrite) {
    return compoundWrite.writeTree_.isEmpty();
}
/**
 * Applies this CompoundWrite to a node. The node is returned with all writes from this CompoundWrite applied to the
 * node
 * @param node - The node to apply this CompoundWrite to
 * @returns The node with all writes applied
 */
function compoundWriteApply(compoundWrite, node) {
    return applySubtreeWrite(newEmptyPath(), compoundWrite.writeTree_, node);
}
function applySubtreeWrite(relativePath, writeTree, node) {
    if (writeTree.value != null) {
        // Since there a write is always a leaf, we're done here
        return node.updateChild(relativePath, writeTree.value);
    }
    else {
        let priorityWrite = null;
        writeTree.children.inorderTraversal((childKey, childTree) => {
            if (childKey === '.priority') {
                // Apply priorities at the end so we don't update priorities for either empty nodes or forget
                // to apply priorities to empty nodes that are later filled
                util.assert(childTree.value !== null, 'Priority writes must always be leaf nodes');
                priorityWrite = childTree.value;
            }
            else {
                node = applySubtreeWrite(pathChild(relativePath, childKey), childTree, node);
            }
        });
        // If there was a priority write, we only apply it if the node is not empty
        if (!node.getChild(relativePath).isEmpty() && priorityWrite !== null) {
            node = node.updateChild(pathChild(relativePath, '.priority'), priorityWrite);
        }
        return node;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Create a new WriteTreeRef for the given path. For use with a new sync point at the given path.
 *
 */
function writeTreeChildWrites(writeTree, path) {
    return newWriteTreeRef(path, writeTree);
}
/**
 * Record a new overwrite from user code.
 *
 * @param visible - This is set to false by some transactions. It should be excluded from event caches
 */
function writeTreeAddOverwrite(writeTree, path, snap, writeId, visible) {
    util.assert(writeId > writeTree.lastWriteId, 'Stacking an older write on top of newer ones');
    if (visible === undefined) {
        visible = true;
    }
    writeTree.allWrites.push({
        path,
        snap,
        writeId,
        visible
    });
    if (visible) {
        writeTree.visibleWrites = compoundWriteAddWrite(writeTree.visibleWrites, path, snap);
    }
    writeTree.lastWriteId = writeId;
}
/**
 * Record a new merge from user code.
 */
function writeTreeAddMerge(writeTree, path, changedChildren, writeId) {
    util.assert(writeId > writeTree.lastWriteId, 'Stacking an older merge on top of newer ones');
    writeTree.allWrites.push({
        path,
        children: changedChildren,
        writeId,
        visible: true
    });
    writeTree.visibleWrites = compoundWriteAddWrites(writeTree.visibleWrites, path, changedChildren);
    writeTree.lastWriteId = writeId;
}
function writeTreeGetWrite(writeTree, writeId) {
    for (let i = 0; i < writeTree.allWrites.length; i++) {
        const record = writeTree.allWrites[i];
        if (record.writeId === writeId) {
            return record;
        }
    }
    return null;
}
/**
 * Remove a write (either an overwrite or merge) that has been successfully acknowledge by the server. Recalculates
 * the tree if necessary.  We return true if it may have been visible, meaning views need to reevaluate.
 *
 * @returns true if the write may have been visible (meaning we'll need to reevaluate / raise
 * events as a result).
 */
function writeTreeRemoveWrite(writeTree, writeId) {
    // Note: disabling this check. It could be a transaction that preempted another transaction, and thus was applied
    // out of order.
    //const validClear = revert || this.allWrites_.length === 0 || writeId <= this.allWrites_[0].writeId;
    //assert(validClear, "Either we don't have this write, or it's the first one in the queue");
    const idx = writeTree.allWrites.findIndex(s => {
        return s.writeId === writeId;
    });
    util.assert(idx >= 0, 'removeWrite called with nonexistent writeId.');
    const writeToRemove = writeTree.allWrites[idx];
    writeTree.allWrites.splice(idx, 1);
    let removedWriteWasVisible = writeToRemove.visible;
    let removedWriteOverlapsWithOtherWrites = false;
    let i = writeTree.allWrites.length - 1;
    while (removedWriteWasVisible && i >= 0) {
        const currentWrite = writeTree.allWrites[i];
        if (currentWrite.visible) {
            if (i >= idx &&
                writeTreeRecordContainsPath_(currentWrite, writeToRemove.path)) {
                // The removed write was completely shadowed by a subsequent write.
                removedWriteWasVisible = false;
            }
            else if (pathContains(writeToRemove.path, currentWrite.path)) {
                // Either we're covering some writes or they're covering part of us (depending on which came first).
                removedWriteOverlapsWithOtherWrites = true;
            }
        }
        i--;
    }
    if (!removedWriteWasVisible) {
        return false;
    }
    else if (removedWriteOverlapsWithOtherWrites) {
        // There's some shadowing going on. Just rebuild the visible writes from scratch.
        writeTreeResetTree_(writeTree);
        return true;
    }
    else {
        // There's no shadowing.  We can safely just remove the write(s) from visibleWrites.
        if (writeToRemove.snap) {
            writeTree.visibleWrites = compoundWriteRemoveWrite(writeTree.visibleWrites, writeToRemove.path);
        }
        else {
            const children = writeToRemove.children;
            each(children, (childName) => {
                writeTree.visibleWrites = compoundWriteRemoveWrite(writeTree.visibleWrites, pathChild(writeToRemove.path, childName));
            });
        }
        return true;
    }
}
function writeTreeRecordContainsPath_(writeRecord, path) {
    if (writeRecord.snap) {
        return pathContains(writeRecord.path, path);
    }
    else {
        for (const childName in writeRecord.children) {
            if (writeRecord.children.hasOwnProperty(childName) &&
                pathContains(pathChild(writeRecord.path, childName), path)) {
                return true;
            }
        }
        return false;
    }
}
/**
 * Re-layer the writes and merges into a tree so we can efficiently calculate event snapshots
 */
function writeTreeResetTree_(writeTree) {
    writeTree.visibleWrites = writeTreeLayerTree_(writeTree.allWrites, writeTreeDefaultFilter_, newEmptyPath());
    if (writeTree.allWrites.length > 0) {
        writeTree.lastWriteId =
            writeTree.allWrites[writeTree.allWrites.length - 1].writeId;
    }
    else {
        writeTree.lastWriteId = -1;
    }
}
/**
 * The default filter used when constructing the tree. Keep everything that's visible.
 */
function writeTreeDefaultFilter_(write) {
    return write.visible;
}
/**
 * Static method. Given an array of WriteRecords, a filter for which ones to include, and a path, construct the tree of
 * event data at that path.
 */
function writeTreeLayerTree_(writes, filter, treeRoot) {
    let compoundWrite = CompoundWrite.empty();
    for (let i = 0; i < writes.length; ++i) {
        const write = writes[i];
        // Theory, a later set will either:
        // a) abort a relevant transaction, so no need to worry about excluding it from calculating that transaction
        // b) not be relevant to a transaction (separate branch), so again will not affect the data for that transaction
        if (filter(write)) {
            const writePath = write.path;
            let relativePath;
            if (write.snap) {
                if (pathContains(treeRoot, writePath)) {
                    relativePath = newRelativePath(treeRoot, writePath);
                    compoundWrite = compoundWriteAddWrite(compoundWrite, relativePath, write.snap);
                }
                else if (pathContains(writePath, treeRoot)) {
                    relativePath = newRelativePath(writePath, treeRoot);
                    compoundWrite = compoundWriteAddWrite(compoundWrite, newEmptyPath(), write.snap.getChild(relativePath));
                }
                else ;
            }
            else if (write.children) {
                if (pathContains(treeRoot, writePath)) {
                    relativePath = newRelativePath(treeRoot, writePath);
                    compoundWrite = compoundWriteAddWrites(compoundWrite, relativePath, write.children);
                }
                else if (pathContains(writePath, treeRoot)) {
                    relativePath = newRelativePath(writePath, treeRoot);
                    if (pathIsEmpty(relativePath)) {
                        compoundWrite = compoundWriteAddWrites(compoundWrite, newEmptyPath(), write.children);
                    }
                    else {
                        const child = util.safeGet(write.children, pathGetFront(relativePath));
                        if (child) {
                            // There exists a child in this node that matches the root path
                            const deepNode = child.getChild(pathPopFront(relativePath));
                            compoundWrite = compoundWriteAddWrite(compoundWrite, newEmptyPath(), deepNode);
                        }
                    }
                }
                else ;
            }
            else {
                throw util.assertionError('WriteRecord should have .snap or .children');
            }
        }
    }
    return compoundWrite;
}
/**
 * Given optional, underlying server data, and an optional set of constraints (exclude some sets, include hidden
 * writes), attempt to calculate a complete snapshot for the given path
 *
 * @param writeIdsToExclude - An optional set to be excluded
 * @param includeHiddenWrites - Defaults to false, whether or not to layer on writes with visible set to false
 */
function writeTreeCalcCompleteEventCache(writeTree, treePath, completeServerCache, writeIdsToExclude, includeHiddenWrites) {
    if (!writeIdsToExclude && !includeHiddenWrites) {
        const shadowingNode = compoundWriteGetCompleteNode(writeTree.visibleWrites, treePath);
        if (shadowingNode != null) {
            return shadowingNode;
        }
        else {
            const subMerge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, treePath);
            if (compoundWriteIsEmpty(subMerge)) {
                return completeServerCache;
            }
            else if (completeServerCache == null &&
                !compoundWriteHasCompleteWrite(subMerge, newEmptyPath())) {
                // We wouldn't have a complete snapshot, since there's no underlying data and no complete shadow
                return null;
            }
            else {
                const layeredCache = completeServerCache || ChildrenNode.EMPTY_NODE;
                return compoundWriteApply(subMerge, layeredCache);
            }
        }
    }
    else {
        const merge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, treePath);
        if (!includeHiddenWrites && compoundWriteIsEmpty(merge)) {
            return completeServerCache;
        }
        else {
            // If the server cache is null, and we don't have a complete cache, we need to return null
            if (!includeHiddenWrites &&
                completeServerCache == null &&
                !compoundWriteHasCompleteWrite(merge, newEmptyPath())) {
                return null;
            }
            else {
                const filter = function (write) {
                    return ((write.visible || includeHiddenWrites) &&
                        (!writeIdsToExclude ||
                            !~writeIdsToExclude.indexOf(write.writeId)) &&
                        (pathContains(write.path, treePath) ||
                            pathContains(treePath, write.path)));
                };
                const mergeAtPath = writeTreeLayerTree_(writeTree.allWrites, filter, treePath);
                const layeredCache = completeServerCache || ChildrenNode.EMPTY_NODE;
                return compoundWriteApply(mergeAtPath, layeredCache);
            }
        }
    }
}
/**
 * With optional, underlying server data, attempt to return a children node of children that we have complete data for.
 * Used when creating new views, to pre-fill their complete event children snapshot.
 */
function writeTreeCalcCompleteEventChildren(writeTree, treePath, completeServerChildren) {
    let completeChildren = ChildrenNode.EMPTY_NODE;
    const topLevelSet = compoundWriteGetCompleteNode(writeTree.visibleWrites, treePath);
    if (topLevelSet) {
        if (!topLevelSet.isLeafNode()) {
            // we're shadowing everything. Return the children.
            topLevelSet.forEachChild(PRIORITY_INDEX, (childName, childSnap) => {
                completeChildren = completeChildren.updateImmediateChild(childName, childSnap);
            });
        }
        return completeChildren;
    }
    else if (completeServerChildren) {
        // Layer any children we have on top of this
        // We know we don't have a top-level set, so just enumerate existing children
        const merge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, treePath);
        completeServerChildren.forEachChild(PRIORITY_INDEX, (childName, childNode) => {
            const node = compoundWriteApply(compoundWriteChildCompoundWrite(merge, new Path(childName)), childNode);
            completeChildren = completeChildren.updateImmediateChild(childName, node);
        });
        // Add any complete children we have from the set
        compoundWriteGetCompleteChildren(merge).forEach(namedNode => {
            completeChildren = completeChildren.updateImmediateChild(namedNode.name, namedNode.node);
        });
        return completeChildren;
    }
    else {
        // We don't have anything to layer on top of. Layer on any children we have
        // Note that we can return an empty snap if we have a defined delete
        const merge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, treePath);
        compoundWriteGetCompleteChildren(merge).forEach(namedNode => {
            completeChildren = completeChildren.updateImmediateChild(namedNode.name, namedNode.node);
        });
        return completeChildren;
    }
}
/**
 * Given that the underlying server data has updated, determine what, if anything, needs to be
 * applied to the event cache.
 *
 * Possibilities:
 *
 * 1. No writes are shadowing. Events should be raised, the snap to be applied comes from the server data
 *
 * 2. Some write is completely shadowing. No events to be raised
 *
 * 3. Is partially shadowed. Events
 *
 * Either existingEventSnap or existingServerSnap must exist
 */
function writeTreeCalcEventCacheAfterServerOverwrite(writeTree, treePath, childPath, existingEventSnap, existingServerSnap) {
    util.assert(existingEventSnap || existingServerSnap, 'Either existingEventSnap or existingServerSnap must exist');
    const path = pathChild(treePath, childPath);
    if (compoundWriteHasCompleteWrite(writeTree.visibleWrites, path)) {
        // At this point we can probably guarantee that we're in case 2, meaning no events
        // May need to check visibility while doing the findRootMostValueAndPath call
        return null;
    }
    else {
        // No complete shadowing. We're either partially shadowing or not shadowing at all.
        const childMerge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, path);
        if (compoundWriteIsEmpty(childMerge)) {
            // We're not shadowing at all. Case 1
            return existingServerSnap.getChild(childPath);
        }
        else {
            // This could be more efficient if the serverNode + updates doesn't change the eventSnap
            // However this is tricky to find out, since user updates don't necessary change the server
            // snap, e.g. priority updates on empty nodes, or deep deletes. Another special case is if the server
            // adds nodes, but doesn't change any existing writes. It is therefore not enough to
            // only check if the updates change the serverNode.
            // Maybe check if the merge tree contains these special cases and only do a full overwrite in that case?
            return compoundWriteApply(childMerge, existingServerSnap.getChild(childPath));
        }
    }
}
/**
 * Returns a complete child for a given server snap after applying all user writes or null if there is no
 * complete child for this ChildKey.
 */
function writeTreeCalcCompleteChild(writeTree, treePath, childKey, existingServerSnap) {
    const path = pathChild(treePath, childKey);
    const shadowingNode = compoundWriteGetCompleteNode(writeTree.visibleWrites, path);
    if (shadowingNode != null) {
        return shadowingNode;
    }
    else {
        if (existingServerSnap.isCompleteForChild(childKey)) {
            const childMerge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, path);
            return compoundWriteApply(childMerge, existingServerSnap.getNode().getImmediateChild(childKey));
        }
        else {
            return null;
        }
    }
}
/**
 * Returns a node if there is a complete overwrite for this path. More specifically, if there is a write at
 * a higher path, this will return the child of that write relative to the write and this path.
 * Returns null if there is no write at this path.
 */
function writeTreeShadowingWrite(writeTree, path) {
    return compoundWriteGetCompleteNode(writeTree.visibleWrites, path);
}
/**
 * This method is used when processing child remove events on a query. If we can, we pull in children that were outside
 * the window, but may now be in the window.
 */
function writeTreeCalcIndexedSlice(writeTree, treePath, completeServerData, startPost, count, reverse, index) {
    let toIterate;
    const merge = compoundWriteChildCompoundWrite(writeTree.visibleWrites, treePath);
    const shadowingNode = compoundWriteGetCompleteNode(merge, newEmptyPath());
    if (shadowingNode != null) {
        toIterate = shadowingNode;
    }
    else if (completeServerData != null) {
        toIterate = compoundWriteApply(merge, completeServerData);
    }
    else {
        // no children to iterate on
        return [];
    }
    toIterate = toIterate.withIndex(index);
    if (!toIterate.isEmpty() && !toIterate.isLeafNode()) {
        const nodes = [];
        const cmp = index.getCompare();
        const iter = reverse
            ? toIterate.getReverseIteratorFrom(startPost, index)
            : toIterate.getIteratorFrom(startPost, index);
        let next = iter.getNext();
        while (next && nodes.length < count) {
            if (cmp(next, startPost) !== 0) {
                nodes.push(next);
            }
            next = iter.getNext();
        }
        return nodes;
    }
    else {
        return [];
    }
}
function newWriteTree() {
    return {
        visibleWrites: CompoundWrite.empty(),
        allWrites: [],
        lastWriteId: -1
    };
}
/**
 * If possible, returns a complete event cache, using the underlying server data if possible. In addition, can be used
 * to get a cache that includes hidden writes, and excludes arbitrary writes. Note that customizing the returned node
 * can lead to a more expensive calculation.
 *
 * @param writeIdsToExclude - Optional writes to exclude.
 * @param includeHiddenWrites - Defaults to false, whether or not to layer on writes with visible set to false
 */
function writeTreeRefCalcCompleteEventCache(writeTreeRef, completeServerCache, writeIdsToExclude, includeHiddenWrites) {
    return writeTreeCalcCompleteEventCache(writeTreeRef.writeTree, writeTreeRef.treePath, completeServerCache, writeIdsToExclude, includeHiddenWrites);
}
/**
 * If possible, returns a children node containing all of the complete children we have data for. The returned data is a
 * mix of the given server data and write data.
 *
 */
function writeTreeRefCalcCompleteEventChildren(writeTreeRef, completeServerChildren) {
    return writeTreeCalcCompleteEventChildren(writeTreeRef.writeTree, writeTreeRef.treePath, completeServerChildren);
}
/**
 * Given that either the underlying server data has updated or the outstanding writes have updated, determine what,
 * if anything, needs to be applied to the event cache.
 *
 * Possibilities:
 *
 * 1. No writes are shadowing. Events should be raised, the snap to be applied comes from the server data
 *
 * 2. Some write is completely shadowing. No events to be raised
 *
 * 3. Is partially shadowed. Events should be raised
 *
 * Either existingEventSnap or existingServerSnap must exist, this is validated via an assert
 *
 *
 */
function writeTreeRefCalcEventCacheAfterServerOverwrite(writeTreeRef, path, existingEventSnap, existingServerSnap) {
    return writeTreeCalcEventCacheAfterServerOverwrite(writeTreeRef.writeTree, writeTreeRef.treePath, path, existingEventSnap, existingServerSnap);
}
/**
 * Returns a node if there is a complete overwrite for this path. More specifically, if there is a write at
 * a higher path, this will return the child of that write relative to the write and this path.
 * Returns null if there is no write at this path.
 *
 */
function writeTreeRefShadowingWrite(writeTreeRef, path) {
    return writeTreeShadowingWrite(writeTreeRef.writeTree, pathChild(writeTreeRef.treePath, path));
}
/**
 * This method is used when processing child remove events on a query. If we can, we pull in children that were outside
 * the window, but may now be in the window
 */
function writeTreeRefCalcIndexedSlice(writeTreeRef, completeServerData, startPost, count, reverse, index) {
    return writeTreeCalcIndexedSlice(writeTreeRef.writeTree, writeTreeRef.treePath, completeServerData, startPost, count, reverse, index);
}
/**
 * Returns a complete child for a given server snap after applying all user writes or null if there is no
 * complete child for this ChildKey.
 */
function writeTreeRefCalcCompleteChild(writeTreeRef, childKey, existingServerCache) {
    return writeTreeCalcCompleteChild(writeTreeRef.writeTree, writeTreeRef.treePath, childKey, existingServerCache);
}
/**
 * Return a WriteTreeRef for a child.
 */
function writeTreeRefChild(writeTreeRef, childName) {
    return newWriteTreeRef(pathChild(writeTreeRef.treePath, childName), writeTreeRef.writeTree);
}
function newWriteTreeRef(path, writeTree) {
    return {
        treePath: path,
        writeTree
    };
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class ChildChangeAccumulator {
    constructor() {
        this.changeMap = new Map();
    }
    trackChildChange(change) {
        const type = change.type;
        const childKey = change.childName;
        util.assert(type === "child_added" /* ChangeType.CHILD_ADDED */ ||
            type === "child_changed" /* ChangeType.CHILD_CHANGED */ ||
            type === "child_removed" /* ChangeType.CHILD_REMOVED */, 'Only child changes supported for tracking');
        util.assert(childKey !== '.priority', 'Only non-priority child changes can be tracked.');
        const oldChange = this.changeMap.get(childKey);
        if (oldChange) {
            const oldType = oldChange.type;
            if (type === "child_added" /* ChangeType.CHILD_ADDED */ &&
                oldType === "child_removed" /* ChangeType.CHILD_REMOVED */) {
                this.changeMap.set(childKey, changeChildChanged(childKey, change.snapshotNode, oldChange.snapshotNode));
            }
            else if (type === "child_removed" /* ChangeType.CHILD_REMOVED */ &&
                oldType === "child_added" /* ChangeType.CHILD_ADDED */) {
                this.changeMap.delete(childKey);
            }
            else if (type === "child_removed" /* ChangeType.CHILD_REMOVED */ &&
                oldType === "child_changed" /* ChangeType.CHILD_CHANGED */) {
                this.changeMap.set(childKey, changeChildRemoved(childKey, oldChange.oldSnap));
            }
            else if (type === "child_changed" /* ChangeType.CHILD_CHANGED */ &&
                oldType === "child_added" /* ChangeType.CHILD_ADDED */) {
                this.changeMap.set(childKey, changeChildAdded(childKey, change.snapshotNode));
            }
            else if (type === "child_changed" /* ChangeType.CHILD_CHANGED */ &&
                oldType === "child_changed" /* ChangeType.CHILD_CHANGED */) {
                this.changeMap.set(childKey, changeChildChanged(childKey, change.snapshotNode, oldChange.oldSnap));
            }
            else {
                throw util.assertionError('Illegal combination of changes: ' +
                    change +
                    ' occurred after ' +
                    oldChange);
            }
        }
        else {
            this.changeMap.set(childKey, change);
        }
    }
    getChanges() {
        return Array.from(this.changeMap.values());
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An implementation of CompleteChildSource that never returns any additional children
 */
// eslint-disable-next-line @typescript-eslint/naming-convention
class NoCompleteChildSource_ {
    getCompleteChild(childKey) {
        return null;
    }
    getChildAfterChild(index, child, reverse) {
        return null;
    }
}
/**
 * Singleton instance.
 */
const NO_COMPLETE_CHILD_SOURCE = new NoCompleteChildSource_();
/**
 * An implementation of CompleteChildSource that uses a WriteTree in addition to any other server data or
 * old event caches available to calculate complete children.
 */
class WriteTreeCompleteChildSource {
    constructor(writes_, viewCache_, optCompleteServerCache_ = null) {
        this.writes_ = writes_;
        this.viewCache_ = viewCache_;
        this.optCompleteServerCache_ = optCompleteServerCache_;
    }
    getCompleteChild(childKey) {
        const node = this.viewCache_.eventCache;
        if (node.isCompleteForChild(childKey)) {
            return node.getNode().getImmediateChild(childKey);
        }
        else {
            const serverNode = this.optCompleteServerCache_ != null
                ? new CacheNode(this.optCompleteServerCache_, true, false)
                : this.viewCache_.serverCache;
            return writeTreeRefCalcCompleteChild(this.writes_, childKey, serverNode);
        }
    }
    getChildAfterChild(index, child, reverse) {
        const completeServerData = this.optCompleteServerCache_ != null
            ? this.optCompleteServerCache_
            : viewCacheGetCompleteServerSnap(this.viewCache_);
        const nodes = writeTreeRefCalcIndexedSlice(this.writes_, completeServerData, child, 1, reverse, index);
        if (nodes.length === 0) {
            return null;
        }
        else {
            return nodes[0];
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function newViewProcessor(filter) {
    return { filter };
}
function viewProcessorAssertIndexed(viewProcessor, viewCache) {
    util.assert(viewCache.eventCache.getNode().isIndexed(viewProcessor.filter.getIndex()), 'Event snap not indexed');
    util.assert(viewCache.serverCache.getNode().isIndexed(viewProcessor.filter.getIndex()), 'Server snap not indexed');
}
function viewProcessorApplyOperation(viewProcessor, oldViewCache, operation, writesCache, completeCache) {
    const accumulator = new ChildChangeAccumulator();
    let newViewCache, filterServerNode;
    if (operation.type === OperationType.OVERWRITE) {
        const overwrite = operation;
        if (overwrite.source.fromUser) {
            newViewCache = viewProcessorApplyUserOverwrite(viewProcessor, oldViewCache, overwrite.path, overwrite.snap, writesCache, completeCache, accumulator);
        }
        else {
            util.assert(overwrite.source.fromServer, 'Unknown source.');
            // We filter the node if it's a tagged update or the node has been previously filtered  and the
            // update is not at the root in which case it is ok (and necessary) to mark the node unfiltered
            // again
            filterServerNode =
                overwrite.source.tagged ||
                    (oldViewCache.serverCache.isFiltered() && !pathIsEmpty(overwrite.path));
            newViewCache = viewProcessorApplyServerOverwrite(viewProcessor, oldViewCache, overwrite.path, overwrite.snap, writesCache, completeCache, filterServerNode, accumulator);
        }
    }
    else if (operation.type === OperationType.MERGE) {
        const merge = operation;
        if (merge.source.fromUser) {
            newViewCache = viewProcessorApplyUserMerge(viewProcessor, oldViewCache, merge.path, merge.children, writesCache, completeCache, accumulator);
        }
        else {
            util.assert(merge.source.fromServer, 'Unknown source.');
            // We filter the node if it's a tagged update or the node has been previously filtered
            filterServerNode =
                merge.source.tagged || oldViewCache.serverCache.isFiltered();
            newViewCache = viewProcessorApplyServerMerge(viewProcessor, oldViewCache, merge.path, merge.children, writesCache, completeCache, filterServerNode, accumulator);
        }
    }
    else if (operation.type === OperationType.ACK_USER_WRITE) {
        const ackUserWrite = operation;
        if (!ackUserWrite.revert) {
            newViewCache = viewProcessorAckUserWrite(viewProcessor, oldViewCache, ackUserWrite.path, ackUserWrite.affectedTree, writesCache, completeCache, accumulator);
        }
        else {
            newViewCache = viewProcessorRevertUserWrite(viewProcessor, oldViewCache, ackUserWrite.path, writesCache, completeCache, accumulator);
        }
    }
    else if (operation.type === OperationType.LISTEN_COMPLETE) {
        newViewCache = viewProcessorListenComplete(viewProcessor, oldViewCache, operation.path, writesCache, accumulator);
    }
    else {
        throw util.assertionError('Unknown operation type: ' + operation.type);
    }
    const changes = accumulator.getChanges();
    viewProcessorMaybeAddValueEvent(oldViewCache, newViewCache, changes);
    return { viewCache: newViewCache, changes };
}
function viewProcessorMaybeAddValueEvent(oldViewCache, newViewCache, accumulator) {
    const eventSnap = newViewCache.eventCache;
    if (eventSnap.isFullyInitialized()) {
        const isLeafOrEmpty = eventSnap.getNode().isLeafNode() || eventSnap.getNode().isEmpty();
        const oldCompleteSnap = viewCacheGetCompleteEventSnap(oldViewCache);
        if (accumulator.length > 0 ||
            !oldViewCache.eventCache.isFullyInitialized() ||
            (isLeafOrEmpty && !eventSnap.getNode().equals(oldCompleteSnap)) ||
            !eventSnap.getNode().getPriority().equals(oldCompleteSnap.getPriority())) {
            accumulator.push(changeValue(viewCacheGetCompleteEventSnap(newViewCache)));
        }
    }
}
function viewProcessorGenerateEventCacheAfterServerEvent(viewProcessor, viewCache, changePath, writesCache, source, accumulator) {
    const oldEventSnap = viewCache.eventCache;
    if (writeTreeRefShadowingWrite(writesCache, changePath) != null) {
        // we have a shadowing write, ignore changes
        return viewCache;
    }
    else {
        let newEventCache, serverNode;
        if (pathIsEmpty(changePath)) {
            // TODO: figure out how this plays with "sliding ack windows"
            util.assert(viewCache.serverCache.isFullyInitialized(), 'If change path is empty, we must have complete server data');
            if (viewCache.serverCache.isFiltered()) {
                // We need to special case this, because we need to only apply writes to complete children, or
                // we might end up raising events for incomplete children. If the server data is filtered deep
                // writes cannot be guaranteed to be complete
                const serverCache = viewCacheGetCompleteServerSnap(viewCache);
                const completeChildren = serverCache instanceof ChildrenNode
                    ? serverCache
                    : ChildrenNode.EMPTY_NODE;
                const completeEventChildren = writeTreeRefCalcCompleteEventChildren(writesCache, completeChildren);
                newEventCache = viewProcessor.filter.updateFullNode(viewCache.eventCache.getNode(), completeEventChildren, accumulator);
            }
            else {
                const completeNode = writeTreeRefCalcCompleteEventCache(writesCache, viewCacheGetCompleteServerSnap(viewCache));
                newEventCache = viewProcessor.filter.updateFullNode(viewCache.eventCache.getNode(), completeNode, accumulator);
            }
        }
        else {
            const childKey = pathGetFront(changePath);
            if (childKey === '.priority') {
                util.assert(pathGetLength(changePath) === 1, "Can't have a priority with additional path components");
                const oldEventNode = oldEventSnap.getNode();
                serverNode = viewCache.serverCache.getNode();
                // we might have overwrites for this priority
                const updatedPriority = writeTreeRefCalcEventCacheAfterServerOverwrite(writesCache, changePath, oldEventNode, serverNode);
                if (updatedPriority != null) {
                    newEventCache = viewProcessor.filter.updatePriority(oldEventNode, updatedPriority);
                }
                else {
                    // priority didn't change, keep old node
                    newEventCache = oldEventSnap.getNode();
                }
            }
            else {
                const childChangePath = pathPopFront(changePath);
                // update child
                let newEventChild;
                if (oldEventSnap.isCompleteForChild(childKey)) {
                    serverNode = viewCache.serverCache.getNode();
                    const eventChildUpdate = writeTreeRefCalcEventCacheAfterServerOverwrite(writesCache, changePath, oldEventSnap.getNode(), serverNode);
                    if (eventChildUpdate != null) {
                        newEventChild = oldEventSnap
                            .getNode()
                            .getImmediateChild(childKey)
                            .updateChild(childChangePath, eventChildUpdate);
                    }
                    else {
                        // Nothing changed, just keep the old child
                        newEventChild = oldEventSnap.getNode().getImmediateChild(childKey);
                    }
                }
                else {
                    newEventChild = writeTreeRefCalcCompleteChild(writesCache, childKey, viewCache.serverCache);
                }
                if (newEventChild != null) {
                    newEventCache = viewProcessor.filter.updateChild(oldEventSnap.getNode(), childKey, newEventChild, childChangePath, source, accumulator);
                }
                else {
                    // no complete child available or no change
                    newEventCache = oldEventSnap.getNode();
                }
            }
        }
        return viewCacheUpdateEventSnap(viewCache, newEventCache, oldEventSnap.isFullyInitialized() || pathIsEmpty(changePath), viewProcessor.filter.filtersNodes());
    }
}
function viewProcessorApplyServerOverwrite(viewProcessor, oldViewCache, changePath, changedSnap, writesCache, completeCache, filterServerNode, accumulator) {
    const oldServerSnap = oldViewCache.serverCache;
    let newServerCache;
    const serverFilter = filterServerNode
        ? viewProcessor.filter
        : viewProcessor.filter.getIndexedFilter();
    if (pathIsEmpty(changePath)) {
        newServerCache = serverFilter.updateFullNode(oldServerSnap.getNode(), changedSnap, null);
    }
    else if (serverFilter.filtersNodes() && !oldServerSnap.isFiltered()) {
        // we want to filter the server node, but we didn't filter the server node yet, so simulate a full update
        const newServerNode = oldServerSnap
            .getNode()
            .updateChild(changePath, changedSnap);
        newServerCache = serverFilter.updateFullNode(oldServerSnap.getNode(), newServerNode, null);
    }
    else {
        const childKey = pathGetFront(changePath);
        if (!oldServerSnap.isCompleteForPath(changePath) &&
            pathGetLength(changePath) > 1) {
            // We don't update incomplete nodes with updates intended for other listeners
            return oldViewCache;
        }
        const childChangePath = pathPopFront(changePath);
        const childNode = oldServerSnap.getNode().getImmediateChild(childKey);
        const newChildNode = childNode.updateChild(childChangePath, changedSnap);
        if (childKey === '.priority') {
            newServerCache = serverFilter.updatePriority(oldServerSnap.getNode(), newChildNode);
        }
        else {
            newServerCache = serverFilter.updateChild(oldServerSnap.getNode(), childKey, newChildNode, childChangePath, NO_COMPLETE_CHILD_SOURCE, null);
        }
    }
    const newViewCache = viewCacheUpdateServerSnap(oldViewCache, newServerCache, oldServerSnap.isFullyInitialized() || pathIsEmpty(changePath), serverFilter.filtersNodes());
    const source = new WriteTreeCompleteChildSource(writesCache, newViewCache, completeCache);
    return viewProcessorGenerateEventCacheAfterServerEvent(viewProcessor, newViewCache, changePath, writesCache, source, accumulator);
}
function viewProcessorApplyUserOverwrite(viewProcessor, oldViewCache, changePath, changedSnap, writesCache, completeCache, accumulator) {
    const oldEventSnap = oldViewCache.eventCache;
    let newViewCache, newEventCache;
    const source = new WriteTreeCompleteChildSource(writesCache, oldViewCache, completeCache);
    if (pathIsEmpty(changePath)) {
        newEventCache = viewProcessor.filter.updateFullNode(oldViewCache.eventCache.getNode(), changedSnap, accumulator);
        newViewCache = viewCacheUpdateEventSnap(oldViewCache, newEventCache, true, viewProcessor.filter.filtersNodes());
    }
    else {
        const childKey = pathGetFront(changePath);
        if (childKey === '.priority') {
            newEventCache = viewProcessor.filter.updatePriority(oldViewCache.eventCache.getNode(), changedSnap);
            newViewCache = viewCacheUpdateEventSnap(oldViewCache, newEventCache, oldEventSnap.isFullyInitialized(), oldEventSnap.isFiltered());
        }
        else {
            const childChangePath = pathPopFront(changePath);
            const oldChild = oldEventSnap.getNode().getImmediateChild(childKey);
            let newChild;
            if (pathIsEmpty(childChangePath)) {
                // Child overwrite, we can replace the child
                newChild = changedSnap;
            }
            else {
                const childNode = source.getCompleteChild(childKey);
                if (childNode != null) {
                    if (pathGetBack(childChangePath) === '.priority' &&
                        childNode.getChild(pathParent(childChangePath)).isEmpty()) {
                        // This is a priority update on an empty node. If this node exists on the server, the
                        // server will send down the priority in the update, so ignore for now
                        newChild = childNode;
                    }
                    else {
                        newChild = childNode.updateChild(childChangePath, changedSnap);
                    }
                }
                else {
                    // There is no complete child node available
                    newChild = ChildrenNode.EMPTY_NODE;
                }
            }
            if (!oldChild.equals(newChild)) {
                const newEventSnap = viewProcessor.filter.updateChild(oldEventSnap.getNode(), childKey, newChild, childChangePath, source, accumulator);
                newViewCache = viewCacheUpdateEventSnap(oldViewCache, newEventSnap, oldEventSnap.isFullyInitialized(), viewProcessor.filter.filtersNodes());
            }
            else {
                newViewCache = oldViewCache;
            }
        }
    }
    return newViewCache;
}
function viewProcessorCacheHasChild(viewCache, childKey) {
    return viewCache.eventCache.isCompleteForChild(childKey);
}
function viewProcessorApplyUserMerge(viewProcessor, viewCache, path, changedChildren, writesCache, serverCache, accumulator) {
    // HACK: In the case of a limit query, there may be some changes that bump things out of the
    // window leaving room for new items.  It's important we process these changes first, so we
    // iterate the changes twice, first processing any that affect items currently in view.
    // TODO: I consider an item "in view" if cacheHasChild is true, which checks both the server
    // and event snap.  I'm not sure if this will result in edge cases when a child is in one but
    // not the other.
    let curViewCache = viewCache;
    changedChildren.foreach((relativePath, childNode) => {
        const writePath = pathChild(path, relativePath);
        if (viewProcessorCacheHasChild(viewCache, pathGetFront(writePath))) {
            curViewCache = viewProcessorApplyUserOverwrite(viewProcessor, curViewCache, writePath, childNode, writesCache, serverCache, accumulator);
        }
    });
    changedChildren.foreach((relativePath, childNode) => {
        const writePath = pathChild(path, relativePath);
        if (!viewProcessorCacheHasChild(viewCache, pathGetFront(writePath))) {
            curViewCache = viewProcessorApplyUserOverwrite(viewProcessor, curViewCache, writePath, childNode, writesCache, serverCache, accumulator);
        }
    });
    return curViewCache;
}
function viewProcessorApplyMerge(viewProcessor, node, merge) {
    merge.foreach((relativePath, childNode) => {
        node = node.updateChild(relativePath, childNode);
    });
    return node;
}
function viewProcessorApplyServerMerge(viewProcessor, viewCache, path, changedChildren, writesCache, serverCache, filterServerNode, accumulator) {
    // If we don't have a cache yet, this merge was intended for a previously listen in the same location. Ignore it and
    // wait for the complete data update coming soon.
    if (viewCache.serverCache.getNode().isEmpty() &&
        !viewCache.serverCache.isFullyInitialized()) {
        return viewCache;
    }
    // HACK: In the case of a limit query, there may be some changes that bump things out of the
    // window leaving room for new items.  It's important we process these changes first, so we
    // iterate the changes twice, first processing any that affect items currently in view.
    // TODO: I consider an item "in view" if cacheHasChild is true, which checks both the server
    // and event snap.  I'm not sure if this will result in edge cases when a child is in one but
    // not the other.
    let curViewCache = viewCache;
    let viewMergeTree;
    if (pathIsEmpty(path)) {
        viewMergeTree = changedChildren;
    }
    else {
        viewMergeTree = new ImmutableTree(null).setTree(path, changedChildren);
    }
    const serverNode = viewCache.serverCache.getNode();
    viewMergeTree.children.inorderTraversal((childKey, childTree) => {
        if (serverNode.hasChild(childKey)) {
            const serverChild = viewCache.serverCache
                .getNode()
                .getImmediateChild(childKey);
            const newChild = viewProcessorApplyMerge(viewProcessor, serverChild, childTree);
            curViewCache = viewProcessorApplyServerOverwrite(viewProcessor, curViewCache, new Path(childKey), newChild, writesCache, serverCache, filterServerNode, accumulator);
        }
    });
    viewMergeTree.children.inorderTraversal((childKey, childMergeTree) => {
        const isUnknownDeepMerge = !viewCache.serverCache.isCompleteForChild(childKey) &&
            childMergeTree.value === null;
        if (!serverNode.hasChild(childKey) && !isUnknownDeepMerge) {
            const serverChild = viewCache.serverCache
                .getNode()
                .getImmediateChild(childKey);
            const newChild = viewProcessorApplyMerge(viewProcessor, serverChild, childMergeTree);
            curViewCache = viewProcessorApplyServerOverwrite(viewProcessor, curViewCache, new Path(childKey), newChild, writesCache, serverCache, filterServerNode, accumulator);
        }
    });
    return curViewCache;
}
function viewProcessorAckUserWrite(viewProcessor, viewCache, ackPath, affectedTree, writesCache, completeCache, accumulator) {
    if (writeTreeRefShadowingWrite(writesCache, ackPath) != null) {
        return viewCache;
    }
    // Only filter server node if it is currently filtered
    const filterServerNode = viewCache.serverCache.isFiltered();
    // Essentially we'll just get our existing server cache for the affected paths and re-apply it as a server update
    // now that it won't be shadowed.
    const serverCache = viewCache.serverCache;
    if (affectedTree.value != null) {
        // This is an overwrite.
        if ((pathIsEmpty(ackPath) && serverCache.isFullyInitialized()) ||
            serverCache.isCompleteForPath(ackPath)) {
            return viewProcessorApplyServerOverwrite(viewProcessor, viewCache, ackPath, serverCache.getNode().getChild(ackPath), writesCache, completeCache, filterServerNode, accumulator);
        }
        else if (pathIsEmpty(ackPath)) {
            // This is a goofy edge case where we are acking data at this location but don't have full data.  We
            // should just re-apply whatever we have in our cache as a merge.
            let changedChildren = new ImmutableTree(null);
            serverCache.getNode().forEachChild(KEY_INDEX, (name, node) => {
                changedChildren = changedChildren.set(new Path(name), node);
            });
            return viewProcessorApplyServerMerge(viewProcessor, viewCache, ackPath, changedChildren, writesCache, completeCache, filterServerNode, accumulator);
        }
        else {
            return viewCache;
        }
    }
    else {
        // This is a merge.
        let changedChildren = new ImmutableTree(null);
        affectedTree.foreach((mergePath, value) => {
            const serverCachePath = pathChild(ackPath, mergePath);
            if (serverCache.isCompleteForPath(serverCachePath)) {
                changedChildren = changedChildren.set(mergePath, serverCache.getNode().getChild(serverCachePath));
            }
        });
        return viewProcessorApplyServerMerge(viewProcessor, viewCache, ackPath, changedChildren, writesCache, completeCache, filterServerNode, accumulator);
    }
}
function viewProcessorListenComplete(viewProcessor, viewCache, path, writesCache, accumulator) {
    const oldServerNode = viewCache.serverCache;
    const newViewCache = viewCacheUpdateServerSnap(viewCache, oldServerNode.getNode(), oldServerNode.isFullyInitialized() || pathIsEmpty(path), oldServerNode.isFiltered());
    return viewProcessorGenerateEventCacheAfterServerEvent(viewProcessor, newViewCache, path, writesCache, NO_COMPLETE_CHILD_SOURCE, accumulator);
}
function viewProcessorRevertUserWrite(viewProcessor, viewCache, path, writesCache, completeServerCache, accumulator) {
    let complete;
    if (writeTreeRefShadowingWrite(writesCache, path) != null) {
        return viewCache;
    }
    else {
        const source = new WriteTreeCompleteChildSource(writesCache, viewCache, completeServerCache);
        const oldEventCache = viewCache.eventCache.getNode();
        let newEventCache;
        if (pathIsEmpty(path) || pathGetFront(path) === '.priority') {
            let newNode;
            if (viewCache.serverCache.isFullyInitialized()) {
                newNode = writeTreeRefCalcCompleteEventCache(writesCache, viewCacheGetCompleteServerSnap(viewCache));
            }
            else {
                const serverChildren = viewCache.serverCache.getNode();
                util.assert(serverChildren instanceof ChildrenNode, 'serverChildren would be complete if leaf node');
                newNode = writeTreeRefCalcCompleteEventChildren(writesCache, serverChildren);
            }
            newNode = newNode;
            newEventCache = viewProcessor.filter.updateFullNode(oldEventCache, newNode, accumulator);
        }
        else {
            const childKey = pathGetFront(path);
            let newChild = writeTreeRefCalcCompleteChild(writesCache, childKey, viewCache.serverCache);
            if (newChild == null &&
                viewCache.serverCache.isCompleteForChild(childKey)) {
                newChild = oldEventCache.getImmediateChild(childKey);
            }
            if (newChild != null) {
                newEventCache = viewProcessor.filter.updateChild(oldEventCache, childKey, newChild, pathPopFront(path), source, accumulator);
            }
            else if (viewCache.eventCache.getNode().hasChild(childKey)) {
                // No complete child available, delete the existing one, if any
                newEventCache = viewProcessor.filter.updateChild(oldEventCache, childKey, ChildrenNode.EMPTY_NODE, pathPopFront(path), source, accumulator);
            }
            else {
                newEventCache = oldEventCache;
            }
            if (newEventCache.isEmpty() &&
                viewCache.serverCache.isFullyInitialized()) {
                // We might have reverted all child writes. Maybe the old event was a leaf node
                complete = writeTreeRefCalcCompleteEventCache(writesCache, viewCacheGetCompleteServerSnap(viewCache));
                if (complete.isLeafNode()) {
                    newEventCache = viewProcessor.filter.updateFullNode(newEventCache, complete, accumulator);
                }
            }
        }
        complete =
            viewCache.serverCache.isFullyInitialized() ||
                writeTreeRefShadowingWrite(writesCache, newEmptyPath()) != null;
        return viewCacheUpdateEventSnap(viewCache, newEventCache, complete, viewProcessor.filter.filtersNodes());
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A view represents a specific location and query that has 1 or more event registrations.
 *
 * It does several things:
 *  - Maintains the list of event registrations for this location/query.
 *  - Maintains a cache of the data visible for this location/query.
 *  - Applies new operations (via applyOperation), updates the cache, and based on the event
 *    registrations returns the set of events to be raised.
 */
class View {
    constructor(query_, initialViewCache) {
        this.query_ = query_;
        this.eventRegistrations_ = [];
        const params = this.query_._queryParams;
        const indexFilter = new IndexedFilter(params.getIndex());
        const filter = queryParamsGetNodeFilter(params);
        this.processor_ = newViewProcessor(filter);
        const initialServerCache = initialViewCache.serverCache;
        const initialEventCache = initialViewCache.eventCache;
        // Don't filter server node with other filter than index, wait for tagged listen
        const serverSnap = indexFilter.updateFullNode(ChildrenNode.EMPTY_NODE, initialServerCache.getNode(), null);
        const eventSnap = filter.updateFullNode(ChildrenNode.EMPTY_NODE, initialEventCache.getNode(), null);
        const newServerCache = new CacheNode(serverSnap, initialServerCache.isFullyInitialized(), indexFilter.filtersNodes());
        const newEventCache = new CacheNode(eventSnap, initialEventCache.isFullyInitialized(), filter.filtersNodes());
        this.viewCache_ = newViewCache(newEventCache, newServerCache);
        this.eventGenerator_ = new EventGenerator(this.query_);
    }
    get query() {
        return this.query_;
    }
}
function viewGetServerCache(view) {
    return view.viewCache_.serverCache.getNode();
}
function viewGetCompleteNode(view) {
    return viewCacheGetCompleteEventSnap(view.viewCache_);
}
function viewGetCompleteServerCache(view, path) {
    const cache = viewCacheGetCompleteServerSnap(view.viewCache_);
    if (cache) {
        // If this isn't a "loadsAllData" view, then cache isn't actually a complete cache and
        // we need to see if it contains the child we're interested in.
        if (view.query._queryParams.loadsAllData() ||
            (!pathIsEmpty(path) &&
                !cache.getImmediateChild(pathGetFront(path)).isEmpty())) {
            return cache.getChild(path);
        }
    }
    return null;
}
function viewIsEmpty(view) {
    return view.eventRegistrations_.length === 0;
}
function viewAddEventRegistration(view, eventRegistration) {
    view.eventRegistrations_.push(eventRegistration);
}
/**
 * @param eventRegistration - If null, remove all callbacks.
 * @param cancelError - If a cancelError is provided, appropriate cancel events will be returned.
 * @returns Cancel events, if cancelError was provided.
 */
function viewRemoveEventRegistration(view, eventRegistration, cancelError) {
    const cancelEvents = [];
    if (cancelError) {
        util.assert(eventRegistration == null, 'A cancel should cancel all event registrations.');
        const path = view.query._path;
        view.eventRegistrations_.forEach(registration => {
            const maybeEvent = registration.createCancelEvent(cancelError, path);
            if (maybeEvent) {
                cancelEvents.push(maybeEvent);
            }
        });
    }
    if (eventRegistration) {
        let remaining = [];
        for (let i = 0; i < view.eventRegistrations_.length; ++i) {
            const existing = view.eventRegistrations_[i];
            if (!existing.matches(eventRegistration)) {
                remaining.push(existing);
            }
            else if (eventRegistration.hasAnyCallback()) {
                // We're removing just this one
                remaining = remaining.concat(view.eventRegistrations_.slice(i + 1));
                break;
            }
        }
        view.eventRegistrations_ = remaining;
    }
    else {
        view.eventRegistrations_ = [];
    }
    return cancelEvents;
}
/**
 * Applies the given Operation, updates our cache, and returns the appropriate events.
 */
function viewApplyOperation(view, operation, writesCache, completeServerCache) {
    if (operation.type === OperationType.MERGE &&
        operation.source.queryId !== null) {
        util.assert(viewCacheGetCompleteServerSnap(view.viewCache_), 'We should always have a full cache before handling merges');
        util.assert(viewCacheGetCompleteEventSnap(view.viewCache_), 'Missing event cache, even though we have a server cache');
    }
    const oldViewCache = view.viewCache_;
    const result = viewProcessorApplyOperation(view.processor_, oldViewCache, operation, writesCache, completeServerCache);
    viewProcessorAssertIndexed(view.processor_, result.viewCache);
    util.assert(result.viewCache.serverCache.isFullyInitialized() ||
        !oldViewCache.serverCache.isFullyInitialized(), 'Once a server snap is complete, it should never go back');
    view.viewCache_ = result.viewCache;
    return viewGenerateEventsForChanges_(view, result.changes, result.viewCache.eventCache.getNode(), null);
}
function viewGetInitialEvents(view, registration) {
    const eventSnap = view.viewCache_.eventCache;
    const initialChanges = [];
    if (!eventSnap.getNode().isLeafNode()) {
        const eventNode = eventSnap.getNode();
        eventNode.forEachChild(PRIORITY_INDEX, (key, childNode) => {
            initialChanges.push(changeChildAdded(key, childNode));
        });
    }
    if (eventSnap.isFullyInitialized()) {
        initialChanges.push(changeValue(eventSnap.getNode()));
    }
    return viewGenerateEventsForChanges_(view, initialChanges, eventSnap.getNode(), registration);
}
function viewGenerateEventsForChanges_(view, changes, eventCache, eventRegistration) {
    const registrations = eventRegistration
        ? [eventRegistration]
        : view.eventRegistrations_;
    return eventGeneratorGenerateEventsForChanges(view.eventGenerator_, changes, eventCache, registrations);
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let referenceConstructor$1;
/**
 * SyncPoint represents a single location in a SyncTree with 1 or more event registrations, meaning we need to
 * maintain 1 or more Views at this location to cache server data and raise appropriate events for server changes
 * and user writes (set, transaction, update).
 *
 * It's responsible for:
 *  - Maintaining the set of 1 or more views necessary at this location (a SyncPoint with 0 views should be removed).
 *  - Proxying user / server operations to the views as appropriate (i.e. applyServerOverwrite,
 *    applyUserOverwrite, etc.)
 */
class SyncPoint {
    constructor() {
        /**
         * The Views being tracked at this location in the tree, stored as a map where the key is a
         * queryId and the value is the View for that query.
         *
         * NOTE: This list will be quite small (usually 1, but perhaps 2 or 3; any more is an odd use case).
         */
        this.views = new Map();
    }
}
function syncPointSetReferenceConstructor(val) {
    util.assert(!referenceConstructor$1, '__referenceConstructor has already been defined');
    referenceConstructor$1 = val;
}
function syncPointGetReferenceConstructor() {
    util.assert(referenceConstructor$1, 'Reference.ts has not been loaded');
    return referenceConstructor$1;
}
function syncPointIsEmpty(syncPoint) {
    return syncPoint.views.size === 0;
}
function syncPointApplyOperation(syncPoint, operation, writesCache, optCompleteServerCache) {
    const queryId = operation.source.queryId;
    if (queryId !== null) {
        const view = syncPoint.views.get(queryId);
        util.assert(view != null, 'SyncTree gave us an op for an invalid query.');
        return viewApplyOperation(view, operation, writesCache, optCompleteServerCache);
    }
    else {
        let events = [];
        for (const view of syncPoint.views.values()) {
            events = events.concat(viewApplyOperation(view, operation, writesCache, optCompleteServerCache));
        }
        return events;
    }
}
/**
 * Get a view for the specified query.
 *
 * @param query - The query to return a view for
 * @param writesCache
 * @param serverCache
 * @param serverCacheComplete
 * @returns Events to raise.
 */
function syncPointGetView(syncPoint, query, writesCache, serverCache, serverCacheComplete) {
    const queryId = query._queryIdentifier;
    const view = syncPoint.views.get(queryId);
    if (!view) {
        // TODO: make writesCache take flag for complete server node
        let eventCache = writeTreeRefCalcCompleteEventCache(writesCache, serverCacheComplete ? serverCache : null);
        let eventCacheComplete = false;
        if (eventCache) {
            eventCacheComplete = true;
        }
        else if (serverCache instanceof ChildrenNode) {
            eventCache = writeTreeRefCalcCompleteEventChildren(writesCache, serverCache);
            eventCacheComplete = false;
        }
        else {
            eventCache = ChildrenNode.EMPTY_NODE;
            eventCacheComplete = false;
        }
        const viewCache = newViewCache(new CacheNode(eventCache, eventCacheComplete, false), new CacheNode(serverCache, serverCacheComplete, false));
        return new View(query, viewCache);
    }
    return view;
}
/**
 * Add an event callback for the specified query.
 *
 * @param query
 * @param eventRegistration
 * @param writesCache
 * @param serverCache - Complete server cache, if we have it.
 * @param serverCacheComplete
 * @returns Events to raise.
 */
function syncPointAddEventRegistration(syncPoint, query, eventRegistration, writesCache, serverCache, serverCacheComplete) {
    const view = syncPointGetView(syncPoint, query, writesCache, serverCache, serverCacheComplete);
    if (!syncPoint.views.has(query._queryIdentifier)) {
        syncPoint.views.set(query._queryIdentifier, view);
    }
    // This is guaranteed to exist now, we just created anything that was missing
    viewAddEventRegistration(view, eventRegistration);
    return viewGetInitialEvents(view, eventRegistration);
}
/**
 * Remove event callback(s).  Return cancelEvents if a cancelError is specified.
 *
 * If query is the default query, we'll check all views for the specified eventRegistration.
 * If eventRegistration is null, we'll remove all callbacks for the specified view(s).
 *
 * @param eventRegistration - If null, remove all callbacks.
 * @param cancelError - If a cancelError is provided, appropriate cancel events will be returned.
 * @returns removed queries and any cancel events
 */
function syncPointRemoveEventRegistration(syncPoint, query, eventRegistration, cancelError) {
    const queryId = query._queryIdentifier;
    const removed = [];
    let cancelEvents = [];
    const hadCompleteView = syncPointHasCompleteView(syncPoint);
    if (queryId === 'default') {
        // When you do ref.off(...), we search all views for the registration to remove.
        for (const [viewQueryId, view] of syncPoint.views.entries()) {
            cancelEvents = cancelEvents.concat(viewRemoveEventRegistration(view, eventRegistration, cancelError));
            if (viewIsEmpty(view)) {
                syncPoint.views.delete(viewQueryId);
                // We'll deal with complete views later.
                if (!view.query._queryParams.loadsAllData()) {
                    removed.push(view.query);
                }
            }
        }
    }
    else {
        // remove the callback from the specific view.
        const view = syncPoint.views.get(queryId);
        if (view) {
            cancelEvents = cancelEvents.concat(viewRemoveEventRegistration(view, eventRegistration, cancelError));
            if (viewIsEmpty(view)) {
                syncPoint.views.delete(queryId);
                // We'll deal with complete views later.
                if (!view.query._queryParams.loadsAllData()) {
                    removed.push(view.query);
                }
            }
        }
    }
    if (hadCompleteView && !syncPointHasCompleteView(syncPoint)) {
        // We removed our last complete view.
        removed.push(new (syncPointGetReferenceConstructor())(query._repo, query._path));
    }
    return { removed, events: cancelEvents };
}
function syncPointGetQueryViews(syncPoint) {
    const result = [];
    for (const view of syncPoint.views.values()) {
        if (!view.query._queryParams.loadsAllData()) {
            result.push(view);
        }
    }
    return result;
}
/**
 * @param path - The path to the desired complete snapshot
 * @returns A complete cache, if it exists
 */
function syncPointGetCompleteServerCache(syncPoint, path) {
    let serverCache = null;
    for (const view of syncPoint.views.values()) {
        serverCache = serverCache || viewGetCompleteServerCache(view, path);
    }
    return serverCache;
}
function syncPointViewForQuery(syncPoint, query) {
    const params = query._queryParams;
    if (params.loadsAllData()) {
        return syncPointGetCompleteView(syncPoint);
    }
    else {
        const queryId = query._queryIdentifier;
        return syncPoint.views.get(queryId);
    }
}
function syncPointViewExistsForQuery(syncPoint, query) {
    return syncPointViewForQuery(syncPoint, query) != null;
}
function syncPointHasCompleteView(syncPoint) {
    return syncPointGetCompleteView(syncPoint) != null;
}
function syncPointGetCompleteView(syncPoint) {
    for (const view of syncPoint.views.values()) {
        if (view.query._queryParams.loadsAllData()) {
            return view;
        }
    }
    return null;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
let referenceConstructor;
function syncTreeSetReferenceConstructor(val) {
    util.assert(!referenceConstructor, '__referenceConstructor has already been defined');
    referenceConstructor = val;
}
function syncTreeGetReferenceConstructor() {
    util.assert(referenceConstructor, 'Reference.ts has not been loaded');
    return referenceConstructor;
}
/**
 * Static tracker for next query tag.
 */
let syncTreeNextQueryTag_ = 1;
/**
 * SyncTree is the central class for managing event callback registration, data caching, views
 * (query processing), and event generation.  There are typically two SyncTree instances for
 * each Repo, one for the normal Firebase data, and one for the .info data.
 *
 * It has a number of responsibilities, including:
 *  - Tracking all user event callbacks (registered via addEventRegistration() and removeEventRegistration()).
 *  - Applying and caching data changes for user set(), transaction(), and update() calls
 *    (applyUserOverwrite(), applyUserMerge()).
 *  - Applying and caching data changes for server data changes (applyServerOverwrite(),
 *    applyServerMerge()).
 *  - Generating user-facing events for server and user changes (all of the apply* methods
 *    return the set of events that need to be raised as a result).
 *  - Maintaining the appropriate set of server listens to ensure we are always subscribed
 *    to the correct set of paths and queries to satisfy the current set of user event
 *    callbacks (listens are started/stopped using the provided listenProvider).
 *
 * NOTE: Although SyncTree tracks event callbacks and calculates events to raise, the actual
 * events are returned to the caller rather than raised synchronously.
 *
 */
class SyncTree {
    /**
     * @param listenProvider_ - Used by SyncTree to start / stop listening
     *   to server data.
     */
    constructor(listenProvider_) {
        this.listenProvider_ = listenProvider_;
        /**
         * Tree of SyncPoints.  There's a SyncPoint at any location that has 1 or more views.
         */
        this.syncPointTree_ = new ImmutableTree(null);
        /**
         * A tree of all pending user writes (user-initiated set()'s, transaction()'s, update()'s, etc.).
         */
        this.pendingWriteTree_ = newWriteTree();
        this.tagToQueryMap = new Map();
        this.queryToTagMap = new Map();
    }
}
/**
 * Apply the data changes for a user-generated set() or transaction() call.
 *
 * @returns Events to raise.
 */
function syncTreeApplyUserOverwrite(syncTree, path, newData, writeId, visible) {
    // Record pending write.
    writeTreeAddOverwrite(syncTree.pendingWriteTree_, path, newData, writeId, visible);
    if (!visible) {
        return [];
    }
    else {
        return syncTreeApplyOperationToSyncPoints_(syncTree, new Overwrite(newOperationSourceUser(), path, newData));
    }
}
/**
 * Apply the data from a user-generated update() call
 *
 * @returns Events to raise.
 */
function syncTreeApplyUserMerge(syncTree, path, changedChildren, writeId) {
    // Record pending merge.
    writeTreeAddMerge(syncTree.pendingWriteTree_, path, changedChildren, writeId);
    const changeTree = ImmutableTree.fromObject(changedChildren);
    return syncTreeApplyOperationToSyncPoints_(syncTree, new Merge(newOperationSourceUser(), path, changeTree));
}
/**
 * Acknowledge a pending user write that was previously registered with applyUserOverwrite() or applyUserMerge().
 *
 * @param revert - True if the given write failed and needs to be reverted
 * @returns Events to raise.
 */
function syncTreeAckUserWrite(syncTree, writeId, revert = false) {
    const write = writeTreeGetWrite(syncTree.pendingWriteTree_, writeId);
    const needToReevaluate = writeTreeRemoveWrite(syncTree.pendingWriteTree_, writeId);
    if (!needToReevaluate) {
        return [];
    }
    else {
        let affectedTree = new ImmutableTree(null);
        if (write.snap != null) {
            // overwrite
            affectedTree = affectedTree.set(newEmptyPath(), true);
        }
        else {
            each(write.children, (pathString) => {
                affectedTree = affectedTree.set(new Path(pathString), true);
            });
        }
        return syncTreeApplyOperationToSyncPoints_(syncTree, new AckUserWrite(write.path, affectedTree, revert));
    }
}
/**
 * Apply new server data for the specified path..
 *
 * @returns Events to raise.
 */
function syncTreeApplyServerOverwrite(syncTree, path, newData) {
    return syncTreeApplyOperationToSyncPoints_(syncTree, new Overwrite(newOperationSourceServer(), path, newData));
}
/**
 * Apply new server data to be merged in at the specified path.
 *
 * @returns Events to raise.
 */
function syncTreeApplyServerMerge(syncTree, path, changedChildren) {
    const changeTree = ImmutableTree.fromObject(changedChildren);
    return syncTreeApplyOperationToSyncPoints_(syncTree, new Merge(newOperationSourceServer(), path, changeTree));
}
/**
 * Apply a listen complete for a query
 *
 * @returns Events to raise.
 */
function syncTreeApplyListenComplete(syncTree, path) {
    return syncTreeApplyOperationToSyncPoints_(syncTree, new ListenComplete(newOperationSourceServer(), path));
}
/**
 * Apply a listen complete for a tagged query
 *
 * @returns Events to raise.
 */
function syncTreeApplyTaggedListenComplete(syncTree, path, tag) {
    const queryKey = syncTreeQueryKeyForTag_(syncTree, tag);
    if (queryKey) {
        const r = syncTreeParseQueryKey_(queryKey);
        const queryPath = r.path, queryId = r.queryId;
        const relativePath = newRelativePath(queryPath, path);
        const op = new ListenComplete(newOperationSourceServerTaggedQuery(queryId), relativePath);
        return syncTreeApplyTaggedOperation_(syncTree, queryPath, op);
    }
    else {
        // We've already removed the query. No big deal, ignore the update
        return [];
    }
}
/**
 * Remove event callback(s).
 *
 * If query is the default query, we'll check all queries for the specified eventRegistration.
 * If eventRegistration is null, we'll remove all callbacks for the specified query/queries.
 *
 * @param eventRegistration - If null, all callbacks are removed.
 * @param cancelError - If a cancelError is provided, appropriate cancel events will be returned.
 * @param skipListenerDedup - When performing a `get()`, we don't add any new listeners, so no
 *  deduping needs to take place. This flag allows toggling of that behavior
 * @returns Cancel events, if cancelError was provided.
 */
function syncTreeRemoveEventRegistration(syncTree, query, eventRegistration, cancelError, skipListenerDedup = false) {
    // Find the syncPoint first. Then deal with whether or not it has matching listeners
    const path = query._path;
    const maybeSyncPoint = syncTree.syncPointTree_.get(path);
    let cancelEvents = [];
    // A removal on a default query affects all queries at that location. A removal on an indexed query, even one without
    // other query constraints, does *not* affect all queries at that location. So this check must be for 'default', and
    // not loadsAllData().
    if (maybeSyncPoint &&
        (query._queryIdentifier === 'default' ||
            syncPointViewExistsForQuery(maybeSyncPoint, query))) {
        const removedAndEvents = syncPointRemoveEventRegistration(maybeSyncPoint, query, eventRegistration, cancelError);
        if (syncPointIsEmpty(maybeSyncPoint)) {
            syncTree.syncPointTree_ = syncTree.syncPointTree_.remove(path);
        }
        const removed = removedAndEvents.removed;
        cancelEvents = removedAndEvents.events;
        if (!skipListenerDedup) {
            /**
             * We may have just removed one of many listeners and can short-circuit this whole process
             * We may also not have removed a default listener, in which case all of the descendant listeners should already be
             * properly set up.
             */
            // Since indexed queries can shadow if they don't have other query constraints, check for loadsAllData(), instead of
            // queryId === 'default'
            const removingDefault = -1 !==
                removed.findIndex(query => {
                    return query._queryParams.loadsAllData();
                });
            const covered = syncTree.syncPointTree_.findOnPath(path, (relativePath, parentSyncPoint) => syncPointHasCompleteView(parentSyncPoint));
            if (removingDefault && !covered) {
                const subtree = syncTree.syncPointTree_.subtree(path);
                // There are potentially child listeners. Determine what if any listens we need to send before executing the
                // removal
                if (!subtree.isEmpty()) {
                    // We need to fold over our subtree and collect the listeners to send
                    const newViews = syncTreeCollectDistinctViewsForSubTree_(subtree);
                    // Ok, we've collected all the listens we need. Set them up.
                    for (let i = 0; i < newViews.length; ++i) {
                        const view = newViews[i], newQuery = view.query;
                        const listener = syncTreeCreateListenerForView_(syncTree, view);
                        syncTree.listenProvider_.startListening(syncTreeQueryForListening_(newQuery), syncTreeTagForQuery(syncTree, newQuery), listener.hashFn, listener.onComplete);
                    }
                }
                // Otherwise there's nothing below us, so nothing we need to start listening on
            }
            // If we removed anything and we're not covered by a higher up listen, we need to stop listening on this query
            // The above block has us covered in terms of making sure we're set up on listens lower in the tree.
            // Also, note that if we have a cancelError, it's already been removed at the provider level.
            if (!covered && removed.length > 0 && !cancelError) {
                // If we removed a default, then we weren't listening on any of the other queries here. Just cancel the one
                // default. Otherwise, we need to iterate through and cancel each individual query
                if (removingDefault) {
                    // We don't tag default listeners
                    const defaultTag = null;
                    syncTree.listenProvider_.stopListening(syncTreeQueryForListening_(query), defaultTag);
                }
                else {
                    removed.forEach((queryToRemove) => {
                        const tagToRemove = syncTree.queryToTagMap.get(syncTreeMakeQueryKey_(queryToRemove));
                        syncTree.listenProvider_.stopListening(syncTreeQueryForListening_(queryToRemove), tagToRemove);
                    });
                }
            }
        }
        // Now, clear all of the tags we're tracking for the removed listens
        syncTreeRemoveTags_(syncTree, removed);
    }
    return cancelEvents;
}
/**
 * Apply new server data for the specified tagged query.
 *
 * @returns Events to raise.
 */
function syncTreeApplyTaggedQueryOverwrite(syncTree, path, snap, tag) {
    const queryKey = syncTreeQueryKeyForTag_(syncTree, tag);
    if (queryKey != null) {
        const r = syncTreeParseQueryKey_(queryKey);
        const queryPath = r.path, queryId = r.queryId;
        const relativePath = newRelativePath(queryPath, path);
        const op = new Overwrite(newOperationSourceServerTaggedQuery(queryId), relativePath, snap);
        return syncTreeApplyTaggedOperation_(syncTree, queryPath, op);
    }
    else {
        // Query must have been removed already
        return [];
    }
}
/**
 * Apply server data to be merged in for the specified tagged query.
 *
 * @returns Events to raise.
 */
function syncTreeApplyTaggedQueryMerge(syncTree, path, changedChildren, tag) {
    const queryKey = syncTreeQueryKeyForTag_(syncTree, tag);
    if (queryKey) {
        const r = syncTreeParseQueryKey_(queryKey);
        const queryPath = r.path, queryId = r.queryId;
        const relativePath = newRelativePath(queryPath, path);
        const changeTree = ImmutableTree.fromObject(changedChildren);
        const op = new Merge(newOperationSourceServerTaggedQuery(queryId), relativePath, changeTree);
        return syncTreeApplyTaggedOperation_(syncTree, queryPath, op);
    }
    else {
        // We've already removed the query. No big deal, ignore the update
        return [];
    }
}
/**
 * Add an event callback for the specified query.
 *
 * @returns Events to raise.
 */
function syncTreeAddEventRegistration(syncTree, query, eventRegistration, skipSetupListener = false) {
    const path = query._path;
    let serverCache = null;
    let foundAncestorDefaultView = false;
    // Any covering writes will necessarily be at the root, so really all we need to find is the server cache.
    // Consider optimizing this once there's a better understanding of what actual behavior will be.
    syncTree.syncPointTree_.foreachOnPath(path, (pathToSyncPoint, sp) => {
        const relativePath = newRelativePath(pathToSyncPoint, path);
        serverCache =
            serverCache || syncPointGetCompleteServerCache(sp, relativePath);
        foundAncestorDefaultView =
            foundAncestorDefaultView || syncPointHasCompleteView(sp);
    });
    let syncPoint = syncTree.syncPointTree_.get(path);
    if (!syncPoint) {
        syncPoint = new SyncPoint();
        syncTree.syncPointTree_ = syncTree.syncPointTree_.set(path, syncPoint);
    }
    else {
        foundAncestorDefaultView =
            foundAncestorDefaultView || syncPointHasCompleteView(syncPoint);
        serverCache =
            serverCache || syncPointGetCompleteServerCache(syncPoint, newEmptyPath());
    }
    let serverCacheComplete;
    if (serverCache != null) {
        serverCacheComplete = true;
    }
    else {
        serverCacheComplete = false;
        serverCache = ChildrenNode.EMPTY_NODE;
        const subtree = syncTree.syncPointTree_.subtree(path);
        subtree.foreachChild((childName, childSyncPoint) => {
            const completeCache = syncPointGetCompleteServerCache(childSyncPoint, newEmptyPath());
            if (completeCache) {
                serverCache = serverCache.updateImmediateChild(childName, completeCache);
            }
        });
    }
    const viewAlreadyExists = syncPointViewExistsForQuery(syncPoint, query);
    if (!viewAlreadyExists && !query._queryParams.loadsAllData()) {
        // We need to track a tag for this query
        const queryKey = syncTreeMakeQueryKey_(query);
        util.assert(!syncTree.queryToTagMap.has(queryKey), 'View does not exist, but we have a tag');
        const tag = syncTreeGetNextQueryTag_();
        syncTree.queryToTagMap.set(queryKey, tag);
        syncTree.tagToQueryMap.set(tag, queryKey);
    }
    const writesCache = writeTreeChildWrites(syncTree.pendingWriteTree_, path);
    let events = syncPointAddEventRegistration(syncPoint, query, eventRegistration, writesCache, serverCache, serverCacheComplete);
    if (!viewAlreadyExists && !foundAncestorDefaultView && !skipSetupListener) {
        const view = syncPointViewForQuery(syncPoint, query);
        events = events.concat(syncTreeSetupListener_(syncTree, query, view));
    }
    return events;
}
/**
 * Returns a complete cache, if we have one, of the data at a particular path. If the location does not have a
 * listener above it, we will get a false "null". This shouldn't be a problem because transactions will always
 * have a listener above, and atomic operations would correctly show a jitter of <increment value> ->
 *     <incremented total> as the write is applied locally and then acknowledged at the server.
 *
 * Note: this method will *include* hidden writes from transaction with applyLocally set to false.
 *
 * @param path - The path to the data we want
 * @param writeIdsToExclude - A specific set to be excluded
 */
function syncTreeCalcCompleteEventCache(syncTree, path, writeIdsToExclude) {
    const includeHiddenSets = true;
    const writeTree = syncTree.pendingWriteTree_;
    const serverCache = syncTree.syncPointTree_.findOnPath(path, (pathSoFar, syncPoint) => {
        const relativePath = newRelativePath(pathSoFar, path);
        const serverCache = syncPointGetCompleteServerCache(syncPoint, relativePath);
        if (serverCache) {
            return serverCache;
        }
    });
    return writeTreeCalcCompleteEventCache(writeTree, path, serverCache, writeIdsToExclude, includeHiddenSets);
}
function syncTreeGetServerValue(syncTree, query) {
    const path = query._path;
    let serverCache = null;
    // Any covering writes will necessarily be at the root, so really all we need to find is the server cache.
    // Consider optimizing this once there's a better understanding of what actual behavior will be.
    syncTree.syncPointTree_.foreachOnPath(path, (pathToSyncPoint, sp) => {
        const relativePath = newRelativePath(pathToSyncPoint, path);
        serverCache =
            serverCache || syncPointGetCompleteServerCache(sp, relativePath);
    });
    let syncPoint = syncTree.syncPointTree_.get(path);
    if (!syncPoint) {
        syncPoint = new SyncPoint();
        syncTree.syncPointTree_ = syncTree.syncPointTree_.set(path, syncPoint);
    }
    else {
        serverCache =
            serverCache || syncPointGetCompleteServerCache(syncPoint, newEmptyPath());
    }
    const serverCacheComplete = serverCache != null;
    const serverCacheNode = serverCacheComplete
        ? new CacheNode(serverCache, true, false)
        : null;
    const writesCache = writeTreeChildWrites(syncTree.pendingWriteTree_, query._path);
    const view = syncPointGetView(syncPoint, query, writesCache, serverCacheComplete ? serverCacheNode.getNode() : ChildrenNode.EMPTY_NODE, serverCacheComplete);
    return viewGetCompleteNode(view);
}
/**
 * A helper method that visits all descendant and ancestor SyncPoints, applying the operation.
 *
 * NOTES:
 * - Descendant SyncPoints will be visited first (since we raise events depth-first).
 *
 * - We call applyOperation() on each SyncPoint passing three things:
 *   1. A version of the Operation that has been made relative to the SyncPoint location.
 *   2. A WriteTreeRef of any writes we have cached at the SyncPoint location.
 *   3. A snapshot Node with cached server data, if we have it.
 *
 * - We concatenate all of the events returned by each SyncPoint and return the result.
 */
function syncTreeApplyOperationToSyncPoints_(syncTree, operation) {
    return syncTreeApplyOperationHelper_(operation, syncTree.syncPointTree_, 
    /*serverCache=*/ null, writeTreeChildWrites(syncTree.pendingWriteTree_, newEmptyPath()));
}
/**
 * Recursive helper for applyOperationToSyncPoints_
 */
function syncTreeApplyOperationHelper_(operation, syncPointTree, serverCache, writesCache) {
    if (pathIsEmpty(operation.path)) {
        return syncTreeApplyOperationDescendantsHelper_(operation, syncPointTree, serverCache, writesCache);
    }
    else {
        const syncPoint = syncPointTree.get(newEmptyPath());
        // If we don't have cached server data, see if we can get it from this SyncPoint.
        if (serverCache == null && syncPoint != null) {
            serverCache = syncPointGetCompleteServerCache(syncPoint, newEmptyPath());
        }
        let events = [];
        const childName = pathGetFront(operation.path);
        const childOperation = operation.operationForChild(childName);
        const childTree = syncPointTree.children.get(childName);
        if (childTree && childOperation) {
            const childServerCache = serverCache
                ? serverCache.getImmediateChild(childName)
                : null;
            const childWritesCache = writeTreeRefChild(writesCache, childName);
            events = events.concat(syncTreeApplyOperationHelper_(childOperation, childTree, childServerCache, childWritesCache));
        }
        if (syncPoint) {
            events = events.concat(syncPointApplyOperation(syncPoint, operation, writesCache, serverCache));
        }
        return events;
    }
}
/**
 * Recursive helper for applyOperationToSyncPoints_
 */
function syncTreeApplyOperationDescendantsHelper_(operation, syncPointTree, serverCache, writesCache) {
    const syncPoint = syncPointTree.get(newEmptyPath());
    // If we don't have cached server data, see if we can get it from this SyncPoint.
    if (serverCache == null && syncPoint != null) {
        serverCache = syncPointGetCompleteServerCache(syncPoint, newEmptyPath());
    }
    let events = [];
    syncPointTree.children.inorderTraversal((childName, childTree) => {
        const childServerCache = serverCache
            ? serverCache.getImmediateChild(childName)
            : null;
        const childWritesCache = writeTreeRefChild(writesCache, childName);
        const childOperation = operation.operationForChild(childName);
        if (childOperation) {
            events = events.concat(syncTreeApplyOperationDescendantsHelper_(childOperation, childTree, childServerCache, childWritesCache));
        }
    });
    if (syncPoint) {
        events = events.concat(syncPointApplyOperation(syncPoint, operation, writesCache, serverCache));
    }
    return events;
}
function syncTreeCreateListenerForView_(syncTree, view) {
    const query = view.query;
    const tag = syncTreeTagForQuery(syncTree, query);
    return {
        hashFn: () => {
            const cache = viewGetServerCache(view) || ChildrenNode.EMPTY_NODE;
            return cache.hash();
        },
        onComplete: (status) => {
            if (status === 'ok') {
                if (tag) {
                    return syncTreeApplyTaggedListenComplete(syncTree, query._path, tag);
                }
                else {
                    return syncTreeApplyListenComplete(syncTree, query._path);
                }
            }
            else {
                // If a listen failed, kill all of the listeners here, not just the one that triggered the error.
                // Note that this may need to be scoped to just this listener if we change permissions on filtered children
                const error = errorForServerCode(status, query);
                return syncTreeRemoveEventRegistration(syncTree, query, 
                /*eventRegistration*/ null, error);
            }
        }
    };
}
/**
 * Return the tag associated with the given query.
 */
function syncTreeTagForQuery(syncTree, query) {
    const queryKey = syncTreeMakeQueryKey_(query);
    return syncTree.queryToTagMap.get(queryKey);
}
/**
 * Given a query, computes a "queryKey" suitable for use in our queryToTagMap_.
 */
function syncTreeMakeQueryKey_(query) {
    return query._path.toString() + '$' + query._queryIdentifier;
}
/**
 * Return the query associated with the given tag, if we have one
 */
function syncTreeQueryKeyForTag_(syncTree, tag) {
    return syncTree.tagToQueryMap.get(tag);
}
/**
 * Given a queryKey (created by makeQueryKey), parse it back into a path and queryId.
 */
function syncTreeParseQueryKey_(queryKey) {
    const splitIndex = queryKey.indexOf('$');
    util.assert(splitIndex !== -1 && splitIndex < queryKey.length - 1, 'Bad queryKey.');
    return {
        queryId: queryKey.substr(splitIndex + 1),
        path: new Path(queryKey.substr(0, splitIndex))
    };
}
/**
 * A helper method to apply tagged operations
 */
function syncTreeApplyTaggedOperation_(syncTree, queryPath, operation) {
    const syncPoint = syncTree.syncPointTree_.get(queryPath);
    util.assert(syncPoint, "Missing sync point for query tag that we're tracking");
    const writesCache = writeTreeChildWrites(syncTree.pendingWriteTree_, queryPath);
    return syncPointApplyOperation(syncPoint, operation, writesCache, null);
}
/**
 * This collapses multiple unfiltered views into a single view, since we only need a single
 * listener for them.
 */
function syncTreeCollectDistinctViewsForSubTree_(subtree) {
    return subtree.fold((relativePath, maybeChildSyncPoint, childMap) => {
        if (maybeChildSyncPoint && syncPointHasCompleteView(maybeChildSyncPoint)) {
            const completeView = syncPointGetCompleteView(maybeChildSyncPoint);
            return [completeView];
        }
        else {
            // No complete view here, flatten any deeper listens into an array
            let views = [];
            if (maybeChildSyncPoint) {
                views = syncPointGetQueryViews(maybeChildSyncPoint);
            }
            each(childMap, (_key, childViews) => {
                views = views.concat(childViews);
            });
            return views;
        }
    });
}
/**
 * Normalizes a query to a query we send the server for listening
 *
 * @returns The normalized query
 */
function syncTreeQueryForListening_(query) {
    if (query._queryParams.loadsAllData() && !query._queryParams.isDefault()) {
        // We treat queries that load all data as default queries
        // Cast is necessary because ref() technically returns Firebase which is actually fb.api.Firebase which inherits
        // from Query
        return new (syncTreeGetReferenceConstructor())(query._repo, query._path);
    }
    else {
        return query;
    }
}
function syncTreeRemoveTags_(syncTree, queries) {
    for (let j = 0; j < queries.length; ++j) {
        const removedQuery = queries[j];
        if (!removedQuery._queryParams.loadsAllData()) {
            // We should have a tag for this
            const removedQueryKey = syncTreeMakeQueryKey_(removedQuery);
            const removedQueryTag = syncTree.queryToTagMap.get(removedQueryKey);
            syncTree.queryToTagMap.delete(removedQueryKey);
            syncTree.tagToQueryMap.delete(removedQueryTag);
        }
    }
}
/**
 * Static accessor for query tags.
 */
function syncTreeGetNextQueryTag_() {
    return syncTreeNextQueryTag_++;
}
/**
 * For a given new listen, manage the de-duplication of outstanding subscriptions.
 *
 * @returns This method can return events to support synchronous data sources
 */
function syncTreeSetupListener_(syncTree, query, view) {
    const path = query._path;
    const tag = syncTreeTagForQuery(syncTree, query);
    const listener = syncTreeCreateListenerForView_(syncTree, view);
    const events = syncTree.listenProvider_.startListening(syncTreeQueryForListening_(query), tag, listener.hashFn, listener.onComplete);
    const subtree = syncTree.syncPointTree_.subtree(path);
    // The root of this subtree has our query. We're here because we definitely need to send a listen for that, but we
    // may need to shadow other listens as well.
    if (tag) {
        util.assert(!syncPointHasCompleteView(subtree.value), "If we're adding a query, it shouldn't be shadowed");
    }
    else {
        // Shadow everything at or below this location, this is a default listener.
        const queriesToStop = subtree.fold((relativePath, maybeChildSyncPoint, childMap) => {
            if (!pathIsEmpty(relativePath) &&
                maybeChildSyncPoint &&
                syncPointHasCompleteView(maybeChildSyncPoint)) {
                return [syncPointGetCompleteView(maybeChildSyncPoint).query];
            }
            else {
                // No default listener here, flatten any deeper queries into an array
                let queries = [];
                if (maybeChildSyncPoint) {
                    queries = queries.concat(syncPointGetQueryViews(maybeChildSyncPoint).map(view => view.query));
                }
                each(childMap, (_key, childQueries) => {
                    queries = queries.concat(childQueries);
                });
                return queries;
            }
        });
        for (let i = 0; i < queriesToStop.length; ++i) {
            const queryToStop = queriesToStop[i];
            syncTree.listenProvider_.stopListening(syncTreeQueryForListening_(queryToStop), syncTreeTagForQuery(syncTree, queryToStop));
        }
    }
    return events;
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class ExistingValueProvider {
    constructor(node_) {
        this.node_ = node_;
    }
    getImmediateChild(childName) {
        const child = this.node_.getImmediateChild(childName);
        return new ExistingValueProvider(child);
    }
    node() {
        return this.node_;
    }
}
class DeferredValueProvider {
    constructor(syncTree, path) {
        this.syncTree_ = syncTree;
        this.path_ = path;
    }
    getImmediateChild(childName) {
        const childPath = pathChild(this.path_, childName);
        return new DeferredValueProvider(this.syncTree_, childPath);
    }
    node() {
        return syncTreeCalcCompleteEventCache(this.syncTree_, this.path_);
    }
}
/**
 * Generate placeholders for deferred values.
 */
const generateWithValues = function (values) {
    values = values || {};
    values['timestamp'] = values['timestamp'] || new Date().getTime();
    return values;
};
/**
 * Value to use when firing local events. When writing server values, fire
 * local events with an approximate value, otherwise return value as-is.
 */
const resolveDeferredLeafValue = function (value, existingVal, serverValues) {
    if (!value || typeof value !== 'object') {
        return value;
    }
    util.assert('.sv' in value, 'Unexpected leaf node or priority contents');
    if (typeof value['.sv'] === 'string') {
        return resolveScalarDeferredValue(value['.sv'], existingVal, serverValues);
    }
    else if (typeof value['.sv'] === 'object') {
        return resolveComplexDeferredValue(value['.sv'], existingVal);
    }
    else {
        util.assert(false, 'Unexpected server value: ' + JSON.stringify(value, null, 2));
    }
};
const resolveScalarDeferredValue = function (op, existing, serverValues) {
    switch (op) {
        case 'timestamp':
            return serverValues['timestamp'];
        default:
            util.assert(false, 'Unexpected server value: ' + op);
    }
};
const resolveComplexDeferredValue = function (op, existing, unused) {
    if (!op.hasOwnProperty('increment')) {
        util.assert(false, 'Unexpected server value: ' + JSON.stringify(op, null, 2));
    }
    const delta = op['increment'];
    if (typeof delta !== 'number') {
        util.assert(false, 'Unexpected increment value: ' + delta);
    }
    const existingNode = existing.node();
    util.assert(existingNode !== null && typeof existingNode !== 'undefined', 'Expected ChildrenNode.EMPTY_NODE for nulls');
    // Incrementing a non-number sets the value to the incremented amount
    if (!existingNode.isLeafNode()) {
        return delta;
    }
    const leaf = existingNode;
    const existingVal = leaf.getValue();
    if (typeof existingVal !== 'number') {
        return delta;
    }
    // No need to do over/underflow arithmetic here because JS only handles floats under the covers
    return existingVal + delta;
};
/**
 * Recursively replace all deferred values and priorities in the tree with the
 * specified generated replacement values.
 * @param path - path to which write is relative
 * @param node - new data written at path
 * @param syncTree - current data
 */
const resolveDeferredValueTree = function (path, node, syncTree, serverValues) {
    return resolveDeferredValue(node, new DeferredValueProvider(syncTree, path), serverValues);
};
/**
 * Recursively replace all deferred values and priorities in the node with the
 * specified generated replacement values.  If there are no server values in the node,
 * it'll be returned as-is.
 */
const resolveDeferredValueSnapshot = function (node, existing, serverValues) {
    return resolveDeferredValue(node, new ExistingValueProvider(existing), serverValues);
};
function resolveDeferredValue(node, existingVal, serverValues) {
    const rawPri = node.getPriority().val();
    const priority = resolveDeferredLeafValue(rawPri, existingVal.getImmediateChild('.priority'), serverValues);
    let newNode;
    if (node.isLeafNode()) {
        const leafNode = node;
        const value = resolveDeferredLeafValue(leafNode.getValue(), existingVal, serverValues);
        if (value !== leafNode.getValue() ||
            priority !== leafNode.getPriority().val()) {
            return new LeafNode(value, nodeFromJSON(priority));
        }
        else {
            return node;
        }
    }
    else {
        const childrenNode = node;
        newNode = childrenNode;
        if (priority !== childrenNode.getPriority().val()) {
            newNode = newNode.updatePriority(new LeafNode(priority));
        }
        childrenNode.forEachChild(PRIORITY_INDEX, (childName, childNode) => {
            const newChildNode = resolveDeferredValue(childNode, existingVal.getImmediateChild(childName), serverValues);
            if (newChildNode !== childNode) {
                newNode = newNode.updateImmediateChild(childName, newChildNode);
            }
        });
        return newNode;
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A light-weight tree, traversable by path.  Nodes can have both values and children.
 * Nodes are not enumerated (by forEachChild) unless they have a value or non-empty
 * children.
 */
class Tree {
    /**
     * @param name - Optional name of the node.
     * @param parent - Optional parent node.
     * @param node - Optional node to wrap.
     */
    constructor(name = '', parent = null, node = { children: {}, childCount: 0 }) {
        this.name = name;
        this.parent = parent;
        this.node = node;
    }
}
/**
 * Returns a sub-Tree for the given path.
 *
 * @param pathObj - Path to look up.
 * @returns Tree for path.
 */
function treeSubTree(tree, pathObj) {
    // TODO: Require pathObj to be Path?
    let path = pathObj instanceof Path ? pathObj : new Path(pathObj);
    let child = tree, next = pathGetFront(path);
    while (next !== null) {
        const childNode = util.safeGet(child.node.children, next) || {
            children: {},
            childCount: 0
        };
        child = new Tree(next, child, childNode);
        path = pathPopFront(path);
        next = pathGetFront(path);
    }
    return child;
}
/**
 * Returns the data associated with this tree node.
 *
 * @returns The data or null if no data exists.
 */
function treeGetValue(tree) {
    return tree.node.value;
}
/**
 * Sets data to this tree node.
 *
 * @param value - Value to set.
 */
function treeSetValue(tree, value) {
    tree.node.value = value;
    treeUpdateParents(tree);
}
/**
 * @returns Whether the tree has any children.
 */
function treeHasChildren(tree) {
    return tree.node.childCount > 0;
}
/**
 * @returns Whethe rthe tree is empty (no value or children).
 */
function treeIsEmpty(tree) {
    return treeGetValue(tree) === undefined && !treeHasChildren(tree);
}
/**
 * Calls action for each child of this tree node.
 *
 * @param action - Action to be called for each child.
 */
function treeForEachChild(tree, action) {
    each(tree.node.children, (child, childTree) => {
        action(new Tree(child, tree, childTree));
    });
}
/**
 * Does a depth-first traversal of this node's descendants, calling action for each one.
 *
 * @param action - Action to be called for each child.
 * @param includeSelf - Whether to call action on this node as well. Defaults to
 *   false.
 * @param childrenFirst - Whether to call action on children before calling it on
 *   parent.
 */
function treeForEachDescendant(tree, action, includeSelf, childrenFirst) {
    if (includeSelf && !childrenFirst) {
        action(tree);
    }
    treeForEachChild(tree, child => {
        treeForEachDescendant(child, action, true, childrenFirst);
    });
    if (includeSelf && childrenFirst) {
        action(tree);
    }
}
/**
 * Calls action on each ancestor node.
 *
 * @param action - Action to be called on each parent; return
 *   true to abort.
 * @param includeSelf - Whether to call action on this node as well.
 * @returns true if the action callback returned true.
 */
function treeForEachAncestor(tree, action, includeSelf) {
    let node = includeSelf ? tree : tree.parent;
    while (node !== null) {
        if (action(node)) {
            return true;
        }
        node = node.parent;
    }
    return false;
}
/**
 * @returns The path of this tree node, as a Path.
 */
function treeGetPath(tree) {
    return new Path(tree.parent === null
        ? tree.name
        : treeGetPath(tree.parent) + '/' + tree.name);
}
/**
 * Adds or removes this child from its parent based on whether it's empty or not.
 */
function treeUpdateParents(tree) {
    if (tree.parent !== null) {
        treeUpdateChild(tree.parent, tree.name, tree);
    }
}
/**
 * Adds or removes the passed child to this tree node, depending on whether it's empty.
 *
 * @param childName - The name of the child to update.
 * @param child - The child to update.
 */
function treeUpdateChild(tree, childName, child) {
    const childEmpty = treeIsEmpty(child);
    const childExists = util.contains(tree.node.children, childName);
    if (childEmpty && childExists) {
        delete tree.node.children[childName];
        tree.node.childCount--;
        treeUpdateParents(tree);
    }
    else if (!childEmpty && !childExists) {
        tree.node.children[childName] = child.node;
        tree.node.childCount++;
        treeUpdateParents(tree);
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * True for invalid Firebase keys
 */
const INVALID_KEY_REGEX_ = /[\[\].#$\/\u0000-\u001F\u007F]/;
/**
 * True for invalid Firebase paths.
 * Allows '/' in paths.
 */
const INVALID_PATH_REGEX_ = /[\[\].#$\u0000-\u001F\u007F]/;
/**
 * Maximum number of characters to allow in leaf value
 */
const MAX_LEAF_SIZE_ = 10 * 1024 * 1024;
const isValidKey = function (key) {
    return (typeof key === 'string' && key.length !== 0 && !INVALID_KEY_REGEX_.test(key));
};
const isValidPathString = function (pathString) {
    return (typeof pathString === 'string' &&
        pathString.length !== 0 &&
        !INVALID_PATH_REGEX_.test(pathString));
};
const isValidRootPathString = function (pathString) {
    if (pathString) {
        // Allow '/.info/' at the beginning.
        pathString = pathString.replace(/^\/*\.info(\/|$)/, '/');
    }
    return isValidPathString(pathString);
};
const isValidPriority = function (priority) {
    return (priority === null ||
        typeof priority === 'string' ||
        (typeof priority === 'number' && !isInvalidJSONNumber(priority)) ||
        (priority &&
            typeof priority === 'object' &&
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            util.contains(priority, '.sv')));
};
/**
 * Pre-validate a datum passed as an argument to Firebase function.
 */
const validateFirebaseDataArg = function (fnName, value, path, optional) {
    if (optional && value === undefined) {
        return;
    }
    validateFirebaseData(util.errorPrefix(fnName, 'value'), value, path);
};
/**
 * Validate a data object client-side before sending to server.
 */
const validateFirebaseData = function (errorPrefix, data, path_) {
    const path = path_ instanceof Path ? new ValidationPath(path_, errorPrefix) : path_;
    if (data === undefined) {
        throw new Error(errorPrefix + 'contains undefined ' + validationPathToErrorString(path));
    }
    if (typeof data === 'function') {
        throw new Error(errorPrefix +
            'contains a function ' +
            validationPathToErrorString(path) +
            ' with contents = ' +
            data.toString());
    }
    if (isInvalidJSONNumber(data)) {
        throw new Error(errorPrefix +
            'contains ' +
            data.toString() +
            ' ' +
            validationPathToErrorString(path));
    }
    // Check max leaf size, but try to avoid the utf8 conversion if we can.
    if (typeof data === 'string' &&
        data.length > MAX_LEAF_SIZE_ / 3 &&
        util.stringLength(data) > MAX_LEAF_SIZE_) {
        throw new Error(errorPrefix +
            'contains a string greater than ' +
            MAX_LEAF_SIZE_ +
            ' utf8 bytes ' +
            validationPathToErrorString(path) +
            " ('" +
            data.substring(0, 50) +
            "...')");
    }
    // TODO = Perf = Consider combining the recursive validation of keys into NodeFromJSON
    // to save extra walking of large objects.
    if (data && typeof data === 'object') {
        let hasDotValue = false;
        let hasActualChild = false;
        each(data, (key, value) => {
            if (key === '.value') {
                hasDotValue = true;
            }
            else if (key !== '.priority' && key !== '.sv') {
                hasActualChild = true;
                if (!isValidKey(key)) {
                    throw new Error(errorPrefix +
                        ' contains an invalid key (' +
                        key +
                        ') ' +
                        validationPathToErrorString(path) +
                        '.  Keys must be non-empty strings ' +
                        'and can\'t contain ".", "#", "$", "/", "[", or "]"');
                }
            }
            validationPathPush(path, key);
            validateFirebaseData(errorPrefix, value, path);
            validationPathPop(path);
        });
        if (hasDotValue && hasActualChild) {
            throw new Error(errorPrefix +
                ' contains ".value" child ' +
                validationPathToErrorString(path) +
                ' in addition to actual children.');
        }
    }
};
/**
 * Pre-validate paths passed in the firebase function.
 */
const validateFirebaseMergePaths = function (errorPrefix, mergePaths) {
    let i, curPath;
    for (i = 0; i < mergePaths.length; i++) {
        curPath = mergePaths[i];
        const keys = pathSlice(curPath);
        for (let j = 0; j < keys.length; j++) {
            if (keys[j] === '.priority' && j === keys.length - 1) ;
            else if (!isValidKey(keys[j])) {
                throw new Error(errorPrefix +
                    'contains an invalid key (' +
                    keys[j] +
                    ') in path ' +
                    curPath.toString() +
                    '. Keys must be non-empty strings ' +
                    'and can\'t contain ".", "#", "$", "/", "[", or "]"');
            }
        }
    }
    // Check that update keys are not descendants of each other.
    // We rely on the property that sorting guarantees that ancestors come
    // right before descendants.
    mergePaths.sort(pathCompare);
    let prevPath = null;
    for (i = 0; i < mergePaths.length; i++) {
        curPath = mergePaths[i];
        if (prevPath !== null && pathContains(prevPath, curPath)) {
            throw new Error(errorPrefix +
                'contains a path ' +
                prevPath.toString() +
                ' that is ancestor of another path ' +
                curPath.toString());
        }
        prevPath = curPath;
    }
};
/**
 * pre-validate an object passed as an argument to firebase function (
 * must be an object - e.g. for firebase.update()).
 */
const validateFirebaseMergeDataArg = function (fnName, data, path, optional) {
    if (optional && data === undefined) {
        return;
    }
    const errorPrefix = util.errorPrefix(fnName, 'values');
    if (!(data && typeof data === 'object') || Array.isArray(data)) {
        throw new Error(errorPrefix + ' must be an object containing the children to replace.');
    }
    const mergePaths = [];
    each(data, (key, value) => {
        const curPath = new Path(key);
        validateFirebaseData(errorPrefix, value, pathChild(path, curPath));
        if (pathGetBack(curPath) === '.priority') {
            if (!isValidPriority(value)) {
                throw new Error(errorPrefix +
                    "contains an invalid value for '" +
                    curPath.toString() +
                    "', which must be a valid " +
                    'Firebase priority (a string, finite number, server value, or null).');
            }
        }
        mergePaths.push(curPath);
    });
    validateFirebaseMergePaths(errorPrefix, mergePaths);
};
const validatePriority = function (fnName, priority, optional) {
    if (optional && priority === undefined) {
        return;
    }
    if (isInvalidJSONNumber(priority)) {
        throw new Error(util.errorPrefix(fnName, 'priority') +
            'is ' +
            priority.toString() +
            ', but must be a valid Firebase priority (a string, finite number, ' +
            'server value, or null).');
    }
    // Special case to allow importing data with a .sv.
    if (!isValidPriority(priority)) {
        throw new Error(util.errorPrefix(fnName, 'priority') +
            'must be a valid Firebase priority ' +
            '(a string, finite number, server value, or null).');
    }
};
const validateKey = function (fnName, argumentName, key, optional) {
    if (optional && key === undefined) {
        return;
    }
    if (!isValidKey(key)) {
        throw new Error(util.errorPrefix(fnName, argumentName) +
            'was an invalid key = "' +
            key +
            '".  Firebase keys must be non-empty strings and ' +
            'can\'t contain ".", "#", "$", "/", "[", or "]").');
    }
};
/**
 * @internal
 */
const validatePathString = function (fnName, argumentName, pathString, optional) {
    if (optional && pathString === undefined) {
        return;
    }
    if (!isValidPathString(pathString)) {
        throw new Error(util.errorPrefix(fnName, argumentName) +
            'was an invalid path = "' +
            pathString +
            '". Paths must be non-empty strings and ' +
            'can\'t contain ".", "#", "$", "[", or "]"');
    }
};
const validateRootPathString = function (fnName, argumentName, pathString, optional) {
    if (pathString) {
        // Allow '/.info/' at the beginning.
        pathString = pathString.replace(/^\/*\.info(\/|$)/, '/');
    }
    validatePathString(fnName, argumentName, pathString, optional);
};
/**
 * @internal
 */
const validateWritablePath = function (fnName, path) {
    if (pathGetFront(path) === '.info') {
        throw new Error(fnName + " failed = Can't modify data under /.info/");
    }
};
const validateUrl = function (fnName, parsedUrl) {
    // TODO = Validate server better.
    const pathString = parsedUrl.path.toString();
    if (!(typeof parsedUrl.repoInfo.host === 'string') ||
        parsedUrl.repoInfo.host.length === 0 ||
        (!isValidKey(parsedUrl.repoInfo.namespace) &&
            parsedUrl.repoInfo.host.split(':')[0] !== 'localhost') ||
        (pathString.length !== 0 && !isValidRootPathString(pathString))) {
        throw new Error(util.errorPrefix(fnName, 'url') +
            'must be a valid firebase URL and ' +
            'the path can\'t contain ".", "#", "$", "[", or "]".');
    }
};

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * The event queue serves a few purposes:
 * 1. It ensures we maintain event order in the face of event callbacks doing operations that result in more
 *    events being queued.
 * 2. raiseQueuedEvents() handles being called reentrantly nicely.  That is, if in the course of raising events,
 *    raiseQueuedEvents() is called again, the "inner" call will pick up raising events where the "outer" call
 *    left off, ensuring that the events are still raised synchronously and in order.
 * 3. You can use raiseEventsAtPath and raiseEventsForChangedPath to ensure only relevant previously-queued
 *    events are raised synchronously.
 *
 * NOTE: This can all go away if/when we move to async events.
 *
 */
class EventQueue {
    constructor() {
        this.eventLists_ = [];
        /**
         * Tracks recursion depth of raiseQueuedEvents_, for debugging purposes.
         */
        this.recursionDepth_ = 0;
    }
}
/**
 * @param eventDataList - The new events to queue.
 */
function eventQueueQueueEvents(eventQueue, eventDataList) {
    // We group events by path, storing them in a single EventList, to make it easier to skip over them quickly.
    let currList = null;
    for (let i = 0; i < eventDataList.length; i++) {
        const data = eventDataList[i];
        const path = data.getPath();
        if (currList !== null && !pathEquals(path, currList.path)) {
            eventQueue.eventLists_.push(currList);
            currList = null;
        }
        if (currList === null) {
            currList = { events: [], path };
        }
        currList.events.push(data);
    }
    if (currList) {
        eventQueue.eventLists_.push(currList);
    }
}
/**
 * Queues the specified events and synchronously raises all events (including previously queued ones)
 * for the specified path.
 *
 * It is assumed that the new events are all for the specified path.
 *
 * @param path - The path to raise events for.
 * @param eventDataList - The new events to raise.
 */
function eventQueueRaiseEventsAtPath(eventQueue, path, eventDataList) {
    eventQueueQueueEvents(eventQueue, eventDataList);
    eventQueueRaiseQueuedEventsMatchingPredicate(eventQueue, eventPath => pathEquals(eventPath, path));
}
/**
 * Queues the specified events and synchronously raises all events (including previously queued ones) for
 * locations related to the specified change path (i.e. all ancestors and descendants).
 *
 * It is assumed that the new events are all related (ancestor or descendant) to the specified path.
 *
 * @param changedPath - The path to raise events for.
 * @param eventDataList - The events to raise
 */
function eventQueueRaiseEventsForChangedPath(eventQueue, changedPath, eventDataList) {
    eventQueueQueueEvents(eventQueue, eventDataList);
    eventQueueRaiseQueuedEventsMatchingPredicate(eventQueue, eventPath => pathContains(eventPath, changedPath) ||
        pathContains(changedPath, eventPath));
}
function eventQueueRaiseQueuedEventsMatchingPredicate(eventQueue, predicate) {
    eventQueue.recursionDepth_++;
    let sentAll = true;
    for (let i = 0; i < eventQueue.eventLists_.length; i++) {
        const eventList = eventQueue.eventLists_[i];
        if (eventList) {
            const eventPath = eventList.path;
            if (predicate(eventPath)) {
                eventListRaise(eventQueue.eventLists_[i]);
                eventQueue.eventLists_[i] = null;
            }
            else {
                sentAll = false;
            }
        }
    }
    if (sentAll) {
        eventQueue.eventLists_ = [];
    }
    eventQueue.recursionDepth_--;
}
/**
 * Iterates through the list and raises each event
 */
function eventListRaise(eventList) {
    for (let i = 0; i < eventList.events.length; i++) {
        const eventData = eventList.events[i];
        if (eventData !== null) {
            eventList.events[i] = null;
            const eventFn = eventData.getEventRunner();
            if (logger) {
                log('event: ' + eventData.toString());
            }
            exceptionGuard(eventFn);
        }
    }
}

/**
 * @license
 * Copyright 2017 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const INTERRUPT_REASON = 'repo_interrupt';
/**
 * If a transaction does not succeed after 25 retries, we abort it. Among other
 * things this ensure that if there's ever a bug causing a mismatch between
 * client / server hashes for some data, we won't retry indefinitely.
 */
const MAX_TRANSACTION_RETRIES = 25;
/**
 * A connection to a single data repository.
 */
class Repo {
    constructor(repoInfo_, forceRestClient_, authTokenProvider_, appCheckProvider_) {
        this.repoInfo_ = repoInfo_;
        this.forceRestClient_ = forceRestClient_;
        this.authTokenProvider_ = authTokenProvider_;
        this.appCheckProvider_ = appCheckProvider_;
        this.dataUpdateCount = 0;
        this.statsListener_ = null;
        this.eventQueue_ = new EventQueue();
        this.nextWriteId_ = 1;
        this.interceptServerDataCallback_ = null;
        /** A list of data pieces and paths to be set when this client disconnects. */
        this.onDisconnect_ = newSparseSnapshotTree();
        /** Stores queues of outstanding transactions for Firebase locations. */
        this.transactionQueueTree_ = new Tree();
        // TODO: This should be @private but it's used by test_access.js and internal.js
        this.persistentConnection_ = null;
        // This key is intentionally not updated if RepoInfo is later changed or replaced
        this.key = this.repoInfo_.toURLString();
    }
    /**
     * @returns The URL corresponding to the root of this Firebase.
     */
    toString() {
        return ((this.repoInfo_.secure ? 'https://' : 'http://') + this.repoInfo_.host);
    }
}
function repoStart(repo, appId, authOverride) {
    repo.stats_ = statsManagerGetCollection(repo.repoInfo_);
    if (repo.forceRestClient_ || beingCrawled()) {
        repo.server_ = new ReadonlyRestClient(repo.repoInfo_, (pathString, data, isMerge, tag) => {
            repoOnDataUpdate(repo, pathString, data, isMerge, tag);
        }, repo.authTokenProvider_, repo.appCheckProvider_);
        // Minor hack: Fire onConnect immediately, since there's no actual connection.
        setTimeout(() => repoOnConnectStatus(repo, /* connectStatus= */ true), 0);
    }
    else {
        // Validate authOverride
        if (typeof authOverride !== 'undefined' && authOverride !== null) {
            if (typeof authOverride !== 'object') {
                throw new Error('Only objects are supported for option databaseAuthVariableOverride');
            }
            try {
                util.stringify(authOverride);
            }
            catch (e) {
                throw new Error('Invalid authOverride provided: ' + e);
            }
        }
        repo.persistentConnection_ = new PersistentConnection(repo.repoInfo_, appId, (pathString, data, isMerge, tag) => {
            repoOnDataUpdate(repo, pathString, data, isMerge, tag);
        }, (connectStatus) => {
            repoOnConnectStatus(repo, connectStatus);
        }, (updates) => {
            repoOnServerInfoUpdate(repo, updates);
        }, repo.authTokenProvider_, repo.appCheckProvider_, authOverride);
        repo.server_ = repo.persistentConnection_;
    }
    repo.authTokenProvider_.addTokenChangeListener(token => {
        repo.server_.refreshAuthToken(token);
    });
    repo.appCheckProvider_.addTokenChangeListener(result => {
        repo.server_.refreshAppCheckToken(result.token);
    });
    // In the case of multiple Repos for the same repoInfo (i.e. there are multiple Firebase.Contexts being used),
    // we only want to create one StatsReporter.  As such, we'll report stats over the first Repo created.
    repo.statsReporter_ = statsManagerGetOrCreateReporter(repo.repoInfo_, () => new StatsReporter(repo.stats_, repo.server_));
    // Used for .info.
    repo.infoData_ = new SnapshotHolder();
    repo.infoSyncTree_ = new SyncTree({
        startListening: (query, tag, currentHashFn, onComplete) => {
            let infoEvents = [];
            const node = repo.infoData_.getNode(query._path);
            // This is possibly a hack, but we have different semantics for .info endpoints. We don't raise null events
            // on initial data...
            if (!node.isEmpty()) {
                infoEvents = syncTreeApplyServerOverwrite(repo.infoSyncTree_, query._path, node);
                setTimeout(() => {
                    onComplete('ok');
                }, 0);
            }
            return infoEvents;
        },
        stopListening: () => { }
    });
    repoUpdateInfo(repo, 'connected', false);
    repo.serverSyncTree_ = new SyncTree({
        startListening: (query, tag, currentHashFn, onComplete) => {
            repo.server_.listen(query, currentHashFn, tag, (status, data) => {
                const events = onComplete(status, data);
                eventQueueRaiseEventsForChangedPath(repo.eventQueue_, query._path, events);
            });
            // No synchronous events for network-backed sync trees
            return [];
        },
        stopListening: (query, tag) => {
            repo.server_.unlisten(query, tag);
        }
    });
}
/**
 * @returns The time in milliseconds, taking the server offset into account if we have one.
 */
function repoServerTime(repo) {
    const offsetNode = repo.infoData_.getNode(new Path('.info/serverTimeOffset'));
    const offset = offsetNode.val() || 0;
    return new Date().getTime() + offset;
}
/**
 * Generate ServerValues using some variables from the repo object.
 */
function repoGenerateServerValues(repo) {
    return generateWithValues({
        timestamp: repoServerTime(repo)
    });
}
/**
 * Called by realtime when we get new messages from the server.
 */
function repoOnDataUpdate(repo, pathString, data, isMerge, tag) {
    // For testing.
    repo.dataUpdateCount++;
    const path = new Path(pathString);
    data = repo.interceptServerDataCallback_
        ? repo.interceptServerDataCallback_(pathString, data)
        : data;
    let events = [];
    if (tag) {
        if (isMerge) {
            const taggedChildren = util.map(data, (raw) => nodeFromJSON(raw));
            events = syncTreeApplyTaggedQueryMerge(repo.serverSyncTree_, path, taggedChildren, tag);
        }
        else {
            const taggedSnap = nodeFromJSON(data);
            events = syncTreeApplyTaggedQueryOverwrite(repo.serverSyncTree_, path, taggedSnap, tag);
        }
    }
    else if (isMerge) {
        const changedChildren = util.map(data, (raw) => nodeFromJSON(raw));
        events = syncTreeApplyServerMerge(repo.serverSyncTree_, path, changedChildren);
    }
    else {
        const snap = nodeFromJSON(data);
        events = syncTreeApplyServerOverwrite(repo.serverSyncTree_, path, snap);
    }
    let affectedPath = path;
    if (events.length > 0) {
        // Since we have a listener outstanding for each transaction, receiving any events
        // is a proxy for some change having occurred.
        affectedPath = repoRerunTransactions(repo, path);
    }
    eventQueueRaiseEventsForChangedPath(repo.eventQueue_, affectedPath, events);
}
function repoOnConnectStatus(repo, connectStatus) {
    repoUpdateInfo(repo, 'connected', connectStatus);
    if (connectStatus === false) {
        repoRunOnDisconnectEvents(repo);
    }
}
function repoOnServerInfoUpdate(repo, updates) {
    each(updates, (key, value) => {
        repoUpdateInfo(repo, key, value);
    });
}
function repoUpdateInfo(repo, pathString, value) {
    const path = new Path('/.info/' + pathString);
    const newNode = nodeFromJSON(value);
    repo.infoData_.updateSnapshot(path, newNode);
    const events = syncTreeApplyServerOverwrite(repo.infoSyncTree_, path, newNode);
    eventQueueRaiseEventsForChangedPath(repo.eventQueue_, path, events);
}
function repoGetNextWriteId(repo) {
    return repo.nextWriteId_++;
}
/**
 * The purpose of `getValue` is to return the latest known value
 * satisfying `query`.
 *
 * This method will first check for in-memory cached values
 * belonging to active listeners. If they are found, such values
 * are considered to be the most up-to-date.
 *
 * If the client is not connected, this method will wait until the
 *  repo has established a connection and then request the value for `query`.
 * If the client is not able to retrieve the query result for another reason,
 * it reports an error.
 *
 * @param query - The query to surface a value for.
 */
function repoGetValue(repo, query, eventRegistration) {
    // Only active queries are cached. There is no persisted cache.
    const cached = syncTreeGetServerValue(repo.serverSyncTree_, query);
    if (cached != null) {
        return Promise.resolve(cached);
    }
    return repo.server_.get(query).then(payload => {
        const node = nodeFromJSON(payload).withIndex(query._queryParams.getIndex());
        /**
         * Below we simulate the actions of an `onlyOnce` `onValue()` event where:
         * Add an event registration,
         * Update data at the path,
         * Raise any events,
         * Cleanup the SyncTree
         */
        syncTreeAddEventRegistration(repo.serverSyncTree_, query, eventRegistration, true);
        let events;
        if (query._queryParams.loadsAllData()) {
            events = syncTreeApplyServerOverwrite(repo.serverSyncTree_, query._path, node);
        }
        else {
            const tag = syncTreeTagForQuery(repo.serverSyncTree_, query);
            events = syncTreeApplyTaggedQueryOverwrite(repo.serverSyncTree_, query._path, node, tag);
        }
        /*
         * We need to raise events in the scenario where `get()` is called at a parent path, and
         * while the `get()` is pending, `onValue` is called at a child location. While get() is waiting
         * for the data, `onValue` will register a new event. Then, get() will come back, and update the syncTree
         * and its corresponding serverCache, including the child location where `onValue` is called. Then,
         * `onValue` will receive the event from the server, but look at the syncTree and see that the data received
         * from the server is already at the SyncPoint, and so the `onValue` callback will never get fired.
         * Calling `eventQueueRaiseEventsForChangedPath()` is the correct way to propagate the events and
         * ensure the corresponding child events will get fired.
         */
        eventQueueRaiseEventsForChangedPath(repo.eventQueue_, query._path, events);
        syncTreeRemoveEventRegistration(repo.serverSyncTree_, query, eventRegistration, null, true);
        return node;
    }, err => {
        repoLog(repo, 'get for query ' + util.stringify(query) + ' failed: ' + err);
        return Promise.reject(new Error(err));
    });
}
function repoSetWithPriority(repo, path, newVal, newPriority, onComplete) {
    repoLog(repo, 'set', {
        path: path.toString(),
        value: newVal,
        priority: newPriority
    });
    // TODO: Optimize this behavior to either (a) store flag to skip resolving where possible and / or
    // (b) store unresolved paths on JSON parse
    const serverValues = repoGenerateServerValues(repo);
    const newNodeUnresolved = nodeFromJSON(newVal, newPriority);
    const existing = syncTreeCalcCompleteEventCache(repo.serverSyncTree_, path);
    const newNode = resolveDeferredValueSnapshot(newNodeUnresolved, existing, serverValues);
    const writeId = repoGetNextWriteId(repo);
    const events = syncTreeApplyUserOverwrite(repo.serverSyncTree_, path, newNode, writeId, true);
    eventQueueQueueEvents(repo.eventQueue_, events);
    repo.server_.put(path.toString(), newNodeUnresolved.val(/*export=*/ true), (status, errorReason) => {
        const success = status === 'ok';
        if (!success) {
            warn('set at ' + path + ' failed: ' + status);
        }
        const clearEvents = syncTreeAckUserWrite(repo.serverSyncTree_, writeId, !success);
        eventQueueRaiseEventsForChangedPath(repo.eventQueue_, path, clearEvents);
        repoCallOnCompleteCallback(repo, onComplete, status, errorReason);
    });
    const affectedPath = repoAbortTransactions(repo, path);
    repoRerunTransactions(repo, affectedPath);
    // We queued the events above, so just flush the queue here
    eventQueueRaiseEventsForChangedPath(repo.eventQueue_, affectedPath, []);
}
function repoUpdate(repo, path, childrenToMerge, onComplete) {
    repoLog(repo, 'update', { path: path.toString(), value: childrenToMerge });
    // Start with our existing data and merge each child into it.
    let empty = true;
    const serverValues = repoGenerateServerValues(repo);
    const changedChildren = {};
    each(childrenToMerge, (changedKey, changedValue) => {
        empty = false;
        changedChildren[changedKey] = resolveDeferredValueTree(pathChild(path, changedKey), nodeFromJSON(changedValue), repo.serverSyncTree_, serverValues);
    });
    if (!empty) {
        const writeId = repoGetNextWriteId(repo);
        const events = syncTreeApplyUserMerge(repo.serverSyncTree_, path, changedChildren, writeId);
        eventQueueQueueEvents(repo.eventQueue_, events);
        repo.server_.merge(path.toString(), childrenToMerge, (status, errorReason) => {
            const success = status === 'ok';
            if (!success) {
                warn('update at ' + path + ' failed: ' + status);
            }
            const clearEvents = syncTreeAckUserWrite(repo.serverSyncTree_, writeId, !success);
            const affectedPath = clearEvents.length > 0 ? repoRerunTransactions(repo, path) : path;
            eventQueueRaiseEventsForChangedPath(repo.eventQueue_, affectedPath, clearEvents);
            repoCallOnCompleteCallback(repo, onComplete, status, errorReason);
        });
        each(childrenToMerge, (changedPath) => {
            const affectedPath = repoAbortTransactions(repo, pathChild(path, changedPath));
            repoRerunTransactions(repo, affectedPath);
        });
        // We queued the events above, so just flush the queue here
        eventQueueRaiseEventsForChangedPath(repo.eventQueue_, path, []);
    }
    else {
        log("update() called with empty data.  Don't do anything.");
        repoCallOnCompleteCallback(repo, onComplete, 'ok', undefined);
    }
}
/**
 * Applies all of the changes stored up in the onDisconnect_ tree.
 */
function repoRunOnDisconnectEvents(repo) {
    repoLog(repo, 'onDisconnectEvents');
    const serverValues = repoGenerateServerValues(repo);
    const resolvedOnDisconnectTree = newSparseSnapshotTree();
    sparseSnapshotTreeForEachTree(repo.onDisconnect_, newEmptyPath(), (path, node) => {
        const resolved = resolveDeferredValueTree(path, node, repo.serverSyncTree_, serverValues);
        sparseSnapshotTreeRemember(resolvedOnDisconnectTree, path, resolved);
    });
    let events = [];
    sparseSnapshotTreeForEachTree(resolvedOnDisconnectTree, newEmptyPath(), (path, snap) => {
        events = events.concat(syncTreeApplyServerOverwrite(repo.serverSyncTree_, path, snap));
        const affectedPath = repoAbortTransactions(repo, path);
        repoRerunTransactions(repo, affectedPath);
    });
    repo.onDisconnect_ = newSparseSnapshotTree();
    eventQueueRaiseEventsForChangedPath(repo.eventQueue_, newEmptyPath(), events);
}
function repoOnDisconnectCancel(repo, path, onComplete) {
    repo.server_.onDisconnectCancel(path.toString(), (status, errorReason) => {
        if (status === 'ok') {
            sparseSnapshotTreeForget(repo.onDisconnect_, path);
        }
        repoCallOnCompleteCallback(repo, onComplete, status, errorReason);
    });
}
function repoOnDisconnectSet(repo, path, value, onComplete) {
    const newNode = nodeFromJSON(value);
    repo.server_.onDisconnectPut(path.toString(), newNode.val(/*export=*/ true), (status, errorReason) => {
        if (status === 'ok') {
            sparseSnapshotTreeRemember(repo.onDisconnect_, path, newNode);
        }
        repoCallOnCompleteCallback(repo, onComplete, status, errorReason);
    });
}
function repoOnDisconnectSetWithPriority(repo, path, value, priority, onComplete) {
    const newNode = nodeFromJSON(value, priority);
    repo.server_.onDisconnectPut(path.toString(), newNode.val(/*export=*/ true), (status, errorReason) => {
        if (status === 'ok') {
            sparseSnapshotTreeRemember(repo.onDisconnect_, path, newNode);
        }
        repoCallOnCompleteCallback(repo, onComplete, status, errorReason);
    });
}
function repoOnDisconnectUpdate(repo, path, childrenToMerge, onComplete) {
    if (util.isEmpty(childrenToMerge)) {
        log("onDisconnect().update() called with empty data.  Don't do anything.");
        repoCallOnCompleteCallback(repo, onComplete, 'ok', undefined);
        return;
    }
    repo.server_.onDisconnectMerge(path.toString(), childrenToMerge, (status, errorReason) => {
        if (status === 'ok') {
            each(childrenToMerge, (childName, childNode) => {
                const newChildNode = nodeFromJSON(childNode);
                sparseSnapshotTreeRemember(repo.onDisconnect_, pathChild(path, childName), newChildNode);
            });
        }
        repoCallOnCompleteCallback(repo, onComplete, status, errorReason);
    });
}
function repoAddEventCallbackForQuery(repo, query, eventRegistration) {
    let events;
    if (pathGetFront(query._path) === '.info') {
        events = syncTreeAddEventRegistration(repo.infoSyncTree_, query, eventRegistration);
    }
    else {
        events = syncTreeAddEventRegistration(repo.serverSyncTree_, query, eventRegistration);
    }
    eventQueueRaiseEventsAtPath(repo.eventQueue_, query._path, events);
}
function repoRemoveEventCallbackForQuery(repo, query, eventRegistration) {
    // These are guaranteed not to raise events, since we're not passing in a cancelError. However, we can future-proof
    // a little bit by handling the return values anyways.
    let events;
    if (pathGetFront(query._path) === '.info') {
        events = syncTreeRemoveEventRegistration(repo.infoSyncTree_, query, eventRegistration);
    }
    else {
        events = syncTreeRemoveEventRegistration(repo.serverSyncTree_, query, eventRegistration);
    }
    eventQueueRaiseEventsAtPath(repo.eventQueue_, query._path, events);
}
function repoInterrupt(repo) {
    if (repo.persistentConnection_) {
        repo.persistentConnection_.interrupt(INTERRUPT_REASON);
    }
}
function repoResume(repo) {
    if (repo.persistentConnection_) {
        repo.persistentConnection_.resume(INTERRUPT_REASON);
    }
}
function repoLog(repo, ...varArgs) {
    let prefix = '';
    if (repo.persistentConnection_) {
        prefix = repo.persistentConnection_.id + ':';
    }
    log(prefix, ...varArgs);
}
function repoCallOnCompleteCallback(repo, callback, status, errorReason) {
    if (callback) {
        exceptionGuard(() => {
            if (status === 'ok') {
                callback(null);
            }
            else {
                const code = (status || 'error').toUpperCase();
                let message = code;
                if (errorReason) {
                    message += ': ' + errorReason;
                }
                const error = new Error(message);
                // eslint-disable-next-line @typescript-eslint/no-explicit-any
                error.code = code;
                callback(error);
            }
        });
    }
}
/**
 * Creates a new transaction, adds it to the transactions we're tracking, and
 * sends it to the server if possible.
 *
 * @param path - Path at which to do transaction.
 * @param transactionUpdate - Update callback.
 * @param onComplete - Completion callback.
 * @param unwatcher - Function that will be called when the transaction no longer
 * need data updates for `path`.
 * @param applyLocally - Whether or not to make intermediate results visible
 */
function repoStartTransaction(repo, path, transactionUpdate, onComplete, unwatcher, applyLocally) {
    repoLog(repo, 'transaction on ' + path);
    // Initialize transaction.
    const transaction = {
        path,
        update: transactionUpdate,
        onComplete,
        // One of TransactionStatus enums.
        status: null,
        // Used when combining transactions at different locations to figure out
        // which one goes first.
        order: LUIDGenerator(),
        // Whether to raise local events for this transaction.
        applyLocally,
        // Count of how many times we've retried the transaction.
        retryCount: 0,
        // Function to call to clean up our .on() listener.
        unwatcher,
        // Stores why a transaction was aborted.
        abortReason: null,
        currentWriteId: null,
        currentInputSnapshot: null,
        currentOutputSnapshotRaw: null,
        currentOutputSnapshotResolved: null
    };
    // Run transaction initially.
    const currentState = repoGetLatestState(repo, path, undefined);
    transaction.currentInputSnapshot = currentState;
    const newVal = transaction.update(currentState.val());
    if (newVal === undefined) {
        // Abort transaction.
        transaction.unwatcher();
        transaction.currentOutputSnapshotRaw = null;
        transaction.currentOutputSnapshotResolved = null;
        if (transaction.onComplete) {
            transaction.onComplete(null, false, transaction.currentInputSnapshot);
        }
    }
    else {
        validateFirebaseData('transaction failed: Data returned ', newVal, transaction.path);
        // Mark as run and add to our queue.
        transaction.status = 0 /* TransactionStatus.RUN */;
        const queueNode = treeSubTree(repo.transactionQueueTree_, path);
        const nodeQueue = treeGetValue(queueNode) || [];
        nodeQueue.push(transaction);
        treeSetValue(queueNode, nodeQueue);
        // Update visibleData and raise events
        // Note: We intentionally raise events after updating all of our
        // transaction state, since the user could start new transactions from the
        // event callbacks.
        let priorityForNode;
        if (typeof newVal === 'object' &&
            newVal !== null &&
            util.contains(newVal, '.priority')) {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            priorityForNode = util.safeGet(newVal, '.priority');
            util.assert(isValidPriority(priorityForNode), 'Invalid priority returned by transaction. ' +
                'Priority must be a valid string, finite number, server value, or null.');
        }
        else {
            const currentNode = syncTreeCalcCompleteEventCache(repo.serverSyncTree_, path) ||
                ChildrenNode.EMPTY_NODE;
            priorityForNode = currentNode.getPriority().val();
        }
        const serverValues = repoGenerateServerValues(repo);
        const newNodeUnresolved = nodeFromJSON(newVal, priorityForNode);
        const newNode = resolveDeferredValueSnapshot(newNodeUnresolved, currentState, serverValues);
        transaction.currentOutputSnapshotRaw = newNodeUnresolved;
        transaction.currentOutputSnapshotResolved = newNode;
        transaction.currentWriteId = repoGetNextWriteId(repo);
        const events = syncTreeApplyUserOverwrite(repo.serverSyncTree_, path, newNode, transaction.currentWriteId, transaction.applyLocally);
        eventQueueRaiseEventsForChangedPath(repo.eventQueue_, path, events);
        repoSendReadyTransactions(repo, repo.transactionQueueTree_);
    }
}
/**
 * @param excludeSets - A specific set to exclude
 */
function repoGetLatestState(repo, path, excludeSets) {
    return (syncTreeCalcCompleteEventCache(repo.serverSyncTree_, path, excludeSets) ||
        ChildrenNode.EMPTY_NODE);
}
/**
 * Sends any already-run transactions that aren't waiting for outstanding
 * transactions to complete.
 *
 * Externally it's called with no arguments, but it calls itself recursively
 * with a particular transactionQueueTree node to recurse through the tree.
 *
 * @param node - transactionQueueTree node to start at.
 */
function repoSendReadyTransactions(repo, node = repo.transactionQueueTree_) {
    // Before recursing, make sure any completed transactions are removed.
    if (!node) {
        repoPruneCompletedTransactionsBelowNode(repo, node);
    }
    if (treeGetValue(node)) {
        const queue = repoBuildTransactionQueue(repo, node);
        util.assert(queue.length > 0, 'Sending zero length transaction queue');
        const allRun = queue.every((transaction) => transaction.status === 0 /* TransactionStatus.RUN */);
        // If they're all run (and not sent), we can send them.  Else, we must wait.
        if (allRun) {
            repoSendTransactionQueue(repo, treeGetPath(node), queue);
        }
    }
    else if (treeHasChildren(node)) {
        treeForEachChild(node, childNode => {
            repoSendReadyTransactions(repo, childNode);
        });
    }
}
/**
 * Given a list of run transactions, send them to the server and then handle
 * the result (success or failure).
 *
 * @param path - The location of the queue.
 * @param queue - Queue of transactions under the specified location.
 */
function repoSendTransactionQueue(repo, path, queue) {
    // Mark transactions as sent and increment retry count!
    const setsToIgnore = queue.map(txn => {
        return txn.currentWriteId;
    });
    const latestState = repoGetLatestState(repo, path, setsToIgnore);
    let snapToSend = latestState;
    const latestHash = latestState.hash();
    for (let i = 0; i < queue.length; i++) {
        const txn = queue[i];
        util.assert(txn.status === 0 /* TransactionStatus.RUN */, 'tryToSendTransactionQueue_: items in queue should all be run.');
        txn.status = 1 /* TransactionStatus.SENT */;
        txn.retryCount++;
        const relativePath = newRelativePath(path, txn.path);
        // If we've gotten to this point, the output snapshot must be defined.
        snapToSend = snapToSend.updateChild(relativePath /** @type {!Node} */, txn.currentOutputSnapshotRaw);
    }
    const dataToSend = snapToSend.val(true);
    const pathToSend = path;
    // Send the put.
    repo.server_.put(pathToSend.toString(), dataToSend, (status) => {
        repoLog(repo, 'transaction put response', {
            path: pathToSend.toString(),
            status
        });
        let events = [];
        if (status === 'ok') {
            // Queue up the callbacks and fire them after cleaning up all of our
            // transaction state, since the callback could trigger more
            // transactions or sets.
            const callbacks = [];
            for (let i = 0; i < queue.length; i++) {
                queue[i].status = 2 /* TransactionStatus.COMPLETED */;
                events = events.concat(syncTreeAckUserWrite(repo.serverSyncTree_, queue[i].currentWriteId));
                if (queue[i].onComplete) {
                    // We never unset the output snapshot, and given that this
                    // transaction is complete, it should be set
                    callbacks.push(() => queue[i].onComplete(null, true, queue[i].currentOutputSnapshotResolved));
                }
                queue[i].unwatcher();
            }
            // Now remove the completed transactions.
            repoPruneCompletedTransactionsBelowNode(repo, treeSubTree(repo.transactionQueueTree_, path));
            // There may be pending transactions that we can now send.
            repoSendReadyTransactions(repo, repo.transactionQueueTree_);
            eventQueueRaiseEventsForChangedPath(repo.eventQueue_, path, events);
            // Finally, trigger onComplete callbacks.
            for (let i = 0; i < callbacks.length; i++) {
                exceptionGuard(callbacks[i]);
            }
        }
        else {
            // transactions are no longer sent.  Update their status appropriately.
            if (status === 'datastale') {
                for (let i = 0; i < queue.length; i++) {
                    if (queue[i].status === 3 /* TransactionStatus.SENT_NEEDS_ABORT */) {
                        queue[i].status = 4 /* TransactionStatus.NEEDS_ABORT */;
                    }
                    else {
                        queue[i].status = 0 /* TransactionStatus.RUN */;
                    }
                }
            }
            else {
                warn('transaction at ' + pathToSend.toString() + ' failed: ' + status);
                for (let i = 0; i < queue.length; i++) {
                    queue[i].status = 4 /* TransactionStatus.NEEDS_ABORT */;
                    queue[i].abortReason = status;
                }
            }
            repoRerunTransactions(repo, path);
        }
    }, latestHash);
}
/**
 * Finds all transactions dependent on the data at changedPath and reruns them.
 *
 * Should be called any time cached data changes.
 *
 * Return the highest path that was affected by rerunning transactions. This
 * is the path at which events need to be raised for.
 *
 * @param changedPath - The path in mergedData that changed.
 * @returns The rootmost path that was affected by rerunning transactions.
 */
function repoRerunTransactions(repo, changedPath) {
    const rootMostTransactionNode = repoGetAncestorTransactionNode(repo, changedPath);
    const path = treeGetPath(rootMostTransactionNode);
    const queue = repoBuildTransactionQueue(repo, rootMostTransactionNode);
    repoRerunTransactionQueue(repo, queue, path);
    return path;
}
/**
 * Does all the work of rerunning transactions (as well as cleans up aborted
 * transactions and whatnot).
 *
 * @param queue - The queue of transactions to run.
 * @param path - The path the queue is for.
 */
function repoRerunTransactionQueue(repo, queue, pat  “ù ‰$Šÿ#B A¤”      î
   €     Â 
       TU  €€!  @ °”B     @    À     h  ²G €äTš   =è   5Å   @  H A)aLk ;] 
 N€q Ğİİ·  € ¨ €  ™    @Jk  	   3 	 ™!‰$	 03 €áï·û3ŠÃQ¯3WWÎ³&móR0Î¹bUk€ ‚)…a(ªi“Šm·ƒÁ47 Îµ&ç1q×Á0Œİ( `L’âi×ÿrB¨w(0ÄP"c¼2 		 ŠgY×5Ì|"œËÃª#k­µÖZk‰»»»»»»™%¥@  »»Ö†²œe)ÖjZï'ÿÓÚ;ÆxJ8Ï‚0MÂ÷J‘²wËÒ:J¿ÇK<Y&ŠF#ŒR) €’K €@ ‚ªwŒ$IÃ´Û)%%NÊ$  €ã ø<(  `!À¥D8Ì <  8|   1”0D* €ÿC1Ò    @é À   @ ª
	 À   ĞJªª
         Àbu
$Y“CT%È &ò+.l @ ÈË  `0H@D˜1 @Õİ€0      û]  g    Àòú@H@h H ¶ònDÒ°@ 	hÀ}l¤Bä”³   µ„Õ  ÆúCõè~B·hƒ ‰•E¹pÚ TôÂ @µ’Ü@¹hS%4ºJX€-­¸I‰˜sà‹dnu®Æ’…Ğ¶„<•¢ ³Àr€KèLÆ €Š¶CNQ @BhAŠ+K,  Ì·s]‡ƒz0@3†¶‡Jƒ÷`€Ô3ÖP+²¤ô ³ô)D/JÀ Š4’ä8± ƒéœ , hÊ©6Àšl Sí*‚O / „Rwp D0«¡®Átvmq Ö DÑUp,b8á)à 
@ëL äÕˆ+Q p   ú  °>Tú 
 ƒâ¦2 ¨í¬l' RQ³ ˆ DH¼¥Ù 
$l@|Mİ KCğ8&¦"DKnñnl¼C ¶)A{:	HÅ¨sKœ\" ³1C8s	ê3ZĞœÛOX
u§+$€Ô	‡£5A(@ªhD/‡áQ K§iåî#¡ÏŠãîŞÖ“Ã5ÄÊšl J,ÀàµÊ˜^]r h  gÁâœ ‡` ôX	à 
@HäLRØ(ÀhIÃ  yP€û ³©SB@À¶  –ĞGLøIö"€;•J Xõ¬*z ÁÄ§ n ¥­Úœ4€ ]ä8K Ø°š–æø	f‚±. 4  )õQ Ô¹Kõ
BJl(Z“ƒKK QÆâ52( 8Š’…6!€ ’ºCPÀ|K°Å!ºîÔG j†è+‡D x¼4)è¡ ÔÖG)D/²J¡iôJ `ÒL  Q+® š Œ±}k@lB hØª³† à%”à†0ˆ@1+hÓÁ;Œ ‹NxJÿPà•à5dğÕ‚:@    õ  0@Tú  j;+ÿ HEÌ€´ 	ğÛéL   ˆ¸Ğa  xP*‚¾LÇ;	p†<ÜÄ?Z·x	JÌğ¶) 	HG1êFAt.N.l>ØÁÎ\3 ´ÍİXÀuä5ŠP€*ÿà CDCçg®œãp¢)+‡ƒãŞİ;¡J PÖ„°ÀF€`€B|O	 8 €*±Ş‚Å&^l¹ =gJEH @ÑCh%à  @éL@$$Âl €ûDÒ6©œ2 ŒQ!ÀìÿCóè~B·hƒ ‰†E¹pÚ šåÂ @µ„®ƒ ÿK€Sb5-ib¾ø	f‚±˜õ5
„ö„Lƒj ³
"ÃJÚ  ğhr ‚K>å9<D!U ™MKf¨YCJQàaøB3/ğT@b8 ƒ1¥>¬É>gJJ h¶¦® –<‹¡}ÏHD@š¡/ @~@‡§ÍD P€/É{hLbØ2¨ªlUÀÁbĞ¶
@Ğµ¢Ø9œ  ú@0Ğ›LP (Â¬d¶ œŒè ÂkèH%  @Ù“" p ;A ªj		ç.+JàRÒCpi,£Ê•ÀK.‚tx$l·àC Ã` ¬ 0D$|"Kx@Áhô"
,lö8ãD)ĞsÎ9§µÎĞÔfë:9 (T‚ò
 ¨f r¡Ş  = c©âœ„@ Çh q¬ü 4®É=\l®rJƒ1N]BÜ@R!j~vØ„1†J Xó¬Ír ÁÄ§@@—#9Îÿ§„0E‰†T¾$L– ³“¡xÂwõÓ Ô  ³¾õà€Ãˆ5Úx4”Â1HHfÓÒÿ¨YC<QJ‚‡áñÌ4€QH5Æ((^<l|<:#‘î-k @¬É™ (€ZÀ© 4°Ì³® <<+w² À‹üÖ†0ˆ@ñ%h¢	`˜‹-Â £VŸ4(¬]p¨q-Á	8`« õ  €#ú  ½É™-$ '#úÿ" A–¶®Ù  â5®@ J <€>s.¬8âÑAKC‡j		çJ\Ä¥)<’À7ª[¤pæÔ)3" Ü†ú‚XP Ã:8A(,“ÈhQ
ôJs­µ,âw» †­zî#U l„¦ÀJ5#\h® hÄ1äšÿ+ @C  ³0â^’{l P,!:ÿÍÓÁOû5ƒ 1K	kL7PnZÿ§¬¦e )ÿ„ğİ@=ÿ5 `Öw2õ"†B¦Al( Àí-
.ùÚJ $ØD€}¨YC<QJ†/Ä03QB+ÎH¤{ÿ X“=} l( J Øš®4,ó¬® òl*‚O"P|	ÚÿÑL‹f"ƒŒZ}3Ö.8P	·-Á	8pÀV¶   Ÿ#ú  Š0«ÿ	 8Å€´ ‚x«ÿ  r º–‚		ç.+"J)  Õ* ¸æ¥¬
€G"bX>R8s	ê3 D·¡` €ã¬XC”}€’ÿMk9âÇ9w[ÄãîîÁF Ğ”=n ¸PC«‡R  ZÍÕˆÃ èÇ`  Hs6 @ÇÊ® Lr.j€%tÀS5ÿõ Lú¨nÇğ ‚¦¼ æ#J	ƒ J4èxH‹- € €VËóŠ‘ÔëÎè"2€ªK  ÀkŠú?èĞÃúˆ™@˜olÌrbd õLQZ`ÀQÅµ  à~ºú b HŠ­¯KĞ qÜè4°Â
ª·¹‰£Ápî4Ô‘ĞaK@ ëªL ThLrÄ€È×”Ò"¸/õ%$HÂJZ ˆ+  ğû› ú˜Ö`Âò Œ4~«òS 0 ¶õ©&š¦$‹L5.($ğ¸Ø8àËá¸¶0 è  3õg($	°J=€}wM] ¸	—ÕnD#Öé•›4ir%<3¥MTANÌ€#„-% ğ@ ßÁ·_|C·ø™-%‘vÆ_K% ‚©äÙd— €\ÁSõG€X @³õ@À™Nõ	 l½‚OSP Øyâõ)¬AI: Œ>‰OSg($	°J ´èsp`Pkâ¬à F¬Ó+7»4ir%<ˆ4µhR]i'3|A°äV¸õFÕÁ·ÒWZ8³/òà¥ğÒPPÛ  PÂ¢eO ”°hÙS ÿ X¤Å ³	p Ã ¸>t<XH-
 ~¶a–¼µ‚À0#3  "€L²H   àeŒ|äà<.i ìÀ¡e!  BÀ~L cˆÌL ÂÌŒÌ¨ËcaÊÂ bFd†F ø/ ¤ÿËÿ ğ¯À ´ÿ ÀÀ? ´áÆ? ´	  à(ïú  ,ıú  `Ù]ú  pûOú óÀ 8   `  õ   Y:úF @!€X¨ÎZ À=ƒÇÄ 0 ¿¯÷ `€…Í  è/ E°Ä(ˆ@¨`µZ`İÃ ® §¿ õB à ˆÎáµË4u®\À Änµd‚qêB°  0 ¼¯2ãP'×® XoíÊ8€3¦Îü§÷ŒŒ®C pÓnéx‡Ğœ ¦Æ?iÏ  ‡¹ÃÅ10€ÿôÃjÿ(¾ ³ê[Ï~®? (ğÂn[³ƒŸÚ®Š ¼0@½¯³Áà§ö®ê °5  ú4  `‡Ïús< @ õ´–	ğÓíú€AŠâA iU£¬®€  §Í`  à!' õ  @k™ ú @ñŸèiG3ÿ  ğÇä‚“Püç ÿÅ úÁJƒ®Œ˜U: ³øc9 ˆ® 0Œ!ö @êŸg@±bÓ©€ `¢•`  T!' õ  0Œ úP¬Øt*ÿŒ(: ³U9 ˆ‰® 0Œ  öP¬Øt*ÿNb¢ë ÿŠT!' õÑJ ï  “úöç„ƒ$G'ÑJ÷@{Rà} Ğ^ ÿ  }  úèœp ÉÑ‰^IÀ'æÒèœpP:^I€ ÆR à€q ÿ  àå‘ úèœp ÉñPØŞØ à
õtã2 €@ºÇÂ X\·ğ·M
@»LZÂAlQ¡Tr<  ¤ˆt x €n\õ `qİõá ¦ƒA!ÜTç„ƒ@F ˆ@„B9š/×íü­ÿ  „]Âe-„ƒš³êĞtN8 ]bN±L Ë  õ° H¦Ø õ^¼Æ0Š/+@¡PæKÿ  )V½ÿ1ŒâË
Ğ ÿ-xqj³.@$jÿ HŠÅ @µ1ŒâË
Ğÿ‚FÆ ÿ@„B9š/ O { ş  @9 ö  ÀsÕ  À ö<	    ú  àÊôúe(åÇ‡®~ >€ëŸ×ğ` e¥ÀJü |ÈÀ}‘x<˜2Æa p°	=sl 4×±Si×°S aœÀa '€-ôÀX× °S  ú  °µCúé P!… ö1 ì`ñsj+em¾J$8°‡-¡ à…YúAC@Ò¹Rh   »,,àçÒ³ÊÚ,¾Á+O‰Ã"p@¬3^˜p   úb(ê×  €‰ ôé  P!… úY›……¯ÿÉ±²Ê)  ğ  ú¡ `‡Yú @¬7ÿ‹0$  ïõi,XÀ¶»"É±²®)  J€~ÿ˜`5Ã×   •ô°í®HÿÄÊpM[3 x@·o3   (ú*!8À¬¹É  È»o1 ì`aj+Wi°J `J€~ÿp¨€¾ÿÏ   â†úÏ 0”ßö>%$M ú0 8¨% & ğ  µ `@I~$p   ú  à«¸| À L È ë´ ?€ù i äàÀ5õäE€~ ÿ  ĞÇ  úlgÁ+  ¼ €ãnÂYğŠ ÿ ^$8p‡-ÂYğ
ÿ1!qp‡.  ğ-’ ú4KÁ+ |‹$€¾ÿh@°¶- àÀ'LÂRğ
ÿ/ $ p‡6İ( €aõ   İ¨ p¦§ `u£è   ¦§ pƒ¤ºËôõ „ÇÅ @µ €©¸‡÷uéœp ÑDA¤à£0­ À8\úÂã  |0÷ €‡÷uDDa‡&‰ÿ D   õ ˆAÿ x @x\õ €©¸ğ·D)ø(Lÿ    ú­ @:\ú  4æĞ x @®Xõ 0l²ğ·D)ø(Ìÿ t”J€~ÿ   ËŞ ÈË  õ 0l²‡÷uD)ø(ÿ t”J€¾ÿrÅ2 @=ÿ†Mö €‡÷uDDa‡&‰ ÿ Ô”J€şÿRã) 'Ëå   pá`úRã	    ú  €~ ÿ/ 5 úú )  1ö   P4.úú 	    ú  €~ ÿ]ã  ú È«ü‰óÿ    ú   –ú$È @¤7õ è+ıœ¹$È €Ñ5õ+ ¸÷œ¹T€aKp@VßG E\K#PXšˆ}  €-õÃ
 hM‹…â-¨<3 àBä h…¥‰Øwqˆ °…b õ @ À0+Q°€â-êÛ2 €KÄ›^Xšˆ}‡è ØBa µ+ ¬ÄF õT› aK€–‚C EZKPXšˆ}   õ›Ã
 h.KB}' ¥VKUE±ïâÂ° ÿ‹ª·I(€
j#»Chˆ¨ @ıÿ‰ê$  ö€0}Iú` ÉB% ”  ]õ§.@Y–Ğ'/Ë¢8çÈ31ÆEQñ$   €ºrú% AW½ÿ% (@Y– ( àáÔõ  ôÉ»qFQT<	‡ÿ ˆ @ˆ·@ d²ÿ% BĞÅÀµ
 x8u½ÿ@ ô	¼»MFQT<	ÿ  ¯\€~ÿ@ÀZoh% $'Ë  õ.@	P¢,•] ( €ú½õJ¶D¿ hO:ïœà """±+   P_>ú@ € Á¬X% ”  9Yõ. (@Y–‚-Ñ/ ÚÿÇ;'äEm Ô—O€¾ÿ  ƒ5J P–å_ÿ
 P¿× õ@ l	z»Mé¼sÂA/*QDD$Šú Ú Ğ`>ú Ô0006Àö  L	ØI } ş   `']õ   ´1ú È“â/ õ °›z ş È“òn °-¢/ õ —^ } ş °-ÒnËL0€{øú    €şõ÷ €-òô    @éÓõÛ €¥Øôù ! 	Jú®X&Ã õ  ‰0ú @… L ¨Àë´ B€ i   P"úOM `?ú=ğÔ€ÀK±é	ÀS´ÛA ´¶ƒòí ° í»
  ‡ úe€¸Ñ ú`½c»ÔB@²’õãÛ€@£ ¹ €	$   €ï…` ¯–I.  »@CŠæA iô"½®@"À¯`  d‘( õ  ğj™ ú 4@óŸpGÃ+ÿ  ²È ‰“Ğüç ÿ½ ¢ÑK†®„\: ³Yd@Dî¦ à
Œ!ö 4@ìŸü@¯Sç©€D iî[  0U( õ  à
Œ úĞëÔy*ÿ„ìğ: ³LU@DÒ¦ à
Œ  öĞëÔy*ÿP@ ‘´é ÿLU    ú  àîú rÀ°p@!   ÷J   2 5?  pÏíú ˆƒf²J6>Ì¿ õº^Aòl ¯áqä@ `AıX  @4õyø`şıõ8 B×+9?Àkâ  ˆƒfÄRSW€¼ ÿ]¯JøA½_óBG Ø`fQS*,Œ| õ&%’l  P@?ÿ l0Ã @µI‰€¤ Û ÿO“Jğ ^“‡B#_ @ıÿO`¤I¡"i¯ÉAÈá !Ğ yÈ ú  ĞÈ ú    ™ ú   ™ öõ ™ ö   } ş  €²‹ öH³€²‹ ú æÅ†+ ˆÅ‚‚…‡”Y*á9hf Hpù¢ Ğà õ   obú°İxh4h³,X`ı®ÎÅ ³-K óJ x¸«h!œ|8¤ÌRqht _ Âœpğ&   ú 4€6€µë ¸+ lÜ¸  `[–K±  Aáÿ‡x ¸Mò8 €”±¶tÀÀ@³äQ'   úî
 7îÿ0¶ –Kë›Ä  à´8õ ãJ÷Å †àL x ^±¶t |  øn  ˜/ú–İ@(2³X z‘Î€0ci ãJà¾À°L1p¼ğ
³t |@@ õŠÅÀìÿÕ X$€eÜ¸  `[KãÄJ0, 5ïÀ+Àâ¾ÍtÀ@@±ãQ  ™/ú– ¨^¤³ÿ0ØÀ# õ   OÚú) &ĞB ®‚?€¡i€¨š´”´ ğÇÀ}ÔSL& $Ö;s   À®'›' &€$a½©z 0À®œ€]OL$	l•	 0UØúXh €´m©h—7 0Wõ¯” àÄkõ% ØÀó+õ ¬% OSõ	 ` -Èá¸§ àµmà   úŞ `Ø`µ”%0   ú   a`cõ  €¾ ÿ£ @YâúÃ  @ÂŞ x`  õ£ ÀZâú´@‚EÂç t¬h€¾ ÿ  4T ÜõŞ €Ö`µ+0   ú  5X­ ÿ Ô¬h€şÿJ4T Üõ†=0   ú      ú   `Øãú=0   ú  ` ú   p¤úw@0   ú <i€~ÿ    ñ\õ @  @L à, @nL à®ãú   Àgõ  ğ™D} ÿ;    ú	øL‚‰õs>    ú  DbÎõÑƒ˜³@} ÿ      ús °è£ú Ü¨+zõ ğt€ 8 À, @n   ãú	1    ú „Äx€¾ÿ  ` ú   €i£ú˜6    ú ÀÚh€¾ÿ  àf úC  Àãú   `{õ  Ø^A} ÿÄ1    ú $ØíuÔ.  q£ú    hõ  (A} ÿ)7    úXĞÀ õIL€îÔõÚ6RÔ le h `sõ ( f³€Ô 5 Û ÿ 6'Ñ¶r
Ì.Ê ³SĞ  õ›RÔ’l lNB õhğBı ÿ› ¨Q L® úh#q
”%® ³2 Á  õq6Ô
lW 0$ôµò ( Ê³È ¾õÚâ6Ô le	\1 úòóV € ¶  pS¿ ú¼È`   ú;Q0±ñ0\®À#ÏN 0ì… ö¸O ˜ótŠ¡À#ÏN Ğ° õN³¸ê:"DÏ¶¡@ˆ¸·¿¹!mŸö,„20¡³ , 
¯ò  0â ú. ¬€Õ
 j¿ PG'ú¿½Íëèğ)À¯´ L  ˆõõI  €7â ú  éâ"„»]§xv  QlˆTùè6	  “û¤/¥ ò  Ô h f£i¤! B  Ùè5†W µ


„Š+
  ôYq…JB”,¾!l  Ü%ËN  ]* + LÃj @C-9¨Y Y ‘ J „ ¸¬|    ÀƒL      ,ÿ  € 	Û  Ğ‡~ÿ{7Ğ  ú¢{  » ‚Ó5   p°dúÈ;@   ú   Ğs»úŞÃ2    út—!¨*ú@ O !L 8   GnUÇ 0ø× ú 0P6€ âMŒD” ÿà€ G5UÇ °ù× ú „«  õ‚O h44+ p{   @*õ À‘ÛÿR¡@C³ ö  ‡x“ÿR¡ÀD³»İ'P   ú²‹#€ÍõmšôR¾	õr	ÑÅŠõÈmüÏÿõ.¢¥tÌ‚{õT8àY2 ú € A5 Xà€Éõ§1P	  ú  °ôIúC+Y`Âz ú  %Ñ  öÑC %Ñ ú
ˆ^ } ş   )¼ úìÔ µYúi°ÆBú  &Ñ  öÑC &Ñ ú
^ } ş   *¼ ú¬±Ğ€~ÿ È“h } ş   'Ñ ú  +¼ÑCö   +¼ úR›+¼ úk,4ÀNíú H”h } ş   (Ñ ú  ,¼ÑCö   ,¼ úR›,¼ ú  ˆB[ÿR›    ú  ˆBÛÿ°S{€~ ÿ   °ÆBú    @ô´ ¬±Ğ€şÿ     zm ¤ï€~ÿAÇØBy·c  ¾lÙ·‰ `  	ÄX¹
 Àè´ú  0	µú PJÀ¶Ğ`çÙ  õ l2<	`µ8B˜  ÀŞ
  ¤€Ê( x»Îi³€2 ³î4ˆÃ õÚËú.öR¥çrR!	ˆ*À%ÈUp¢Ô370BX _$†  °†ı úB˜  İÀŞ
  ø¤€Ê( ¼»Æi³t2 ³ê4Ã õqİæÊ/öD¢
pÉÿ+!p¢jï@(X9`q)ğ€!WÀ8Ê# Öñ úb( °s ªÅP©LB`)¦ÆôCöh0@Šà5  2 úîA˜  ­ÁŞ
  Ø¥€Ê(  ¼¾i³h2 ³æ4˜Ã õÌà¶Ê/öD¢
p	ÿXd
ÿÃK€	A34$_  ö[ tìú ¨" ¼fµ À °É×è[ğ!€ÙŠŞö 0   ú¡ 0¡ú)» ¡ öÊ    ú)» ¡ĞÀ`Fh 4p ÿ#¿<)»öÒ•`¥‚ĞÀ¡0bñ  öÒ•`¥‚ ö¸©` €} ÿ ÌßZºõ  ’cLéï@ P€2 ÀtÈÀ7 àúÂÍq(ánÎ:)j¤ŸhE }3@«‰şŠg6)8œšô­3´šè¯F	`  ‘Õàrà8i  û@# ´  Pˆ}ìâ ĞCÅñåD êÍC)'$
§ §Jë‰¨š–R)'¦fÊ”2©3¨š–iz’ )©™šêz¢N)u¸@á4›auP—.İºªs˜"€\¹ÂtêÔsr ·n]ºª)¬Ì¦³WTßzŞ²3ÁÂ T®Œ7Ôó–•Z3a•kGWT>JKfˆÊ‚	lNNN/)5BJ $%%d8J¯TM¸œË4%d8NNNJEé›– •3kšhz²N)7Zàäb¥3é‰¦'{Ó)ªìpºlº¦*iz¾,Ê¦T).Û²-š¶R)h«®òûºV)* ª° Û¢Fiš/‹²R)«Ë¶l‹¦Z)(Úª«ü¾\)1:N(`µ32À›P»ëÕ3B o’I<NJZ»^-¦Óá3I<N2@7BJ7662o’J2 ªb:87Jªbîv³Ù3  ˆ= ¨´:
{€*èìèğ­ìã9	€¦œ¾}
`Å©;ƒØ{»]«¾ğ8À€LC‚EDw8>J>îºs)9C‚EDw8J,u€NÏÇa  ×²vœëvËÏ±›C­$6dçJaO@Îå»Xa@R °·Q4 'Ê­!:JM³J'=šñÙ 53ÈÎÁÓ´/Ş4 ¾ZÂ¶vJ MÄ’¶Ï®OP#ã	é3€7À©§a—	ğÊÀ[‚Ï³(p-Q+Ô!:JM±J9xL‹7ÿ€Rğ2ÄìV  T a¤¤T`D  @5i,8B @ÇM"$%K$Uà#=È¨ÁIú<S< lsÀ,%ŠÆC ‘€öy,LUAê ™  d%\³V<`E!uOğ  $ëè"#Š8¢f®ì¶éˆÊ 6„6ÖˆK¥.Pì›+ğcëI`€,0‘ËşTBÏ\agÿJihQŞ€\J o‹_^Ju¥(vJ6ø0€Pö8 À d'º…\BÏ\agÿJ^ihQŞ€J.o‹_J
A#DÚQ3JšmTÍ¢})ñÉ&#™(ÇÉ˜j¶3€€(VäÁ®³`€
Rï;P=8ÔtlDAèitä4	PVn:&"¢-ÒÕ(ØT‘É8ÖBêĞA–4£CÂÍå03&S-„JˆB¸fˆ˜37püEĞ–ô,$ÖBêèX#ÖBêèX#Û'HœJ¢`€
Rï;G’
ª’
J).b-ÒÓ!(`j
“’JÜ`€îï;Mh
ˆJ!£:T3?„áQ±E¤åÒ±yÖBêèX#=ÖBêèX#ÖBêèX#ö`™ .;",ÂS(U³h_Jÿ,ÃdL•f('ÆT³e‰3rˆ¢Nì$`…‚Ôûÿ j ºÇp r P×p{‡¢ ô43€PÃêO&"„¶Zt3:t…%ÿ	n.‡a-¤3Sh!Üø¸ñ,áš!bb˜3¿@€–"½/)7JéÑ!ÉX)Hà'ÌZ:)Û'Jª ‰¢ÌZ:G’
J’¤ÀˆµRH`j
“JˆÀVê~:B¡ù@+3BÈ(„3?„°:T3E¤
%+µ::ÖÿEztHÑ3¬)"¨ ­‡Ê `6ÑÙ®œ "ŒÕ3\VÃK0Pv óÏ0@v ³ìÏ0@v “ìÏ @u sìÏ½`€•"½/¬
R¨+ğ°	 Äğ¨	 ›Äd%¢lî°	 ‡Ä,Œ]Ñv³a!‚	E
5,CÛ/¥Û_J €@àæõÃ	
/|)J€àÉ!—XÏ ùvº®óXT€ÖÙÂ, 8xeå ®H2jp’ÿ €@•Í
1})JhÀ`Î,—X):H!Kàˆ¶ÌZ:#hû&}ÀÈ æßYq À 0ÎÉ0ÈÀ˜Ÿ @u “ìÏ‚ÔAêJÿ6ø#ÃZO5 `şÏ
d%¢ál6à~€‡¼%€	@şÍW @°şÏ"SQ¡!°Æ4Û^J·¿Âln¬g‡‰
/|)J ô‡L™8 u¶pÿ8 ¼X¹ù "d 
K°(= 0Ò€›dLGŸgŠRÿL ` }Ş!ÿ¤R€LK
¶VJ7F<|\¸Í"} FHşt .$.1Î®>Š …"í3¦’fU³Ğ)-1'ì˜–%	Æq2¦š4×äE# l:
òàljè†ŒµRH 9P}sCûCQz3H*ch&B[H-3Kp„M¨-šDÖBêèX#›ctH¸¹4&&S-„J)‡¢®3pƒ#¿H[:$c-ÒÓ!(œc-ÒÓ!(Û'HJ’Pe°–ô,G’
ªJ’HqA°–ô,Ã€•ºŸÎÿMh
ˆÜJB¬„G%')E¹t¬Çÿ©CQ(2¤4D!Ï°3ˆBa-¤3( b°–ô,½`€
Rï;lÀ_ ñÿIZø[ÿ(8"’JÜ§ˆT
P3nC€Œ9üQ-y)Jhğp­Ì˜8š¨ Ë’b‘@¾` Œ¢YIê_†gá˜ D‰ÇŸ ÊM€&@Kh7
-‘£‘J@€g6_  i©r×¶Zt3©VxÈÄ®6àpè¨rW @°ÓıÏÒí¯° ›ÿëY†¶_Jl¤x@ n³(	ÔûùIj > Ê'‡§³ÏpäÛé® ÌXT­ÅX,î••Kà¦ˆT
P3CÀÇŒY
1})Jhğ0³LY8:HKEztH3¬)n³ÿ#i ƒ[ğİˆs®0ˆ ›Äğ`à_‚Ú0(; L€¸À$û³ÿT€	0v˜ÄABA¯ÿğ'V…ô5ğ°	 ›Ä’8DÃj³ğ8à¨²’ôØ	à~›Ä¦¢BC`¯4¥ ¶|ÃŠ…ëX m¿„@õ~~ÿG¾®†+ÿ‹
 uö3ª¨Â,Šÿ#¸)"•3¨ õ¸ÌÁ,Sÿ 2€öy¶`€•"½/
JÒÌwŸ!(ğ
ãÜÇÈ®~?ğSK PÑÅÚ{^ÕHs"†) X
ĞçĞÄÅ	˜X„4
h¤E*Š,!	Ø)€û]h5,´O´03ci*i¶QF©fË;±3¢ °ŸCğ\ äˆ–ÖBêĞAò4 Tà5/ÿÄ€•ÛŸ7ÿ¤‘@EKµ™Œ)"´)K›J7>n<Ë.ÿ5†ˆ‰aš¶3ÚBêĞAÂ47¿J( a°–ô,œ†µ:t3D¬%=‹ÿX‹ôtŠ6ÿjP0	I©‹8VÅıĞAÀ4Mh
ˆÜJBáQ±B!G%å%R…Ò¢C(ü3©CQ(23©CQÈ3¬4éÑ!EÏX)€ú`ÃZO
à"$ H  “ü›Lm T ïÚ›š İ"‚¸®XĞ¬®¡Y”–!v”›OKšJšm‡ÿqœŒ©&!ÿr@¢ˆĞÄ= ø_{`€•"½/z9?ÿ¬ö¨+&cŠmJÿX‚#6k;\c-ÒÓ!(ø€F.İGƒ(„kÆ!ÿ¿@€
Rï;HA2ÖB
7ÿÁö	’‡ÿ‚ª¤‚¨’1ÿpb­’6ÿ`j
“’)JÜ«â~è 3È(„ÕŒ>ÿExTDÁ!„)E¹tDñ#„)QÈ3¬ÅŒ ÿ=c-ÒÓ!(8c-ÒÓ!(ê„
!•+¸‘ĞÔ«ûëè. pF‰äU³h_
GÿÕlYbçŒ>ÿ	Æq2¦3× rˆ¢šá c­’(ÿz9FÿEG$PÑÒÿaj‹&(ÿ)KJ	7—Ã°3S B¾C ¥H¯ã>ÿH¬%=ÿö	’§ÒEÿ)ÛJ
ª’
¢VJ)G’J“ I
.ÈZ:w‡¢ V+3h  "ÜÌª:LQ4)Qüa
ÿ…"CZ(-3y†µ:t3`¥H¯ãÿ     ’J0Â        ƒ  À   À?      ABA¯R<ÿğV…ô5Ô€¿Mbşğ°	 ÿšÄ8D$hál"d ‹ (9 ğÑpŸd p@”ŒÍ	-y)JhÀàN+X/ £h)ÿ,8DI¾K£C"©úg3-@•ô?‘  nœvN7
-‘£J $ğt-¤İ)ÿ × „pğ€•ÇÇğTà¨İ’î°	 ÿšÄ`B‘Bc@ÿ CÛ/!"ÿ¸Àz°¸=Á!S&Î9ÿ¾N¨ù”“¯ Vn>("ÿ,‹ƒWK#pSD*3TÀÇnÌÁ,SÎ(ÿ (€öy`0°2]G1ÃÇ…£‚R"ÿ‘Ò€?İ9ÿ3 7âÜÇÈ®ÒğÀ¬Eÿ5à`*©rq  `şÏ0À(A˜Åğ¨	À?ûÑ  €?pæŒÄABA¯GÿlÀß&ñ(ÿIZøÛ@ÿ)"•ÔL3ÿÜ%c3
ÿÜA+3&3ÿ KˆöyP h6T ³ 	D’¯®	„êŸıâ2„Q!=7(‘€ph¶YI <ƒ'İ	ÿ2,(’™VAjé˜ V 2ñ5®(tÃ³ğ¨	 ÿŠÄĞXã+,ÿ_€° ‚©¨O,†¶_J·lËŸgŠRJÿOÀ!%’¾ChıŒ£ê	ÿV ˜¿¨ØÜ‘JÁÌ	ÿUà#=È¨ÁI‹A‰¢ñeÿ(ÁĞ<æÌl0ŒÕÄÑ±ÃÇ…£‚,ÿ‘Ò€?Cÿ‚ÔAêŠ	ÿÒ ø#ÃZO`‡³	ÿğ(’tø±  €?}˜ÄABA¯RPÿlÀß&12ÿ´  PşÏ(8"’J\DH%4+nsøÑAÜA+3&<ÿ(šÀl	ÿ	@$/¦R	I*Ù/N$ Üšm	ÿ
-‘£‘
J’ xfÀtŞ\	 È`©’×‚-ÒÕ(ø:ª<ÿ;à~`şÏXÆ®èlí—Òm Ûÿ7Ö°†Û—ø<S< lÓj>åäÿóUkñ;ÿII ÅÉRÿ=„‘Ü‘3LGŸgŠÒRÿJ ` }^nÿVLR JKVEzè3(
¶JŸ£ÇçBÛ#8Ğ‡‘Ic{6ÀàáÈÀØ„ÿ3Qÿ0 ; L€¸`¥H¯ãSÿ)½JÀØ„ÿ3Sÿ8ª¬ä:ÿq  `ÓıÏ;À(A˜Å,ˆ`*êÓÿ›ëÙa¢Rÿ@õ~~ÒAÿG¾®†ëXÿ¯ Vn>¨:ÿÜ‘JÁLÿÏÈ[ÿ%
1}Jh < Ì1Ës¢€-³–NXÿJÒ|¸1ş¿10 àÜÇÈ®0Pv ³ìÏŒÀ$ûó^ÿ P`³(èe°–ô,a€
Rï;6 `şÏÑ  PşÏÁ  +éP–±+ú[ÿ"`‚"4²B›ëÙa"Xÿ@õ~~RGÿG¾®†k^ÿ¯ Vn>(@ÿÜ‘JÁÌÿÏÈÛÿ sÌò\ÿ`¥H¯ãË?ÿèñ¹Ğöˆ
ÿ€³~.Fÿ0Ä ş õ«   Ø»ú  PÅ»ú–˜[€·“¤ H“†­+àõTXn‚”œuï È®A=ÉÛĞeğ<­Ğ­øP½…Ğœ¾¤Ğ6p'0|«ĞğpÆU¡Ò˜©Øå è¼ú‚°&î¹Ì„œæİŞšì ˆG@KÏÃE8,ĞR†ÄÁ<Ø>ÎÊˆĞ=‡×Ö & ¬ÒÎÔÊ–k® TÈ“Ò 'èã[5Ô£‹®ÈAÂÄG àrÀÏL„] Ùò1¨
 Ä„ÃeÌ/ŒÊWèt0ÁÄl4šÇÒcğ©Ğ˜ƒÏSp0äşÅ6@Ì‘Ú.AÃpQàÖèO X(¬ÚR°3 ö¦Á4
p !«ÌóÀA –‡Ïp.`e¦Äm#vÎb¢®sà_€tÏÁepA„˜äÚÀ[à›šÛÜ¸è¾“Ğ ¯­ÉĞ éÚP ˜†Ù}¨Pÿ‡×‡ğ‹QA±Äó€tÒãp#5‘É˜F5‘É˜F5‘É˜F¹ 5‘É˜FÆ;5‘É˜F5‘É˜FU	5‘É˜Fæ
5‘É˜FQ(5‘É˜Fš5‘É˜Fò5‘É˜F•HÃÀĞJ` òÁĞÅ-g’É®}àPçÉĞ œPmÓÊQÀ#`ğ£È÷ ğæÆÌ9@
ì8ßÙ7‡ˆ)âÚñ 	0¯ƒ× Pı¶Éõ@B fÿÂ˜&@W§Ât€%è&øÊR€?êÙôÒ»œ°áçÅ`{ÆÉC<›Ù	à€;¡È}€"@GŸÃè.±6`(·È l@ E–~ $&‚‚}	 ø5‚‚}DĞ*H­,	 _Ş;,õ E)‹5 $   õ€  ©íQ  ĞÛ ú°R€G‡8  `\Iõ¦u¬
©ë<´H±è”- $  ğõÕ¬
©í<ŒŒUAjé˜ ¬H±è”-):<l 1³\)  èDıÿ €§K‘{ù´
©í< ›€”œu ÀŸ€· ˆ€·) xÛ€·	 €p€·`E)‹5   õ€  ©ëQ ¸  &õÙFÕ,tÊsÿ';L†d*R)'cªÙ²Ä3äş×ÅRÿ5İ Ûqÿƒ(=ÍL‰ÿa€öï;"ˆVAjé˜ › 5‘É˜FÍå0¬ÅKÿƒÉT¡ÒQÿH®h2(µ::Ö‘ÿEztHA2¬)'@‚8­G¤‚(ƒ•KÿG’
ª’J).„µ:t3`…ûûÎÿExT¬ZÿáQ‡)åÒÅ)D¡È3ˆBa-¤3(èa°–ô,8†µ:t3€Sğ+ÊYûJğö€’h„ Xp£“€›	&”Jš f ò™  Ü 8ˆ‚«  °ˆkŸ£@@ K¬1Æ8cÌ-ÀeŠJ(-.fHZ€¡ˆ1MC‘1%)M€Ğbx4- f l‡… À—Å²4·JÌ Ì< çj«½v^·JPQèE]TzQG ÿ|­ÖZ{1-UµÖÚS°yæŒQÖ`ñÔ‹«ÅYƒgfS–aÕZÚo&W€Öj¯µ5_¶æ[­¥ıfßh­öZß[í­µZµÚŠï”k”ë‹ï­öV,4.a+[o{÷½—vvãüyçyka²¶&ÛŞ}ï¥][8ŞyŞzkóŞ*EÖÖn—^ŠsõöZöÚyo• £(—bYõ'ü·ÖÚÓêöb
$°( ğ3V ¡˜ÆœH Ü€ÓÊ”'“ÚÖ'h™î':ë¾egÇ‘      œ       d 9	 †,Ø( 40Â8U‹IÜ	 MRJ 	ÛâãÚÛJ  Œ  ÀBˆ! `  ›Sx5ƒbˆCP˜… 
 æ
A”¥0 I
( @!¯
˜ˆ‘( € Ào ÙíuÏ  @À´X À€4èğÕÆã
;ˆÍ@N™ÂÃ„,áşè    ñÖ2 µ<@ş  úö € {ö‘  Ø 8ïõö À À õâ  PBØú©å%@³àù~ (ó“òq@ ûŠ úˆ[$)!àµ[‘DI '	õô9P8r³=TïÀ/®‚&$rl‡`À}ÓÃı ıÕJ6 `iê²«Ş>  À¹ì¸   °$Õ ú  ,A   6/  [&ÖõÈ €¡ ¶  0€ ¼¸ ¼  ÙâõüN=†ö ğ€z şüN È–¡€ø4   úi °ù.úèDüŒ"³   ‘´Ñp€ğ9‚Ğ,	 Ûº’t4) õ øïêÑE\=›t¬?®ÒD+âT®ÀÏx¥	è ŒÖé}° ğ@Á¶K²4 Ü®GÒ R†¨€üŒè`| 4ÿ8€”¡ğ ÿ€ñÈq
è °XÖé}°€ÔD¶MX²4 Kİ®p|‹€! õ	  p¿ ú@`6h†p¥ -·Æ‘å& L	 D|@ë |Ğh @¤'  h#0D£+òÇT;  ú  PŒ ú  àa¶ú  PM¶úYTÈ
@h @h2@TYLé6õ„€+õí®øÀ¼*õ  à¦õÕüØ^À£õı6ä$AÃ:õ<! ğï úJ \<úE ğŞQú Ô `#jò €_ j  ğ@°-@„ L.h  À pÕŞÄ  7å–°DÀ6 ´”P‰•X‰Û&	p’'yªÛ¡  “ı ’'	¼Û“É¸SÀè¦è§
¨D@%´Á`’'y’Û,	P‚“Y’ÛÚ@£›†Rè&	8Iœ¤ÒËS p”½ÍÛh0±À\œÃº)„R•t’İ“	p’ ŸÍÛ–PB'Y‚Û•P‰•h‰Û'	p’§J‰Û*	p’Àk’ÛãN£›†Rè±?PÑ2õ  $Îß÷õ‹ù'` qşù&	8I˜$ÒË'	p’&y’ÛG@ÙÛl’Û&0 N¦¹'	`’~ŠñÛÙŒnJ©$è'‰Iâª@@è%8™Û P¸n’Ú&	p’'y’ÛL,0çpÿ›†R*	ºÿ®
”Íè¦èáÀ–“i’Û'	p’ ÕÌ€“i.ÿ*ÉI‚™Xè O6£›†Rè'	P‚“y’Û§
P‰*±„Ò”àd–äöÿŒnJ±$è•°D€%´§
@‰•X‰ÛÀ`’'y’ÛLæ¸ğ¶ÿ   ×ÄEmÆ–,lâŸœäIê6ÿçHBB}ÿ•  “I¡Û'	p’§Z‰Û“	P’Ák’ÛIH°$ õ'	P@ûÜÎ¢ØŸh’œ$èI ’'™¹Û“	p’¢ôÜÒTb%Zâvÿ'	pª”X‰Û'	 ¼&y’Ûç$!A%õÏ “I¡ÛÎø&ˆ×ùĞœ¤Iäö
ÿ˜¤Ÿ&évÿI¨$à$õ&	àª‚öÜÒéP‚“y’ÛÑ z&Û'	Nà$AµIŠ(ö'š$è*	p’&é§ÛAœ #	õl	0™&éªÛ'	  ˜Û¨äIfî6ÿQ
œ #	õT¢J,¡ô	ÿLfIï¶ÿ, 8w³ÿœä©RâöÿIHPI@}ÿ“	P(àÜ õuµ>à›ïú\ <0â†ò ¦X»r G@ ¹   ë»ú Šºú{À^: (Ğè%   n(³°SNGbd­WJRKIU¢KSJÈ$UùÜJ4­W Y@EFJKSSNGbdJRGRKIU¢JE#2IU>·4=b‘FY@J8G=8F=ŠJ:¶ìòXÃé3‘F Ò×ëı3=8F=Š=bJÙå±†ûá4"Ï£‘øÓÙ3ªZ–e©–J)jY–¥Z– )  ¦*l•* @Áh(€(Ô¾D, Ğc¯;{Ó ¦¬Ÿ\ ÿ 8Â³i®TÚQ0&#Ê01 8XÍ‰¢¼ :Dğ¦„p¥ª½81 ´³ È€õ¾C§])J*€×¦% 1¿ßƒh®x,Wv®=ûäOèJ] B”àüÄ®) ¨5€È¸¢¹ =Dğ¦?†ôÂ¬ÿWşñ5#C§[+J'q„Ú™¦% 1[İg®¸‡Ó€¼Rÿ ˆ ÷ã™SWLRÊ®O‚€ >íWô$  cñ¢  ¢ğª¦p>uFª2áÏKTì 
Îv/€¼©ÿ*Ø,ˆx³Ú„¸e®õ Ä ½±]¤Tä³=á0ÔpÚP* H ®»Ë•İƒ«ÿ ğtà{< ôûä™dº„Jœlˆ  @ªÖÍ 5@.ô¸î €@¨Ú ø1 ½„¾ÃiÃÖŠÒÿ€Z0ÓhØO= Àp2Ïe=”³Õ®  ï„åªD÷ãNJ ¨OûÕÿd ˆ÷¤®`ğĞ—{©d CÀ’t€vqØÀ4  `Wú   XŸ´[u4>O©¹õ €¡Æ"èvµ@· ú$  ëC Ğ¸Ï+57  õ*Ä"`«õt  @† õH  Ö'-ÿÏ‹57  õËC!@ı ÿ4 Pøïqö   Vš´ú\ Ékøšõ  È^°×ÔH €•&í ÿHò_3  õ½ ô@Ÿõ…ÿ·@†Ï$ ÀJC Ğ¸Hòn_3  õ –!@ı ÿ41 @ `µo ğÚà úã vÜËƒ©¯d:Z`®|  Gõ) D|ë*®&r 0  ú¹s  °ÍúQ	x7JÊ%E0*  úD  ¬QúÏÄ (š‚û4@¥\³d'%-¦®a"‚ŞÊmq$æ“6l`4«JqÀCtB$˜LW  ¬7‘Œ8 u˜o¡®&Yh:`ƒ^®lÀ³= °¬èU­µÖªVµÖZUUU_ €h@~ 8Ã6rh1FéH=ÊKŸŒp† @Öİî!1F’“'Í¹
å¥OF£PÈİ#´ºa­PF`>*#¼Ö
IÑIĞ£2Â‹VµÖZk­•G ‘ ˜Zÿ	@R€@dK	™™™™‰  €ÄÌ”™™IBffff" D™™™!ÿøÇÿÿ¿9ÿÿ ÿÿÿÿJÿÿÿÿÿÿÿJ  h €şÇ`  À¥¦ú` 	°3^ h¦Ÿ¸^ É°3  @  ú   ğ`gú ézĞæÕd|]QÔ €¢ìèø(Ùù:D Øi§Ê  D ƒ=óÔFy@€|ÚÅÅª²ÉK4WB|K0¬EF±p „‚üÇ@›N ”@#®q€,'8V³æ0¬ äÈòiW+šKJ*
Jl: èXº“Ğh œ2š—®“A˜. —·½mÅª‰K4W‚wKı úE±p u‚ùÇ@›a E4	®K	Û±—å®WV"…Œ]l H€ÊèX›ã€ËÈ6ºMc «HF¥® A+ •·½Ã…ß’ÿ¬Pk}6„òƒc ˜§N€’ q­Qk y rÿWV4—Ìal H€Òòi›ÁÒtTºM §Œæ¥®ÓàÀ-Ğ¹qb*
›S J.QŸEK0UŠLËL%ĞˆkÿÎ X!€“’Ç  ´œrh.™ÃÖ³¥È¾®Kë¤
¶G§ £y©CG®Ó\Ü‚îybT6§Pl "¾Å8ª6.À¿; ° ¯®
¨@(ôĞÁ8+N|	M® °œÎ’K ğ‹dÂò€4ÀuEšg:º~$4ÿ §Œ§®Ø€À-Ğ¹c3¤â‰ƒùUª²Úú¶l}}z}VUVJ}}}}}ƒƒJî~¿ßï÷û4×¬ôz½^¯4×+wz½^¯4×ë5;½^¯4×ëõš^¯4×ëõzÍN¯4a _D
Å®_ <…‹Åd‘°‹ÃdX Â ‡¿daÄÄÂ¾(lb a|A)®_ 8
À†Å‹l Ú‹Ãd J©¯lÖªµZÍ¶û4}ƒƒ}}z}Jı~¿ßï÷û4×ëõêî÷û4×ëõzÍJ¯4Óëõz½r§4Ùéõz½^³4×ìôz½^¯4P ¦p½^¯daÄÄÂ¾|lb a|±,®_ ø
À†Å‹Á°‹Ãd±°¯`›ÿ_ p…‹Ådr Ø‹ÃdJ¥PAD‡®^ÎÎäàÂlF#Ø}E©4T¯Z]·34K£qï"‹4 îzŒtmqpJpIe†´JŠ µœ™‹ q ğ€œ†´ŒtmJµqUeJü¶üLF£Í4iv`s‚|Jğ±=ÛÉ4rjóxm3g>İï·‹Ó4®ømÎÓÇ45'/?@@@J –j€œ2l=@@@7'/J¢j|¼l9 :´x[ŸFøØ@&h­çï¦cŠ4E  :ƒ(x Øà·‰y~
…RË4w¹ú~¿Óõ4w¹ú~¿ßï4Riù~¿ßï4Ô¦T*¥:¥4R©”ê”J¥4LNRRRTNJR \tÁ£®.ºàâQW şS \tÁ£®] \$:®} $	€Ÿ¹‹x  4Ê®¾ßït=ÿê”J¥e ş÷ûı~¿ËÕ4R)µ)•J©4T§T*¥:¥4ª˜à”J¥d\ T%åv¹d\ ¨
˜r»‹\ ´
˜r»‹\ œ
˜r»‹? ’ôFs»dÆX (¹ƒ±‰h…J{ß°lwˆ~h ‰J‚ m YH!®5‰ |ÿ‹œ46Úb^®F3Ò8ãŒ®ÏğønŞª7 €£ùkIª : :†µœ37Úb^®S3Ò8ãŒ®À°›·p@ª7 €£ùëıª ©òÒZª½ ÀÍwÛzªpåš´vç lÖ¨©…Ğ³– ğtÁl™º‡&­Û9 ÿ#h¸rM›ºÉ¬İØ l¡ºÀ,?l  ¼øÕâš[¨¾<åœ lR i(
ÕM® Á“e¬p¡J1ñµCÇl[T_šrNœJR k0*N®†Á“i³uJ•¯±o¸m›JîˆÈqÛTT)µ*ş3R«”*N¥4RéT*•J©4? ºÖro¥dR*UJ­J§4T©UJ•N§4R©T:•J¥4RH§~ØÂƒKS ¦0N§dA| Z`lÁ`04 Øª Œ~hlû8ÔÆ¶)tèø9İ1YQVWaRJ–d°{pfJVWsO[‹Jt~€ ^Jpsv³½4‹–p¼{Je_aOaJŞnSMÎ¶Ó4dcYG[QXJ VUUoc‚JZ-³Í>ÛÓ4f?yœv±4úw¼~°n±4yuwvih¥J{mvws„J WŒcZ‡ƒJih¥zoquJws„yuwvJZ‡ƒ{mv‘JUZ^WŒcJZjÕjµbÑ4Ukíf±4^“haVgVJ\Ö-«3[¶«ínÁ4aëù»µ¦Ë4˜
 x'ÑŒè$àÑÓÄ5jŠúº 	l:9İÍ<›Õ3Jµ}Û¸®ï*¹è(E"Ÿ4˜
 pã¾Ù]¢¾.H[ÿ7ólV«ÕŠ4·ëú®çt)@R$ò©¬Ù377769QCJşÿı§Â®üÀŸ?ÿÿªˆŸóÇŸ®¹°81ãÃªûRWZPlN8á„®VXa…®ˆ}_a…®‹;1ãÃªÀjm¡]Œ±”egŒ’JgŒ’ ¢J¢Œ±”eJÊª«¸¹÷J¦¸¹í ¾Jx¾Êªšøã!I$’®Ç€*æ®fWn†SªJÓŒBšèF®j&á¨¤|l§Z§Z²…eJxcja« ZJ[_ñì¦oJ‹ˆFJÑ®Fpß¸è¢®ÖPzôª UÀBÑœ®f à pç—k2ë´°>l†g²£W´uJYböœşµ  “ş|JADF4‚æ®b?¾»]®S ª¨¡®Í®Ül>Ó1
i¢®…ej“pTRJ Z§Z§Z²J¦oxcja«JK¶¾âÙl‚‚ş‰(E®ÿ FÅí¢ ®s`½:õèªs«`)lX |0Ã¹‹W´uk™uZJşµ|†g²£JšdböœšV´˜ ºvl‡‡‡‡‡‡hJâ´½LF{å4dt‡‡‡bZJ‡‡rredhJd]UQLq‡Js^Z`iyeJ‡‡‡‡‡‡‡J„t}tyy€J‡‡‡rÌ^qJMr‡‡‡‡‡Jãı6ŞŞbŸ4fìG»úlf}^ZZZZJVs†††††JñpZNV¿Í4t”’nqJˆiVZLP]J^rˆˆˆˆˆJc­8}/“Ñ4dhdtˆˆˆJqˆˆˆsseJi}™Ü­F™4ˆˆˆt^ZbJyy€ˆˆˆˆJÍ^q„t}tJˆˆˆˆˆˆoJ]ZOMrˆˆJÆ°ïÆÛdZZfwÈ£]J‡‡g}^ZZJofVs‡‡‡JapkrdjJp v–”tvJó·W«kÑ4|‡‡‡‡‡‡Jó·WÖÆû4Z^ª|‡‡‡J‡‡‡soƒJs¯¬=Ÿã4FŸá~>ÿä~š›ı^Í4Ö+ß¡’ÿîşÛ«Õjµ4|‡‡‡‡‡dJ}dcšoi„Jqf£‘‰Jô·W«kÏ4|ˆˆˆˆˆˆJô·W+ÖÆı4[q}|ˆˆˆJˆˆtozWZJbZggrˆJˆˆˆˆt^ZJgm|ˆˆˆJı5W+¾šÏ4ˆˆˆˆˆˆgJZZ[­|ˆˆJg}b}^ZZJ†|ˆˆˆˆˆJdcšsmJ¾ ¤—šíí½÷áv®‘‹õæ|{®Àø±âÅª+ - Šc¿©ïnçq*®Šÿ¯·Şz®ÀøqäÅª¨…TÀì ÿ'¥T0.$2J7·›—{º3Š¹Î¤{ƒK¨ [b½U2J(¦[-.$2J..Ù+îº3Ši®ëºFT8[bûU2J‰VT VJz  }•sî‘¿l‚‡¾J‰ J„ H$ò½®€zTv swJºßø¿ßğ4€zTv vHJv$M ÿµfÑfû48FœP	¼ù3:C'Ôé3><\Jâ3ÏÂŞî–¼İ3<¡7	åé€4 ÉåÚ3'ToşŒÿ@<¡N‡3â39Pâ§ê3;·äíæãá3P<:@ÏB=J@ ĞÆz“4ÂdŠhI.‡4<H —CÖ3»"òxÓ†4É <B“4H —CÖ„4EÈãqLò3F²“ğMî3;Êì[Kù30R¡MçÓÁ3F2¶ïÈî*hÓùtğŒ ÿiûìK…)NÑ'y¬Z3Pæ’Ze2§4É¤”9µJ‘4Q€{<4Ì©3ú$µ4R$”¹¤V™4Fc2)eN­4Gb0âÄ4G!’ùL‹4Öe“7…4Kµ´ùKJ¡4Ç+2k|Ê—4Ii²Ø¬r›4A€Ï¹E&§4ÁâÓ7L‰4Â£ğ(\>“4Òæ	µB4È¬ñ)_.Õ4Éb³Êm¯4>ç™œ&¥4ZFkùº|>3Q¨ÙıR½4U+UºgÇ4^ó¶¹H2—4WåuÚ%#Á4Ó¦³JÕ>µ4T©u‰24©‡™OUJ‡” ŒŒŒ›JŒŒŒ›©Jj äAØ‰:®‘u_ZE_ÈJW¾j|e§±J¿®™ˆÒX³J¹îöö$lê ·®Æ|Jk ÔØ1„®§±ŸukfEJX³W¾l|eJ{’¿®™ˆÒJ| @ ß÷‹‰pq·®ÆJzbn|m¢›JyqqewŠ|J€÷<-ÛÍ4ro Œs{Jbr}bnJi±=oÎÇã4 ~Tßæ l? Q@Ñ ”®P B	Ê®::::SZÿJç.“tt lPœ…™l·Ûíşı4:º©P_Û3r›I::::JŠ Â@ Sˆ©·ø÷{B4DP@}m»İ3zUªr¹_æ3P ¶ãé”4XJOK9DJ0Ø§Sêµ4$"ŠXm®¿ ¢ˆÉ‹˜ñaÄª+ - Êâ½©&¯©“ˆöO8á„®{°81ãÃª§SWZPlJV€…ŒiTJÎ*v­V;“4QNOT˜|nJTSKMIJ…ŒiT IUJjjNIJV€JT=ßí¬b×4SKMIQNOJ~IUTJ‚„H?nJy~xƒ}„JGer{xyJH?n~ WJ{ƒ}„‚„Jå6>Ÿÿûû4½•¤ WGJ¤ ¬t§m‡Œ¥¬¥J¥»“¢ ¢yJªFE«Ïl¡yp……Š£Jx\«S'3Uj6©õv•4Şc–èv™4_¥vkuŠÅ4]]LUTYJØÌ]D·*:ÿÓ=zôª” à-ûsêª` É¬1Ş®arZ§’iJ‰¶¥¬W´Jú]sbjqqJXÎFçæÌl»oe[PRWJÑ=º?Úª  0]ºtéªm2 ë<ÿÑÑÁßÛ©ş “úôèªÒ =zôèªŞ şútéªÉ kŒw %®Z§’i``J¥¬W´brJsbjqq‰¶JXetrss«J#oe[PRWJ„ŠaZW€‹JaZW€‹ ]JChx]„ŠJ(1*¦
‰4Q1UH¢é4) X ˆ¹oŞÉlw¿^¯oêİ4qq»…………J 7†Ñdr’‚U~xJnMkJz½¾©w¿ß4»…………w~Jc †FÇãdTğ	ı˜l‰İšÖ l³  	‘®€à.	´Åœ4”à.	´Åœ4 ™¢sÑÖQJ™¢sÑÖQJ/Nğâ„4ÿ4ï¾KÛè4÷µ{N·Í4€µün³Û4§Óm3 ÿçp›İ¾¯İ4Šˆ—’krJ‘ÅˆŠ¦—œJ|ƒ‚ƒ”J—’ äShJˆŠ¦—œŠˆJ‚ƒ”‘ÅJäSh|ƒJ™õ¡ânèÁJª½’nìzJ»Ô˜šr}vJ9e¯Ä¡mlnèÁ ìşÑJìzšõ¡ãJr}vª½’nJãĞ¶»Ô˜šJ
Ä†ÒX²WJqj{‹k||Jk|| qxlJqzlqj{‹Jzl•‰nJ~~…t•xƒJnƒ~š|\QJn }|sJ•xƒzl•‰JkFG„~…tJ|snƒ~šJrm‰ˆ}J–s}{‹o‡J`_zm‹–J‰ˆ ip€cJ{‹o‡rmJm‹––s}J—4üİt–õ4rfk{’k…Jt pˆz]®† Î øË—y kÀái¯®¨ÛÚ2lhn€•k‡ Jp ¤‚ë{r›‹ĞüÊë lz mÌá©¯®«ßÚ4läA#ğöÖ†Vf  M8ì§d ˜ rŸ}œp¹ÛïãË4ï¶œG¿ï4qpm ttoJš ';ùGªy >3è¦npzed¥Jt<ú}‡Ëİ4'ttoomrJ¿ĞønŞª7 8 Íó×©Àvğµ“4 ğüõïŞªË  ¨uã€Ñàœùsåª@*X[ êhÎÀü¹òåª+ - Šóè©#!‘\©›På É’ª¼@ `uã€¯ğ»7ğİªQ-¸q ıh&PrdH‘ª7 €£`•ƒªÀvğ†¬“m jİ8`ÿ€²xoj&ÿˆ  Hµ¶€Ö€.Ú%ÿH~×'©›*ğw¯o¶ªHü¼¹Ÿ(^lB®j ÿà×1ãÇª‹ jm¡]~à˜ñcÅª@*X[ êhĞòbÆª+ - Êã¿©0©“( ğøïãÆªg  Hµ¶€Z íZ³ª@*X[ ªhj€v­Ù³ª+ - ŠZ­©;lÜˆTŸ( :ª¨#®7  Hµ¶€*:â¸£®@*X[ ª#h[•ëU¬ª+ - ÊÖ¥©\®`©“( °uéÖ¬ª  Hµ¶€r ğ`¿ª@*X[ *h‚ x°ß¿ª+ - Š`¹©}ğä©“( À—-_½ªy  Hµ¶€lÀWo_¼ª`RWZPøl@óüu*ÿ¼  ¨uã€7 8ú9©ÿD   †µP ® \™qáª4k}>Ÿ4O¨W
•J£4Ï©4-u^›4U§´ê}f§4Ï'ô
fÃ4Òh¸*ÍZŸ4×æê•B¥4Ùés*MK4ÙpÕ)­zŸ4ÑêÔ	m
¤4Ïgô9…R©4PiT}F¡4S'´	ğJ£4ÑçJ¥F«4Qiô…>Ÿ4ƒ^RQPRJ„‚…z{‡J„y«Š‰‰ŒJP[†u„o€JRiT•J¹4÷û<¾^G¡4tqr€txJƒ lrtoqJ„‚…oz‡Jaá ’ÿXRF\PO†Jëu*•Fõ4txwwsqJtoqtqr€JÖªµjÍ°å4V Ø2 QËÍD ´l­Z«dQ¦®¦ª’lSköz¥B£4n-™[¯4Ñèô:­ 4×©5{½R¡4P·–L†­4~qq†qJjk‚rQU}J}ZZ‚ljLJYhIf}Jq†q ~`J‚rQU}~qJZ‚ljLjkJhIf}}ZJr}~`YJ›CnÜ¦¥¸J‡ÇªxÜ¦J r€Êªq›} ¡…ˆàJCnÜ¡¥¸rJÇªx×¦›Jr *Tí‡˜
¡…ˆàJğ+ÖÊşÎ§4ã,=—¿4SÀwsÉ4_økeç4äq‹†Ë4Z©á»Œ¹4SOULºMQJZR “dX^JÃ$–—R ÿzÔ¤³¹…:3GoYm6“Ï4	¤Ğ
›dNrt)”>™4P>Kå>©4Qìóà]>™4f³ù¬§64R „ <¿dK„¤˜ŒlĞ1K­v£4juà]2¡4Z}Pk`fJ`L[m‡fYJY+óIæŞ­4à¯Ú_84nYZ}JqmJOm^^T[ƒJàlÂùÌN™4Ö®4;‹­4R%Ô-­nÁ4 ½¢k]ŒlJÒìX,¶‚³4P·´º[»4¢k]ŒlRJJXSVQY
½JÖ£9U*»4Ë¥ô(…·4ÎkóÇû5•4J]ìÔÍ3Ç­õèDN•4Mår)=J¡4Hçµùãı3À-÷*^›4ÚêwÖn›4=¡ ´Z¹4^è®ÊÕ–‘4Ùï+6Ÿ»4ÃÃ°½àÜMJNrzLqlWJQM„pZGƒJÕ¡óÌ7G£4½å Rƒ›yJLqlWÃÃ°JpZGƒNr}J|fQQQM„Jƒ›yUCNgJ¯µ °ÍRJşÿ÷{ÏëŒ)Ğ@A=CNJ=:D±¶£±JCÍïçÛù3EH‰ ö(€òÍdæîÈÈâFlhr  “ÿKnNvJb2™ŸÉ4á6|Ü‡Å4eòx\ÖÁ4k´|—Ï4gjlvi€[JNvòüN&3cdhrKnJa±˜L&Ãç4cp¸·Ç4ås™<—õ4[khrc`bJgjlvi€JÜ n¨§±›¾ ¬Ú´ß ŸĞÿÑÖJk 6íw§ ÿŸĞÿÑÖ¾ÖJLIF¢âJ¼ ±Ç3:­4I# y+:4AcgtZ™4s™æ­ètò3ix›„KJP<Oj¡AdJUW<KO<J^Š]/_c‘Jx›„Ks JOj¡AdiJUWŞòÉƒò3Š]/_c‘JÓÜj”)¼4J£-jEV½4F2 £D7dJÖcÒºÌİ¨4D7dJF6QJ˜³®¾²8£JG•°«»¯ JØÚ×ÙØß·J3xïÔÖš·   %CtªÖØÚ×ÙØßJè ÌŞïÔšˆ ˆ€FJ†©‹~ˆ”™J‹y‹‹ƒJˆ”™ˆˆ J‹ƒ‹~Jîdó‹˜îĞ}{÷ªĞ¹AÍ^{®ºzå¥ t®~À–o_ªºHµ¶€Ì°Ü9rçªÌĞ<9ræª¶zå• r®( À—-ß¾ªx ¼Hµ¶€[ˆˆ¨G®-Ğ u“( €ˆşõª= €Ğëó‹ämjv
l D@ßû€Ì¼¶ ÄÉ²J¡š”ŸŸ½¦J~¢„Œ™‹ÎJÎÓ¼u}‘J­ÁªÖ¿ÂªJœÒ¥˜£Ju}‘~¢‡J™²i~J‘‡‘£œö“J€ »¶o^¤Jé™–Œ²hJl[„‡£JÙô‹Ó
»–J} €¢›Øš³¹ÊÊ®§€JØš›‰›¢¯Jû‹Õ«îJ ˆ¢Üİš¼ÍÊ±ª€}Jš›‰›¢¯¶J¨bY«îØJÀ%†Ob'¨b øU2J2Àc€^Å]Àzƒ]øUJ@JLbwŸvJ¤wie]JQJiyÛ$ûı4MRMk““‡JvÀzƒ] MJQ@JLbwŸJ~¤wie]JJirMXMd~Jw€&ÿ»"N—İV‹4p³¹ûâ€4F€ò·ûîó48]v[-†»4æîWˆîŠ4Êßî»·¿Í4£«öÜ4ll ×
æ¦ş~	l~¶lbSxJ– ÷µA¡® ˆ$˜şã‹õô²„‹‰JùùùüüùùJùùùùùùùJüü|}}% şúúúúúúúJôòôõúúùJùùùùùö÷JnXèùù›Lp	§›®z t	'–®JXZy®yòòöü‚lM´ğïó‹*yğa›’®P jX³››®‘˜‘n\]LJgk^ƒ‘˜‘Jnn—	UwÑ4ƒ‘˜‘‘˜‘JL¨º‹>¯½4‘‘˜‘n\]Jhgk^ƒ‘˜Jn\]LPj]Jî¢Ïk/ÿ‘n\]LPjJ^ƒ‘˜‘‘˜J]&TİEŸ×4˜‘‘˜‘n\J]hgk^ƒ‘J$fd$¦ÿW”šR ÿÙš×`¤Ò ÿÜ.ªî¢Ï4‘˜‘‘˜‘nJj]hgk^ƒJ˜‘n\]LPJk^ƒ‘˜‘‘Jv~ohgJvvvvJvvvJš] ] R ÿn\]LPdkJ|‘‘‘‘‘‘Jà²İ‹>¯½4kj‡šhJd$f¤›ÿeBÕ]4Mÿš×`$¦’ÿ$f¤—Rÿ„ª»è3ÿ×`$f¤’ÿu<^>¯½4v{{||“|Jÿÿÿÿÿÿ×4…şğÿÿÿd““““““J“&''''l™ìíú~l›{vJØ''ïşş l``] Rÿ~~‚u€u€Jfüà÷ûıd%%–X"‰®($’H"Š®((ˆ"‰®($¢ˆ"Š® C—ˆ"Š®n÷qå“œkqÒ¹&®Y®™¤›®Yñ™e–®•Ff™e–® (%@è¾×4@@ÊÎZLP]`…J‡‡‡‡‡hVJyedh^r‡J‡bZbis…Jedhdt‡‡JOˆè9÷$û'©lıùø¶‘Jı Ğ À~ªıııııııJşşşşşşıJşşşşşşşJşııışşşJúûøöøùşJ¢ŠÆI+ÿhŒvÉ¨¢®z É(š®]tÊy®yòòöüºlj´ğïó‹*yğÁœ™®P jhs¢®×`$f¤Rÿf¤W—’ÿUwÑç5Mÿ`$fd¤ÿW”’ÿÚÙš× ÿfd$f¤Rÿ”Z—’ÿš×`$¦ÿ$f¤—Òÿ„ª»è3ÿ×`$f¤ÿf¤W—RÿUwÑç5ÿ`$fd¤Òÿ~ohgk^ƒJ] ] Rÿ ] ] ÒÿkvvvJ‘n\]LPdJ^|‘‘‘‘‘JhpÙîEŸ×4gkj‡šJfd$f¤’ÿ”Z—Òÿš×`$¦Rÿ$f¤—ÿ„ª»è3Íÿ×`$f¤Rÿß$Ÿ’ÿÿÿÿ¿¶ï÷4ÿÿÿÿÿÿÿ4“&''Kl“““““““J v ıŸ“œ›{v™vJ““wJvíí°l€u€uvJ~~~‚u€uJ$Ìæ÷ûıd(%–Xb‰®$(’H"‰®((¢x"Š®((’ˆ"Š®“ ]"Š®~ò‡r®Š‡–yh¢®$Bz¨¢®¨®3*h ®
hà†‘¾ Pˆ&ZYªhVZLP]`Jr‡‡‡‡‡‡Js…yedh^J‡‡‡bZbiJœ{edhdtJO1!Â¥®cÄÀb Flâ°øûı~Á4bq8ƒÇ4á°™“Å4_/8<ƒÉ4`€×ëõv¿4âï÷ûÅ4áp8‹Ã4f2XL‹Å4àğX&‡Ã4^¯×Ûı~½4EÃá°8<b3Fb±8ƒ4Ãåñˆ‡4Åb±‡4Åá<‹4Ãa1X‡4Èàph$‹4Ááp8\4Éã°X,‹4_àsx<îN3_àsx<î3LÌäR©L*3K¥2	h*•4MJJLLIJSWİÚ´Ø´JİÚ´Ø´ eJH ¶(O¯4T Î àq
sû=~o‡B¡4x<Çãñ4T§T*]ãñ4”J¥T'ÍìÿRjS*•^4N©TJuJ¥4L œ •J©dS \tÁE¢®]p±¨+íÿ] \XZ…®] \<*®.‚W ş] \H:®o â öv¹d <Ñ…®vP(”zÆ3x<¿ßã÷4Ò5Çãñ4uJ¥R*ÿRéÕ)•J©4T§T*¥6¥4¶2á„kñÿ\ r5åv¹d\ Ô
˜r»‹\ ¨z»‹\ ´
r»‹jL¹İE ş\ À
˜r»‹p à öv¹dK <ñƒ…®8`N ıŸÁ<ÌÕ±ã®  ğupîWª6¸ œú¾¹ù§\ˆ®X€JRÍ§1¾ø¢˜®/Î§áz®i‚é¦¹w®dQã¨97Ş¸ã®ÖÙyâ®Æu	FTÑ® ŠğŸœĞÙ+Q¢ªD@´è"Íª]`|Z¢ªD@/sšÀ¹³W¢ªD@”hÑEª ]ø´©NpH2ŠĞ^åMWŞ®M À«àÜª1êÕÈ¤“®¨œI]^y®päª¤¶¼lÒøİœo‹á4tos|kkJåùÜ•Ê4·8>OóÓ4Mhp…rvJM'5]:“4sqYskJNÎäÒ™l3;}«¶ã¡4iı0È0sjJÚğÖ·ßdĞ87©µl5œe;AÕ l0lÿaÎ&lØBmaNlı0Ê0wj Jm Ş o¿Ód› Ô wC„©Î² lĞœJû ` œ'Ö…¡¶0¶û0¶JAcVIB ØJ`*µE4Ğè0®L6Ñ4B ¾nÁºVJQ$ğìr“4Êd¦R‹4ÁºVPQCqJ^fhX¾nJItÙ<.~Õ4İtİkXnï4Rôš^~Û4Èø\nõdDŞıòåªÈpü8¿µªÇÀçòåªÈ€<9òãª f÷^r®ş E™n‚§Jã`Ÿ:õèªÑÑùã×®ş 0”:õèª„¿/]º*¦ÿu ‚„b~®Ğğ ˆ›–àÜ¾téªÈpüø>ÚªÇ°çòåª1€yêÿpÜ¾Ùcª´°ZY¡…®4.Âˆe®w!âØc®s¥t‚gk“Jÿô”zl¾`¿:õèªÑÑıCÜ…®ş 0”úôèª ë:]º*ªÿĞû~Øcª¦£¾‰Ÿƒ®'’è$š®tºã®w£w‚an“Jf’ è“Â— `8 ¤|wG°C µ›£H€ ñˆ 0 ÛEõ‰ ‹ùò5Ô+ÀP#õ€1õ$ÏLÍ‰õí¢+RöÔŠœ8 ğaõ  ´j–õ X€5 œ&o—
P W´’ĞÀW”Ïÿä@æ¼ÈÉÀ%j[uª ¨#`İ ¿• ƒ™™Äœ‹ xƒ§zi 1pvïÒ  ón õ< ”}n õáu MMpÀO€ã¬W,ˆÀ“¦†b  ’Và©]=I³!€3Q@Ph¾Y àÎÌäx(µäĞ¨d°Âä‡x$Pq¼;òÒÁP*ô‘è&¥ÍÄÊä^\PáÀDÆ0`@„€Çº ¸ ŸÀ 40Œ®  ±Ó¦(À ½·AÔ r¸ö„µ® (2@&¬¸ˆ™+(œ3  €ä‰ ú°0  úÊ¹30¸ú6TL ‹3õX  P(Äú§Å c|úP  :0³@	 B	À%h à@   À x„pü1ğ-³Ü®‹ƒ§Rƒ0‹ÄÆj;<30Æ¡ë»ëSğœt«ò5 / ò$#ò&¥&vŠ0Ä§\)yµIQ([_Š 2Åˆ ’ ZIÅp  ( h:& $ ¦(³˜`ä~ÚÒ& D@_¾ÂÓä@fºÈZà	æÜ­ä ¼ø4×Êo7Ê¿yJULİ¢”) ¢R  l· J¤€““.xh‰ÛC»K•`ÂòÑ5+ °Bîaª^ì+<®"4NxÈ
³‰ ˜çPèˆ®"¤¥]I®Õx(ÊÉ“>˜=P   ú  h= ´ï±5   úÙ:8 ½ğ·şĞpÒ‡®ç†ìR²³  R Àr - "ıJ »   , —$_    J €~bµ  ÈÂÎ´1ÆÒìÙ”¸'OJÌTcõÎ$—Ğoõó`ã)‰ÜÔ«, õ7+:À›ú  Hò ¼˜ ÀôLNÆp2”&³œ ` Âœ½Ä;—wkÿ"õLŞlWdõ!*ÊS+ÁNg h@U::e&h'l#LATaPAK&m  mÿx(€-=ÂË ‚GX€‰ Ò h@ \5!h   pDÀ«‘ÔXRc8 õ   ‚*$e$/@†ö¹8bc´’°Y€uë‘¢p ~¦Ù h_ğí¦Ä °@€Ô¸2Å‰I
$® u€Ùª© ¢ |´·“X@™T® ¼>%_^®Z ğÁ®(90® 8+ÀãÀ½¢à ^ÄÙÈ€$ÓÍ
 @‰F]ò©s tŸµÚ [ x ÕbJ   Wè\h € àBmKÄ3x6 õÛ Œ/Á§õ²(€zÎaÆàY\ús €GaÛ¾æ@ÖHLµ#èa 1ëŠÊ‹LÁ–yœqpA­Äßè‰€n™Éè”‰”Ôà ¥ïê‰ï¼0xbÛ¾z"ºP& ñèJ²€à» õã %@}ÿ¨	ĞMTÔ°ÑBp€|Ú·c €‘ò¶Å ÂÉÅè@ „ÚÇT p	€}şÏ.  .®×À 3¨NÈ® p'€P’ÎHB ì;ƒ…à(îÑ±°³Ï{ `ìîÊ…à  =ÔÚaxæ“Ê, À[±r³ÓÑü³ƒ‚è>ÉØ%@[é·GÀ  Â·¤à ™p¦pä¯›Ë%°È‡•â6àâ<ÙÏ§`6ÀË½Ã…<I³c °ç¯Ìè
àáäÄzĞ@øÎ¨•À³ó¾R Í ·ƒ†œrd;È9“Ù †˜«œL® ò@&¬ ôÀ¤Íº‚0¼…ÚÛ ğ@ ğ» R,*®…¥h' òÛÍ( ØB@©Ç'(@q	^³v öAš(Ä :@oª…,Rp¬û… u±ü†¦	ø À&üÍ p5İ¬ß åÈvPJë†») ˆlZôÑxà½Î3` IœÕ
 Ø.5ÀĞ |,„9qĞÀO…×08‰™ °Àº²¸é9~Ø’³ÃC»AàäĞè]#p @ï…×€vy€Í/ı›Ù>Pz¯#™Ìê€B€ ò¹Ut¼<èÖè{ì¨Ú©Øºğ
ázõğA ‚Ü4Hèt0'@gõ£+€ iÅº5^|aŠËY ×À”á¶¨`Lãè%JQÛà×‰ÃC¨ uÚ·c @‹ò¶Ì`(CÊÅâÈ ØÇÄ¬}Z³° €Ö(@w–Í} à%À3Ì¸ ÀÆšÍ‘`$ !»P¨àxq®-TğÑ (À®÷º…x<\´Ñv€° ü$³rp6 ²ÿ½ôĞa>¹ÙªUÀ À«°¿’ˆ¾¬±•øN½Ùµ¡†’‹ù›FøØÙÙû d>ÖÑÀ¤:Ÿ˜˜É@€ô¦ÖÓ à@PöÊĞX¶‹|Àñ> 	`°¼Îm@˜ TÏ»¶pÎ
ñÚ ×pˆ¶®/eüÑ+ª®0pŠ á» 8àí…¾X`›€ÎB ¬@ºr €ğ™Ğ;p! *„Ö‚ 4AÁI¡^9P€½ÅÍĞ| bUõ Éè)® ÌPŒ1 ( Vú·ì;;àµ®Œ!X`ˆØ×E? d½Ã	 €ß )Ğ»¦.@[­¹ú 0Tº±ü ¸B@„ùÇ Ä‚ú¦
°  8Ø8¸H€ª¹\Å2œgüzô PDÈşßĞ”H@Ûè¶È[Á õ=ML& äõ<	
Ğ”‹´è ø1·9AÔ@†š¹èÑ›3`5üø”Û»xğs"¢ˆòç4:
ÀAµiÔR7EÔö‡ñ|ƒ»èòÉğ.€èF
< @¨›ÈD0 cÉÀÈ ¨]¼Á®— ÀÌÑÀä \©º3 ¨ t»€î @’À¾! { tÜwJ4<³KÈlüÑÄ`,Ø]£Ğ @`¨÷ºğd¡É¼ˆ•€6ŒÑÈü°²€ä Ğ&9ÚÒ¢ h+@‡ƒÇX D V²ÖÀÉ‘­nÈ_ÀMÒÃ%-æÃJL³˜ğrï°"ÿ’ .ôôĞØø@!¹]àáäŒ•{H >‚ÊµàY€y½Á7 8ÀÃÎr0“ RÍ»Ê äšíÚ `ˆ¹®&f’/È®^ … ı» €@ğƒ¾X`ûÎH ¾@ºr  (µĞ\x! º…ÖT `ßsóÿ> @¬#”›ª,¦Êu®[øêDœh  @Gú·)76ğ®®ë 0|ûÀË›†èa'”’èÇ €^ )Ñ»	  N\à³Ë¡0ÀL­¹0t»±ü  B@„ïÇ, :b öÿI	Ğ `µ®×94ÒÇ›§^0<ÚMàÚı Ğ5ô»1 øå7¿…î@R‚APèS\ÅÀr
õA‡ğò…0¯è`@ˆ ”é·(€—·=”Ü/„‹áƒõ´`+=èè‡†?ÎîBè‹”O˜|°Ê×ĞW`Û¾Ë%«èÒ
¸ÀRâÇÀe „ÕCx féÉæ À)`Û¾À¨8«è ¸)ŸÙ“Œl¤ØjL„ÙØ0t"ƒ{ù  ® T—»ªŞ¸ €³„ $ 0Ì¸ ÀÆÍ@@ö¹ùa³ÀíLŒØ Ñ¥ÄjÀ˜ÍÃx@¡à¶±€£çÊÿØ° x³İL2ˆ6²Ê¢ ´ƒäp drd+ÿùn’Ö8®ù ÀJ0­ÛÊÉl€’‹¼^ìØèâÙÁœÙÑØ~
Ÿ’˜Ò € 2ÿµÅ¼^©Ì$€Tóîÿ2+d»œËb®r®f ä2®Pp ĞoÒ×ğ <`ĞëÎ­l? ³HJm'´ÿ/@ÀÒ¶4QN ¸l7©Øí³Ö| âPù ±˜*® „P1<…««ÿ”’Àºáî3ÿÑ l½ÊÏ8 z¥¸V  ÄyÛÛx¦Ñ€ï,˜£Mcb¦3ÿë
° èˆØ6HJ€¬¹94¤wüzô ğB¾İĞ›½ôw ³o(((" õ§<õ*4ĞÀÕ¥¸/?<ëi´¥İìôm¨'¢ÊF7@  ®õwà3nı·Úš]F[,¤İè c €™á¶ÂÂ†S­Ò,HäÆ õ`a †®$`¤¼mEÿ BÀdÆ¹èÈ@Ğöó®T p	€ıÿÏ.  -»×Ü	 Œ¥3Eÿ! { lÜwòµ4"ÿz°'f³ÿO(€ã×Í¤#ğ|˜Ğv€°ğÂ%³•ˆ6 ²ÿ½¤8HNw®€À  ²¢¿à $$p$Ö®ùh é†Â$ ä"¯ÿgØØñäÅ°¨¶—Ê·>Ir³É€…Şµ  Ğ5ÄÏ¦ğì®Æiª0 À™ÈÚc>¾ vDÿË æºíÚ¾)ğ¢«!ÿû0@`¾Ç^@† ı» €`ğƒ¾X`;ƒÎHJm'´!ÿ5@ÀÒ¶4QN ¦l7©Øù³Ö| bPù 
ÙÈ*® ”1 p@Gú·)76ğÔ®®ë 0ØûÀË›†èa'ì’èˆøì²!ÿ¥ÌÔšq¨áûq!ÿ ‰€‚úü›, è â¶ô öãæi!ÿí8-:=Ô  xáğË›ÁĞtè]`Y" õC¨± Z®ìS˜ »½Ô®X: õ–ˆahõÆ pcèî­æÊÙè/è÷
pÃ8Š­Ù˜ˆeÖ0Ôº!@g ˆÕâlÆS×îÔ¥àb<¥åÓ( à%õµ¨@Åõ\TP  Ó§¶Ø`  T‘èëx`Û¾¼X®"& õè”	ÀÓ	õ9I ¥ïê‰ïex:wõ |=]ÔeÏ»EIè |=]Ôè”	À!õ<!(ñ]è‰èßè‰€n™ÉÃ1üE” õé ªR úUg‘ öƒ ôÆÂ’)õ‡EàuùÃ-˜¶` õ­ ~Ãú{XˆıÁÔ·¦QŞtè¡è 0ìĞÀ¢dÈoèÏĞ¦öŒ†äJ …pïĞÚ
YL«éTĞè÷TğB  õC
 ÜÀ¶õ…àáFˆÅè                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ”E„%"âh»G…ª¬jÛ¶‰åşÿwww§JDDDC”Ñ-333‰)¥”!B™™TDDİ™bóÈMÒ™›¶mÛ¦KImÛnœ¶moÈ¯Û¶mÛ¶ªZµ»»UèU5[3î®ªÕ†ÌÌjÛ¶…¦ZW™™iz‰šâ:»Â¢ªÂ®«ÇİåÍà‡nËq"»{wˆ³“÷ÿmÛ¶mÛjr
ªš™™Yªªisww¤ªª#3"ªªªf»‡»»#¹m'IR¤I³
!„ˆXÕ¶c„J¨&$I®îÚ¶U
!„ˆˆH÷×… D„ˆˆåœjuR&™™™‰ˆ˜˜#bF˜™™™6çnÓ¦m·!dÆD3Ç¾»ûÿŸˆˆˆˆ‡‡x»ÙİÉŒˆˆ$¹Mâ&IŠ
™ÄÌ™…ˆD¤ªuww”™="#²»»	
I‰ß$B!„"€Û4j’&IJƒ”U£®NÕ¶mÛölÛo¶m›ãiÛf
öK<öoÛ,Æv³}ûÇ9Ç ÃĞ·BÆÌÌ÷ÿˆˆˆwv¯yÖsÆP[ò„BˆŞZL›ŒaŒ$„BpÅ½ºè²CD„H’B.;Ç.ˆˆˆˆˆˆˆ„)!DC1† B¥mKN-3Dˆ!d!’:ESˆHD4ˆ£…ªªjÛ¶ˆÄ+#7ÎˆŸˆˆˆˆ™™™™™™‰¶İÿ­]®™
¤dI’#Gf™DTWDD”cÛ´™”ˆ•Ä(ƒbÈÛÌnä8’C
ÿÿ“$I®D
"“™!Éé$I’$ÉmoEu´FTˆtçÿÿ²TfTTDD‡‡·»W­¡Ñ&¶mÛ¶İ6{«VUu†…VZ¶mÛ¶m{£
Ça´†aØl"Æ+  †   4¨94s(ƒ*†8."l•ï$J°Æ‘’')dy#*Qàglh†@  „‘‚ØH 4A ‘d’DŸ€E˜–Z™™+ "RJ•f„‡¤‰iƒ"ˆ†@¹¤´q 7¡ &D´  ( 	„i“zä„ô„©AiYˆA:'© ¶D@¤§€¤	    €  aHd   €¾²  \X    İò  @	² S  Pm…  ± ! @-  B¡)S  ´4Àp4	  @D‚xÊg „yèD&#-â²³œFÛ¨QŒl	¥„b@$®¸s  i	          C’ D !ˆ   (   ¤ +Iœ ¸a € HS   M    UVh   ‘ *   *$  5   @!‡ˆ s        !A :     @!  i± âv  ‡       C±DWJ‚ø–/ùÎš@  7!º	 $I [şûx,µ          B@Â `  	 [’  1"Q     H!ˆÀÀ    …  E]ÆŠdÆc–       € Ï@ÄPä$ @     d""tAwÔaa » b—œP   	    ¡
“˜  À@  ä>8€  ¢ŠªB@   ŞQ°È      Bˆ$êX˜’#=M        @0,HXeE‹„AÍâÊË@X,˜"¨€    	™Óh`m— €9íD!'  b€¸€¸5¡ J’$ Tè[â;¾¥      	    (Õ³Xµ2!}#  Ğ@ ¤ 
!C      AÁ©ÎÀ~        A’¤w  ¬! @ c(1X$À @!5!  „ĞĞA%@‚@*ğA ó‚l    D+Ç  T@#„ ƒT   %¦'xXt¥ó¸   B
‘DK¥ÃDLn     ‚D0Ê€ –      »Da“    @ °6     b šÁ9lé@!ÿ i- •Î“Òñî101J³lŸü"lzîÕÁ±_P ³ QƒŸœ—|00?ONJ ñ KF›.ù'àñÎJb |â‹‚øŸ6h?ÓJ0ÅVsÒ+©J\€ì1ò|3Ó  ±¨Á'ªÏÀ#Ğü…OPî?>} ”J=ÛÄODK`ŠÑœl¯À„¬oô?ğ›+’ J,À†&ˆ>lL¯®•1`êÑ
¥lX òe—!laİ	FiJ @P%¾HÀ%ß¯ó9Pv uÜÖ ¬œ¶3»¦8à§!l
0bPªñ ğ¼v£N»SşJœv§á ¾?`aJ. È	€Bşo-\ØïàaİŸ|J… ¤d®‚ˆÀêÑ+BáØËJa €ˆÎÏ›ü Dî¿àšø^Z¿ lS8ü¦!l- À(¨À]ª” <pÇ€ÏR_FäÍJäÍ®—|J; <xñ	rñÀ%Ğø*DPı ® cP©,RçÒJÒı,=Î-J <Dvê…1ÉC*²íJŸÀ¦¥ã]lU—|}|`J'ÒO\­ÜJÜÁC®5áô—|} JËàO,ÏÒJA@|¾y»Kˆc@ËhOF—|¿JñàŸJÒñ ğ	Jì³V?ÓJà ` ˆhÉs­ 0àÄá”Óliæ‘JğÛRmèJ.Û´Àâl³Ä­·¶ñJF—|±ÜJ¤Ä;·„
?¡ÒĞlR r|Ü{=ÛÙjÄKÎâqlÒ.ù'àñJ—øZXÀ"l'àñÎñşJS ¸€ù“Ptu_ü»J7 Ë ˆİi›?Ó¨X/DJä¯§ßQ%B3O,ÏğÅJ.Ä ÉÈ­š ¸óñ’.cbZƒl¿ÒŸ”ÏJş.ùú2(l—øÁŠlñp&‚',ã8O ~ ˆÒœğ	zŞİÛvàf£ lğ	zşêÛÓà³QJñ ì³V?JÒ\ òOÀlvğ#lÏ <š¿ø˜` É”“ñRäåÒJè—O£RJ¼ Ó¼Ûš®ÙáÿP, àuĞë¸¿&$ÓÌ#[l` a@ Ç¢©¿'IdÃŞ4`>ùúj l£b?àÃ l ñ …áœ* ”¿BJ*  pŸ·•q Hãß¾®· x Î˜«5Òñ¢,'Jù'àaİJ `C!¸X±R”¿BJ
ZùÒÑl —ğÉußT| $6ŒN,®˜ Ò(t­“ÅV[øŸ[Jñª^ J ”¿BJ à3Dä{
ã¥øÀòl”¿BñJhJ­Üİ JXÜëR=®­ÜİS¾-J	FŸ>	·îJ5 M<¢,5 O¿BJBñª^J„2(l¥ ø€ˆÊF›FˆZ¹« l­¸üZl~ Z0Br1•ML;ğOJ] X€m–# ˜&ír ”¿BñJØP.ÿŸ”¿BñJ¿„<ã7lO¿B”JÒ»ºJ
Â€Ï•cØ³”’ÿ|"	FŸ>-J (·²›šB”¿BñJ
àn@¯´­¸«¶ál@+÷Ï“ÿ=Ğ ¤§Wÿ/@¾í ÿõĞ”$Z¹l- Xp¯K®.òÜ€¾“¢ ,ÛJXå;ğ»SJ· ”m—ùù˜-|YJ¢i æ#­ÜJb*›˜v lg7@¥Rÿ.ù'àaJ âlPs»”¿BJñª^Já B”¿BJŸ‘RaXJQ ‰[¦@! 0½ÂP4”¿BñJÒáä/ÓJ ñ …ÓœQ ”¿BJĞÒ Ö…? Z ÆˆÒñRa?–JŠPå¯RÿÅV‘R|•+JhP‚D+÷ï9/@¾í`ÿ`ù·î­ÜJ•ML; R|Jª^1JÀ`'N	ÿQ ·ä!J ¤ x¡‰;€İëÛ pì$ 	 .îˆ;Òp@8ù9Ú"ÛJ  ¿Š?€På¯Rÿ P<:;5à Tó¦ÊÛş1å&J^ O¿J”¿BñªJ°0%ÛBJ” Ï /Ó›ñ  (üÌzÓ4”¿BJR ¾ 0<¾…  a>ÏJU –D<¾…aİ5Ò0J€€òÁ	h
j]¥ÿ a>ÏR_*JÆóø*@-Q”¿BñJ— ø Äú¢“+ ÀJÁ…®^»ø/J¥c®P†ÿª^Ÿ|•J „ØpaÿºO¿Jáà#ÏÒ»JÓ¯[¥ÿ.CÏ+üOJ@å¯’ÿ ÄC
qŸ,7Z_lÊ_! Ûÿ D$Ìy Ÿ|-NáJ»På¯ÿ9È_Şl„2(Ÿ]lgü†ªRÿ°¿B”¿BJ§—çg†ÿ¿BñªJ"ËB”J ` ©ír (p Uñª^ÓJ'B”¿BJ Ä³ó¯Z9B S …Ÿœh d”¿™
  $—Ü™˜ªe! Ûÿ
@Ã„çÓ/‡”RÿÒ»J
A8 ||öWØKŸ ÿî­Ü?ÅVJV`O,ÏÆ·JÄ ­Ü?ÅJà O ş–FœÒñ¢,'·J^ ÅVsJÆŠP¥Rÿhiä}ŒİJ „çtn”¿BñJ.C»SJ:Ø=4h ñÀ†Â†©Û^ ”¿BJXÜ+®
Ä ù¶ƒ7¾L [$ÿ H0pİ‡Å ¬€U…ñ÷ºTkÿ ø-–h”$Z¹_lç„o‹R%ÿñª  ”^ŸJ¿B”¿BJEL €s£‰Ğã€@ÁÒ+©Q&¸J›€şêÛ-`  ¤éÖ­xN«:R³ÎíÕ!=-íPÓ > x Æ…ÓÒÓÒ­JRä}F81JÏØû¦P<KVh½*B”JÀ¨Ë¥©-œú]ı"lè—|ÏÚJSÎ¿BñJ ‡à»›mC*Ò»øJ1 ú€„°›L˜PQ½J R6ìOL®S&ÈJ#:Ğèt[P% €Ó_2 1‰ÒŠÁ® ?Ìì~®.~É “" Ì+¦IDî“¸r ‰™ÇàĞê´ºÛĞë Î»İ àıÙÛAĞv°êÎ¿“ Àû]l °
æ¾âÛŒ ÂşàÛ“   àı®š‹N¬š;V³ 8˜Ë…B#-|YXJ¤ Ğáï• ³°`UDJ àñğáÜJ 0UËJÓ @P%† ¤DôøsÏÀˆ/!¨C+ (àZù± Î-m'J?sÒñè—|J ñ —İ|›0 0?ñ şJ Œİ”ÏÎJ F W¥œ<63È?lä/ı®~9JJD ŒÓ’ ÓÀÃ†©; xğ>•Š­Ó°½J0   =-íP0Âml €…ªn›A˜ 0Bÿ‚Õ  —İÜ›–—|™º )JÓ  x„†A@Üê	PBš ³+BJ¬<` ¢†H^aJÛè—J_œÂ; l — ğ)·z™ øœ¡â®õ.DcŒ®¦  ³‹»Û±  ¬ıÙÛÜ@ ]åÕ÷`o@y´@~ûà hŸ ¾#ŒßÛáˆpFà×­Ü  èŸÍğ@ìßÖåè@ìßÖ: ; øÎpêpå Æ—Á¯À$İu Lî: m iz°ç¼ÄÛñHaİšİ  ç{ÎËÛ¶jÄÂ—Ÿ`Ú ÜÕÎ´; ÊÚØİXcbWlÜ°Û a»ëğg@Ç¸FÄÄø7¦³.á€0­P®iäÍ”ÏJRCB>JÎ¿ÒO iJ*àïœ`ñJ. Y¬•¦¼÷:®ñJ€JğvíVaİ>W;JB|:]Óll+  ÎİëÛR<Ã<»k l5¤?ùÀ^l,Gş¥¾¼ lğ Ó@šùe>~%YÀ^l.Œ>lS èÍñšñD>¹ólŸ¸óo6l+ h€¢¸N›
âØ†ü—iæ‘­,Jî­Ó+®JÚ"ËJZ‡Ücl®“ìA…©+ 
…Bè›ºaİ¬1±J
cıc2lşšøÇÛ•Naİ¬1JâŒ®”l æÙP.'…¸#l/¤ d”Ï™ºÅ¶O~JàÓ„NŸÜùJ ˆzåz½€îÒ’îb`Z=¹l> /—1ï›ñ xğ>ŠyÙ†+ş8~pÀK "ğ;ÓÊ F?OJ (g—GŸ ğ8 ×…º>-ÃîáHøJ9.ù l ùû/O®áEüP­ø¥<lîÂäPèsUËàO¿J  Ûiˆ§OŒ ^wğl¿ „ îˆÒHC^”ÏÎJ—ø¥<lZh…ï¨¶9 Ò» J1 ˆ|ì{¯¤|²‚„ €@ìÜÖ „2È?lÙ"¤øÀ"l:ğaW¶	Jh?)¹-JM ĞÈrÛ€00õ;R–³Jä/ÓÊ—|J»²—CîáJ`ONñRÈJİ SRaJæèu 8Â¹  \4š«“ ,L‰ız\ ¤h  <‚F¾€§ä bPª£Z”63 lñàaİSJ+ Ğ …øsh“BÜa}J„ ÏLÛ:ôİ|PJ7¡è»k¤l@
ùÀi—|±*;J¿àñÎ±5şJ [ ªˆîœÊ]mZWäJ–î †Ó¼?Ó Ò	—J~"ÁÂ<l-WÎOJGF—|Ï¾J–é†ÓÊ›S¾Q®—Jxm—ù/  ğx—§¦?< lñÎñh+Jè\vX:º³ n ÙÉs9X ŒJ¢À+è†äMÒ0‘`.QÏJE‹È5  Àë­MÛÛ¼ĞÚ´]ìÛ­ m@µƒ·´pë ÏëÕç ¼ŞÚÛ& Àë­MÛÛ¼ĞÚ´½îÛ­ m@¸ã·´¨u@ÄëÖ›°¼ŞÚÛ' Àë­MÛÛ¼ĞÚ´ıéÛ1 87`Ï‡ºÈú"Ş…híß$ÒÈ]lä àÚ ¸¾Õë8Ò†¼¤‘»6 ³ x_Àµ¬¸H¦‘\³®@ ¼ï•Ëé¼ Óä–ä àÚ ø¾ÕB ¸ùÏÒ^D¼¥¶š    ÀŠá”ÁBi7®³ZW¶O|JÒñRŸJ½Ò­ÓJåÀ´M¼@”lûÇ„¿„<¦l ² „ˆîœÚ0a‘RJ^|?‘ğá€J]€BÀAÔìT|¬KY:³ DÆ^Rá&º»SJ+XùS:J °tĞìğ¿»`
8ô]ï@àÄdZ:º³¼ô{Ò0?ÓàáJ€ˆğ4ëTá <|H¨*‰B¬²Qü9V 0¢¯™îÅ ²KŒJD ;ñE†^> åƒ«dÎ-–ÔñsA3Ë à‘lä{Ò»SàJ  í ÑˆªàŸ ¥JG,Ğ»SJ»Sàñ:JİÌ/ONáàJù²—|™ øJÈËà.Jó  ?Ú…*   îÙÕm ´ Ï+® À(¨À'ªOà BÜh;`À?h  pÁˆ¢¾Ún¤<l ö„¦ ¸} X: Ù¥ºO*îÆìÿìí¢¯BJ  àDü{ÍR€ç@©úĞ; féh®İì2åyñÎ±5&àÓJ "øÀNr–hv©nÿ»Sn§ë`3­x>©8º³ tÁ•y| âª‘ @t“ù¦Üj¤a l/Lrˆ¡¼pÃ€–ğÚ Q±»,01ëRJ— ø€u†OZ €B–›Œ AOm›½  àı®š8lC@X³ Â Ï®5 œİ®ú °({  „|œ{L~ 0¶…  
“ƒ×„¢½™ğ =‡Œ‹K0A èãJ  A˜ƒbÁ®  
 |]H®h ŒŸ§Jñ PuÀn¯¸8ì à»!ºùÀ>l 8áàÏš±`îÛºùÀ>AlœÃÁŸ=lñ 8Ò³®—|¿àJÏè—|Û:JBä/a>Jo|G¥¸_l&' àŸJ³hm·àñJ­¦Ğj³•d‘¯K,³Ä ] ù.>®N¥€Á™a¶&t˜ï™*  ¬ıÙÛ¬@ ïÕ `@«ê½ÌŸ¦pzĞìä^îÛŞĞìÔ>èÛ¦P À¯Ï¸Ñğ ¬ıÙÛÿBŞÜ°!C [,ê¦{ÀÜİèÛ+˜ °jî×.h Ş×æ4;Ğ0îà°â «»0 !—J›” á  UµyÂÌĞPq3*h|/O¿J¦ÁÂlW ¥ 1*¯ZOü‚P¤H  …ñğ›8Âh+"6!O¿J„à1¾~n]Ï¥¦è—O\aJ,ÖpˆÛ™à á@šùe 3\"lğáÜ ³;:J ñ …ñœ àŸ|™7RJZ |€u‚Ù: Ê2®50p ™ïÖ Ğ	€r—¸ú À¯ï¸V @s@¹ßÖÛ°ãÜÏV% ho {“¸ğ„ƒO´³Ÿ0m ™ïÖ \;ÈİØ:À×YîåÛF ÂqÎŞp–æJ§¾˜hiä7—J— Ğ–şêÛç@1¼ûÜÛÜ>±.;,lÂÆ^ó(L}:]v ,³·ÀŞp~İñRJ  ñŸ‡0ÒñR|ÏRJñ  %ƒlO\­|	7`JÅVJÛè—J4 Hw 8à¶^ør€—£·^0ìÂşåÛÏĞ*^şåÛ;àÃ|™l àŸ|-iJ] †W l?Š;¤#li+ Ò+hJ|	h?»È]JÁi09ÃTÜö@Òáµ^€İ(…§»¦8üâl—‚# ÀòŞhğ ¼ŞİÛşĞİô]îÛÚl0Æ… ßzû®»®- ĞVİ—{ àÄãoŒ®  °ÿ“4¦åÄl0t‘İ•¤ İúÌÍ)¦®q ¨`!\±®ª $8±)®€ Rõ‚èp ñcfÇ®™ Ü å}® ŒaJ±Ò 'XJ¸N	¯®—|JÉhãƒ¤à~9 pl °
æŞëÛ¿p@€:³æèu 8¹ëêĞš«“ 8 &VÍlòĞì ğ»»,ş L‘Ú™äx(„;Ğ8| ` ¼$Íy.¦¤?lÜŸ Wä/ñJpVÎªP+K1 ÿ ˆùœ`òo< l˜ àCpªB 2 P>Û„  à®İëÛ¸'‘ÚËJH†8
†G? n.ì
	®  ÂÇà—®ˆ ,qşÿš¡ Àn§\¦®³l~YJs Òñ: ²J.E¢Ş÷$K  ÀˆÜ´{0 á ˆõh›+hJ—|J,RçÜŸ`J|"· ËàOJíR|±²—JgÂLöÎf<L"7àPí Úë»O \ °ˆÊœÄh/8ßûÅï Ø¬8FŸ} Pqpé˜¿È°Ş±ÚÎ÷~1ÿì+ Fâ¦*pé ˜ï»°±ú l} …@Âƒ©6 5›SP›§ ÌÈw!®2  ™dt›' ŒÄ@_Á®õş€„Â&›P E–ÁÁl¿Ï@Ğ¤©zWŒ¶ülÏ Ğ6Y6® €B°‚V•V¼ \ğ·í®›`@¬¢¹HàÙŸmÚÛè 8híÅ¼ĞÚ´]ïÛ­ mÀ§Ÿ¸< ¯·ööEÿĞÚ´}ëÛ­Ğ6à´ÏºÜ(oPí×å d» Ş è‹ØŸğÄUP> €6ø›/uè»}À l^¦WŒèlôb`–!l0p¦¨B  ”*ôù'*ô1J a¿› Œ/0S±JaÄBnèŸHà ¢Œ›a­¾NaW+J­àFUü9-ğáàO\J®šÀ¯·•ê€„@Fh¯ÀÇ•©~Â<»õlà À; ¸9*_Âİ[k l;8 8Ç®„
âHÉä“á*h|ÍSJğ3şP W¶ñÎ±5Jüı,Ş­ÜŸJ_><ûl ÀÃ$i+°`Œ>lÓT†Ò—ÿ Ò1•MJ¶.psÎhø›”ÏJÏ¾-NÓ+JCÏÒñR|JCÒ +à.J  €‡·ŒÏ6	FJæ#­Üİ”J æç;¢i JÛ \ä³ÓŒğO,³¼J ­Ü?»SJQ~<»ß4l1*›˜v<lİ	—|-\JèıöÀÓïO/ —¼„{ÓÒ	—ÜUDJ 0?ñ:V`JE À§Ál hNáÒJ0¤ÃM†¤l–ğê éÁğœ×¥  r|ô{b< <Âû	h¢ <ƒ¾“Ü³øŸW;JôÀ%ÀûV±  ğ!Ä–ªR2(Ÿm0l¿ ¶À­Ç—©€‘F¶a>JÓ+ø³+JR|³¶JàÓ„ ¯^SJ€p5®]+àO\­ÜUJ•Mğ+XåJ vL5Æ…|  |nŸZ>ÏR#-J àQÿ®5RS	|¿J  Ò»˜š ğğtºˆ »Ì€ğÅ®öPÜùOA ÌÅÀÇLP
PSÀ³KùèÂã® ˆ`h×Z1¦9ÈlÜ€ĞK—©Îú<¦l ÀİìÛ  İãÛQÏRÛJ{2 Êg—§Î4 ¸œl- p97š³ÎÏR1JN  Š­­˜#´ğ¹6V³ô:¥– gx„{;ğÈË4,ü9ÎHw€à·.h¸^=æ p²í™Fh¸œl:ğl@¯‹·QÏRÛJ @‰g¸Œ{]p97ö³Â	AĞ	€>Œ‘’ ¯øD/J  À4À‡©İÁ§i lF 8@ÌAƒ©Ò­Ó„Jğ 	À2Ä—©RÑ°ÏJ> ö„:€ À…T}×;PTÿŞènÀ.¦¹Î ç‹ª "|ìí™–hv©®Uÿ„·w÷6Sÿ 1¿„Ø	Â}Á l³ƒÔs iWÿ:t`X:š³İ]À®ï[+`½¼³îÂ9¢l–6p6@;	h?À4Ä¢©0 Ò@¸Ç©b\¢Âl| áJQğŸ¬ŸJ¤IåzÏ\ÓV<lL v¤2(lW @. ´ª*ğÂÁÃ c‘ÿ"À.=Ÿ la¶Ğœl|—ñRJ–xu iŸ‘Üa0 —JU¡; ğ	—JR#`İPŒJ¾•V£J»Z`ñğÓ+J+ ÀÀeDiªAÍ=¤§ltHšollt(o@7Ğ¹%@.AßÙ]>Xíƒ¬š4:€mÂÉİt"İJ&t«æ•X ö}£¥§  ÿˆ`í•®  `ûÏƒŸ ¼ğW½«+uÜÃrJ |¾±ÒšÒà €B±ùc»ÄàÿÓ’p	Oç×1 87`¾‰ºğ ¤ˆª¦¥Âl¾üJY:º³•o¾Q>­JÏÎPQÏş1Jhiä!—|JN —/¡sC:ë1<ãEl›ø*)al#™7°JR#-|/JôıæËJùtãá§W lİ2(¶]lnàÃ<lÒ1áLJ	 #g‡T@™XàÇCŒİÜ1­_FJ|[|lÏ6¶»äJ™ZWäÍ”J” Ï —,›ñ \â%$ÌUO ‹hDiª”Ï6ÒJFD„;„Hº¦I{Âò”-Ï — mÛbñRäÍ”J0¤Ã#6l/»'à.QáJL€ 
ÿ¡_àÀ³Ë ©¢^éBp-´|D»S+ p²³™RCà»äJ}ÜŸX—|±J| `lÀn¸X› œ®t7aİšJ!"  DL 4‚bó§D Ašùe	—O ŒUJa ® ò‡àåş—hŒ?J1ş¶lÓ ° ØK—©`€?Á $PhÜØ®t7x¶µ‹1åP KWäJ¬Å ÛğanJ¶ñRaİJÛğŸvXçJlNÅVsÒJÁ] Û lA p¢Œ™áÒı®ŒJÛğáğJàa>¿Ø—|J;<`ÃE l.Tñ¸Œš—»ºÓÒñJÊÜ90Õô’áõ ,Aìpá  
Ğmç×” Ï ¸tÜy¸  pÀË·¾Èr@¬¸.>Ìá²“Î^r1 şx Æ…²ÏÒ#	"FJA#ğÊ9çV|Q¿ØéğÓJJÄ ­Üš—|­oJ ® Æ—èœé €ï£Çˆª,@O»:^³ÜV@àı™¶ À~Ç¸âhl@:È·é2h2Vpl Hë6³ä `l@£ã·™¶««›Sh¸  pğÚé¿¯M::¯Y:ÒÀİi­(@ÎØ×Å@·áÃ4l Ğv@}ÜÖd€ÎkVÿa àqğÚé¿û Ûğšé È €­“¸¬àŠ7³Œ46.Ó~ÀûY §lÇÜ½êÛé È €¶×¸@à8^³*ÍÀ—Ê•©- ğKiŒ…ªôa0­|áàJ Ğè ¬Å»Ä(v ±“¸ÄPìÄ^ìÛ¶PìÄ^ìÛ×,–şêÛÅ@ìïnîÛÅ@ìÅNìÛš  ÏNìÛ®ĞÚî[ÍÛîpïŞşÚÛ¶h ÀûÂ¶ °ïâşÙÛÊXm€k“·½LĞíË i¯àÚ­í¾Û$n3¤¹l.#¢!|l”0ñJ* @¼ô{lŒİ”ÏJç c·RÑÿt·Ñ/=læXøh6V³¶M¹£ºïQª@°6>³ Ü}îÛ<èu . ¸<8 ùøŸš¤¾ŒÈwl|D.¹#l V (t­“| Ä—©à á  œ®+xxç>.b ´ú•» S ‡Ë—©D   x	r/À4 °¿Q>3(Ÿ=lB”Ï6ÄJ iv	ŸE±RCà#ÏJ04»ÁG lä¢`Şl–Ê\Èt—ÜUDQëRJ|C1ñ“9V`ñ:²›J+ê2(Ÿm l»:0¦l8Ÿ'hÀ.¹#lñàŸü^l?¦y·7l DğùÃƒ"à»S,¶XJ'   8Ä ¨Ÿ”¿B»J”ïÎÏğJå;ÎÏğŸJ  |›à  €ÏK—©”¿ àÓ'JS}˜}J:;àO,RJ;Œ ˜Ï™:"à`îrÚ¦Ál? Ó €}£›­ Ô Àw—£ÎıÜÛF €>|ô{ä /@¿Â…©Œ`'ÀbÚP­|ëÎ-JŒ ì°v…¤+à¹Paj ÿ t­“]N#ÏRJ’ ^½†Ül;8à„>l¢¯àÓJñ€Ä—©¿Ln¤<l m Àª  `À‚»¸ù `éÏÁÛğ a€gDˆ©­Œ«#M<K °t@w»¸”ÏÎ¿*J  øÍyÿˆãÉ»iñ p	@äÚÖ  à¡ÃáŠPİ  çÿ­áÛê°à a».®İëÛ: p àŠñ’½€í PğÕa ğĞ­ç×
   ²àÕF ÀÚ­MÛÛ¬ĞÚ´½ßÛ+ `;Èv–ÅĞtà…¿´°é BÜ» `¾³;®³0 ĞµÊM>®Ë àÓà J    EQ ş”RJ)¥”RPP(J( ³P(¥T&€ş€ ¥u@ŸdEè_|ÅÆPP|EB ³YÀŠ@ ·((((¾"l¯H`şRJ)¥TF ÿ(€¥TJşÕğmÅÆ!   €-“ŞeRuômEè(¥TÆşYX! µpª#°­­ ÿ[  €-“Ş((((ª#l}E[ú ÿPP”Iª#³P@,¥RgÑW´@è}R™dUGè€ m‚ßt ‹ í“ßYXQŠ5N EÉlÿ ¾bHh} €À ¤· t$ ¥v (  ¥v@ŸD @èPP¤I( ³

Š2	›ÿ   ¥=@¡PJ©Ì ÿ ”$è"¥vg  ¥vPPôIì#³'ÙGgz	ÿ((((ú$lg €À ¤· @Ÿ´çPPôI ³( %¥v}’}t@è”¢´ç…ş € ¥u

Š&[ÿĞ $”¥Q

Š>	[	ÿP€‹¡Q[QV„@èöQut}Eè,PôIÊ$³_°5ÁÆ  @™¬ç   %€ß!@Á‚B)¥2_ÑVD @è@ŸT&UGèöÑYD @è   í“ß €l‚ç;	6I©ÿ!,ÀJì$³_ÑV”aEè_A™¬ç_ÑV,¥R (EiOÿ}  ¥vˆH©
ÿ@Ÿt@èmE:ÿ}R™T}EèPP|EB ³PŠ5N'•IÕºÿPPP ú$³(((è$l

Š2	[ÿ[1(Š¢@@™T}Eè  @uÜŠç@™TmEè_ÑV$”¥Q[X@™¬ç((((ª#l_[! µp

Šê› ÿ € ¥=!   ¥=!P, ¾"³e’êÈ¬ÿ}E[úÿP K©ÔÿM@ ÀÆÕÑYômEèe’’¤#ÙGè’¢Ô'ÑIè @@)¥S[1(Š¢@@™T}EèÕ_[Q¸pÕØŠ€-“Ş((((Ê$lgQD @èJ’dÑEè €ì“çª"Ğémÿ:Òj¤yEè  @¤·

Š&[ÿ¥Eé‰ÿ:h‚ ¥uøŠ! ¥=¶"`Ë¤wÿ @Ai]ÿ  € m‚ß

Š>	[ÿ ÕÈlÿ€@ ·[QV„5Aè|EX1„1ÿ! °   ”|ÅÆX„`™ÿÕÑW´eEè€ -“ßŠ…¢(Pÿ( @Ëä·ÿ((((š l   ¥v¶"`Ë¤wÿ¥EéIÿ  ‹ ¥u[YXQ¸p¶"èmÿ!@™T}Eè |E üŠàM((Š¢@(Ğ'íã¹ÿ((((%l(((( l!@Á¢0(¥2XJD @èbÑW´eEèŠRŸT&UGèÕlE°(¥S ¨ÀV´¶[X@uÜŠçÀEiÿeEXzÿ

Š>	Û"ÿM
Àlÿ ‹ ¥=!h“ ¥= ÁWÄŠçÎ"0 é­ÿ,ôIì#³_°eÅÆ@ŸT&UGèÂ Eé	ÿ}E[ú$ÿmRšT&UGèÕØŠ€=”Şe¨ÀV´¶ ¨ÀV´¶


!ÿ'ÙGgº)ÿ€ í“ß  € í“ß@ú"ÿ@z*ÿ'@º*ÿ'ÙGg:ÿ:h‚ í“ß! B °"¥vPP|E¾"³Jè"è"¥vMPŠUNbÑW´¡DèJ’TÑEèPP(JÊ$³RJ)¥T†ÿ”RJQEQ  €l‚ç( `û¤7ÿ}E[ºÿe’’TÑEèPPôIè$³PP4A( ³}E[zÿRJ)¥TÆ ÿÅBQH˜ ş[8‰@ ·eRu$}Eè¶"`Ë¤7ÿRJ)¥TF,ÿ PJéÔ1ÿ!@™TmEè   ıŠß”$@éí&ÿ!øŠ! ¥=! |E üŠà@¡PJ©Lÿ   ¥v((((t$l@ºÿÑÔ1AètÑYäUEèJ’d­FèRŸ”&•Iè&UG_ºÿMPJéÿt‘((Š¢@M   ¥‹ß}ûÈ¬+ÿ øŠ@¥u ( š ¥v( @¿â÷.ÿP K©Ô/ÿ}E[:ÿe’}TEèRJ)¥T(ÿ0(Š¢@_ÑVÔ@è}R™¤$UGè”R¥'MĞD @è^QU4]AèJ’dEè    í“ß 0 émÿ!   €í“Ş( Ğém7ÿ `”@¤·š @Ai];ÿ:4A@)¥S”¤š EhM0((Š¢@Š’¢D}Eè
 H©])ÿ h‚ ¤·€’¤#5Aè Él=ÿk”$è"¥v´Ò¬T'ÑIèt›¤$ÑEètR¬+ÙIè t$š ¥vŠ…¢(Ğ>ÿ}E[º&ÿ}R™TÑEè    U”ßPPTG¶"³À(Ei@ÿ_ØŠ@ ·e’êÈ¬*ÿ 
Š¢(Ğ@ÿ Ğ‘@¤· ”RJ©FÿYPŠ5N_‘Àl>ÿ ¥Eé	+ÿPŠ5N[QV„5AèÕ]t}Eèe’’¤#ÙGèŠRŸD'¥Iè   ¥=@™TmEè  @  ú€  †    €`Û¶	P0)±€E  @ ‰%1´
lÛ¶U	 ê  I’´¥m´¥ªmÛ¶- ´U
mÛhÛ¶U
mÛ¶mÛ¶U
mÛ¶lÛ¶U
(Ú¶hÛ¶U
mµ­   mÛ¶EÛ¶U
mÛ¶m•ÿ¥”R
`»¶ú´Zõ6Q(EB¡”š Æ“'PT"  r $‚  Ø8"„F
QÁ  U„ˆá×¢P“$5Él   A€A0qw	 `7   0$:I              
¨!  („1”R*ç‰ ÑrN)!„P,çp#ÃåˆÙ–Ì @’$ÑJJ¥Ã¡T:3ÿï} % s¥ ğE€	UU„ "àPÈµm²µ
…1ÃÀ     €0       3‰   	     h  @ 66„ + °ZæÉ0 @	$@    H2 P £HI¶lÁ
  µÕ@         €„
P K²­Îˆ³³ÃJ   ÃJ g   9!œã Ğ @I´@    P	İ3İ Ğ  ªª£İhH’W   p1W€£0È~3 @`€½:²4(      ÅÙ Q  €¢    Lp‚AÉH’     €  ç¼ËÙ3Û    I$Û–$!IµM’$I2„7  HR	0Â€ À- À0k)€  @  ‘$I $¡Á”ˆSy d¨Ò¤5
à¬µÖ9K/mµuÛ´hˆ3¸Z§(    I2  ·m[´e
™    €    gØìÿÿ  E     À `ªm@EK n#¬%à  € ‘
1=jÀ ¸	cŒÀ @  !„ X¸ €À€è `Du! €ÁÉ:@   S3(
N @l!If3CD 0  ğ )  -@  h @ Æˆ 8ÀA:A 0P@@±@U  @$   „ €!U  À U"AH\	( Y $   P
  °  P€@àK+  	%’$	€
ÄQ,xDjÕ"iw
/ g ¤*w‚k*$”3#<P¥  ¤P    7  œ  Oa•©@2mõ²‹ı õ Ôÿ  õª¢*ª¢*ª """ƒtÀkõr‹ÕDÀõ tàm1õ  ¨g ßÇ  ¤ ‰ŞKˆ”  ´  7ÍÒ ´2ã
 õƒt@&!õ_‹ÔDÀõx-"""65Ø3bp õKPàÀ4õ ˆ @ìj€“ÿÎÔ&.  °·"n  0½ñ
¦‰©ÀQ´÷Œà@¡ÃÍª¡ 1€&õöŒ08:ÜÄÚ‘Ê(¦&õöŒ@ıÿ  È(¦&õƒt`F1õ2ŠÕDÀõ  oa¢Yõ¤á3 õ" líàË®ÔD õv÷SÉÀ[<B¦¢Lmm`ÙGHì õ@”CÂA õpœFÒOÆêÄ’6ıÿB$I ""ç}AVH  õ  ÔD`õÈ$ä¯"õĞa -ÿô?ŠˆˆÍÿƒtà’6õŠˆˆMÿ5°gDıÿ  êA4Aµƒè0€¡‰Ş–´ÕDÀõ¿[Ôİ  ngÙ ""6  Ş"2 õ$A’ÿ‚  H$5°gD=ÿ ÔÿÈ’6õB’ I""    €µ”cÒ÷  ´¡	À,6 Œ-‚Åõbõ „S     ¯K  š ô	q              "ˆˆˆ¯K— ""! „IHX/õ¨Šª¨ŠAÿš#"" ""•-  @‹Gõ   LBõ¨Šª¨Šª¨ª¨Šª¨ŠªŠª¨Šª¨Š’ I‚$	Jot ""!¸Ñ ³¢*ª¢*ª¢¨Šª¨Šÿª¢*ª¢ÿ*ª¢*ª¢*$IÂ ÿ H5°gDıÿ"ˆˆˆ¯EŠª¨ŠªÁÿIˆˆƒÿ € @5 Ğ1¤!õÌ·h¾Eıÿ  4ß"""6;  „  m: |$$4 õ¡a    L ô à#!õ¨Šª¨ŠAÿĞÄÿa‚I(„Sª¢*ª¢Aÿ	’ˆ€ƒÿ¨Šª¨Šÿª¢*ª¢ÿª¨Šª¨Aÿ’H$A’D!	  "6æ,è3ûõ"ˆˆ‹¼A @µ›(>D}ÿ* ˆÈ	ÿ     )  \ 0   k 4d "§è@  mN!=  g ğ,s   /@lK    À„ÿ   P  -  œ¢ƒõ     X   €            `  p   X@Ë1  0 ÀAÿ    €Çÿ     Ps0  Àÿ§è A=ÿ ´€  t    €µŸ     §èó	 ZşğHĞ   k >"4€µ |$$4 õ  |$$4 õC |$$4 õğZTíÿ5°g¼ÃĞ     oşózEj  mxÀ@—ßámQšSî×è     ,R        å   ÿ    €R.     ¦    €Á ÿ          €éÁ ÿ    @ÿ         À€éÿ    @Aÿ         À hMÿ>35 smO ü m<õ×ê` õSv. Ğs  \{ÄÚ#õ€f    h  ¸ökô§ùS€ ş     ğ¯h%`*  ´s¤  õÇW€?@ºõ` b¿Fœ®q  @˜¯¿Î+ T©è@ x®‚g°Çsôù´½Ôô§¡uH×è|
Èr€©àµöŒHà²Ã¯—»DÆq Ğñec*ò<(Ád@õÒ5Ì |(õq_üõ¯ `ˆı
®U*ˆ@G˜×ÁètÀ+À¹  =W=ò5“ˆ@§ğábx ®ğát  LÀ+À¹¯ €*5ÔÏUxÛéÙ&^Á : l]	&¼ÆÎ®` ¨RAè¹i`ç &¯»  ğ\Ï«Á[<ˆX'ü¸Xƒj  jƒt€øõ±ÔDÀõ    €°Ùô€L   phöÀ¼ ¨»Ü  Á iç9sŞ€| `B  5eM ÑõîPM €¹s>Åùõ  €%´ĞÙ A5 °@î õ À3 €Ş Lh5    6î.Là!aõ ˆ @&’j/M c{³7ä³B õZ€V˜FRè°x¹ ë¹!@A)n“å  ”æğ.ÕÓ?ÆMœÄè[öİcÅä™›à¬“   ¥õ<m" An  L &|  ˜Ğ„«  Àiõ/X×" Anõ?z(Æ{õõ?Òÿ(""6Ò (•G½-ÿ(
@&Möj     
«ó     (
s'ä™Ç+õ  8!òôƒt€>õ  ˆ¿Z½0ÿ  p©  õ*    ½fõ *  €
     °4\ÒplõĞa -;ÿ  ÔDÀõĞa -ÿA±ÕDÀõ 0P ,L$Ñ³ ÄpÓAn €e@¤«è  ^ \b*l. f ò J\XË   6  ¸!,0õ  Ø÷  ´  šĞh  «   5 À*   0           À*h     €Vhÿuà'€`m§ñ  ".Ğ)çÂÂÒÉô5 €€Tn  p €2ô j¤ > +w¬BVNp2lå
€êƒ h°sñ Q‚ H	  °Ë U"H! èêŞ  @L     ğ.|; ğ,@Ğ¬h…ä€AL5K.	²J~€éOP0PP  `"  h|m ³‚‚ÀR 6R ³ µ¨ µJ@†    hâ9 P+¤‡¡”HH õõ¬Èã#!õ=°DÌ¢ò½5Ø3"4õ   Î0õÌ(  6  @F1½Gÿ ˆ¬  ´€ ""  © ²Bõ @@€2‘–©€Lõ1à*$""6     ²Bõ      	 t`F1õ5Ø3bo õd*   Åô³“  ìlõ© à*$""6  ö  õ DV  n1à*$„S¢5  mÿÅê)¶7³#è¶@qà'õì¤JU
õÅ*œb`oõ¼Èà'õD$¨F‘õnx©è®ò     5    Šó ;€  Œ3 *0 ´ +/   <SÛ`"Iş|ª8*â2 õ  ¸á‹ ³  <* S@2   À"h   v B +    €ÎeE ¨ÀQõ   ÜğjnxE€j  Nª   !   Xÿ  à†EÕÀŠ   0@6x ¬h  q  D÷ä Ÿ®r  Hb‰³¤ >  €éŸ& 8\½êÜ³”   <ßÙ	  À¶Ihš| Àš@gK ¬è¸šs©
ä?@Ÿ
õ    À  € ?j	 È   ÔJ   &° K üBS„`o "M—Lm#qõ ¸zâ‡´ €ğÛO  ll(³À
H:C‚h:à*€&LùĞ§ÂÃºÍÀ   ªh  ‰   N À0 €6XX   @ ˜3À^  @h €   ˜3r    üó )ø  0h   ¦ƒ LW/¨s)ø· xğ6&à8’Î šÿW‘ M> ¬BúTx €ué¸#   ©šÿiW/     6›JÀlÿ+@g	h 5:pUš 5   ¤*Úÿ‰    ŒCÀÕ‹øR›JÀ¬ÿ•€æ@Mÿ `] º«À  0€‡	ÿ  ?  L ¸zâ´›JÀ,ÿ•€æ@ÍÿùĞ§B}ÿâ  €“ÿ € ?jœ€	€aK    psİ6ıÀ¤,õÿpÛdj õÿ°‹±ô  ş øsg€)nà? õ    H 7i   @HiWëÔ ¸  M   À‹æ´x <ŒŒfõOs"˜ŸÇDôŸx-
 C	õo ìã5òúx&Iş|ûxh
hi¡Ê-E¡èã Mš´(”¦«˜: ûøx şšh)@¡ ´=-`G€c$´XÔmjªİ Pl\€âò  €ƒ§˜5j       5î!ÈA õch  M•¶ P À=õÿ¨ÄTğ×Kn   q  m    €•ÓAÀ@DÁ¦mÉ  ·Åô«    Ği   à· ³A  üO ?f     €@K Àƒ# 9     `¾b   5 zí(J  û P V   ‹Ÿ?   LÙl ˆ%      €>8´    $q €‡#   °  €„Kæ(   ! ĞS€vÚ8;;   å%+   ÀC  ˜² ³b	ğ ğ  èƒ  n   ôü'³  ğÛ °
r  z ğ  ¿°
 P øŸ  ? =     PK  À  5Š  @ˆÿE;P@c>h;  @Áô) `  àìB& ²Q€n x À úà   %+6    -  #   Š  @Èÿ Pp }
ÿ  `;; +  C6ÿKÀÀ   èƒ@S   B L    áÒÿ    @°)( Ğ ó){ zíJ 0ì;  `  i	À G
ÿ  èƒP²6á  Àÿ   H ğM    l +(h`>Å  8 û €ÿ  €E  85& 0dC¬¸  @ÉŠMÿ  0G úàó  ˆPõ  ÌÃBì õ  `@Glÿ  `@‡lÿ  `@Çlÿ  `@mÿ  `@Gmÿ   Àh¤mÿ    €©]õ d¨õ•-Éã&„S~èâK}[ÿ  ŒéÌk`õ   ÀF L>°ã â¹? ĞP @/ Ø 7A‰ zà#h @ÙCş  Ğh1>GR ×gõê  j‘ i  ‚ ³$Á  ³–ˆÔAnÙ   [´m*ˆX  r   }H´""""¢  d¨1 õ (GbO õæ³(   P*Ù“A=ÿ  Ø€•ô  ÀPÉ ´{   C%m`aÂÇóĞa m‚ÿìï04Gÿ½¼œ¤&õ  ^nıŠä  p ÎÀæxÀ ~3mœ, Pï­Ê   œãA ³½)  S·øŠ@o ´  € œW½±õ·`ÿ Ô{«r ÿt‹MÿĞa í…ÿ€C8D=}ÿƒĞa ²%mìï04Kÿ ”Áj"³V ¬3àí,õŞE8 ÀxÒ* m
õ  `Ã mí, ÀŞ«    `&mng‰©\~İãH 	 	0ºY   >ÜóÙÃš  À¸ ŸÅ  Àg1´öŒ´Û…,õ&8	€7]´^X8X=#ÿ—Ğãƒõ  ˜G"¦õ ˆh  jø«cˆ  ¶    €RJK  P¦m+Á‚P¦’©è     €JK0¼YÙI ´KEfÅõ  X€ıÊô   œÕ–³3cáÍ  Ssà@Œ…è"ÅB0l¼è-ÔXËèN6Pà¢õÇV¶ öjõ ä*à"õ Ir‚ÆĞC8P _[Š!5zğœ[àùõÅTà2 õCå  Bõ €€}` Èó0 '”  m /ã±õ   öX½‘ÿ ÌE‹²õA p¨²  ˜ Ù—ó ( , NÀ+1 . & à% 	   U   JQx	   ,k©I l TÀm—Íü š œğüs-
 €vªL à'YÀ„{+
P…Y˜Á§ &„­ª X0ÀÓÍn pQrŸ²ª ¬,`÷º£X.õ "7]	àŠ¶É®µ r ‚¡µ™x48.s…`ÌŠ€¥õ$ œ ³—	<ÇbL Ø—Up  d_Bu)õ Ì€ÀDK   ‰ + U / ¹ J &@@1 ;€03 /À,€    ªGÂ‘Ø›4´  *> ³ªKù   ô[@Õ Ù—ó‡
|  ûõÙ—à @mT5Ø ª´ €ŸwkÔ Gbk õ     `¨hÿ™ìÁ#(õ ˜0 K  @" < ;U ^ ¹ +F. @ S:   Teõ  @à  õ  ¤û%Õ6õ  Q(€g´ÿ¡½ õ  ºwŸÿ   ÔÆ €	 ¸Ì­ !N‘| Ûõ+  LBõ@à:àÓÑ ¢°"Ñe¼a	<paEL;nX¾Q`®D¿r³¹°(€Â¼Âõ¤°³± H(Ä\³ 4qB
õ PŒ›´» À¸ì( õ     gõt­Y½dÿ~8zLıdÿ    ÀÑ`õH    ÉgõE! 0P 4+ ‘  +7 œĞ%‚ÉåOà'‚˜Ò@g0 À˜è M• Øšè P @êõ€.ÃùWL·   (*l‘   E if  +    @Q!L¦a6
‘‘  € `q  ŒX`õ’   ††  (â CÅó@Qn€h     m	iÅ %-õí  í J¼h  - ÀHÃmª®”ODì õ˜PuÀœŞ  Àœ€ª+´˜Puª®ó)—ÙC€ª+´  $Ôµ,õ¼	  @ä†(ÊğA€ †¹  ÀjÎ_³ `LÀd‹µ ğ‘ƒÎÀ À_ õ |$d  õÆ‡ˆ„m õ¨g¾A=k&ä¹Ï ‰Ş‡Á³,fõbâùõ•-Ğâ‘ØA´  ø­å	-õ   dêzpÿ   şGõè àù"M  àÀˆ´   ‰ õŒóöŒ Ã`µƒè0 ¨‰Ş‡ÕDÀõŞj  Ş[õÇ² K½rÿ”-;Hì õ ìç¬²õ2M`õ  0dš¸tm  ÚÀÃ{†  ğ „,    àrş @€   51 ö  õ™1Ü`©`õ  áMl´   ”ÕïÙø&¡€õU          0ThÕ   Ì,     ! ÀÀs´  €§ä¶{  8q¸«‡Å   N   ™  q
  €«˜Q  T) ”} €+¬¸ñ\i       ¾pçê“ÿ @ €Å«ÿ       Å    SKI 8=D}zÿ   I5     ÕL    @Èÿ    €¡    $†ÿ
   ÀŸÿ     Œ     ÿC     , @ €¯ÿ   `ÿ  H   6 7¬¸ñ\i1 „X «Å   €SK €à¹ m    À•K    @…ÿ  î  L     _    $†ÿ¦   €£ÿ    @Åÿà© ¹íŞÿ À)b@¸L  @ñ   5á€çjÿ    àTKI œ¢x õ   h)´   ’  h   ş3  h      0        0     ë‚  À”¦ÿ  À6      X`(   ÀØ3 À  ê4  €y$õçöà*$ An< P ÀÂûo ˆ @ğjæöx`!õr®Ís$""6 À®õ4TŠ¡Rõ   @@Àn  Œ€±•2  ´tã4Wá¶NõÈcá2 õ  P{l¦ õf  e´  Øó  6©    !8Le´Ğa mÈÿ‹èX=ÿáC\ŞÁ õ• [¼ÃĞ—@İ ¬‰Ş  p,¦õÇr €ËÀ33Ş	  õ  ğB¡ç)m' hšjP°	Ga  0 SM   L   À©!h  $õ=O  p¡Ş/Ğó  m4 |   œX    €„£ô ÀTÿ‚¦l ÿ   á(ı ÿ À  'Vÿ  H8
´  ¼ÔA§Ãà  + 4ÆJ‰‚ ¬õ: „Ubjõ+ hŒ^êl/u:   N + 4Æ JÆ  ¼Ôu M: €°Ğ3  Àƒÿ4¼Ôu ³Öê À°UbåU nb}`Áõ§[   <°ñ¡ÊB @µ       «•Hàgõü×ı³ üáA @µ5fğ¼D=‹ÿ p õ  0€ªôÌª  6&ä‘ Zõ!
  ™
´É.( Rõ  ÷„ õ1Zh@=–ÿ €=´_ŒŸ€@ÖB„DM ¦K·bˆ\‡ÇàL 0  ×Aj0€ h:dÀ@\h‹{ ØĞh€¡¨À¬ P á'&D êñ´1®›‘Pé 7 õ   „ õ JK    !€8 @°³húTÀ»
õ ˆ @\ej¢ b†v{ÀLˆæÆ:è  °€«‹óÎÒOÍ½R  ö@û´BM(ÚÀÿÀ„„‡5ë Ü
¢ëÊ!Àƒˆ‹0 h  [ úÃM
3PÀâh ä@ŠjL „³  r z¨  lFbJõ    „ õJ;,   !     iÉ.ÔÊ  r    €‡	ÿ‘¢Ën  ^8›B¤(´ ‚‡µÿ5 @ L	{·†èº²ÿ À€ š	ÿ– €şpSÿŠXÜÀ–h ÀP
Ì»z<mL€k	ÿ›‘ ÒåFõ¡   @	ÿí ÀÈ’«| P€€=j„¢hh p BÖB·àºÇÉ À€ šÿ– €şpSÿ0óîÿ ?@“'6êxlšt›‘¦t Ès   @ÿÉ.ÀhaI õ    €ÿAŒ¶I  Q ‚‡µĞ
ÿp !áaÿCp€Ç ´ $¬ƒn³@+È ³°¸Ú
ÿ@ø‰IQÿê'6SL›‘ ÒAn€¶   @ˆÿT `Zÿ˜€@òN 8 $xš, ÈuÿÄÀ¬ÿl	€>˜ÉB
  ,ps@ø‰I	ÿ¨t ­ÿ  È@- õK  @ÿ   * °³Õ”^^È“õ à  x 5{  x -0T¶€²õ   ²BõTsTÍ! Anbi ""6     GBõ      Ÿ   Ğgõ²9x¦Bä¬½’9¾‡@ @µ/ \ uõ ğ–@›9õ  àK‚  S>`›9=âÿ¬ ø  ¬‘ù’  ÀÔ ÿ  	Ş›ÿ  /	 m«qQK}±ÿ   $xKl     |I…> @5.m;cÀg ´ ¨ÿe´k1j± !NÇ‚I(""6™òaÊG8	õ?ŠÙBLùô ÀÒ½½@[äÿ|X'bDì õ5°gD=ìÿ	 4¼Ç’6õ :†¦©ŞÆà´suan  ]†¦õ¹¡°x"4õ
 „³ê™  3£È     mõ  €Õêüÿö	|Q`õ  œ! ˆt   ¤ü`ò  ìPpÃ¯  öBŒ´H  ÀL´ö	x†ÀK…´    €¥·À> Ñÿ ˆì@h DØ'  ³  À¤™”ó    Àm  ”5‚¬õ     Óœó+ Ì  6      t  $ &–‰ò‹ø§üœó§šÁÍÖwüÄ¡Xõıœ8]  õ       ËD     !     @GK  P@Â…h   ½HØÿ éE À,áÿ   İ
  €	,  6    €H eâ¬+ÿ	 p!   6     t 	  LÀ2´ °Lm ÿ  @ç ¶ X @€MĞAB #õˆAñá¼ßÿa8Lıßÿbõ ""6  Ğ €Ö‘ô™ô4 S øÃÃO õk­‰-"„S  @n n à Ü@ÇÂÿ   à à     ¦     f L G¹@çÿ 8  @) K”   ¥K @)   5     ÜP¨  ´ÇBLäô%  ¦§j8 rõœ	   ŒŞ ê `µx£ :  L  ş  ‘µzŒ€ƒ‘¶    ˆŞ ğÀ$ÁÍ  ø@#´Ô2; ‰Ş  D±  õ Àp  €P  œ €P  " €P    Ó!L  È(Î²Z,ç h€× @ĞOh  ÁÍÁm     Ö˜ó   Àp èe  (§ è‘  Àtÿ  @œÁm€  Èèé' Z,j   €×LÖ˜ éìæÜ˜   ²«jho‚® ;Ø®Dˆ$$pƒc.¨DKN Pæ¸­
Ä¸¡û«+ 
¦ ¦+Tg€Q€YK’€AA"`9™x
l< 
e ¦+FCÁKQ& @1Fh®ƒĞt:¡V >LƒµK)@T0€ .P ñA£5 ì·?n @b:0ÈMÀ"/Â8~€ä- ³‚ À§Kÿ=¸`ÅhW±A¸ı`‰ (€$ÖC(€á¢h	 ®	Â¡M >xİ¨Úƒp8¶ıÑ" Èÿˆ¥®”%h80Ğ(öÂrÓEı}#ğ³G vÀ°Kµvòvlà ``•(P~À  @ÜKØÀ pÑ,Æ‘!‚è.£;áÖ/€+ N €nØU<vÀ õ 0  : 9ˆ   Ôÿ`D  ""j   2ºÿ ôšÿÒÙÍƒ à5i øº‚5¦´G zB°h  .äB    Ps p g›Õ Ü wõ àñ3Ô POÆLôD. € 0–±  Ğ ¨«l  Ğ l  Ğ Êil gpÛÿ8 È ³  àu Sÿ  €5æ|ÿ§  Tÿ €ˆˆZÿ8 À4hˆ‹ZÿHgpspÛÿ    €5¦´§  Ôÿ €ˆˆÚÿ ² Àlÿ ôZÿ  `)­ÿ§  ÿ €ˆˆÿ ² À¬ÿ ôšÿ¼™f)íÿ    à›iõÊi  zdÿ   : L  ˜F L  €8ƒ´ˆ‹ZÿHgpsp[ÿÊü¡€Š5¦´ 0 zÙÿå4   t N ¦@ Óÿ  ÄY Lˆ‹ÿHgpspÿr°-„5¦´S7  €ÒKõü\cµ$õ ¼š²õ   •!L®f €JÀmƒè0À%m´ìï04êÿ5°gD½tÿ—´  KÚô5°g¼ÃĞöŒ@‡´5°gD½uÿ: j¢w ÿ5Ø3bp õ: j¢÷ ÿĞa m ÿìï0´ìÿL_¦&õìĞ´Î*l{ §õ'È (õ Ø  `_ K   ?¦²äöÀü 6 5 €° 7 l   u +€7 ‚Àhˆ 0+fŸ·lP€À”¸@V9ã™Ãè €ÂÁò™ÈÀ C ¶  X sÀ 3€7 9`èr…Ú ´     *'³  €³G
  ÁªMi(Ã“€0 X  ”:„œ!õm€å ÖÀ=¦Ò©€„aõ§ØŞÌÂè2üÄ¡Xõe*ÈÊGqõÊ'Ü]+¦õ  L% n[d  o!N š± 3+ @´İ´P€V+   P¨`	¨»Zdú)Ì ËŸ˜&* ”Ok   0ÿ S À  P>´  ¦‰B @µ    €7BL  àSÿ €ò	 j  €i¢ô  å  Şg  À[ˆSÿ Ll‘ ³4f €€¡;ÄTx¥Lõ  ”"ˆˆˆ¾[
 ""!‚ ’‡Hÿ ™$ An"ˆˆ “‚ˆˆˆˆ"@ˆˆ õ¨Šª¨Šª¨r28l'""6  àa;õ  €$$ An’	’$HJ`;Ho ¶   LBõ½ˆˆˆÍ ÿ ™Dì õ <lÇ)`õU§… !N‚ €`Zh–èÁ£õUœ@¦õ  € §şU  ÀC´ t[  S‘BV(„S  €$$""6B$I ""õ?ÒÿH  õª¨Šª¨Šª’H$A’Df*8V$!õpI  m~ôIÿA}ÿ  ¨  ´¹¬   N&   š + €  `:   À0   À€	 &           `qËÕ‹»lõ%æ ³Cõ$~$$4 õ ¢Ìcõ€³(   P ğÀDÁÍ.»iây9ÿ  _"¦õ6t     ! €€@@@ÿ  Ä¢7@µ €€@@Aÿ½ˆˆˆM ÿ   ±ƒôbõ  An½!€ 5½@› ÿ  ¤Dì õ       ÜÃ°Áfõ.//Àc õ       ƒt`F1õ_‹ÔDÀõ  ¢$""6 ğ  ¶€8   ¨,Ğ1  m 8‘%  @           R †ìl ƒÿÃš`GÀÜ¸€ ·ÈË Ğ_„tÑÏt¾³õ7N 4³’À@º« pP ‡ƒ»ƒ  ¬Æ¦ìÌŒ ÄÀÌÍX 
ø½¤i
¸1 ğ‹½Q	$ €+¶É HÀëÄéø8½ÅÑëDu)"®x,œ‰˜tÔ]ğàúÏ”C)Ü³—ğ€ü²¹    €À
    €Ç“ÿÀ P( 7  <vş®ğ€Š©Ş   | ıJl @ üe   ¢@$D  Àÿ À €`        Á  “  ³ FdgÀĞğ3!„Mw@^ Øh @  ƒ0t€& L> ¯ÄÂÉ^¿`ãÁkõë jxxh H7€í´E T‰Á1h  À¿Òn®8K€ƒã‘˜¨º¢îõWÀœ€ª+´ à  5   ’h t%Gõ˜Puñµ¹°  
 ®0j@2 Òàh. P À€m €IIñd‹ l@–Dh$x° `±ºdÜXP ³ xÀ‡´ L€zåLŒPÊ—Ú(€ ³ À #S<À• +9õ    	´ó È j@% K €(À
ğVõì]Å -õ   ø¤ ³%2x—  6ğZDıWÿ‘»L}1ÿú—Á`¥õ P  ìõ`ø  3õ  ğƒÀ €´ìÌ	iõ2 ŠC}1ÿ ˆîú’j^‹@B @µ}‚Ø“A½4ÿ  ˆ €Â—ô  |È"Ğ†   h +      r2ñE$õê%§1®kõ­. ÀÅ´ìpÄzùÍ¦J£’ ”Šè,P0€ûÑ¶5D~ÁYõPl\ğ ³( @¬¾·` €€g	® ` ` !ìxƒ ıÍéƒR” ˜ˆèƒè0@ûÑ¶_«àc õ ÀiÁ¯‰âFÙlpanìxƒsıóÖğJµmÿ{à)e›íP 8-³Ü±¨àó¯Î  ¼;§óõ”4    6ù‡<°cİ8õÔ   ¤B Äø€ YPĞ<£âü‚O!r
a+´ ko³0@gA5@ HlÃõ‡@h  äÿõ Ä> úJ > €‡ÿ   @r  `/ °·     Í…óÍ…`. ˆ¸Ì… €  N  Ğ y‡ó‚@	b@| k  ”™$„SƒĞa@86m  ÔDÀõĞa ­AÿâµRŸm ÿ ‘     
 B     @	 @  
€! (Àƒ€€ CÂ  @   ¤    „ €p@    À              

        €         @ € 	 8       D)€	œ0 @@ƒt€œ6õÓ—ÕDÀõöŒM êËôxL(  ³0  Àqµ{¯…&  `n
  ˜"ø\J Àï1 K7 Ğ z@µ·,ğ@x	õ¼3ßpì >BUÄÒ À3€ÿ¨·U€ÿ¸™ÕŸ p£Õ¥å  "…¤çÀ ğh  ~  ğ ğ  €5    @¹çô  0:4~†ûíè·c¿õ}#ª`ˆƒ~´C J L>0€ ´ â õ Î€ BL,aæõ  0  šhÀp
 L  ğƒ¹õ  H) kÂó7 Pi ÙmLàˆàLõ  Ôq€³ÉºûíøF ÀµùL(¬üòËqâÑD@ÆÑ_£´Ò—qTÀßõ èLPíPÿ ş  ‘µ ` íuGQÿ   ` `º8
@Hõº£2éĞp€£ à(h>|ÕÎq”£«@K½‰v£h'ÆBŒ€ÀL– 4ÙòÔ»$ ê+  @	Zğ±ó,Ø    6  øŒĞ«¸¸ø€¶‘ 0U@£’¶`Ã ¿Ñbiˆ„R {S i”&ÀFƒúKÁ ğxåE  { ³  ø^àh H¤ 6à¹  ĞÕ¢ 	„è®
À ‚5( Àà´¤ H vi_°  ˜–ˆ±"d@ õ¦,k @Ş·³b³•ª°MõN8#á  ´P¥¢š¢2dõd*S¡Lm¦²õëµ²¤M=Yÿ4$ Àmõ H£ õ1üpanw¸ `Iõg* š´  4”ÍLõ»@Úî5cèLQü´¢İ3 €@áµ  ©@®Çğ XĞÅãá hb8õ¡RÇbLìDRğ	àn  °§à¤õ]	ğQìšÛé8!<ó¹l,ÿ õÑ}…Ö† õP^€á T‹ñ  ©@¢£ÍÑ   ½¶ÅÓ P `2õ„°ğ×Kn-#ŞŒ½õÈ¢õ $ü^ŸPò_˜4?Á/õfÅTÀ~õğZD}‡ÿèË˜¾Lı`ÿ  `Ã m  ¨gÀŞÇ¯    ‰Ş# ÀÇÔËÆ O³@°@ 9M
RÕ¯‚õŒ `A*õ	 †'l Á  (nÄË„¡~õ  ìOãBõ"øì¯/äòƒè0À%m´OÕDÀõ   à5`õ   Ì|û¥o *„}    A L PGaõ=TÀBÃ õ(vó~´Î§  @½fÿ  ÄŒM’6õ\Òflõ2Ø3bp õöŒĞa@n °'×Dè&Îö)=hÿì$2õƒC‡!*õ´è_8«ÀR õS8‘¯‰áƒtDŸÔ–´ÕDÀõƒtà’6õeÔDÀõ <À‹"+õ  0dEŞ2´ú–  [´ ì[  S    àÿ.m   ÀZ´  È[F /õ
<ü+  S›œuxÔ	 {ˆ4^%è<Ğ "™ä hB/õ1õ2ÎÇbL%@èì’–³Æ
L@ƒõ  °WÀÚÖ¬"ïÀ
õ_ó(ó  ´^êH õ€õ""âkQGoÿ`
  "6õ?jñèØAõ   ÿQı–ÿß*ÑcM1õõ³Àe2 õ  ä  Ø\s
    ÊŸ¿,< ¤*³
<…pï3   ØÉÓ¢€ì¯|ëèíco "M    û¿¿åŞ(@”`h¸œ  hQ /  ÔB´ ,ÿ	 ³Œ×  | iD  ä¡tô³ğHĞËÁ±°Àe2 õE H i   Ø\sÈ¯  °}  Àe2 õ¹¢Àe2 õ“àŠ"79õ°D™ X¢ô  @ ±°ó'.À/ ¸  ¼ã¡ÆAõe* Õ
  õÔÃ¬†Lõa 0í
õ0´~|,f õ À' xñ»Ñ\]À‰`õpam  NdŠÅT ¨õô?
@Ûğÿe*€¤ $õÑ
¬fà.õG€ r@µL°Cã> õ  ¬&¦õ       5 DVHà.õ  @V(„SG €±;fÔ"ˆˆˆí/  +D @µ|ààï`õ  0õÅ õ `oŠ i     =#´2Š  ˜‰Ş5°gD}`ÿ‡Å@F1õĞa -ÿÈ`?8½ÿĞa ­ÿìï0t>ÿ5°gD}õÿ: j¢7€ÿĞa ­ÿnğÕDÀõ¼Á õĞa m€ÿìï04@ÿ5°gD=÷ÿöŒ08 Ã€mŒ|£¦&õá¸ÉYKì õ]8Äu6 ø9’ñ¬Ë   ¦Ì  h!  _ -à>/< U  ³›/ğ& ¶‚ğ÷KD ¡ “ÕRø¡óõIG@Æ³váî    0eh  ! €´e    á‘ô³Ğ…#®£müÉ³d˜@´³Ğ…’ñfĞ…­ ÿÜ>{ü ÿ)YÀÁ€¸¤È  ³A&	 ""!k1 z5 €m‚  H$1\ LBõ‚ ’Gwÿyx^" AnWxòÀˆi´KèÍpå†ÉCÇ& À'iRJ )€|•Áîmzû„Òp[š'”Æ5¨´‘æ‰ÒO4°.³é	`@—œ¸å>†l'R‚¦ù½»Ì ä•­  ÈcÀá ´Q€   À3  @ 0€3 € À €¡    @ 
       š RJUQa@ 0°À/Ã’S˜ À‹Í€- ÈÈ0  –0ƒ„0 D @ "    u =Ğ˜ò®ÚAÜ­ct@u¡ Ä_Ô¬â0³  @ P:5^ão”l @  ˆV º½ñ ˜  @ šÚâ!+@à       @ +0     €   …  Ği `  ˆÃ oH  „;,  ¢@Ç ÿ!  Á  2…MÁXìx3ÃÀ` ’3;‚LS0"   B¤     @&; D… J  @ˆF×X3EÑ5 ›Á“óå3Ûi `$   H# ŞB  +  éºÎÀP®      Ğ1       Dğ‰B¡Kè‰ÀÂåKMvA7h l P4 ³=d 8 L  ğ)3£° ‚€Ç ğ©|O°r hA5àÀ¦\X˜'ÀÓÙ·< èJ@/b‘	ÕL $  É e^    ¤L  R@â–Í-h HŸ×Ğ 6 €ßŸˆ€R {S id¡ûÿK `Ò ¹¦À ı°³´   „½ñ;Ğ)) ³°* ",  âÆ hZ€(š…nO
À  SB5€ç.£; €   €    à1X   /  8 ¾#ĞG~ ( @à„´ m<~ìæN(Ô;l    P% Kœ( KP 6 m ÿ $	gVÿ °Œ      j  h   z  L    JKy    ğ J    ƒ€h `H	  €P   Œe	  H@• L8 »ÌóÎ €X3 x€  :  ˜  ĞB( 	 @   €fà9 €k ˆ3Ô` "   €& à

7D `  !  Ğ8†²´V À“‡·çV`!   0  ÔÍ   6w   €’K   l  L) |  L  @<   D óŒ ) 6  Àã  5  €• <j ÀJ €… L  u3   N ›  Sÿ  ø   6   @JKˆy   ñJ    @dK   ‰”lÙèm ÀãÇ'P4 Ü;³i(q[šKì#\ €:ÚÉéÉ\—'”Æ5¤” i~/"ÿ	\	N³  8 »Ìó àñÀ ÛÍ    P% KU   |¨    .IHø.³ °Œ      j  h   z  L    JKy    ğ J    ƒ€h `H	  €P   Œe‡  ÀèmDà’ÇÕŸ˜# ¦§ËPLp%
 õ&P*–Ÿl ˜   + àŞ³İŒÚ€Ûæ‰ÒO¨´‘æ‰ÒO4°.³é	`@—œ¸å>†l'R‚¦ù½»Ì ä•­  ÈcÀá ´$•Ã7À%õîd Órõp[š'”Æ5¨´‘æ‰ÒO4°.³é	`@—œ¸å>†l'R‚¦ù½»Ì ä•­‘ ÖÂá ´õ ÀˆÎØ1     ê&ÖOP6ÀıÆÂ(Ø†Cçõ  „Ieõ   g ğB € L  5’×\Èõ5Ø3"4õbP‚‡õÑ1³  ´t@[Èš  Qå> ×À#æÊjµE Íõ‚  H$                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  ”	  )„   P €j¤!  " (€P € @  „  @    @@            €    °›   RÅ  @AÿÜˆ ’  @   %P@ ÍP)¨T¥Ñ  Ğ|óKˆ0 N	„ P $@      K ®l¸FÀBK`ÎQP  ( „ÃF! 2"  %@×(”Ğ 8 Äˆ…  @€  @ƒ 5   (     B	ÀSK       ğ       II @Ú`   s 	 ğ    @À        @ğ @ÿ    @` À           ‰È `     Š }     cŒ1}ß÷@@  ""–eYA )cŒ1FÁZ$É¶ímo¡ªª
  |DL@@D    ¥”R» ó©H0µX»-0EÂ‚)Ä9	¦Jˆˆ     †aXQ´ŠÃ0hfe      ` 13ÌÌÌ     t)$   ÀAÿ‚EDˆˆ” rÎ9g £& Ö4  ”$Ò¤	 €i˜       €CKCè”RŠRQ@DD433Š¢}ßG£)ˆˆƒˆ  mÛ$Š    Àÿ, ¢1 %   ªQBˆ Æ‰ ÆcŒ1F N‘ˆŠ%  ‘   1   "F 4Ê"R        *Ëš!        ¨                                          „         °                           [                         ¡`  K   àŞúK¬4±]VV³C3V ûõ… ÈEœâî”Kx°ğ(èP @!@¬ ¶DÀ[9ÚòÆ[·A`• ú•rl¢è<³MU—®BxÀº°Ç½»¼ú° l½ÓW@ €³	 Dpÿ%+0bèËèÕ¨`ô½Ê½8w€©Ö+ Ä Àãõ*€’·3Eÿ@!ü¦èe?ÆÒ.õ0aX÷ÎâšD°´yĞÒÛ X¡æ‡õ»0\®a<úíà2 tÁŞÍD ×õÎ  Ğè( è6€¨¡¸àli+¶ÔAØ^Â–¹àfuÁßä°lSßÒÔ`è¢ÚK™4æÃÍ`€#`$­àk V62Ô&ª	ĞtRæè4ø~,³˜²Mf£Úp3å% .!x õ¢4  Nõ2»A^-°¶ dC9'µí4 QŠNãÔ€¢—‰¡Ğ- Ô»Õë4Ü¡‰õL¤À
õÃLü¬ DõU Ä®Ñí l ‚ò ¨À–ŞÖÔ¨’¾õ¶\‹4æÃÍF€#`Ô¬à– Œ#62Ôª	Ğtzãèxş1€Y
õ7:M³Ô ¤ `~õŠ  ¼Ôè¼ Ğ@×ŠÎ! ÀEÒÄ<èÜ¹-p ³op@ºÂ¹a PY oÀ `İs<³Ï„0B@V¨ßéL=€´õò/”! ‰õGĞ õÛ ü£øõã €qĞ&ÃG2]²Í¤É¡O(›%`7Qú     úb; œ»ú! ­ ú   À®aú
   “NúH@P™”.Y? ğw ú<ĞsÍú¥@Ñ úF@'€úßPÕ úÚ  0×
ú4ÕõöK1ØEÁpõ@  iúƒ&,`¬Ì’Í ˆ÷b›Â4ÕõêK¼=EÁp¼@  iúƒ3’Wë®Úhû{¿òÀ@¡Aêı2> ˆ	Àş¯ĞÌ ê@Ğš“6LXd¬r ´hÄŸU £ĞH½2 šøö®Æ(0×3  ³°ˆÙdâ"®*×§½6K1!}ŞJ¬L 26} l‹+0"³¢ØàF õ]  à£úa     ú2Ğ¹Núå1ô„ <õå1°a¡;õ‘W”Ç cõ9I”Ç@XõQ”Ç@XõÜ‘”Ç€õå1ğ„ <õ"@[ úy  €şlúU  ğÑˆú   @ õ  €ô:úrNpl7ú÷[Ñ Ã õ  HihçµJ– Õı¯áæğ9U¢ õ  ˜3 O5õrKêÜ1`kxŞÛIöô„@?0õ ^DTõ{¤  /õœ!@>÷È9IàsÂeõ¢ŠDI Ÿõ‰†ñ„ Éõc>À‹GúŒ8 Gúd‡€3@¶ß„°X ú·Nø>Lõ·½3`îõÃì&õ¿ÅøÀsõèª\¢fõBl”Ï Ñ õ34õ‚f õìÀS i¯Öı„vŞ%É÷ÉM `=õbÖôi¥<ŞíÑheà‡õ‡,ÀõŠğòÀĞõr;¸,Àî#õ*\şœõ ŒÌåb!õ¼<¨" õ(àCD‘ õÜ”P°"Uõà%Ä0´³Æ0ah/8RBüh¯h[Âß?õ½ğGàÛ‘¾±bp@õ'päÅà@ õ	"¬<@µõ  `™2õ ä lĞ>¦€åcµoˆÌ/øç~pRuÆ·)ô„à6õ·)ˆƒ!YõÇİ¦€€õø¶`-Ø×Ú9IÜ¦ gõQÜ¦ gõÈ¢Ü¦@õ·)ğ„à6õ	 À4ÁšõhV pŞú XÀN‚¶Ğ  JÈõ¢øÖ{\,õš P8€ ú°¹¥³Óö±#é„Æ õRİé‰-Cõ¤ i=ìõÔì ø"¾õTE%PKp úV ĞTú±©)öÓÍaK¬÷•ØĞ%‡}õ  F €ÅŸ¦ PE  ú?í€å§ú¼1Ğ ú  Î; ´O„-   úÅ„$¢C³àõ.  @šõ¤ÏP¦Ù ú>lŞgŞÊ¦€J¿Ä $àkàõLz³BÌ€Ùõ ûIõ‡ T-ûÏ õ"×q 6ö` „Ò/ñÿ%„3ÛÔ õy€3{ õ>lŞœ õXÇv½ÿs2‰fŞÊº€0I¿Ä à¿Õõe   ú8¯ ÙÄú  €  úê P¶rú9Y}¦¨!õ!XÊ ˆöàZ   €	   ƒ d­) .,   +S €œ
õjH“äœõ ,Qà¢íä Æã ú©ƒÈÑí| õOõ¥¯ ¶ƒó	   ^üõy
  €Š´ö H(€nİ‘@‚= ´ Pš  ú™Ø ³)ú  ‚+ ®©S 0Eé®:À@¢ˆÇØ´ ¨¹¿â—
D
‚bõ  P,ÀSõ-W€bõe ĞI­ úË `+Â úM  ˜ö úù 8nAĞ·€ p"ùù»€ê ë ë l~øáÇ®ƒ.T  ›»–ÛóĞ ıı»€u€u€u€J  21€ş ÿ±…+À/õ® p¢•…V@8î%‚® 00ÏªEIàˆÜÈ¢ÎÄp•…V<#¤CÚ® äœD»¨ J6Š·\Á>i³d@à `¸—É¸"7²èÿ'´²  àl†^"è
®< á¸ "ÜHÀ¨fÈoş=\Š·†C‰ ëÿN€á¸¤i™âí ÿ’.mÂ ®ÏIîÇ2h d	 Eõ ŒÒ&®6‚ >Zå¬†û±ÿ   ø“¡ ƒIêÒ&®Ş NÚı¬’.mÂœ® °@ ‹ÁÀ‹Oú(º|; yÀNc»©G   ù»¤ @TQõ®Â •sœº)Mwàj´®h"@¨¹) Ğ!GòÁTÇÙ…÷K‚Î”\ÊM1Pqğ®*‚8Àu‡Èœt-R ³F“ƒ…Áê/1 T4}®®  Ø°‰Ğ' t°Ànr\î+T Mß«Ñ®äØ@Å¸ıxWGÆÌ-‡+ÿO1P!õ®WoíœC‹ÌÏ 0   ú    ú“9˜ 
¹W  0Oú¸ 0®À;U €Ü† úÀº,Âé‰D @€o¥¸Ú *0½¹ÏÇ  ¬ƒÊàœU²‰³D T,ãÊÚ @(0ı«ÏĞ :PSñÄ‹àĞUÌ‰³(ø)°İ¡˜@s‰¹È ,€ƒÚ½W pİ†ÇöÖmˆ+ À{İ ,àôª¥(ğ-@
°Ç à voÿU xpùÏPĞZ)nÿ¡XÀx‰¹à -€ƒÚ½W U‡Çö @}WÀQ  À©İ ú 0 àˆğµ ` €Šõ àëLİàFj$Zõ„‚HàNõÂè– è–ŞÙ ]Ü
ö@VŠÓÀBK Ø¯~+ã;A€„ z½)(0,@eè€¼7ó= Öƒñ¶Ô‹˜ × ´¢‡YN GõÎÀTî¥'õ}W	p£¶õ€0dõQ, "'	õ<!ë¢¶õ°ªÀSúä@R€6 ¶¹ p(‹üºõzÙĞ>Åúšf·¦jê÷Bú'ª`–8õ  Ğ|búP?OŠ0Ø  ( À ’¹ºv# mú¥
  ‘ ú9 °³úç8-ıÄÑØBhc v
öÚ ¨»†ò b°,<˜l1 `ˆ±` b°,Ò’l Ê$[ ÿ Ò’@ €t     ´  `éĞúŸ£u  öSì ğ,úá   ÁŒyd€ÁoúÀ  €¾/ÿšğDØú6lxll†Ìl> P €S õv6Àôú/`    ”
 úMØ`   ú   °Tú ğ Ì|™Y šKëb€Õ€µùÀË81‚
T3´õlTæ+ï
a˜K \0`ff6ƒ+ ¤B¼o‹ÈD l è Ø€*Å×à9P³rL 8ÉÀÅÁ( ÀG„´v: ˜<s6ĞŠâ×•qvFÀ 0lg7†ñî K0,, .³(®€’
õRXDÀD‚´ : »¨»9à(³ÅGäÆt’h
  &[°¬Çé¦¢8ì³­ À›@Úõ ¤®‚sõ½(¸zJ  õşmA	pœò 0 „‡„³ ÚZÀøtÚ8 €o´)läÜÃ®	 ˆÃˆ€¶ T¢i õ#¬D×‰9 $K…‘wq7  À˜õ  à=&õU¬Q@_Àµc|`®m| ø€rÑ¶  DIõ < äX õ à1$ õ (Èà¶İ'ú6‹ õRJHßdõìëĞõ½(¸z*€ ná!!Â¬ÿm-Œ >ÀMÚ ¾	àpbç0" mÿ ¨.DÓ¶IÅÀÿQ¹p‡h ¸s  õ ¼Çä& õU¬¡ /£n> \´íÿ  ˆ’Ò¶xI@½ÿ Ê‚€—t@Ò7Y}ÿ{ÁZ@JIõ®
 €á¯à8¡õBø·
 õø   ÃCj  8€¶ÆÔ - ç›Õ RÜFç¨M$ÄÎGò$ @ €Õ…ó#T ußŠaäÂ]\
ÿ pæ Qo(èË¨Ûÿ×4  Š5õ Æ à#ÀµJ
h øP¹ĞÇ4    õi  à%õ H  !õY4”Aòú&wŸ Ğ·õ‡H)	ë@õ  0`/Xõâè…” €´…dA	Æ’ò 0  z³ jU¸øtÚ8 €ô´ldÒ ®	 öÂ^€¶ ØÒO³Àü	è¯Y TKCP A²à)    „õ  €^!õŒŠQ@_Àµº>hŞs È€Ñ¶ $×Ã<õ ôy$F õ ,Å# õ `ËA†à¶-$ÄŞóg õ¢›5ó&åSõìÄépõâôB*€ n¨ÄÁ¬ÿµªk >ÀMÚ Òàp¤½° mÿ °¥Ã‰¶óË+‚8 K (aıÿ Ğ+$% õŒŠ¡ /£n² D´íÿ H®‡yÒ¶KñH@½ÿ Ørd—tÍ¼IùT}ÿ;qN@t3õ½
 €á¯Œ%Aœõ‰’
 õø   Q=j  8€ZÕµÔƒĞÀAúÍ :ÜF“¨'$H{÷òVlélPÀ€ùe‚{œB†B@  ‘M  WH=
ÿ š÷ÜÿB4 ĞõÅÉõ` ìÀ èóHŒĞ¶Ør!¸­	ÿ-$ˆ½çÏĞ¶¢›•ó&åSõ 8Ô&€~	ÿRu°š` ö* €€äõ>,@°@Á½<LPıÒUÆ4¨ +õCP(ÚÜ›	Œû õ°,DlEãõ* ŒÀÌõ4 €B¿½K\¿ „õ3Œ ùõÈ$Pº.ÕÛ²Pdü øõY·ƒ õŠ+è
õ* ” ŠõT¿t•†±ÿªd¥‚‡	õ@$Ú0Ğèi0`(†ò
¤ó¢Iõ ˜ ¼õ  (ì
ræBPïe@ 
ƒ+`Là ÕPïe . ‚œ$-€¶´zp ³w8Ra‹+9Öf@Gò^* ”€a,õT¿t•†qÿ‰64ºÿØ B€ß¹IÈ Ëõ½pj$/¾â%eÜó8H@'¨¸To^EÜ±èª §‚€	õ 
ÔÚÀĞèSy`@eõø¢Äõ`S,\@5	õ   ´õ@@ &Ïò ‚ñ  µ°Ù ª  õâqDI '	õé| õ`  `¶úpŞ úÙ`ù úâj|`¤
õ”Ü †¤ò- ÈÊÄ‡õ«$ôİ==ò  `§õ! P &õ 0%£T7õJ< ìõ—3È b>õIHÀ>…¸ P¤ õ( €º’ÿõu4¢Ãã2õ"   B$³fãŒE õ
bdddOl  H À± ´ ˆ Î|Š­hÊ h<" €<'wÁ P 8ßèDAX€§ŒÍ  H(sÅ ^ (h;X Àµƒa€,5‹FcP˜Ÿ)  ï õ. D†@dÇºJL>Ù“( ²]  ôpy\`Jõ 4= “şuq €ÁÑì
 
 zV|a8g€‚ÄÇ ÀRtËÆ@# ~õÌ–	@àÃ €…Xl€  “ÜÇ+²”^Îº/b>HKå+
 ¸*Àµu
™U~„Â[hæ È€‘‘·
 4äµõÖ( €iä\
bddLl $ä× ³C 8Dp6hÒPlE@ShÀ5wÁ P ßè{@X€§ŒÍ‚W 
ÿ;àLS†Ñ”Çÿ÷‡<³fM NÏLCõvHT-lJl·ób`„4HÀ;Ğ §VâK­#ŠSPj'2h´€æìÔ- ­Á-6¤$ #õæŒÒ"%õ§…ÊûYÿ® ˜pZ³
Ş	
l d$DÈĞ ³_Àõ,¨2áQ è€äïctˆÀ—õ°c¤^ ÌåÅÈõä¨ €—…Ö ì7§™Îí 4di®	 HÀ°!hÀ (‘°h Oƒ%J à  ¿;tC  p…(  ø/ @´   (ş³ñ   4>õşˆC€MÒŞC»i¢‹…ûö       úÚU	àÓáú?%`‰Üú•´ICr(‰  ĞÄ;ú‰   5ƒú# °Dƒú#  5ƒú€p?ğhtú ø  õ  0h×úiç@   ú" N Æ´" œFªšÚ0Ó š  ú[_F   örSĞ,ú d à(õ  €7õ4 ¸Şi õÃœ=ò5ş55 5J´f ÖÖ §ê  €åú‡ƒ`ıêí' (t„0†¹Q$á Aõ (x ~Òò§{9… úª&ô!¼¬­»3’†Zõ5 €"!9õuß¨ıEİ/h è™FÄ^õV  À†Òõ5P+ V<:0 Ğ1 İ,  @ºUú@ ‘Q   U• ú T[x€~ ÿ0  úd]& ]• ú<    ú c p–GúDƒš•™È0Æ9ˆš¢,  `ê€‰XV…dMS"æ¢& 6Á  µìĞ»ÚE4H*©s‡¸fÿššª««›š›ššª««› ¸ò¡««6CšÀ0B5 Xí jE!©bP3"ç j$ 0hb ¦‡ÇP«BYDPÀÑ5Ú…Ğ ƒMÀ`×»7©©©w‡¸fº©©©ººººº©©©ºº ˆ+_ jic+D+£µ   «Åú€\ (ŒM> œ÷%Ê)õ @ ò¥e·¡"õ¤±H À†…ô>Àd?ú\CytO õz5Ü(Î¤_   ú     ®Í2  à®şúİ;P  ú :?Y  µŸ,  €Nª   Ğir  ĞùÉ ú  ŒáNÆ   Šµ ú¢X+  €Nª ˆb-€>ÿ  `   ú  @ú‘Y€y„\$  à$ õ³ÓğÃëúâ     ú   ø˜úhZwÍ õ p@Ø·  ğú{€Æ´÷iÆdï`Liö@ À‡“´NÄŞàDiÀ‰ì L  €¨jõ¹µ> `øÍ•  *­Ò°Í: ¦üú 4" oõ ĞˆBŠ‡Ú @õ	 )  õ e€	¬&5_Ş¨F%®]LÄù“® Lh»4ò $ À¤Eõ• ­BÛ9:]{3 ®‡O   ÎÃ]JGããÍ	 (R ˜·7ªQ‰kÿq?éøµ7®³¼§§’Ô  ¬@¸Ÿ² @«IÿªU‚ ³ÄĞëé-ÿ €(dîÕ	 )  õ7ªQ‰«ÿ €Ïè-ÿƒ    ú£f {ŒúÅW9PÚ  ú%‘lG€3õ	x8–›ÔÜF £õÄ6 Ğõ%‘lG`4õ  x8–›Ôå¦u ÿŞØ¦L õG7Ğ  'õ¾¤àá  úõ   {uú Pøin$õØ àÇ	 ú~ ĞêúîJ \	õˆ"üÊ¯°’L2AÊõ=(‘   ö
  ĞÌúiI Àá ú£
 ‰ ì’ñ]\%” õ  €È’ò°€¶@©‰ÚMJÀ"àŞ õZJ$!À›õ  €EÜÖÑ~€EÜÖÑï˜GœÚÑ~ h)›”òmJ0˜0“ÒL §4RüÏ¢F0¡‘Ÿ Xe)‡Ø $“
™j¶
P$‘ÕNKü' h	õî`« `«Õ  pïâ
öô8œã òõ-¨é&´	 ·@’¶ @Ğ‚YÙèGğ!€lÁŞI6¯À‘¶ €xee÷RèBåCˆWèo7¨ 4èÉ¡|7¨ 4h¿¡“*&lÀdX*İMâi|<ZĞ¶,F0| ò  ; Ào—Ğí† úÍ‡À§M úâ€€€  #€ BL  Èb Ğ¥Ş”  @9óÏ‡P QÂ l€    ğÎT

èlØVC5¬D

T¦+ì€†¡h¨Ü  €Ş{· ÒX	ÒÀ`¯,/v8»<' ³v ÀÀí´`¯  ” / úp pû­ ÿ`¯,/v öÖ% ©4:ÿ<i@-ëà  @¶ ú	 ÀPœh …-€¾ ÿ
@Y— \ÿdaKÀZ*»  8?kıÿ  HÀ_ õÛÍ s,úm   ƒ€8% @	ÉÀ	:Q* ¢*àlÀ€À >:h‚6"2 ‚ÅB@d ‚0@5ˆ»4l5AB$^#µL1¢Fj¤F4
³â5R#5Mâ:uêÔ…’… @aÁ¶    @ŸÜõ    `yõ à‰ú †à@†e 0À ´†  ßUõ   Á L à@ õè ĞÇú
 @«}‰â     Âšj   ;Š ú ,é à P   € ´¢; LúMßÀñú    `ymõ @‰ú ˜ >˜eı® LÔ{˜ >€˜Pi €	 >€m€J 0M¹ú  œå$ õ   P  	  pƒ pƒp(ğ‘p	  RĞ%l 0øbÀüKp?©Å5¤‚çÈÀ·O  `]ÁI®h€Gu&hdW `zK/±µE531Ôàõz½3B/¼ïûÆF£à €¦´	?$]$@@ªh ĞŒàà¡Sğ&@c`h  ³ySó'ÚcÁ?KCL46 ³ ¹³ùÙül  XÄ¥L0 €ih¤Æ
 Œ¿L*4¨à., ³³X€³Ö¸<  ³@£4`@qh  )#i¡Û ˆ âê¥  b˜ŠÄ^b©b bübJb   FUOœ$  Èá¢97  7’¬Ô”€ ù¸¼J @
ÀŞ¯·	 lÙ®ÌĞT±£RS!3d®a…i8¾³‡>•L‡Ã4Ÿë  AÏáX¸,ÙŒ¥'(°À¿G,`$‘Ê  !t­ß®Ú Ê' ®|     ×J @_0æ°7       × Ú8(Ÿ® ø    +å è(Ÿ® >    8. 0“½®ÁB 
Ê±pòAÄÀÚõ`Èƒ sõ X  ¹¸Ö&® ¤ ×™{È!şõLÁ ÈƒèLa  SL ï¸€G®X      µŠ,c•¨ ğDw5    ĞQK9   | 5h   @­K  cg¡RÄ® l   -\x]+®    !  +  Y¨1Ú®^×
‚ë ÿ „@ ğ·K  :TîŸ €   ˆ  PkK   dÑQŒ®  ø   +   ,©b­®Y@%R ¤L 6     GšX+®*¡bØL      ²GD£V@°i   d¡RÃ®  ,Ø  -  yku­®    @       €  @×SK   d¡RŠ® ‹©U«kÿ     @    à—VK   "o~ +       Á  ÀÆÿ  P« °8>x€tlfuCÄ½Kü…À¡	²Z¸h8ék°T8
x_ŠØ, PL•ÅµØÆL2; ü2šlQË67¦Ì5  Rø‘›ˆ        Yø…|ÿ*1±Ò\p_Æ¡EØŞµ® Œ©ñ®]Zpíhq®ML‡­Ø® Œ©„ƒ£®¸hÀH.õ?Qû¬á®b Ò@¤øZ ø ÌÕ¾CHGA,®®^]İÑ+§®®çè{®ôPÏíŒªæ üeĞ¦g¤0VN¿®k Hh@âÅ¥È) ûÈ¾*Èì¦¿ãN‘®ÙPÏíŒª
Œßá³ÿ| ²ñ‡ª| bd’ts®* €9±¶Z °
«˜Õ   FVØ®  î@ª_r{Cô  L  Ğ¶  88 6w®&,c•¨H <ğ‡Û™HÀ#Ás¸PLÄŒ³ÔØv›€Ôø‘›ˆ ° &İæŞpéê‚¹Nhê ¨ìAJ®&oCñ‚J5O™oÆ õ ¸+‚ €´³6¬o Dõ/¸´H ³&˜˜(Z ³&˜L¦iMK¦iš¦išL).0.æ¨l
@bŠôŒKFõt Ç¦6Ü) ³H€9–-I
R/lÀé@áP@Ø3²ia^ Ó@ ç¨…0Œ,ÎQ pKE°ZP®œ*ÎQ@†K
¨u…6À×§L·AÑ‚),GáR	KOEÌ\ 8š ¹¼W¶Wzv‡´¼×QÌQä·) LL"êBYK8¹A…ÛH) ¡€Ñ–§asËa)AäLu…"&
Ãa{vJr\"9ÎM(
HeJl+R³™Ë;¥×X(>F‡Ã!9m3…CÁBâv3Ÿ¤ÍJÑ½ô¹‰ZAqIE‚H)thò³_ÉÁ@!53	 
fLl›]P®ò …C!ÁÚ#	b­Jc_P®vÆ¡°LT3	ºJØ1(#á3'7é*÷:×$aãT‡Át»ŠJ(gZ€DK:½#J}ÒØpæ7A9¥­+àO¤ ³€(0©M…Ÿr3ª…:dÓX
.ıÒpl "€Q9r" Œ|P€xrØ ÀÉ{O ğà(t|Î•pz„S$hNp°„S$h[— öö³	°<	‚½¥	™J'8>1àƒ ŠC˜Pœ(Ía –K”
(2÷^x˜*Å lÄr@Á»hà Xš§Là (€OéL %x€£ºfçÜÂŠí5XÑÇ¡KY¼@ï

Ğñl Àä##®ö-Jî[‡ú+Â[~ú+5à  VùL€J2ŠlÀ jÇLø-¡JÁÂûì5[ïÇ+Àp¸g7JŒ@€â2h Xö`àµE’:Òr²) *g'€rN  v'spÁ u…Üî¼  ƒ³™˜ ÂCh D" ³ğ«ÊÆ|xa#| z¸{…à 6ÀZ	L.,À-RJ‡b( ƒ‚4) 6làÏˆA Zp¼ 5†ƒ­e 	 >'}p ¬…°€ón ~§LóD÷<|>GB—%3BùA« . °yµkY!Tş&D%tŒJ~7ˆJ   @$/õÎ`  @É}  §õŠx€ {İŞ p”¨» tíPön ¤	 üÕà`  € %õ<* )lH@¯;& ( €š¥ó5(Pü °,
ôß JG   …õ 8Ô~Ád³ôú¹¿Aàµ%Ì…İ:éê`ñ/3Ô!Å~¶)€å ³'4Ğ{9†- &8š=…ÁHÉqà)(Dyr hf>Ì[iB8v*+€à`ÖğQ8 0”De`@ñŸhBFGõ+  …#üRl  Ôˆq#p 7 ³LlÀ¦N@Ô‘xwMø ³ ¡ ³a*¬ƒ‚N1S)T¹ú_	ò­©@BÉ·ÉQ$åq<_p ÒBF04lò`úXãh $(r„1 ô»p€ônâ8úOy h°ÀHë5	PÆQ …K P +8àl“çœãl;î" pww6Yq@~8U% –Æ¥€›t
.""¿h
~<l`J3  à‘%	O] †"5  à®Iõ! H!B
°·%pÀ\Dø5|T ¼<³ (õ`®è P+Ãğ¶ úÆÀàµ ¨y
 îo!ú
 à®Hµ’0k¯oè&@Òâuµ]¦	0i‚ihæ¨`šæß/ÿO€Q.0¿;› 1•< Œê¡àµFà€q§h©—Š;@ˆMÃ®
Ài¡+Èv&J€OƒàW-GÃhı  P‚Õj D]¡Í/ÿáZ@ hñ‡ s'Ì(\Byh1s§$¸$ËQ(‡q<)‡´œÊÅ{eEŞVP®"P¸({9u-~ŠÛ$8™F @6ÅIb‡8@!h¡œ‚LÌQ,ÀTÉ§`ÓÅg{eJY¬Dâp3)	@L‘Gp@±­hçÖd.J)\tàƒÑq—0Í€8Ê’DŸ¤Jd¼—>g²+ùAq(’J‚($4:4l3¦/&£<	´¼	¦4ËQ°í)E«\P®}Â¡?3K	ŒJ†®]Ğ®’K|–»é*Î5|$Ç¸zLwŞp Æ®b˜>g
($Âå hè5¡1ÍÁàĞ}3
ú$lR@…¦5	2ğ'l(0 ³–AáÀ§E€4
udk'`TÜ/ÿ
 p„¬ N¼DŒ|lp  ¶Á¶ 	àxt|N‘p®„Ó#hİÛ{À±jØ(¶Fy h™H€}‰-À(Õ•7'à À@óL&'Ê/ÿ€`	8êM2
°ÂA:Ø#L¨„e¡9 xğÂ‹rGx –s h°4t ³  Pğ™sp …b{Í/ÿ:  À9·¸ÀJ_w-YÀ`ïP0ùÈˆë/ÿöZÇ+÷Z‡Ç+áZ~Ç+58p ¬Ùq8€ÁÂ1“/ÿøZ¡Ç+Y{Ç+ îÙM/ÿ(à €ËşL,{Æ JŒ­IêHËQd)*g'  
WM@ØWM Æà,/ÿó@xr h İ(kY+•ÍÌvXî^!x °V #ø5À0-Rì5Ê(À  æF€µç  ”|OM á`kÙ.ÿ>' Àš¡káœ •M@ğ3G8ÏÁ‘Ğe‰.ÿå‡V;”ù(›WÑó8Ç#)ã(%tŒ!J7ˆ&DJ É‹€´Îà @AvO À^·w.ÿG9 ñò ]{–sN  @I=.ÿT@SÀ ³ˆ‰„â¨Ø#? ,Ë-ÿ€@ß€º5 €Â
ô»µ_€Éà õôúq£Wè6º:XüËŒ-ÿÜá°íè¤4`9@CÆYz/rİ&p€“ó5ğ@'l	8P Œ”86  D9j˜ùÁ¼•Bœv*J  Ìq˜Pü'š-ÿ£Ï±r(%3@` ñ„Y<ü‡‘ºKÀÀl-ÿl° Šñ5<  ˜òõ'ä êHj@ ¡}Ma*¬ƒ‚W†6*Cò--ÿ  à¡5õERş `hp‡w6}Ü ï.Æ;B	EBòa'ıî ‡‚Qà úO	çL‹Ci9ˆ3”ÈÊ,ÿ p ”pœJÇqHŒtü~ ¸‹t€ãnYá  ün‡ˆÈ¯Ç,ÿ ~<H ½t ‰ÓB-’ `¦+à:0(„, Ü5© õ! ¤‘ën /Ï¬,ÿ À\Aø·U+ÿàA)jj€û[,ÿëÑG}\ÿ-@¤ ÌÚè& (P@ûº¦	0i‚ihæ¨`šæ\ÿHL%,ÿw
Àì[ÿì”„FÂI
R/llgZ˜ˆ[ÿÈJ( p¦!á;j§8£u' ÂSÁe¡œ‚tgå(Ô ³!p€C¸h.QÂÍaü2¡œ’@sŠƒáğ,Äæ4ËQiy¯l¯y[Å)—%.Ê^8P8|-n“àdŠP)F¶<ÅÉ}( s‡+ÿ‡ …Ü4(W#Üd^sxoP€SäQ+ÿ ØV@J€´.<§ÄCcıFN[Ë'‡‚…Äíø3Ÿ¤ÍJÑuÑ÷ÉW{š|À,[ÿ†’>'Ì˜¾ ³lvåJ‚&¡œ’`J³b­rJc_ 8š Ca™¨,3ºvJ‡Aù˜K7é*×]Ã&aãê1¬ŸÊ™ ÑÒZÿs08tßÌ*ÿ ú$üØt¡œÒ³ 
Ej“Zÿøh"@Ã+¡C65  i¸Òp0*.ı l¯#›*ÿ ¶Á 8½`N‘px„C!h^'p,Z*ÿ,O‚`¯ZÿH@&*±G¥J 
çaÀ@K Åy0>(ÀÅ¡‰/0ÅÄú•@”ŠÊC¼xÀ™‡à –§LG8Xš~ h	à¨®*ÿPÛAJK:  À9·¸ÀJ_w-ƒt¼*ÿ “Œâ5ì[ù+–ƒŸşÊYÿX3Â[ l8`€¢õq8,  ` –ƒF(È€Q#h{ÈÀ~{98` ‹ìa8 ¡…cïeà €CëLJŒ@€â2h´…lÊYÿ €J¦Cµ;ñ TNjÁPWÈíÎ{æ€ğšYÿ3sà…İF*ÿP üJİ>#Dá»#p‹”RYÿF†‚š9°¡›ÇÙQ>M á`kYÿ '€Ce*ÿ ¬… ³ò3K3BÄ}ğD†—x3å5JÇ#•¿ÇXÿp Œğ5ˆ&D%J DòúAÜ} ÀiD=*ÿ ` ¼nÆ×^åœ*ÿ À¯ötíè  @ÉGšÇ"¡8*ö*ÿ Ğ,™oPü  ³, 0ƒı.*ÿµ_ÀdàÀµsa·Ç)ÿ+8°ğ[Ôò, PàqÖ)ÿô^aKWÿ¼†Ğ	›)ÿGa0Rrğ)àˆr ³†
Ã
WÿV  à!ÄÉP((çaÀÁK  şÇL#øàXq”É.(` Gø8 5â_\)ÿÀp 7gML6LEş„À¬Vÿë à•¡)ÿ®~¼ Lõ1È @Ê«¹ <´¦	õ	‡BBywy[Vÿ}ŒàİetO HÁ¡`)ÿ%ô» ³ÖÀÑh ®G Éú ®¡B¢X#¿ß)ÿĞ€NÕUÿ Ä5 õ% –Æ!àµ
.""¿h
~<l xdIÂÓUÿwMê@ı(ÿ! …˜bqpP /ÏlUÿÿ xPŠš(ÿ  ÔÊò 88 úÆò &å¢õjä
  °Hµ’ k»oèLš`š Uÿ`2MSØ„ÿ\œJM ³  `D½vØ”D¬9  _£ õLXÀ³–hl’Š+lvLÀi¡+ÈJ   Ÿ†zØ9Œ:(¯0j@Ñsu2pÀÓÒ“h%)FÑ”'ĞÂ€KK\iaË†(ÿ0Ç6¨“0`DxÕK›Û#åÚ‚O€ G³TSÂÄ{vÀ à håX—uÎMìÒ^òê"3X8(R³gÓd {J)wÃçH(Ÿ( )šµ;ÑuÑ÷É{vù@!Q­´2(,f-l øÁ€´,À†=¨„0­r¡$¬$`D*ÅGº ;Ëœ<î½È¹¹«ÙI8¾>ìR0-@2[APK4½J0,<@ş ³
RÅ=€à*©M…ƒr3h…:dÓXE*˜glê2 DOÃ… h¡ `NmpXƒ#hNmpƒhøip ¤wæ¡F4•lK¡
“å+ (€Æ'Ç;Ó€b!øÀ€
†5
(Lqü°F•^ŒJ`
8 @^¹º	  ŒıpIˆ AhN ¨ hş  XVå¢ \EÖ²âg &°#¡L÷ yö €JŞOı}ÜST
K[ø ‘JLE Ÿchñxû;®dH¦‡ ³  
—<€÷È èÁ  PÈíÎ!¨€ ¤îè@ğÉÅ8	¤•ÑÄ<	 À‡¤x˜/,-H¡ ØhF†z9 ¬ A›~ûM$‚¦ãÀ	 pƒèõ@  |êŞ	rHÌ=Œ×ÇVHæ'‹ @Yh>ĞaÜ“1~ è ö‹\ , `à)õ¨x¨fB ´ ^4 $Àµ  ºÑ!ò³u ôÖèÁ ÀÌŒ· „I$ õ4€pO¾ ³ t‚¢¹É6À@  ¸+€@Í€³5 à0
ô» \ĞÃà õšß‡’1 „tN d«bÌÍ'¡UÃSíÈ3a ` $^³X Ïp†D	 `€dñ^6  2j˜ùÁÜ”B–

v*J P LÊq	 „ 2¦M¢á6DKE Ñ¸®	 Ğ3œ÷ÀÊ@^@¥NBH ÀÌç· 8’B Ğ¶Q$ÂÀµxD’ÓòkAÔAà¸·Ä¹—B¸'lÈsVh7å(­Y ¤ ³€ªÑúC€äalÇ‘ãœÛ HÆsÎ9S) Ø#‡°·  $ ` õ ûš…N7…€Ù`0Ğ3( Ö8'Åu€¹n" HA
À·! @ 9‰Š‘ˆ  ³ï· hÂÿo 888 
¢³E ÀÂ¤ô°6Ü7@¼µ&ŠÀlªÿ¦iš&SÊMÿ§R&˜¦išFHL%zÿè×t Ç¦6$<¬% ³  Å“pÀi6IJh€;æÂ8Ài œ…<ÁjÅUë$ :Yå vK èI€€´“p€h.QBÊaŒ2…ó@!Ë#ËË¬' Œ<Û@á›#"@@ÀU÷LFÊi8a Gc7·2Qhò' ›ø¢mÆxÿW¤ÑÅrî#8P)ƒ¡Iè:¥×X(0îpy6(¤€f
ÖŞEŸJ••6Ş‹>Ÿ ³V¾€’N? /Ô6Elv¡$¸$Ñ‡€Jñ‘$ÿK	ŒJ	
ºvJ—‹ƒÓ‹œ'áJ+»ŠÁ+4½æ]$ ĞŸ çNR  P;Î6
$(( Æ#³à€«hdÓXCqèi)Ì3•1<§ÁŠƒRÉKG P—¢¹’ ZŒY j8`ís éy¨$ÿÉ„  ?õp€±9¤0¡“J ãJŠ‘i@±ˆ€8c K`,P@Q¸,\`Œ*½ l  ¦ .¯Õ0%@0l ³ Lˆ Ùr  –U9¤$ÿYÀ`!îPØ
Dql= há p÷ yJ Š¤àØs‘  ÿç¾U?¶ğA l * ñÌr’é!À¬$ÿ€Bbã,²4Ş#0 Â±ÔÁ€Óî¼|2ÂE1N$ÿÒeÄãŒ 	üT<P’ ¡ÆF$ÿ+˜Æd`2ûM$‚F œ$pƒ ³ó@ãÅ#ÿB—ğ $ Ê×eŒ8x,—,Æ/ò-è4 <Å õ¨xT3‘  n¨ntH€¼#ÿ @  ·fµÀÌÌÖè &‘àÀ hn²M#ÿ`@ `58ƒı.#ÿ \èaàÀµÙªsóÉ"ÿOµ#s‚È4 x®È1† hx4, 82 ³È	0Œ\=ÁÜ(cà(

v*˜ù$V   …––™P( :)hÛÈ ‚hƒ šñ…Y=3xOñ^Ê@x¬š6¤ ´­"ÿQ$C†ou8îm"ÿ $  b-õ 8@K¹-òœ•ÀZ"ÿHH+„=@Ê± Ü”è >ê5	 ‚s( `V„v<çœc›38À—; h @2õ) ì‘Ã õ¾&AA¡S"ÿ6   ” ö5jØ n*ÌGÌ
A	 GK œ§ õN‰¤R ³  	”· ˆ„@döß hÂğ¶ °0)}"ÿjä  °õ ´x]mWGÿ&M0M€Y h`2MSXÌÿ' Q¯]Gÿ6
1
OJ Ğ¯;ø¦¬$†â
Gÿ Ã¹0"ÿÓ à°€Ş-•å pK `à‚Õ=©F €z®288€' ³´0àÒÒFÿˆ1•-‡´¤åe«gW/y[¡+”kN˜¨›[Œ'‡8@!h…Ó@™¼#4‡q@PK#Î'¾&
HW.$Jè’GÊFÿ08ÎIgk'Í 
öî?ŞKŸ›LFšÿù©Z’'~@ğ ­Fÿ«\(	+‰!ÿ	b­JK	Œ}J	
ºvJØ½Îµ±\Æ®bğŠ!ÿÙ(Bå h@C¯…>À<@şáMãHµã60 \UR)DÙ4FÿG ó‹¨Q§0 ˆ¡•e €VcV!ÿ X8ìsÎqpbƒShNzp˜ƒSh`l)L!ÿB
“­+ 
ãó+ÂÈÁ!ÿQ¥£’Eÿò
p ‡6 x  ˜â¶0  ÁÕˆ* Ú ÿ °
Èi¡€Q\Û ÿ `ÀÎr„‚÷SEÿèÿ¹o• ÿ[ø ‘JŞ~à+Eÿ $ÓãG…€pìu ÿPˆµ;/ï¨# ¤î‚³ie41‡Dÿ		ÒJ) €hšÀO1,,´J“A` 3š ²‚hÜ``¿‰DW OÀ™pR/KVE™'äH~$ @Áp}PX\ bh~D|tŒá+Oñ@@Çu¨xT3‘@YS¨ntH€|Dÿ3E ³5º ÿ €œ™ß_   L"õ	@¸'l è…¹oĞ@3àlDÿÀaX!X¡d !İCÿ h~g(õN yQ1»2 ‰×ìCÿ 0`Ä{r#WÏ ÿ>F07%‡Cÿ–

v*%(`RÀŠ …_	`€ÌLQDG4î€kCÿ=38 ¼§øq 0óùm ÿ ¤Ğ­Q„ä4‚üBÿˆ:ÀÏÍĞRnK ÿ‡P¸çq hyÎJ`Ãhr, 7%z ÿ‹  ’rh¤¡K A 5ƒç8ç6†Bÿ8Àã8É/“äp@= ÿ	 # õØ×”‚@I €£µNBÿ]  `¦@”X) ` à¤8õ8‰ R³Ìt  $à<<DB" ³Q8  =
õ  è‘Œîo1ãÔğOõË€ûßõI”Ò©kõDP	 N­ÇÛÜL™1AŒK~  Mµœt0hPíĞuLX§f™\)ğ Àpé·‚ ¨`Uö|Î&oCñJaĞ,³ƒ„¶ÀÜÅ‚a  Hy©Ë2 p_a.ƒÛ …, Ñã®8 µ&`G®    …JKÂ  ,Øs[´ àfºsµBÑ@6 „Y    dUâ… °ÀF   5  !Ä­ä®! mkÁ8 ®J    ~ä&"ÿÿ7˜2Æ¤ÔÎK M µÛ› (µ³®Z°ÍÍÌTJtø‘›ˆµØÆL2;:ødM¥ÛÜL™1RKŒ1)¥óRÿœ~ä&bÿ7˜2Æ¤TÎK—?ÀlÿQË67¦Ì5 .
ø‘›ˆ‹~ä&b ÿ·	ÈÀtQh?À¬ÿ'~ä&"ÿÓ}Ğf‚æDh‹~ä&¢ÿ@`íö&ÿ 4
ø‘›ˆÄ~ä&bÿ x$`±Î íB{y€ 0-ır•ğ ˜çĞ¹"0[àJ{à1÷EÃ²KÛ ° LÙ" ”  §¼D ÷>øÉF®Mb3«53! ]üÅãp1 ï‘ s®·	ÈÀjh”€C®ÿ¿Œ& Ûÿ™˜”àh~ä&"ÿÿ7˜2Æ¤”ÎKJéì‡ë	ÿ~ YS`)	ÿ·	ÈÀtih?À¬
ÿ‹~ä&"ÿ@`íö¦ÿ ,
ø‘›ˆ'~ä&âÿ~Ğf‚æDhŠ~ä&b	ÿ·	ÈÀTQh”€C®ÿ~ YS`éÿ·	ÈÀTAh?ÀlÿÄ~ä&â
ÿA{§Ğ^ÿy@€ié÷NÌ–¸†ÿ4 ,È%Â€Áöxø¾?I	PpºV÷„Ï"b3«5ˆ4! ]ãïÃ›^p\}èÿ@S`-gÿæ Ğ µûÉ{æ:ÑX€I7h^Jğ†KWhCâÕ-TlJg`é}O~ YS`éÿ@`íö¦ÿ²Í)sMÿ20YZÿÔØv›€ `ø‘›ˆH ëî×É…®Eb3‹53' FÒÅãp<  ŞÀÿ›# Ü xÀûsà
 écbŞ0ñ<‚7Gh@ K ó»Ó}Ğ.L¼h‹~ä&âÿ@`íöfÿ²Í)sÿ	E¬3ÿA ´œÔt|æ9´ÿğpÀV)h 4,›‹®6`ßZás£H§ÿ5 qïƒ®&„…ØÌ3ÌÄ¿ÑŞ1¼GD‚®~ YS`)ÿ@`íöæÿJpø‘›ˆ~ YS`éÿ@`íöæÿ²Í)sMÿJ]Ãl ÿ~ä&"ÿ@`íöfÿJvø‘›ˆ~ YS`©ÿ@`íö&ÿq“ŒçNÿJéì‡kÿ~ YS`©ÿ@`íö¦ÿ .ø‘›ˆ‹~ä&¢ÿ@`íö&ÿ²Í)sÍÿ20YÚ#ÿ'~ä&bÿ²Í)sMÿ20]š$ÿ~ä&"ÿ·	ÈÀ¤QhF€X®ÿÚŸ"h 	³y‚i¤ÿƒGÌ–¸†ÿ=¾ïÏÿ$% è)ïÿØÌj"ÿü}à€&‹8D 	2B×®™˜BMüh~ä&"ÿL(p³ª~ä&¢ÿ àø‚êõÖ 
Ğ½•¿È„Ü€ˆ
õlş¸ Åtè(l.ÛÅlâÈL„+F³^ 0ø òVƒv Hıè” 0] ö»ÈÂL;ˆ³Ìî›PápÉ˜ÕÆv/Bèg xSâ¹o¨œ  ¼õ’¼ü+K£òFX]@%À·m5ĞL¡ õ. P  †ìß‚bp«AEõ#ü@Ù“”œÓL Ğ³Ò
Ôöøò£È À>âÇ0 0î€ä”¼y° W³e5Ø>@õ91´û<õĞ¹¬	 õ °Ä˜¸!7X¢Bıÿ. P  Fößíb8²á?õ#è(l.lˆËL Â³R?à üñö ¬@ãµÄ ÀtÉ·…(d<íhÌîX`õV°İ`õ0 ¨} Îõc¶è-¡—õ¯Rxà„M¸@S`-§3ÿ4`ÒÿŞpéê‚¹Nhê ¨ìAJ®&oCñ‚J2 ¸/@¦Î½@fÅãK£ÀÎ#0û
N­¢Ie;æAD
8@¢„;D  cÉåÉ@8ˆÖ8À˜7Gâh #5	ÓÛµ~ä&¢.ÿÖ©Y&7ÿ †têš…ÛÁP¼ ’ÿ1ÙâŒ5ß`$Po˜× VÆZıÍ!7X¢BıÿløO@ıÿáŠ‘À¬ÿ 0øÑˆËƒ2 ©ßÔÓÈ €½ÕÇ0 0í â”¼›° W²el7,B€Ùõƒ…ÔÁjõì” `‡õÇå0
 ôõ  ü ¯õs¸ZÒ¿œŠ	*ñäè{®LÂhi¹sŒ*áàhÓ®=êha¿s ğB 2ßÏakEÕ
î³ Œ©T1Ó®"`z¦ÖâÃCP`@%À·_7 RÁ õ. P  >òß\cl±á?õ#è(l
lˆËL ¶³m?à ±üñ,@]ò­ÿÈÂL;Œ³óï›Páp_¹ë&ÓBèR¹ öéJœØoĞóÆ d³[œØbnø¥ Q'õWÈ@$Bù…´@S`-§Bÿ4`Ò,ÿ*{’ëÿoCñ‚êJsàÌš~|™â,¼® êcB$™Ñã    ¼ú› aÂÂÈÂlÜ¡O%“Ã4 ˆúõ^‹~ä&"?ÿ·	ÈÀdQh?À,@ÿ‹~ä&¢=ÿ@`íö¦ ÿ²Í)sÍ=ÿ20YÚFÿ‹~ä&b.ÿ@`íö&>ÿ²Í)sÍ>ÿ20YÚGÿ~ä&b/ÿ ¸€†Í1  ±î~®"…ØÌ3ÀüŸ	¤³^xx€ÅhMN 1#nJàæ®ÚÌ{< ³1 X 0–Í·	ÈÀ¤mh4 @E.EÿÛÜL™1K20YZKÿ‹~ä&â2ÿ@`íö¦Bÿ²Í)sMAÿ	E¬sLÿ`Zú½0ÿà9@^€¹ €Z¥¾Ërˆ€·‰lØxğ}iI@O·±&,lŸ Ø€˜”Ö	a!6óL0ÿ! 1äïÃ›^209Ú@ÿ!~ä&b6ÿ™˜v„7Dh‹~ä&â6ÿ@`íö&
ÿ²Í)sMGÿJÂlPÿ
~ä&"Hÿ@`íöæ=ÿ "
ø‘›ˆö0Ë43¢N5 æäQ•ˆ‹~ä&bIÿ@`íöfÿÆ nø‘›ˆ>Â‰£«¯z%  <İy˜ü™&ÍLÿ‹~ä&âJÿ@`íöfJÿ²Í)sKÿ20]Tÿ‹~ä&¢;ÿ@`íöæÿºûur¡ë;ÿlfqƒÄºKÒ…À¡ˆ¢Z¼Àüs‚‘®n €×ƒ·T@àŒ]àæ®ÚÖ ŒesÿLhb ³*~ä&b>ÿ™˜üƒFEh‹~ä&â>ÿ@`íö&ÿ .
ø‘›ˆ~ä&âOÿ@`íöf3ÿ£ ø‘›ˆ`éÿàQ3ÿà€V©¯gÿË‚\ lëOm¬	‹ ÿ" I	 §Û›…ØÌjÍMÿü…À¡	²Z	`@(†Í œÄï‰!~ä&âRÿµ ¶İ& ÿødM¥~ YS`i  ”Uÿµ ¶İ& 4ÿCødM¥7˜2Æ”ŸK?ÀlWÿùyT%âÿÛÜL™1ÁüKS†ÁÄÊ]ÿM Ônß„ 0‹úÉ{‹~ä&bXÿ@`íö¦Uÿ²Í)sÍMÿ. as_ÿ H¬»_'®E

±™Å3' 0€tñ¶7 ğş\Fÿx€9‚«öÿÚ<ğ†‰çh¦û ]šÿ‹~ä&âXÿ@`íöfXÿ²Í)sWÿ	E¬3bÿ`Zú½ÓEÿà€V©¯§ÿ |_Z	ÿX  ‹l{›½~²‘+	ÿlfuCÄ½K˜Ša3	ÿq“ŒçNEÿ?À¬^ÿ~ä&"\ÿµ ¶İ& <ÿødM¥~ YS`©^ÿ@`íöf\ÿJvø‘›ˆ~ YS`i_ÿ@`íöæVÿq“Œç`ÿ—?À,aÿ‹~ä&¢^ÿ@`íö&^ÿ²Í)sMÿ20YÚgÿ'~ä&bOÿ²Í)sVÿ20Ušhÿ~ä&"Pÿµ ¶İ&à@ÿq“Œçÿ?ÀìcÿÄ~ä&baÿA{§ĞŞ^ÿ|æ9tVÿËæ"‚«Mÿm¬	‹ ÿ½~²‘ëÿph‚¬–ÿ! è ñ¶Œ1)ÀÒ]ÿ~ä&âeÿ@`íöæfÿq“ŒçNKÿJéì‡+gÿ~ YS`ifÿ@`íöæ]ÿq“ŒçgÿJåì‡+hÿ~ YS`igÿ@`íö&^ÿ²Í)sMÿ20]Únÿ‹~ä&bVÿ@`íö¦)ÿ œø‘›ˆ™˜îƒ6h‹~ä&bWÿ@`íö&gÿ²Í)sÍeÿ	E¬ópÿ`Zú½“Tÿà€V©¯g$ÿ |_ZÒÿRèéö¦ÿŸ Ø€˜”Ö	a!6óLTÿ{D$Èœë\ÿµ ¶İ& Kÿ8ødM¥@S`-'lÿ"`"È·™üı‡ƒ õ €(àÓ­Ä     Ÿhƒ78<À63µ Ğ^ÔtÑ±õqU `qŠõ²Ì£4 õ	 ¨ä×æãÍ@Ó	˜ÀK
½L ú< 
ñRşid@õ „¢Ì)õÊ¦¡œõ  dcÊF³?¨@q€£â¹;! ˆnÈ³H,@^ ‚Ah  Ì!¾}£"t 8g‰Å9 €NªÃ ¤ºo³@°Gb8iŞ Åbµ-Ø&è²çË%´ğ³êÉ[p
@nğÈ  p§áÊGW@¡²¾CˆTœGèã `*ÚĞ´ĞØğ9]±8õ'  $)õù 1E¡õI’G 	N  ´#œG+$Ğ” ‡
Ğ3,¼\"  €bn  pI2ú»b €@P  ,~©LÆB³¼¼ICáMh€ Ã!Lp‚’ƒ‚‚	 P @™oç” N•õ^ Àoš­ó İ€·à­©Zx DÍÅ-`À×/ ˆ(ú°ĞÁğÜí÷Á{ˆĞ=‡×aÀÆ¬ÒÔò‚åš5®@ c€IçÄä@ ÓĞ#\d¸ä ÀÂ­Â.`b®Èa(PÀQ€Â!HXà´ËâÀù¸*ÔôPöÜÉ<à†ĞÙéÚ`#¯Ñ×ıŒøÔÆÅ¸@„©ÙÀ‘0•³ÏàÖ°AâÅG 0MÃ® tÅ@±®€x @…¹R;³ÓÔÓ®4
p !«ÌóÀA –‡Ïp.`e¦Äm#vÎb¢® ¸Ÿg®sd ˜MepA„˜äÚÀ[à›šÛÜ¸è¾“Ğ ¯­ÉĞ éÚP ˜†Ù$Î”Àjòl•ƒz0É–ŠT±TÛçØ  ”g–äÒI€ÑC2 Ğ}ØÏ&¸ô`À w.&€d`ÌèI<`‚§	Åª’0Àáš¸†
li°æƒ‚O 8º Ïš¸0 ƒÉ¸X€#€&›¸Sø0ôŠ×Çà '}h@T¾¯s&˜P2ãÊu{×²q®E’4•lMèCÀâŒ¸€8]µĞD	€ÇÁÅó¸ÈÄ›ã"ä éÉI€à•˜Ï× èâÊÕ¶…ø‘ó˜ø\ôÅ]àê‹„Úİ 4všÑõ`zè–Ú6< %şÉ¶1°ûœÏÏˆMhp°ĞÜ 0d’Ä:à¿ÒCÀ-ĞŠÏÈ\øìÄÚİíX
ˆÄÑÅM $1Øò     °…hlğ  ³¾ó&lO¹€é^0˜!Hõ  üÎÃ õÀ#D6  L–++„¤õG6ò
 LÆ D”àó ¥A‚«Ç ô<õ¡•5ƒ©ÀÀ¶è:©€º¦Ç 8`İ«Î 8Ÿ‚´?õU'¸éÑ—³’B õA98@ hÂ2C@h G ,Ÿ> P™Âå®®ÛP3„eÍh€@Ö€Ï" ¤Ğ|¹ò Ğ*@ù¾Í«K ;†ŒZ€ Î)h‰Œ2#DÔ¡e õ 8çiõŒĞ”°ø^¡ŠõÒW	 @‹´ p  ’¸Õ‡nDÇ"aõ , à.õí8§E‡ª¬FæÌzÍƒé˜èÄ‹(( + ¨tÀŠ¸ôÇ8 Ä„ h2dL@€õ™Èå&+‚°|Š+$PLpéıq° €5ƒ´d®Û*¾R ¦ğQ<Æe ¸/€¤¸ØbF€
lÚ- ³Htp2 §âÑ|«pFğ‰è
 øü ŠÂµ<§ ³F4ÑD#±³€¢àµ3 64vÖ…p `L½f ³|–Ì±‡91
8@M£^+Ø±"Ä
‡š"J€<3 h4 @s³À±Xu$©èh LMDD 7I¢oÀDúM ( @õLÄB+³µ~ä&"ÿÖ©Y&×¥ÿ*{’«nÿ ¸w¶¦Vcÿ <
PB‚Å  ĞÇ  ĞŒ  ã	      Ğ  €2    4  0	     ¨*ü ğ0©- @ü+h* sü€?®   ğ+ ¶"AŞ®°… „=Õ$ ¨&„1Ğæ ”€?h$à"À[LË—(àM<3 (¶ñ4,Ã w  ¬B " Ø@s6 ˆ ÔÆ£ ãhğ‚ M" ›Iş şJ-\œ›öDèK c˜Ö'£Ñ´3Í» <`h  ”Óf› œfn€-  ì.Õ€7³  ¾[)®G ¬ Jê  l€¶Ÿ6 ¼@‚N  @I æŸÀ! À Sh      
  	     À  0         rJ` t €œr 0 €V*%™"  (   hl  )   €¦ )•^  g$ àƒ p  pƒ E  €Â˜ÀY"         P  `               ¨   +õ&@2 ¹ ‚È¿ˆ(  ²a: tË` pƒ pƒğƒ E pCp&  àH H¶J òM ÉDµ pƒ pƒp˜„  8ÿ    €8ş çéğ¹ÿ   ğ÷÷×˜ñ÷÷×   Ğ@@€ÂøğÀáÿş øÿÿd şşş&l üğçÏ¯  şøş†t ŸeÄ+³ª@×ã½s˜‹d³s æÔÇo8® ÈZ0 õb#Kœ · ´˜4}, ú¤‰IúAl>`Àú à  Lb î`ós ĞBC õİVê~B·h  0 ™ ú @)ø€», Š
^l 6%À@' Ä˜€	nÀ&¡Z: ^ Ğ ) E@U!ğ° [	:àg·U 0 ™ ö¢‚€×ÿE Š)1f & ØE6U%+PU<,Èÿ~vØ-’Ù [	ö (
p7M	0PÁÉÿÍB@UÉŠÿ  G›J%x° x€{ y!ª^ %Ğê|  0¯‹ ú Ğ(ˆ€´, ¸@°gh (%ˆ@' 8]€	n9(ø+„¼, jA€@È‹ (Qê~B·h
" -ÿwö h(" ‚$NW À€N9EÀ_!È ò"@ ÊÿğS{ 5óºØ D	ö 
6J	"PÁÉÿNW &  EZ Æ‡ÿy€€¨(b[P   úOa Oaàk   2Sõ…À£0ğ £˜Ğ/ú    “ ú2É 0Š©ú»˜    úÑC  “ ú/£ ÀØoúŒı    ú   ğ2
 ú/£ @Úoúò@  ·õ6 @€}  `0õ   ñĞ úK„RK@D˜!3@#ÁE2ª#j öı òk  à­?õ  İó úß¶mİ¶msF  å; h X#ŒGÛ·  ñĞ ö‘G fÈLÿÂ%Š‚ )ƒ}ø ¢€ ŞúCÀµ İók ö@ù ÿk„y4°#n 4Z  µ‘G fÈLÿ~ €¨#àÿoı© ûò â{~ÀµÄ{ÿD3Úÿî}ı2ĞCn vˆW  µ±G jÈÿÂ%Š‚)y¤ ¢€ ªCÀµ À,Êk öÄ{ÿ  Úÿâ Híÿ  °C¼ ú±G jÈÿi €¨#àÿÕ© 6òò ˜EyÀµÄ{ÿ  šÿâ H­ÿÄ¡Á’ÿö  Ôˆ"£pI†¢ ‚) lä‘ª TG õ @.Êk ö ¨ær€¾	ÿ$€p  ú*Õ;Àu  ú^<@v  ú ˜ HÉõ  ğ>Ê útƒXKA „¨(   Ğ|ú ¼2€>ÿ  b1LA!ˆŠtÍ·    ú   ”0’´ Â Áÿ"ˆ *Ò  €   úÏ P~ú ˆ¤'êõ  à“¶ út³TKA „°(   ï ú ø¤-€>ÿ ÀR1LA!‹t  ï ö ø¤-€>ÿAÂ"Ç ÿ   „pÌ¹ï ú$pp  ú*Õ;°u  ú'^<0v  ú  ªÛ ö˜§ãhæj ş Du[  µ ˜§ã ‘ æé8  µp/  ¼cö   @¼?õâı Wñ úâıá*^  µuÕÁùÛÒ·Õ  ñ­z şuÕ9[  µ N6W(QµB‰ €€Ÿ  p²¹ ú œl.€~ ÿ Æ0  Àµ ìÖô!Oµy  €Ÿ  `·¦ ú Ø­)€~ ÿ Ú# Àµ   p¤Û ú  p¤Û öÑp¤Û öØ¤ã  { ş  °IÇ ö9 à  ÿ1õ|$PÓ MT  µ  ñ’ õB  ”Cbh  h¢ ú åØš ÿH¼d@=ÿÊ ÀşÀµú) 8Áâ RÎˆÀµB  ”C²_h   ï ú ¤œ‘ õ è;$€¾ ÿ	 Ø/@=ÿzö+   ú   Àğo  4 `Wõ     õx@ /¶úª2<A õ  (Àå«¶
”(±/ ³¾ @ë ú  àq‹ú 0uÊ´¸ €h-» pÊ´¸  Ğ  ú°Ô0˜ú  pfê ú	  („>‚ %ˆ"Œ½°   ú pfê  öA	b È ÿ     õ   ÀØú 
á£ÏÿAˆ`‚©  €   ú½
 @Úú  PGÕ ú	  ¦Œ? ×û€ıÿ PGÕ  öA	b ˆ ÿÈõ¾  A» †)ã ÿ/  €}ÿòW ^ï ú  èt  ´$ 0ÿ°  ö$ æ`3% FÓ‚	àµ%   @	 ´SÛ`wú%dD"ôÒòg €AÁ)É·  ÀQõ  @v° ú ìd	 õ ¤Yd	 õd+   [ª vb	Àµ
 À–ª ÿ;Y@}ÿ Ò,b	Àµ  œ`[Æ  `R ú ğÜc	 õ œ'd	 õ&Õ)   [ª xîa	Àµ
 À–ª ÿ<÷X@}ÿ Îb	Àµ ¬*Ê õ5%¼J LJˆ^T+›8#J51l’&@ƒcšÍæ*ƒÙ *Ş &ƒLlh
,ÁJfŞ†I©P3$Ğ Vø] ˜ğ8<†S(M†I‘‰&3 ø›=‡‚²%´27Ÿ @(àôşÈvrÀ*P[ H@9¥¸Ïà €ùõ „` —õ P àh¬6A(x;€ÁŒŞ;4h8-^ ³WqZ€„’Ç[6/ >3@UbYØŞW èâˆpa õ* €¥ÁÈ·= ` ‹ÕÉ¾À"j™×9t¸y€¼õo@±œ¹
€áıè ¬§Fõ4%½J ”KJ>ˆbT+›4JL5âab%(@‡a°°±3z˜Á *Ş ÄÀ€Œö d
,ª&JfÆ”P¢‰¼1$Ğ€Uø]0Jd¢ÉLÿ<Gæ+Z È àL!jš°l 	‹»Ÿ ) µúÈeá rÀ*P[ 8 6¥¸Íà € õ ´*`3õP, 6¬"(°:€ÙŒŞ:4ht,^ ³Ñp¨W€Ä‘Ç€v ƒMİ;¨ò Ú&õò ´$®KqòVÈœÈ¼Õèê ¸5€a£Í
`È©w³[1+e	Vëèk@ëáõ°GáG 	–»PEï™> L5JD¼‚@iGÌ#›¤‰ÿÎm³yxdHáĞ 3ƒL†7è(ĞXÁ&+^&Jz(ş&6ôqÌ4X¼ã18E’"M€3à 6›_ƒ¨#lû8€²‘¶¬|B(N³å„`"tO8 À|[é»!äâ™ õ(Š+N‹ÀlÿR JÂŞpğ1  ®â³Úx,ôã õ@@×Oºè:Ê  
~øèË È+€á‰Í# SFğşèw	”!ğõ 
„ØõÄ†£“ÿ¯  „N(Ø8àÈ‰I1V"©Ã™¡Ùl^#:ò R84€4„Á€L3?h
,Á&J^˜P¢Å1xN‘GÿŠÑ#Æ¤i83PG
 ³‡”H0¸r „°
Ôÿé Ê	¤°‹s8 0ßc õ„ƒâ
ÿoÇ €1uR€Óò¡è€«ø,ÿà`®s_€Ïs}…ü{ (õÿi	@feè|}`"À"È·Á@”Í÷âH\âSõf€ f³ L Àü'õL5%¼J J#G^!P(	›8J,95ÈÇ±'ÀÃ0èà3ƒÙ ëÁ&<Ã sÌg‚V°3³·aRj3¤d€}3	 &xÏà(“¦á@Rd¢4 SQÄN
8½?rÿH8Xğ	hé PN )»À8 ìßc õ!Tâ™ õè`0£·ÿ€Óò¡èR mÂŞpğ1  ğâ³A ”ã õW*x/ dõ…ä° áògx;`ŒèàyÌü%À/õÇBü Àvõ7øƒ!0õv|! õ@  `Ï	õŞ–=€   ú PÀ õ”&Ğ¨ËúŒ  *Ú¡Û  PQtèñË3 ¬-ãËP É+î¸{L:ThQ ¨È&õûLTTáhP „äÆõ; 3	  ^ÆQ Z ÿ+¹1‹ »™À°G
XH!¡c!
€h×ÔÛ-àÀã±¹°¤ ÀEaµR D»Ì³ ˆvÁöÎæ ¨=€ãÇcÁò1 @¹è@à¨Š87àäp‘e©- $€‹›§›bl(¬T@ …ªŠ87àäPqe©- $ †®¢›c (Ò£T9\dYjËÿ6œºá‰  €µH^Px ¨À±¡®,#¡  œ4Á&pê†ç]Œ€µH^H('Í ÿ§7p9.YJT  $œD ’_®¬® 0ğ8 Ïsü \¾¡ÙD $ ¨“t <L<
³( ‘äÇD $0Ÿt 0ğØ Ï  `%úå	éJ ŠªJô°â#  õˆ;°VSú  FY"L  „°,i  (E"Lpe@& õº„ ¯&õäp   wÑólÌ   ÊáñåpDI '	õM °Z úT  ÀÍú hô¥8Ó:Ã®^l}RT JŸ£ŒR ÿa \½0û—{7SšNª Ş¶J©d¨~vnÊGloiVRT Jš•ŒR ÿ7 ”0½ÓŠıß8¨~v "‡â¡' Ú·Hàµ   À ´*  ğê‚úÔı20   úÀP0a@P  ¸sB`)¦Æ¸BÖ1h0@Šà5'È:lÀ ŠA©L 
 àÀ“ (€sÂó€ô¬{&‰)µc @dx8d7,Jpø` Ø @+¹õ H@’¹#  Û/úMH<¡rB›<mZ›!d> Í	«†%5ôÆ°Ş­ä)@@´Ñ˜@Åy ú   @“ ú4É ©úé˜    úÑC @“ ú1£  Ûoúºı    ú   3
 ú1£  İoúŠ  séõŠ `±@µ U    úy‡`“?\3@à ØQ `4-õ€  @&O $ `õ\õ  >Ä úL ƒ õ% L™É‰8@bi~„WN - ?@¥p  ¤ €•‘õ >Ä	»“ BfrbÿKf!JLlí¨ ^€h) °2Ò	 õáCŒ 	 »L  °¶€Xšá•ÿ¬Œt@=ÿ „{# õ	 p`² ö4 x‚ õq#Ş@-\J6  ,@´¸J ' 'À¤p  ” À÷xõ	 LÖàµˆ7P—Rÿ66	mÜGH% ğ=^=ÿ&k  »4 0ğ¶-nÿJààd‚ÚX¯âW€ïñøÛ  e¶ ú  P´D ú  €~­úÜ£
 õÃ®€ã
 ú  @•oúµè‡§
 õmÔNx3.õ ˆê¼¡Ñ! ğt²±ÃÚ@1ChÂ 8 â«h 
ÀÀ¨h
 ` Ûğñ 
àpÈœU Ö,spGÃ¡8 tÍ2M‰‡ç˜™ &©|“oùzD‚´À@VBœB†(äliV  ñş’   `è6õÀ´É,…èÒ	çmJ&‰)µL 2éÄ+î0¹g!ÙM ¼ ³	 ( À…¸
4L+Ğ³ ‘È“KÅÙ, ğ  µqCâ&|l FÆa©Lqv‰@Gÿí—4 P“«6  °|sa€0;hG@’Ia hù pp·©6    #n6  ‡³ú° 6à8 zpl6ÄÃï nËyğ¬ŒÇéîÂs¸²X õ^L0à…4–ÑAvìÛ¿õ£tˆ¥õì˜€5õb5¥o
ÁÙÈ	$á«3õ+ 8ˆ fõ*4îz…ã‰} Ôg õæ ğf2ÃÌ„¼ü\‘ÙÄ!h%+ õ+ ø„ |]õó2.s•ã‰o ¼´F õå ğğ±ÃÌ„ä,Ù+ à ÂÁ»*4üz…ã‰¼™Ì0³ÿ¯øP‘æ¿Ğ"à\°`ÂÁ+ 8ˆàeõuÀY@=ÿo ˜É Ãú¹˜:,˜p0    ÿ["ÏŒ¤õù¾„ Š¯Á­  uõ~T¤ù/ÿ^=@xÚÏ6"ŒHõŠôh‚/õ=šğÅÁFõ   `’~ú  P…¬ ö7”?   úW  üê úÄ  €Ìõk/ 0UÆ úÈRyŒ |Fõ?•E€*JúÜ €#<,çä_æXàm¥ºd  Ğ¢ úÊ\
…9 õI<[`éãÉìD ©¾ËÜ‹8ê®~®ü ‚m‘n$ƒÑñTèKq®V0èqÊì às ¯À»2 °è?ÆÒ[À('D†å'`Ğ”‘ÏM€0Æ¼¬ KIsÓ·šm§à³¯0`¨ÙØ@ÀüÏ¾‚ ¤jªÛş J6³£®²ü.ÛM?®–ĞO tıÁÈxÀ7ıÈÔ@*DXªş ‹É¯š– à™Â™šp€ñ§Ï›
È]€S¬Ã^ğˆ9³Ñuà&ª¥Üáô–% üöÏX
ğ¸²É ) o’É¸ĞÏîØÚ "¹Áí"®‰À À¬×¾ (ppÄ=u2O…®>¯ °U†© s Æì¾ŒpBdÈâÜ6h
@s«ÑĞ	€qŸÇ²DÒ4Hkèfl§Ã+X³ÌÀ$ê­Ë¨`~xİâ
P˜LûÒJÍ¬ã¨ ®ü,İışš–ıº~Ù©¤
–*ÿäWê§8dÂ™şš·Àà§–¾å
À)€µ¢Ï^ f2›«Ún‚¥¥xEè^…”%˜tİÌlŠ•H=NèÃ€@¶ÍÏS Ô‰‰¥;õê™ @
€´3É  ‹©úº˜    ú   0“ ú3É  ©ú£ı= ú    3
 ú0£ 0Úoú»ı    ú©Ğfõ¦iš¦išL)&€	`Â4™]¯ €$ú5É Àß‘ ú&˜L¦™ K˜  €>ÿ ğw$€ş ÿ& H 5ÉóL `šæßÿHr`  ú5É@á‘¯ö&˜˜¤¢‡³› €‰Éîÿi @—ù ú  ÈŒâëõ&˜L¦iBjK Ğe>€şÿ2£üo= õ¦iš&SJÿt™oLLL2£\p= õ  ÀH›úÅ& $û‚¨  ĞKL$ ä
 ³ã p·©6" €Šb>%ÿ  pz´ úàu < < d à›|õ 2 À“#  àD¢ úˆFFÀˆhK# Q bŒX | àWõü¡>!Ài!€? á„X  0©ú DH&€~¾ÿ‡Q,°İFxÀ h=P2(  \€v¸F
|ÌlE ãa*ü˜´@¬FFAÀ
?;ìE g"’Ùl¥ÆŞ0 Zÿ  d@z¸`‚ô1%ôÀx˜Êÿ- 4Àz¸+ , w¸
F¦+[ÙÁOû5 !™ öÇ”ÀÛaT€¤Šÿà}EĞ. „& jŒp€Áx:EĞ°ŞV Àİ`@ÆÃ5İOèö@rh»Ø D	ö 
À5 P@€hhà‚]/P Øº ˜·,(ø+„¼,X —àƒ{" E7 @” ŸÀ  °‹ ú 
 ¼+ÁT ÿ[W & Ø<,EÀ_!È @äÇÿD	:àg·U °‹ öÔ šÿ`—ğÀÖ ¸Qè%ø †ÿyA@   wú8%°   ú #€âa*ˆE À§K ƒOFÈB F dˆ¦+ ìVşS õöF€ûEK F \_+ ¸¾E ƒ}Kö  ` õ  É×ú:   À õú±0soú  %+   » ú @  £5@ ÈhM ÿ 'u!µ <O*€¾ ÿ <O*€~ÿ@  †"5|¸@¾ ú4 PX* ú  À¬ú4±TÙƒ õ   ©ÄúHz-…‡ õ’õ {Äú’J° ú?z€ úš¥P„Äú¤Y ©ÄúH pqú  `ãà úix  Îä¶ ğºz şiáp¦LSx €0ä¶ °t¯} şSá„¡L  ’| µUà9   ú  $Ö#Bõ*   °  ÀLš€	  R 0  !Bh²í8A¼ĞÇ«ó3;8ï;{L:JM ØùvÀã<Ü®ù»ñ‡488F76–:JBÜˆ+âŠ4A´86FECJIÂ@Ó<=wJE  ĞËŠÍß?l’476–:<8;JD\WÄáŒ486cq(Äí3@Ó<=wA´J@{‚Ê’„lŸ•—|$‹4!‘qô‚é3+)—¯•g¾36R«,z,!J­–kf…4IÀh¸oÎ4#¯ïÒò3­<ó
‰Œ4,z©•rù3&"#H6V«J„®SºsÁ3 ¨‡‰¿P5 ö¹ÃÁLıX¹˜ZÁ–`
¸a»&PÁ&ª>S ²€™s
`PstK€M*`¬<Ô\‚T)›€@ŒªÂ2` U„5pÜÁ= .€œçL›`¨*Ü$.¨Wˆ+`Uqƒ&UW›JB°‰ªOÿX5€ ®)›@P0H¥8BUÜ IÿW›`JHãRE 3 ÂâÊ ÿ
¶$n°¹ Æ ÿ Ââ
ÿÁ&B˜’ ÿ. Wˆ+ÂÁ¦RÿHãBE 3 p"Óÿ4\ø| ³ €*RÓÿ\¨,€ ³¨ÑfÙÿ 8 @‰ˆo
`Ps‰KP"ª¢ÿ . ‰çL ª™ÿZĞJ õ4\´X ³ˆ€P…PNÿX€ÀuGh P"SÿY À‡„¶4\xë¦ ³ P"“ÿ4\„W ³ ª	ÿZ¬R õ4 ÍeK T¡[ÿlC€   úÀ  €>ÿ  Z­ú4 Í%çK p"“	ÿçX3E„¶P¸œĞh ÂâÊÿÁ&B˜R	ÿ0'W„5>Ö@âµ!  „ó¯ÑB°‰ªÿàô}DUf¨Ñfÿ pn€¶`
7Ø$W›J ›ğ Á&‚’ ÿ ˜¯^„¸ , ú¯sT 4õ,d8 ³ [˜¸0@¼ˆú|ü´áÄõ ,·{òæLt  Ø$õ Xş Á7õ hNB‰Â–&  P  úQæà¥–ú L  õ  àX§|õ]\Ğì0 ú  @Ã¹¹ €   €         À      h‘P  æA   ¿“,.Ë³ÀmK%
Š-Í$Š³4õû[Ûl5ÆZ[Í:3.îÇÌî@< ÌÌ"«)wß½c­­Æ:u#°ÜÏÌî¼(Œ±Ë$î¾8ÌÛ4!ğ»¼Ëê­Ìv¼µæ1ns9^öÜÌNSî¾0Ö¡ÇÂÂ»¬Û·¦=fZÃ\çÅÏÍ  €£f03 ` @       Ô$MR` š¢«Z’*)          &°™N`  * ƒÀ € Ìˆ @   `€
         @   À@ ', -¹	     D “&`   È!À  `      ›4  ¨À    Â k°iÑ‘  y÷Õ  ô `":õ P4p) 8Œ  õ  €şgúø"…ËHuŠ¾	J  € µ`  p{lúĞzñœbh´q€îdŠ› àÿº            €       €             €             €                    <0; ³Ğ`®àChĞ`šÍ3h  @›ïG j                         @      @                 À                  À) M M€øpX0máã8  û9@øp              y    !a&ˆC¸õ*H@ş	õØ ˆ3€´Ò¹Ø(Øp·Ğ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            ”   UJ@D   °ÖZËˆÃ! ’@ “  $Alttt““J‰i#)Ôö	    €$±
        ÆŞûPd‰„¤ ´mÛÚ¶
    "                     Á   D 0   @ w   T2ƒ à~¿ßï4ÿÿîîî PÏ{1Æ(ÆQ9wçşÿB B 1F³›  "I   (1!„À²,Ë²2“!""R      ÿ  U  ğ    ¤  áîî É $II  á J    @@    ´m°ÿ   Uô<2<     m      @=       ©VU`D » »Àf&Àµ!      —I- 	 à € -             !"B   g†        ÿ  0     TTAUUUÌÌ    ş3 P¥¨   B!‚HU @Uª J         €7ãA “D3     I 	   ›  U%D dD  d   1 1   Àÿ!        PPİ'V  PÀLK `  
@ @U         „Bˆ%%J¤i     ÀŠ @        å"@ D $@’  ‚0MŠH  €#À6,Ë  „ÿıO0  m,F   $ÂÌÃ    Á¨€BAQD p  àêh  ÿ€+   $IH0  !9 @@ " Bˆ€, ¨* l# $Q #„s {  ’$iR o™)""" kyÀ½÷& @@ !L×N  N  Z« @é„õhE óÍÀ &@|©   †$­NÚÖƒå$  å¯AyªD•JÊĞ 
)ÓÚ D€Ÿ½³›4`ÊóHÌ é^å  _€ìêğ›„D	²J¿	°Ô¨æ) ƒ¤HJ®½k3ÑJ)Ğ D|ıÚ# L€ÔÚÚ›z?¢J\€Üh‰èÆ X @øöÂ „ê4u–K. ”¶ò$Ôâ°Œ‚ä8 €–O¼›Ì” Šœ”+øHÀ&€Ö« -€ ıŸ©Ã€pºÕO`ñÎå$ h€ûùî›‘G»J5 Œcà¹ õş2úfâäb @*HT®y  ïË”í H<@ÕÍ‘   Xå»œå·$¤\L@Y0T ª˜Öó .ÒfP®¹ 
@{ŒTá	 H²æ. n¤˜ÒJ®IÜJî}ïàïuõÚTI@ì	õÓ 82@–Ô¹3 (pÑ·ĞH »Ì ˜·ù `À‚·0]ª¶lEæñŒ_ciw" ³†³Ãh¯¤4 Ø"°È¿2( u°ÎœâŠø†ˆ4 `5€7Ô¶É¨`üîÎL	‘(_JÎX @!€·(èÀNÚ·3@o5lè¥áAç_™ı> ³‡±cø¯˜48 TµOPÔãˆ NèØ4  YÆ›>" ††”ğ |€ÔÖó ağR|r® òŠëV°°Ò‡å<  (üÈ‹ªÇs-J¤`´.Šå. h€ñßä›ŠP“J5 @``=õÀ -t¾Ôäb PèIT®¸œ ïå”Ù :@ÄÌÍu ( Xİ«œ~ 	 P”Oş	€‚¤Lµæ9 †dÙÑP®ºt#ÂJc	 !ªæ6 n8ÈPF®šR©J)ä÷@( õÈ?!Ìõq¦â×Ø`·Ø¾Z« @&ãÍ	Pd*ÿI
À&J ğgèAPÆ0@^ûÎD•$RJ…iÀ#°¶?õÎ* ³™bŠÈkˆ4 H7ÀjÔ¶ÙP ùÎD	² _JĞ¨ !€·T@	ÀoÚ·3¢SSl"æú¡×_©ik§ ³?¢#LJÈĞğ¶áh ×ã¸gPÀxÎ B¦TQPÚ”èO ¼ˆ¿Ÿ‚® D8q:Zl  €0Ó’ÆôüXù‘Øh-«-‚JñÈÀ0¸·÷å»7 , ³G»$hJãx.H$·)Ğ70ñÆ ÕßÏÈ°³,¾V‰¨Î“å  X å»í›…9"	‘J†
€Å,¿æ. †´TV®¹l#óJ)¦´’ëÿIÜ.nJ)äÂ= ğ¼z¤¤©øµÿØ- ;ÎÎ È d|£Ñ `@k5õğ€àØŠØI
À&UJĞƒ (À·€ö‰·*I¤şl…D¦á‘Åˆ_˜"ò"ÿé†ÕØ@c¦OÙ©Ã3$ ³4*@àmÿ(øMÀvÖÑ )’"U®½ 	ÁzæO µ¶öf#ÿ´D ¼-ÿíàz ÜÖg)/Â³”6å#ú
ÿû  DAÆ -=Á‹ø®%€  râƒ	°d¦æ-  ˆşÏ•ªÃh-«Jà~¾ûæ"ÿ	ä­
ÿ}0q Çà‚ DğwÊÊ:v–Å. l €jäIäÆÊØ =ñÎ9"	‘ XJŠ	(¿@!·AÈ
€¡¢¸#"æ]Ûl‘hÈ"JÙ_)X	@xÒ·¸]ÜRlÊ>àÉ½’`ìO,™@õ¯æ, ;ÎÎ È ´ü¢Ñ À 5õ €àˆŒØI
ë&UJ”€„ (À· ı‰·bI¤l’¨á‘Åˆ_IõÎj ³3SDJ ¬QÓpåP >üÎD	Ù _J/X© !€·T@	@zÚ·3äSwl4¦ı¡×_¶ik ³?¼#LJ÷hğ¶áhÀâã¸²PÀ…Î\7
$ºÿû  üAÆ g=ÁËş®l ç©¹ÿüX©“Øh-Á-‚JH‘À0¸·æ»§ , ³GÕ$hJ?ø>H$·)Ğ7tòÆ uæÏD@«ieyÿ×Ø =óÎ9"	µ XJ¡	è¿@!·AÈ
€«¢¸ )¹`HŸ®Wr¡ e\+øO€¶‰Ö.¸¡cJ® À…¤¿N ÔĞä âñ³ @z‘lõEÀ€ûeÀ 0 P Ä> ¤&jû +Æ üA€0hĞ¶"   úÛ  l‡íŸ  t Knõ   3 õ   ğ@úd1ÈH  ³q?ªÆ  õ h€äÉ´ ° à)$õ™'	 €Ì“ôŞ( @ õ(   xúhâA šxhô„‚Ä£ {$ ÜH ³ä! ygh> °Œ.ú.À!$/® è
áE¬Àæ×È2ÌÁ! õğx ‚¥¾>ÔÊÛEˆT h›Ã@ğú ŠòE ¸ 8‚È Ğ+€áÇ^ >Ááçÿz…Bx‘§ÿ«Ûô5H®¼ ğ	 x‡Ä.¼€ É HŠæEbdÀæàÈóD(2yÄE Á½ õE<@‘´c	PHà“ê¿¥Z<›0èc‰Bâ“8Ïè
	à“ `ÂÕ-	 x&<–Ûb
0Èu‰“Û` 0ŒòBGÔÁ®õÁè¤ï´,A õÁ`A õ‚
 Á p9Epa jüÕˆ* x ŒŠñ ïòwG; ´ õÃaºõü @À°´K  PïŠ úÓ hT ·qÍà  –¹   ÁúdÏÀàK   pŸØ úà ƒKL  pŸØY»<(¶@Ön ÿ   l\õBÀÀ h   ğí³ ú|û¬@Önÿ` ÄL÷>+  Y»šPpúdÆu  ú=Æ°]– ú  @1¥ú  @]– úh  0İúzJà´)#ò @„ho€&Š×‹IÎ8 íq˜I 8`ìq  Î8 íqšÎ8 íq}•¸¦ÚúÜĞ¸   p†õš–RlN – ¤
s½ 
 ¦š‹Jêîîîîîî@ A2*$     ú$;%@•uú  Î; ´Q„-   ú–|·D³àõ›	,,È£§Ê…ô  8õ\pMÊÔ‹ÒX"@:õ  °™6Òäxj‰Ù "ïº™İÛ§]H¶‘ÒòV[e´VJV ¸>ÀÊ¹ÕÈ`Åò’ß¯<‹Á4apX&‹Á4ä0˜,Å4ar8L›Ã4^¯·ûızÁ4^ğ,¼4a1˜,¿4`²X<‡Á4á09l“Ã4Şî÷ë‡É4â±Økğz½4â±Øø–Å4´Ø´	_ebJ´ WRWŞÙJWRWŞÙ´ØJ2v`ë úX PÖ¯ú«l&  ‚çyã #¡» àÉõeı    ú& Às¬¢:B„VÙòB <yc\ õ      úX Ğ×¯úèA@è.ÿ   €ş ÿ‡IP= ú& Às¨¢&:„È±òB °óâT õ &]€şÿ¡ r¬ü ÿì¼œ
ÀDõ   @ õP ğ™túäX&  ‚çy§`¢¡» €õ dà3ºõVW `¼Dõ‚” éÉvõB Jå$õ6E  äõFo• € õ~I@®9†¶ïD7‚ õµ4Äà³$õõIœ
À3õ¿Ãl ‡õ^P!8JvÃ „pØ¤ş`¼"M…îír+º% õHøètå¤ö, ‰ò/ P  'õum+  õ‹:DÉ0§õ˜4|‡aãCõ@ XÂ õA 8‚ø  ”õP¢Œ,áÀõ18&Z‡òøQD,›¸Ty„éõ£KÔR¤ÜİºH jõEIÈMXê^ ,8¿á\8 PÒ‘ö 
 „¸* s€X;{\VT À‘põ P  4‹ŞtçD XòöõXVĞncÆh07 AnLƒ­µ:7ÈÌíŞıìßİLÃ´nÙíÌİÀÌÌÌÌÍcîµ¦±†ĞíÌÌíŞÌÌÌÌÜÜİ`‘µ†5`c°ˆµfÌÍÌÍÜ    úÄ @Wú! ‘DC õÓ\‘$ õ œÙIC õZ € øñõ„ñ«'i.õ÷@  ú  y€5D; ÀîúXM,“I òıs0_˜Ébö8 +˜¾"®mà
š°¢Ò½G8uföä¿C8F<ù³Ù[ğ~ ¿¯»áÙ€5ŒèZ °@)×¸½9 LcÜšÜ¿$ÀÊõ½
àëÚ­q  Ó®½Ğ
!¦õ¯  öá“ÌÚ  k ô¿¾5 €îÌ†ƒäãĞ€]ÊÖÊ9@àïõû$ õM*
à«é­f @Û®½?ôáÔõ¯ 0ã“Ì#Øjàı¿¾5 ÍÆ†ä­	Ğ€]ÊÖıs  À4Æå¸‚&¬¨tÿ§Î0{ yÛÇOü, ÷õ¿ xÅtŒçØPÃ¸Õ¼è¥PÂ¡ÙÌÆhM€Ø¸‰ ø«û˜Ñ+8¬Pùş®ä ˆàZ†×ÌĞˆ#€× õOü, 	õ¿ ğ
şwŒæF‘ÚÛ¼èZØX)uÌ³H>uPÙÜšÜ   ĞÂÍ ú  À   Bb    ú   DVú     Âh×à*h •  –Š;KS-»JÒn
 „* ˆ‡V¨Ã ³‡ ‡‡‡š‡ ‡Ì{È!®‡
³r‡‡‡š‡‡‡‡‡‡‡J†â*³ˆˆˆˆ††Jˆ @ ˆˆ»ˆ ˆÔ‹"®ˆ "³p ˆˆˆšˆˆˆˆˆˆˆJ‡+³‡ ‡¸{È!®Æ‡‡‡š‡ $‡‡‡š‡ ‡ä~È!®‡ ‡‡‡š‡Á
l‡ H‡‡‡šˆˆ‡‡‡‡JÊ ˆˆˆšˆ lˆˆˆšˆ ˆø"®ˆ  ˆˆˆšˆÍ
lˆ `ˆˆˆšV ˆˆˆš è¢ÀLªÂ!´Ë¢]3(p,€©&jÂ€.€#{hˆˆ‡J|/ŒJ~_ADĞÿ
3"übQ+YøÅ4b‚ˆÀ_D3_,ˆü4~bADà3D~1ÿ¿XøÅ4b‚ˆÀ/D3,ˆü4~cADà3"ücQ+ÿXøÇ4c‚ˆÀ?D3øÇÂÿÿ˜@Ô
ÿDş1ÿ¿XøÇ4"?MÿA,ˆü4|_AD°3"ö]Q+‚~XôÁ4b‚ˆÀD3øÅÂÌÿ¿˜@ÔÊÿw wÛ,ÿ˜ "L
‰t°D˜‘‘š‘0##1#l˜"#1#a	l‘`FÂ"³‘ À‘˜‘š‘ `‘‘˜š‘°„HÀì ÿHÌHX[ ÿÈHÌH Ûÿ˜@d$¦&ÿ‘DFM"³ x‘‘‘š‘  L š»HÌHXÛÿÈHÌH [ÿ˜@d$¦¦ÿÍ#àFĞÂHÆ%%–Xb‰®'%’Hâ‰®((¢xb‰®(%ˆ"Š®gP”€"ŠÉ‚ˆ@Úª 4‡ ‡`œæ®‡‡‡ša‡‡‡šŠŒ‹õJ€/ŒJˆüŠH,‚b‚_Jˆ‹H,‚e‚eJ@„€ ˜R ÿb‚bJ‚b‚Jˆ€ ÅŠ;‚b‚bJ@„€ ˜’ÿc‚cJ‚c‚Jˆ€ ÇŠ;‚c‚cJˆ‹H,Ç"K ÿ@„€à˜Ò ÿ˜@„€ ’ÿÅ"ÿ@„€ ˜Òÿ˜@„@ Rÿ€_`J"ş]Q+a`Jˆ‹H,Å"ÿ@„€ ˜’ÿ'ˆ„àYÿDìüq³˜"#1#'l°D$f¤¦ÿHÌHX[ÿÈHÌH Ûÿ˜@d$¦&ÿ°D$f¤¦ÿHÌHX[ÿÈHÌH Ûÿ‘ M‘˜‘š‘ D‘‘‘š Ğ ÒƒÎ˜"#1#	l°D$f¤fÿHÌHXÿTD˜‘‘š%”ˆæÑÔ$'–Xb‰®'%X"‰®((¢ˆ"Š®((¢Xâ‰®@­NK³iN‘ EK‡1l‡³™HdSéĞ*ØĞ$É=(‰ëÙ AR-Jº˜²÷Ğö@$/¶ÛB¤@{#hl¸=‚†HµL:R³ÔD3€@ğtabPVÀ8‹Úrà÷@µHµˆ;Ä³<]˜ÿ³B"‰t\@€¨•èèÓ°;‡è$M@L‹Âå À À¿ÍÍ;øˆpI¦×<Bf'l¡	ˆ	X$ ãøÄÛáÑ•¸0¥å¿„<¸t